node_modules/
.next/
dist/
build/
coverage/

# Секреты. Ключ служебного аккаунта Google и пароли сюда не попадают никогда.
.env
.env.*
!.env.example
*-service-account*.json
*credentials*.json
gcp-key*.json

# Объявления типов, которые фреймворк порождает сам при каждой сборке.
# Так же с ними поступает и собственный шаблон фреймворка.
next-env.d.ts
*.tsbuildinfo

.DS_Store
*.log
