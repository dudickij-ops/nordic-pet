import { LoginForm } from './form'

/**
 * Страница `/login` — единственная дверь, через которую выдаётся cookie.
 *
 * Исключением из закрытия она не является: она ничего не отдаёт — ни чисел, ни ссылок на
 * отчёт — и существует ровно ради того, чтобы получить cookie. Поэтому `proxy.ts` пускает
 * сюда без входа: иначе войти было бы некуда.
 *
 * `force-dynamic` — потому что страница отдаёт форму серверного действия и разбирается с
 * cookie; отрисованная заранее, при сборке, она жила бы одним и тем же снимком.
 */
export const dynamic = 'force-dynamic'

export default function LoginPage() {
  return <LoginForm />
}
