import { resolveCommit } from '@/lib/commit'

/**
 * Страница собирается заранее, поэтому номер коммита определяется один раз —
 * в момент сборки, а не при каждом запросе. Для номера коммита это верно:
 * он и не может измениться без новой сборки. Но всё, что добавят на эту
 * страницу позже и что меняется во время работы, так читать уже нельзя.
 */
export default function HealthPage() {
  return (
    <main>
      <h1>Состояние</h1>
      <p>
        Коммит: <code>{resolveCommit()}</code>
      </p>
    </main>
  )
}
