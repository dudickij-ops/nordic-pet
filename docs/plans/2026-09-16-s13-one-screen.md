# S13 — один экран: сколько магазин заработал на самом деле

> **Для исполнителя:** обязательный навык — superpowers:subagent-driven-development (советую) или
> superpowers:executing-plans. Шаги отмечаются галочками `- [ ]`.

**Цель:** отчёт — один экран без вкладок, который отвечает, сколько магазин заработал на самом деле за
выбранный месяц, и сам говорит, что из этого следует.

**Устройство:** сначала слой метрик (задачи 1–7) отдаёт новые величины и признаки готовыми из SQL,
затем экран (задачи 8–13) собирается в один и только печатает их. Проверки прошлых кусков правятся
под учёт по ходу своих задач; сломы прошлых кусков переводятся в задаче 14; снимки переснимаются
один раз в задаче 16.

**Стек:** Next.js 16, React 19, PostgreSQL через `pg`, Vitest 4, таблица стилей на токенах Fluent 2.

**Договор:** ветка `contract/s13-one-screen`, файл `.contracts/s13-one-screen.md`, коммит `29fe322`.
Договор — канонический текст: план спорит от него, исполнитель читает оба.

## Общие ограничения

Каждая задача неявно включает этот раздел.

- Экран ничего не считает и ничего не сравнивает. Слово выбирается по признаку из запроса.
- Округление живёт в SQL, один раз, в последнем выражении; доли считаются от **показанных** чисел.
- «Нет данных» — не ноль. Пустое поле печатается словами.
- Наш вывод помечается «наш счёт» везде, где появляется: вклад, порог, маржинальный доход.
- Оформление не меняется: только токены, уже объявленные в `app/globals.css`. Ни одной новой
  заимствованной величины. Крупный кегль в таблице стилей один — `--fontSizeHero700` (28px).
- Каждая задача добавляет строки в `breaks/s13-one-screen.ts`; у каждой строки `tests: 'все'`.
- Каждая удалённая или переписанная принятая проверка — строка в `docs/сверка/учёт-проверок-s13.md`
  **в том же коммите**, что и правка.
- В коммит попадают только перечисленные пути: `git add -A` запрещён.
- Ворота — своей командой, код возврата отдельной строкой: `npm test`, затем `code=$?`. Ни конвейера,
  ни `&&` после команды, чей отказ надо увидеть.
- Имена переменных оболочки — латиницей.
- Числа в прозе коммитов и отчётов — только из вывода команды.

---

## Решения владельца, на которых стоит план

Приведены в договоре дословно, раздел «Решения владельца» (Э1–Э9, 16 сентября 2026 года). Здесь —
только то, что правит задачи.

| # | Решение | Где в плане |
|---|---|---|
| Э2 | Один экран, вкладки уходят | задача 8 — **отмена решения S11** |
| Э4 | Первая пятёрка, раскрытие до всех; порядка по адресу нет | задача 4 — **отмена решения Ф2 куска S12**; задача 12 |
| Э5 | Постоянные — ⚠ только от 100 % | задача 2, задача 10 |
| Э6 | Принятые проверки — под учёт, снимки — одна пересборка | каждая задача; задача 16 |
| Э7 | Средняя — чистая выручка ÷ все дни месяца | задача 3, задача 11 |

## Что снято до начала работы

| Что | Чем снято | Значение |
|---|---|---|
| Набор проверок на `main` (`b94066b`) | `npm test`, код возврата отдельно | **88 файлов, 966 проверок, код 0** |
| Местная база | `npm run metrics -- 2026-03` на местной цели | код 0, все суммы — нули: база пуста |
| Файлов проверок, упоминающих вкладки, порядок по адресу или каскад | поиск по тексту | 21 из 93 (с живыми и снимками) |
| Строк сломов прошлых кусков про то же | поиск по `breaks` | 56 в 3 списках из 11 |
| Эталонных снимков | `ls docs/screens/*.png` | 30 |
| Токенов крупного кегля в таблице стилей | поиск `--fontSizeHero` | один: `--fontSizeHero700`, 28px |

---

## Карта файлов

| Файл | Что меняется | Задачи |
|---|---|---|
| `lib/metrics/sql.ts` | каскад от чистой выручки; `МАРЖИНАЛЬНЫЙ_ДОХОД`; `FINDINGS_FROM_TOTALS` и `MONTH_FINDINGS`; средняя в `DAILY_FROM_MONEY`; один порядок товаров; `in_top`; `has_holes`; полоса без доли рекламы | 1–6 |
| `lib/metrics/report.ts` | тип `MonthReport` и сборка отчёта; `monthlyReport` без третьего довода | 1–6 |
| `scripts/print-metrics.ts` | два блока в конце вывода | 7 |
| `app/page.tsx` | один экран: результат, выводы, каскад, ряд, товары, качество | 1, 4, 8–13 |
| `app/globals.css` | правила новых блоков на объявленных токенах; удаление правил ушедших блоков | 8–13 |
| `__tests__/metrics/*.test.ts` | новые и переписанные проверки слоя метрик | 1–7 |
| `__tests__/screen/*.test.tsx` | новые и переписанные проверки экрана; удаление проверок вкладок и порядков | 1, 4, 8–13 |
| `__tests__/screen/fixture.ts`, `__tests__/metrics/totals-fixture.ts` | раскладки с новыми полями | 1–6 |
| `__tests__/live/march-screen.live.ts` | живые утверждения марта | 15 |
| `docs/screens/generate.tsx`, `docs/screens/fixture.ts`, `docs/screens/run.tsx`, `docs/screens/README.md` | состояния и опись снимков | 16 |
| `breaks/s13-one-screen.ts` | новый список сломов | 1–13 |
| `breaks/s10-fluent.ts`, `breaks/s11-content.ts`, `breaks/s12-reading.ts` | перевод цитат и вывод сломов удалённых механизмов | 14 |
| `docs/сверка/учёт-проверок-s13.md` | учёт правки принятых проверок | 1–13 |
| `docs/сверка/метрики-2026-03-до-s13.txt`, `…-после-s13.txt`, `числа-марта-s13.md` | живая сверка | 0, 7, 15 |
| `docs/сверка/прогон-сломов-s13.md` | полный вывод ворот и сломов | 17 |

---

### Задача 0 — исходное состояние и живой «до»

**Файлы:**
- Создать: `docs/сверка/метрики-2026-03-до-s13.txt`
- Создать: `docs/сверка/учёт-проверок-s13.md`
- Создать: `breaks/s13-one-screen.ts`

- [ ] **Шаг 1: живой заход в местную базу**

```bash
NORDIC_PET_DB_TARGET=local npm run ingest:sheets
```
```bash
code=$?; echo "exit=$code"
```
То же — для `npm run ingest:ads` и `npm run facts`, каждая своей командой и своим `code=$?`.
Ожидание: три кода 0; загрузка сошлась с независимым счётом владельца — заказы 519, возвраты 19,
цены 11, ставки 2, расходы 5, курс 62; реклама 279 строк. Расхождение — стоп, к владельцу.

- [ ] **Шаг 2: снять «до»**

```bash
NORDIC_PET_DB_TARGET=local npm run metrics -- 2026-03 > docs/сверка/метрики-2026-03-до-s13.txt
```
```bash
code=$?; echo "exit=$code"
```
```bash
diff docs/сверка/метрики-2026-03-после-s12.txt docs/сверка/метрики-2026-03-до-s13.txt
```
Ожидание: код 0, дифф пуст кроме строки цели. Иной дифф — стоп: база не та, на которой сдан S12.

- [ ] **Шаг 3: дни без заказов в марте — числом, для договора и отчёта**

День заказа — по тому же правилу, что в `MONEY_CTES`: самая ранняя дата строк заказа.

```bash
node --input-type=module -e "import pg from 'pg'; import { projectDatabaseUrl } from './lib/db-url.ts'; const p = new pg.Pool({ connectionString: projectDatabaseUrl() }); const r = await p.query(\"select count(*)::int as n from generate_series('2026-03-01'::date, '2026-03-31'::date, interval '1 day') d where d::date not in (select min(o.date) from fact.orders o group by o.order_id)\"); console.log('days_without_orders', r.rows[0].n); await p.end()"
```
```bash
code=$?; echo "exit=$code"
```
Создать `docs/сверка/числа-марта-s13.md` с заголовком «Числа марта: кусок S13» и строкой «дней без
заказов в марте: N» — N из вывода, рядом — сама команда.

- [ ] **Шаг 4: вернуть базу**

```bash
npm run db:reset
```
```bash
code=$?; echo "exit=$code"
```
Затем `npm test` и `code=$?`. Ожидание: 88 файлов, 966 проверок, код 0 — как на `main`.

- [ ] **Шаг 5: заготовки учёта и сломов**

`docs/сверка/учёт-проверок-s13.md`:

```markdown
# Учёт правки принятых проверок — кусок S13

Решение владельца Э6 (16 сентября 2026 года): проверки прошлых кусков, которые один экран делает
неверными, удаляются или переписываются под учёт. Дописанное рядом правкой не считается.

| Задача | Файл | Проверка | Что утверждала | Почему больше не верно | Что утверждает теперь |
|---|---|---|---|---|---|
```

`breaks/s13-one-screen.ts`:

```ts
import type { Break } from './types.ts'

/**
 * Список сломов куска S13 — один экран: сколько магазин заработал на самом деле.
 *
 * Набор проверок у каждого слома — весь `npm test`. Перепись экрана снимает весь видимый текст по
 * порядку, поэтому слом разметки красит и её — это объявляется у каждого такого слома.
 */

const ПЕРЕПИСЬ = 'перепись экрана: весь видимый текст, по порядку и без единой потери'
const ПЕРЕПИСЬ_ПРИЧИНА = 'перепись снимает весь видимый текст по порядку, а слом его меняет'

export const BREAKS: Break[] = []
```

- [ ] **Шаг 6: коммит**

```bash
git add docs/сверка/метрики-2026-03-до-s13.txt docs/сверка/числа-марта-s13.md docs/сверка/учёт-проверок-s13.md breaks/s13-one-screen.ts
git commit -m "S13, задача 0: живой «до» марта, заготовки учёта и сломов"
```

---

### Задача 1 — каскад от чистой выручки

**Сегодня:** 9 ступеней от оборота, доли от оборота, признак `largest`; на экране — «% оборота»,
строка «Съедает больше всего», доли статей в карточке «Затраты».

**Станет:** 7 ступеней от чистой выручки с маржинальным доходом; признака `largest` нет; на экране
«% чистой выручки», строка про оборот; строки «Съедает больше всего» и долей статей нет.

**Файлы:**
- Изменить: `lib/metrics/sql.ts` — `WATERFALL_FROM_TOTALS` (строки 596–661), новая константа `МАРЖИНАЛЬНЫЙ_ДОХОД`
- Изменить: `lib/metrics/report.ts` — тип `waterfall` (строки 204–223), сборка (поле `largest`)
- Изменить: `app/page.tsx` — `ПОДПИСИ_СТУПЕНЕЙ`, блок водопада, удалить `ДоляСтатьи` и её вызовы, строку «Съедает больше всего»
- Изменить: `app/globals.css` — удалить правила `.cost-share`, `.cost-bar`, `.cost-bar-fill`, `.waterfall-largest`
- Изменить: `__tests__/metrics/waterfall.test.ts`, `__tests__/screen/waterfall-view.test.tsx`, `__tests__/screen/fixture.ts`
- Удалить: `__tests__/screen/cost-shares-view.test.tsx`

**Интерфейс:**
- Производит: `export const МАРЖИНАЛЬНЫЙ_ДОХОД: string` — SQL-выражение над строкой итогов с псевдонимом `t`.
- Производит: ключи ступеней `'net' | 'cogs' | 'ads' | 'fees' | 'margin_income' | 'fixed' | 'profit'`.
- Производит: `MonthReport['waterfall']['steps'][number]` без поля `largest`.

- [ ] **Шаг 1: переписать проверки слоя метрик**

В `__tests__/metrics/waterfall.test.ts` раскладки `СХОДЯЩИЕСЯ` и `НЕСХОДЯЩИЕСЯ` остаются. Проверки,
утверждающие 9 ступеней, базу «оборот» и `largest`, заменяются этими (старые имена — в учёт):

```ts
test('ступеней семь, от чистой выручки до прибыли, маржинальный доход пятым', async () => {
  expect((await ступени(СХОДЯЩИЕСЯ)).map((с) => с.key)).toEqual([
    'net', 'cogs', 'ads', 'fees', 'margin_income', 'fixed', 'profit',
  ])
})

test('доли и края ступеней — от чистой выручки', async () => {
  const все = await ступени(СХОДЯЩИЕСЯ)
  expect(ступень(все, 'net').share_pct).toBe('100.0')
  expect(ступень(все, 'cogs').share_pct).toBe('35.3')
  expect(ступень(все, 'cogs').base_pct).toBe('64.7')
  expect(ступень(все, 'ads').base_pct).toBe('41.2')
  expect(ступень(все, 'margin_income').share_pct).toBe('35.3')
  expect(ступень(все, 'fixed').base_pct).toBe('23.5')
  expect(ступень(все, 'profit').share_pct).toBe('23.5')
})

test('маржинальный доход — разность показанных сумм, а не своё выражение прибыли', async () => {
  const все = await ступени(НЕСХОДЯЩИЕСЯ)
  expect(ступень(все, 'margin_income').amount).toBe('314.46')
  expect(ступень(все, 'margin_income').kind).toBe('итог')
})

test('расхождения цепочки остаются у чистой выручки и у прибыли', async () => {
  const [первая] = await ступени(НЕСХОДЯЩИЕСЯ)
  expect(первая.net_gap).toBe('611.12')
  expect(первая.profit_gap).toBe('215.59')
})

test('чистая выручка не положительна — долей и краёв нет, суммы стоят', async () => {
  for (const net of ['0.00', '-10.00']) {
    const все = await ступени({ ...СХОДЯЩИЕСЯ, net })
    expect(все.every((с) => с.share_pct === null && с.base_pct === null)).toBe(true)
    expect(ступень(все, 'net').amount).toBe(net)
  }
})

test('без строк рекламы ступень рекламы пуста, маржинальный доход считается с нулём', async () => {
  const все = await ступени({ ...СХОДЯЩИЕСЯ, ads: '0.00' }, false)
  expect(ступень(все, 'ads').amount).toBeNull()
  expect(ступень(все, 'margin_income').amount).toBe('500.00')
})
```

Тип `Ступень` в файле теряет `largest`, получает `net_gap: string | null; profit_gap: string | null`.

- [ ] **Шаг 2: убедиться, что краснеют**

```bash
npx vitest run __tests__/metrics/waterfall.test.ts
```
Ожидание: красное у всех шести — ключей девять, база оборот.

- [ ] **Шаг 3: запрос**

В `lib/metrics/sql.ts` перед `WATERFALL_FROM_TOTALS`:

```ts
/**
 * Маржинальный доход — кусок S13. Наше определение: чистая выручка − себестоимость − реклама −
 * комиссии, то есть то, что остаётся на постоянные расходы. Выражение одно на весь проект: его берут
 * и каскад, и признаки выводов. Слагаемые — показанные суммы строки итогов, поэтому своего
 * расхождения округления у маржинального дохода нет. Псевдоним строки итогов — `t`.
 */
export const МАРЖИНАЛЬНЫЙ_ДОХОД =
  '(t.net::numeric - t.cogs::numeric - t.ads::numeric - t.fees::numeric)'
```

Тело `WATERFALL_FROM_TOTALS` заменяется целиком; комментарий над ним дописывается абзацем «Кусок S13:
база — чистая выручка, семь ступеней; прежний текст про оборот остаётся ниже как история». Новое тело:

```ts
export const WATERFALL_FROM_TOTALS = `
steps as (
  select s.ord, s.key, s.kind, s.amount, s.edge_from, s.edge_to
    from totals_row t
   cross join cur_state cs
   cross join lateral (select ${МАРЖИНАЛЬНЫЙ_ДОХОД} as mi) m
   cross join lateral (values
     (1, 'net',           'итог',      t.net::numeric,   0::numeric,      t.net::numeric),
     (2, 'cogs',          'вычитание', t.cogs::numeric,  t.net::numeric,  t.net::numeric - t.cogs::numeric),
     (3, 'ads',           'вычитание', case when cs.has_ads then t.ads::numeric end,
                                       t.net::numeric - t.cogs::numeric,
                                       t.net::numeric - t.cogs::numeric - t.ads::numeric),
     (4, 'fees',          'вычитание', t.fees::numeric,
                                       t.net::numeric - t.cogs::numeric - t.ads::numeric,
                                       m.mi),
     (5, 'margin_income', 'итог',      m.mi,             0::numeric,      m.mi),
     (6, 'fixed',         'вычитание', t.fixed::numeric, m.mi,            m.mi - t.fixed::numeric),
     (7, 'profit',        'итог',      t.profit::numeric, 0::numeric,     t.profit::numeric)
   ) as s(ord, key, kind, amount, edge_from, edge_to)
),
base as (
  select case when t.net::numeric > 0 then t.net::numeric end as denom,
         nullif(t.gross::numeric - t.discounts::numeric - t.refunds::numeric - t.net::numeric, 0)
           as net_gap,
         nullif(t.net::numeric - t.cogs::numeric - t.ads::numeric - t.fees::numeric
                  - t.fixed::numeric - t.profit::numeric, 0)
           as profit_gap
    from totals_row t
)
select s.key,
       s.kind,
       s.amount::text                                                          as amount,
       round(s.amount / b.denom * 100, 1)::text                                as share_pct,
       round(least(s.edge_from, s.edge_to) / b.denom * 100, 1)::text           as base_pct,
       round(least(0, min(least(s.edge_from, s.edge_to)) over ()) / b.denom * 100, 1)::text
                                                                               as scale_low_pct,
       round(greatest(b.denom, max(greatest(s.edge_from, s.edge_to)) over ()) / b.denom * 100, 1)::text
                                                                               as scale_high_pct,
       b.net_gap::text                                                         as net_gap,
       b.profit_gap::text                                                      as profit_gap
  from steps s
 cross join base b
 order by s.ord
`
```

- [ ] **Шаг 4: отчёт**

В `lib/metrics/report.ts` у типа ступени удалить строки поля `largest` с комментарием; в сборке
удалить `largest: row.largest === true,`. Комментарий над `waterfall?` дописать: «Кусок S13: семь
ступеней от чистой выручки».

- [ ] **Шаг 5: убедиться, что зелёные**

```bash
npx vitest run __tests__/metrics/waterfall.test.ts
```
Ожидание: все проверки файла зелёные, включая последнюю — настоящим путём.

- [ ] **Шаг 6: проверки вида**

В `__tests__/screen/fixture.ts` у `ПЕРЕПИСЬ.waterfall` — семь ступеней с ключами из интерфейса,
значения различны и не выводятся из соседей; `largest` удалить. В
`__tests__/screen/waterfall-view.test.tsx` заменить две проверки:

```ts
test('у каждой ступени названа база доли — чистая выручка', () => {
  const разметка = сВодопадом(ПЕРЕПИСЬ.waterfall)
  expect(
    разметка.match(/<span class="waterfall-share">[^<]* % чистой выручки<\/span>/g)?.length,
  ).toBe(7)
  expect(разметка).not.toContain('% оборота')
})

test('итог и вычитание различены признаком в разметке, а не только цветом', () => {
  const разметка = сВодопадом(ПЕРЕПИСЬ.waterfall)
  expect(разметка.match(/data-kind="итог"/g)?.length).toBe(3)
  expect(разметка.match(/data-kind="вычитание"/g)?.length).toBe(4)
})

/** Деньги внутри предложения стоят с неразрывным пробелом перед знаком — так их печатает экран. */
const вСтроке = (сумма: string) => money(сумма).replace(/ €$/, ' €')

test('под каскадом — скидки и возвраты, снятые с оборота, тремя суммами отчёта', () => {
  const разметка = сВодопадом(ПЕРЕПИСЬ.waterfall)
  expect(разметка).toContain(
    `Скидки ${вСтроке(ПЕРЕПИСЬ.revenue.discounts)} и возвраты ${вСтроке(ПЕРЕПИСЬ.revenue.refunds)} сняты с оборота ${вСтроке(ПЕРЕПИСЬ.revenue.gross)} до этой шкалы.`,
  )
})

test('строки «съедает больше всего» нет', () => {
  expect(сВодопадом(ПЕРЕПИСЬ.waterfall)).not.toContain('Съедает больше всего')
})
```
(`money` ввозится из `@/lib/metrics/format`.)

Удалить `__tests__/screen/cost-shares-view.test.tsx` целиком.

- [ ] **Шаг 7: разметка**

В `app/page.tsx`:
- `ПОДПИСИ_СТУПЕНЕЙ` — ключи `net`, `cogs`, `ads`, `fees`, `margin_income: 'Маржинальный доход'`, `fixed`, `profit`; ключи `gross`, `discounts`, `refunds` удалить.
- в блоке водопада текст доли — `${percent(ступень.sharePct)} чистой выручки`;
- удалить вычисление `съедает`, `подписьСтроки` и оба абзаца `.waterfall-largest`;
- после `</ol>` каскада добавить:

```tsx
<p className="waterfall-gap">
  {`Скидки ${вместе(money(report.revenue.discounts))} и возвраты ${вместе(money(report.revenue.refunds))} сняты с оборота ${вместе(money(report.revenue.gross))} до этой шкалы.`}
</p>
```
- удалить функцию `ДоляСтатьи` и четыре её вызова в карточке «Затраты».

В `app/globals.css` удалить правила `.cost-share`, `.cost-bar`, `.cost-bar-fill`, `.waterfall-largest`
вместе с их комментариями.

- [ ] **Шаг 8: полный прогон**

```bash
npm test
```
```bash
code=$?; echo "exit=$code"
```
Ожидание: красна только перепись экрана (`census.test.tsx`) — список текста меняется. Поправить в
ней строки каскада «Главного» и «Денег» построчно и перечислить их в теле коммита. Затем снова
`npm test`, код 0.

- [ ] **Шаг 9: учёт и сломы**

В учёт — по строке на каждую заменённую проверку `waterfall.test.ts` и `waterfall-view.test.tsx`
и на каждую из шести проверок удалённого `cost-shares-view.test.tsx`, с колонками «что утверждала /
почему не верно / что теперь».

В `breaks/s13-one-screen.ts`:

```ts
  {
    id: 'waterfall-base-gross',
    claim: 'вернуть базу долей каскада к обороту',
    mustRedden: 'доли и края ступеней — от чистой выручки',
    file: 'lib/metrics/sql.ts',
    find: "select case when t.net::numeric > 0 then t.net::numeric end as denom,",
    replace: "select case when t.gross::numeric > 0 then t.gross::numeric end as denom,",
    alsoRedden: [
      { name: 'чистая выручка не положительна — долей и краёв нет, суммы стоят', why: 'при обороте больше нуля доли появляются и при нулевой чистой выручке' },
    ],
    tests: 'все',
  },
  {
    id: 'margin-income-own-expression',
    claim: 'посчитать маржинальный доход из прибыли и постоянных, а не разностью показанных сумм',
    mustRedden: 'маржинальный доход — разность показанных сумм',
    file: 'lib/metrics/sql.ts',
    find: '(5, \'margin_income\', \'итог\',      m.mi,             0::numeric,      m.mi),',
    replace: '(5, \'margin_income\', \'итог\',      t.profit::numeric + t.fixed::numeric, 0::numeric, m.mi),',
    tests: 'все',
  },
  {
    id: 'waterfall-share-says-gross',
    claim: 'назвать базу доли на экране оборотом',
    mustRedden: 'у каждой ступени названа база доли — чистая выручка',
    file: 'app/page.tsx',
    find: '`${percent(ступень.sharePct)} чистой выручки`',
    replace: '`${percent(ступень.sharePct)} оборота`',
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: ПЕРЕПИСЬ_ПРИЧИНА }],
    tests: 'все',
  },
```

Список «что краснеет заодно» уточняется **прогоном** в задаче 17, а не догадкой здесь; объявленное
«заодно» не доказывает своё утверждение и в отчёт как доказательство не идёт.

- [ ] **Шаг 10: коммит**

```bash
git add lib/metrics/sql.ts lib/metrics/report.ts app/page.tsx app/globals.css __tests__/metrics/waterfall.test.ts __tests__/screen/waterfall-view.test.tsx __tests__/screen/fixture.ts __tests__/screen/cost-shares-view.test.tsx __tests__/screen/census.test.tsx docs/сверка/учёт-проверок-s13.md breaks/s13-one-screen.ts
git commit -m "S13, задача 1: каскад от чистой выручки с маржинальным доходом"
```

---

### Задача 2 — признаки выводов

**Сегодня:** признаков нет; порог считается в `PAYBACK_FROM_TOTALS`; сравнение «окупается ли» не
делает никто.

**Станет:** запрос `MONTH_FINDINGS` отдаёт признак рекламы, маржинальный доход, долю постоянных в нём,
«убыток» и «приблизительная». Порог берётся **из** `PAYBACK_FROM_TOTALS`, второго определения нет.

**Файлы:**
- Изменить: `lib/metrics/sql.ts` — после `MONTH_PAYBACK`
- Изменить: `lib/metrics/report.ts` — тип, импорт, запрос, сборка
- Создать: `__tests__/metrics/findings.test.ts`
- Изменить: `__tests__/screen/fixture.ts` — поле `findings`

**Интерфейс:**
- Производит: `export const FINDINGS_FROM_TOTALS: string`, `export const MONTH_FINDINGS: string`.
- Производит в `MonthReport`:

```ts
  /**
   * Признаки выводов — кусок S13. Готовыми из SQL (`MONTH_FINDINGS`); экран выбирает слова по ним и
   * ничего не сравнивает. Необязательное по той же причине, что `устарели`.
   */
  findings?: {
    /** Пусто — рекламы за месяц нет, или окупаемость не посчитана, или нет оборота. */
    adsVerdict: 'окупается' | 'не окупается' | 'порога нет' | null
    marginIncome: Money
    /** Пусто, когда маржинальный доход не положителен. */
    fixedSharePct: Maybe
    loss: boolean
    approximate: boolean
  }
```

- [ ] **Шаг 1: проверки**

`__tests__/metrics/findings.test.ts`:

```ts
import { Pool } from 'pg'
import { afterAll, expect, test } from 'vitest'

import { projectDatabaseUrl } from '@/lib/db-url'
import { monthlyReport } from '@/lib/metrics/report'
import { FINDINGS_FROM_TOTALS } from '@/lib/metrics/sql'

/**
 * Признаки выводов — кусок S13. Строка итогов подставляется выдуманной, как у каскада: признак,
 * посчитанный не по своей колонке, даст ответ, которого раскладка не предполагает. Последняя проверка
 * идёт настоящим путём.
 */

const pool = new Pool({ connectionString: projectDatabaseUrl() })
afterAll(() => pool.end())

type Итоги = {
  gross: string; net: string; cogs: string; ads: string; fees: string; fixed: string
  profit: string; roas_by_gross: string | null; honest_pct: string | null
}
type Признаки = {
  ads_verdict: string | null; margin_income: string; fixed_share_pct: string | null
  loss: boolean; approximate: boolean
}

async function признаки(итоги: Итоги, естьРеклама = true): Promise<Признаки> {
  const колонки = Object.entries(итоги)
    .map(([имя, з]) => (з === null ? `null::text as ${имя}` : `'${з}'::text as ${имя}`))
    .join(', ')
  const { rows } = await pool.query(
    `with totals_row as (select ${колонки}),\n     cur_state as (select ${естьРеклама} as has_ads),\n${FINDINGS_FROM_TOTALS}`,
  )
  return rows[0] as Признаки
}

/** Вклад (900 − 400 − 100) ÷ 1000 = 40,0 %, порог 2,50; маржинальный доход 900 − 400 − 200 − 100 = 200. */
const БАЗА: Итоги = {
  gross: '1000.00', net: '900.00', cogs: '400.00', ads: '200.00', fees: '100.00',
  fixed: '150.00', profit: '50.00', roas_by_gross: '5.00', honest_pct: '100.0',
}

test('реклама: окупаемость, равная порогу, — «окупается»', async () => {
  expect((await признаки({ ...БАЗА, roas_by_gross: '2.50' })).ads_verdict).toBe('окупается')
})

test('реклама: окупаемость ниже порога — «не окупается»', async () => {
  expect((await признаки({ ...БАЗА, roas_by_gross: '2.49' })).ads_verdict).toBe('не окупается')
})

test('реклама: вклад не положителен — «порога нет»', async () => {
  expect((await признаки({ ...БАЗА, cogs: '850.00' })).ads_verdict).toBe('порога нет')
})

test('реклама: строк рекламы нет, окупаемость пуста или оборота нет — признака нет', async () => {
  expect((await признаки(БАЗА, false)).ads_verdict).toBeNull()
  expect((await признаки({ ...БАЗА, roas_by_gross: null })).ads_verdict).toBeNull()
  expect((await признаки({ ...БАЗА, gross: '0.00' })).ads_verdict).toBeNull()
})

test('маржинальный доход — из показанных сумм итогов', async () => {
  expect((await признаки(БАЗА)).margin_income).toBe('200.00')
})

test('убыток: ниже 100 % — нет, ровно 100 % показанных — да', async () => {
  const ниже = await признаки({ ...БАЗА, fixed: '199.00' })
  expect([ниже.fixed_share_pct, ниже.loss]).toEqual(['99.5', false])
  const ровно = await признаки({ ...БАЗА, fixed: '199.90' })
  expect([ровно.fixed_share_pct, ровно.loss]).toEqual(['100.0', true])
})

test('убыток: маржинальный доход не положителен — доли нет, признак стоит', async () => {
  const ноль = await признаки({ ...БАЗА, ads: '400.00' })
  expect([ноль.fixed_share_pct, ноль.loss]).toEqual([null, true])
})

test('приблизительная: ниже 100 % — да, ровно 100 % и без доли — нет', async () => {
  expect((await признаки({ ...БАЗА, honest_pct: '99.9' })).approximate).toBe(true)
  expect((await признаки({ ...БАЗА, honest_pct: '100.0' })).approximate).toBe(false)
  expect((await признаки({ ...БАЗА, honest_pct: null })).approximate).toBe(false)
})

test('признаки доезжают до отчёта настоящим путём', async () => {
  const отчёт = await monthlyReport()
  expect(отчёт.findings).toBeDefined()
  expect(typeof отчёт.findings?.loss).toBe('boolean')
})
```

Раскладка «вклад не положителен»: `cogs 850` даёт (900 − 850 − 100) ÷ 1000 = −5,0 %.

- [ ] **Шаг 2: убедиться, что краснеют**

```bash
npx vitest run __tests__/metrics/findings.test.ts
```
Ожидание: отказ ввоза — `FINDINGS_FROM_TOTALS` нет.

- [ ] **Шаг 3: запрос**

В `lib/metrics/sql.ts` после `MONTH_PAYBACK`:

```ts
/**
 * Признаки выводов — кусок S13. Экран выбирает по ним слова и ничего не сравнивает.
 *
 *   · реклама: «окупается» — окупаемость по обороту не ниже порога; «не окупается» — ниже;
 *     «порога нет» — вклад не положителен. Пусто — нет строк рекламы, окупаемость пуста (сумма
 *     рекламы ноль) или вклад не посчитан (нет оборота): сказать нечего. Порог — тот же, что у
 *     блока окупаемости: `PAYBACK_FROM_TOTALS` вложен целиком, второго выражения порога нет.
 *     Сравниваются показанные числа;
 *   · доля постоянных в маржинальном доходе — от показанных сумм; «убыток» — доля от 100 % или
 *     маржинальный доход не положителен (решение владельца Э5);
 *   · «приблизительная» — доля, посчитанная по настоящей цене, ниже 100 %.
 */
export const FINDINGS_FROM_TOTALS = `
payback_row as (with ${PAYBACK_FROM_TOTALS}),
finding_parts as (
  select ${МАРЖИНАЛЬНЫЙ_ДОХОД}      as mi,
         t.fixed::numeric         as fixed,
         t.roas_by_gross::numeric as roas,
         t.honest_pct::numeric    as honest
    from totals_row t
),
finding_shares as (
  select f.*, case when f.mi > 0 then round(f.fixed / f.mi * 100, 1) end as fixed_share
    from finding_parts f
)
select case when not cs.has_ads or s.roas is null or pb.contribution_pct is null then null
            when pb.breakeven_roas is null then 'порога нет'
            when s.roas >= pb.breakeven_roas::numeric then 'окупается'
            else 'не окупается'
       end                                        as ads_verdict,
       s.mi::text                                 as margin_income,
       s.fixed_share::text                        as fixed_share_pct,
       (s.mi <= 0 or s.fixed_share >= 100)        as loss,
       coalesce(s.honest < 100, false)            as approximate
  from finding_shares s
 cross join cur_state cs
 cross join payback_row pb
`

export const MONTH_FINDINGS = `
with totals_row as (${MONTH_TOTALS}),
cur_state as (${ЕСТЬ_РЕКЛАМА}),
${FINDINGS_FROM_TOTALS}`
```

- [ ] **Шаг 4: отчёт**

В `lib/metrics/report.ts`: ввезти `MONTH_FINDINGS`; тип из «Интерфейса»; в `прочитатьФакты` после
`deltasResult` — `const findingsResult = await client.query(MONTH_FINDINGS, [dayParam])`; в
возвращаемый объект:

```ts
      findings: {
        adsVerdict: (findingsResult.rows[0]?.ads_verdict ?? null) as
          | 'окупается'
          | 'не окупается'
          | 'порога нет'
          | null,
        marginIncome: findingsResult.rows[0]?.margin_income as string,
        fixedSharePct: (findingsResult.rows[0]?.fixed_share_pct ?? null) as string | null,
        loss: findingsResult.rows[0]?.loss === true,
        approximate: findingsResult.rows[0]?.approximate === true,
      },
```

В `__tests__/screen/fixture.ts` у `ПЕРЕПИСЬ` — `findings` со значениями, не выводимыми из соседей:
`{ adsVerdict: 'окупается', marginIncome: '3141.59', fixedSharePct: '64.2', loss: false, approximate: true }`.

- [ ] **Шаг 5: зелёные и полный прогон**

```bash
npx vitest run __tests__/metrics/findings.test.ts
```
Затем `npm test` и `code=$?`. Ожидание: код 0 — экран поля ещё не читает.

- [ ] **Шаг 6: сломы**

```ts
  {
    id: 'ads-verdict-strict',
    claim: 'считать окупаемость, равную порогу, неокупившейся',
    mustRedden: 'реклама: окупаемость, равная порогу, — «окупается»',
    file: 'lib/metrics/sql.ts',
    find: "when s.roas >= pb.breakeven_roas::numeric then 'окупается'",
    replace: "when s.roas > pb.breakeven_roas::numeric then 'окупается'",
    tests: 'все',
  },
  {
    id: 'ads-verdict-without-ads',
    claim: 'давать признак рекламы месяцу без строк рекламы',
    mustRedden: 'реклама: строк рекламы нет, окупаемость пуста или оборота нет — признака нет',
    file: 'lib/metrics/sql.ts',
    find: 'select case when not cs.has_ads or s.roas is null or pb.contribution_pct is null then null',
    replace: 'select case when s.roas is null or pb.contribution_pct is null then null',
    tests: 'все',
  },
  {
    id: 'loss-from-unrounded',
    claim: 'считать убыток от неокруглённой доли, а не от показанной',
    mustRedden: 'убыток: ниже 100 % — нет, ровно 100 % показанных — да',
    file: 'lib/metrics/sql.ts',
    find: '(s.mi <= 0 or s.fixed_share >= 100)        as loss,',
    replace: '(s.mi <= 0 or s.fixed / s.mi * 100 >= 100) as loss,',
    tests: 'все',
  },
  {
    id: 'loss-misses-nonpositive-income',
    claim: 'не ставить убыток при неположительном маржинальном доходе',
    mustRedden: 'убыток: маржинальный доход не положителен — доли нет, признак стоит',
    file: 'lib/metrics/sql.ts',
    find: '(s.mi <= 0 or s.fixed_share >= 100)        as loss,',
    replace: 'coalesce(s.fixed_share >= 100, false)     as loss,',
    tests: 'все',
  },
  {
    id: 'approximate-at-hundred',
    claim: 'называть прибыль приблизительной и при доле ровно 100 %',
    mustRedden: 'приблизительная: ниже 100 % — да, ровно 100 % и без доли — нет',
    file: 'lib/metrics/sql.ts',
    find: 'coalesce(s.honest < 100, false)            as approximate',
    replace: 'coalesce(s.honest <= 100, false)           as approximate',
    tests: 'все',
  },
```

Слом `loss-from-unrounded` на раскладке 199,90: неокруглённая доля 99,95 < 100 — признак пропадает,
проверка краснеет. Два слома одной строки — по одному на каждую половину условия.

- [ ] **Шаг 7: коммит**

```bash
git add lib/metrics/sql.ts lib/metrics/report.ts __tests__/metrics/findings.test.ts __tests__/screen/fixture.ts breaks/s13-one-screen.ts
git commit -m "S13, задача 2: признаки выводов из запроса"
```

---

### Задача 3 — средняя по дням

**Сегодня:** ряд отдаёт дни, доли и края; средней нет.

**Станет:** `DAILY_FROM_MONEY` отдаёт среднюю (чистая выручка ÷ все дни месяца, до цента), её место на
шкале, подпись базы готовой строкой и признак пустых дней.

**Файлы:**
- Изменить: `lib/metrics/sql.ts` — `DAILY_FROM_MONEY` (строки 691–732)
- Изменить: `lib/metrics/report.ts` — тип `daily`, сборка
- Изменить: `__tests__/metrics/daily.test.ts` — **только дописать** проверки и поля типа `День`
- Изменить: `__tests__/screen/fixture.ts`

**Интерфейс — в типе `daily` дописать:**

```ts
    /**
     * Средняя по дням — кусок S13, решение владельца Э7: чистая выручка месяца ÷ все дни месяца, до
     * цента. Дни без заказов входят в делитель. Пусто — в месяце нет заказов.
     */
    avgNet?: Maybe
    /** Место средней на шкале ряда — тем же способом, что края столбиков. */
    avgPct?: Maybe
    /** Подпись базы готовой строкой: «по 31 дню месяца», «по 30 дням месяца». */
    avgBase?: string
    /** В ряду есть дни без заказов. */
    hasEmptyDays?: boolean
```

- [ ] **Шаг 1: дописать проверки**

В `__tests__/metrics/daily.test.ts` у типа `День` дописать
`avg_net: string | null; avg_pct: string | null; avg_base: string; has_empty_days: boolean`; в конец
файла, перед проверкой настоящим путём:

```ts
test('средняя — чистая выручка ÷ все дни месяца, дни без заказов в делителе', async () => {
  const дни = await ряд('2026-04', [
    { день: '2026-04-01', оборот: '1.00', скидка: '0.00', возврат: '0.00', выручка: '100.00' },
    { день: '2026-04-03', оборот: '1.00', скидка: '0.00', возврат: '0.00', выручка: '200.01' },
  ])
  expect(дни[0].avg_net).toBe('10.00')
  expect(дни[0].avg_pct).toBe('5.0')
  expect(дни[0].avg_base).toBe('по 30 дням месяца')
  expect(дни[0].has_empty_days).toBe(true)
})

test('подпись базы средней согласована с числом дней', async () => {
  const день = { оборот: '1.00', скидка: '0.00', возврат: '0.00', выручка: '5.00' }
  expect((await ряд('2026-03', [{ ...день, день: '2026-03-02' }]))[0].avg_base).toBe('по 31 дню месяца')
  expect((await ряд('2026-02', [{ ...день, день: '2026-02-02' }]))[0].avg_base).toBe('по 28 дням месяца')
})

test('месяц без заказов — средней нет', async () => {
  const дни = await ряд('2026-04', [])
  expect([дни[0].avg_net, дни[0].avg_pct]).toEqual([null, null])
})
```

(300,01 ÷ 30 = 10,0003 → 10,00; 10,00 ÷ 200,01 × 100 = 4,9998 → 5,0.)

- [ ] **Шаг 2: краснеют** — `npx vitest run __tests__/metrics/daily.test.ts`; ожидание: `avg_net` не определено.

- [ ] **Шаг 3: запрос**

В `DAILY_FROM_MONEY` после CTE `peak` добавить:

```sql
mean as (
  select count(*)::int                                   as days_in_month,
         count(*) filter (where bd.day is null) > 0      as has_empty_days,
         round(sum(bd.net) / count(*), 2)                as avg_net
    from days d
    left join by_day bd on bd.day = d.day
),
```

В итоговый `select` перед `p.has_orders` дописать:

```sql
       m.avg_net::text                                                           as avg_net,
       round(m.avg_net / p.top * 100, 1)::text                                   as avg_pct,
       'по ' || m.days_in_month ||
         case when m.days_in_month % 10 = 1 and m.days_in_month % 100 <> 11
              then ' дню' else ' дням' end || ' месяца'                          as avg_base,
       m.has_empty_days                                                          as has_empty_days,
```

и `cross join mean m` после `cross join peak p`. Комментарий над запросом дописать абзацем про Э7.

- [ ] **Шаг 4: отчёт** — в сборке `daily` дописать:

```ts
        avgNet: (dailyResult.rows[0]?.avg_net ?? null) as string | null,
        avgPct: (dailyResult.rows[0]?.avg_pct ?? null) as string | null,
        avgBase: dailyResult.rows[0]?.avg_base as string,
        hasEmptyDays: dailyResult.rows[0]?.has_empty_days === true,
```

В `__tests__/screen/fixture.ts` у `ПЕРЕПИСЬ.daily`: `avgNet: '271.83'`, `avgPct: '41.9'`,
`avgBase: 'по 31 дню месяца'`, `hasEmptyDays: true`.

- [ ] **Шаг 5: зелёные** — файл, затем `npm test`, `code=$?`, ожидание 0.

- [ ] **Шаг 6: доказать, что прежние проверки файла не тронуты**

```bash
git diff --stat __tests__/metrics/daily.test.ts
```
```bash
git diff __tests__/metrics/daily.test.ts | grep -c '^-[^-]'
```
Ожидание второй команды: `0`. Иное — правка принятой проверки, в учёт.

- [ ] **Шаг 7: сломы**

```ts
  {
    id: 'mean-over-order-days',
    claim: 'делить на дни с заказами, а не на все дни месяца',
    mustRedden: 'средняя — чистая выручка ÷ все дни месяца, дни без заказов в делителе',
    file: 'lib/metrics/sql.ts',
    find: 'round(sum(bd.net) / count(*), 2)                as avg_net',
    replace: 'round(sum(bd.net) / count(bd.day), 2)          as avg_net',
    tests: 'все',
  },
  {
    id: 'mean-base-word',
    claim: 'писать «дням» при числе дней, оканчивающемся на единицу',
    mustRedden: 'подпись базы средней согласована с числом дней',
    file: 'lib/metrics/sql.ts',
    find: "then ' дню' else ' дням' end || ' месяца'",
    replace: "then ' дням' else ' дням' end || ' месяца'",
    tests: 'все',
  },
```

- [ ] **Шаг 8: коммит** — пути `lib/metrics/sql.ts lib/metrics/report.ts __tests__/metrics/daily.test.ts __tests__/screen/fixture.ts breaks/s13-one-screen.ts`, сообщение «S13, задача 3: средняя по дням по всем дням месяца».

---

### Задача 4 — отмена порядка по адресу (решение Ф2 куска S12) и первая пятёрка

**Отмена прежнего решения.** 14 сентября 2026 года владелец решил (S12, Ф2): порядок таблицы товаров
живёт в адресе, шесть порядков, незнакомый — отказ. 16 сентября 2026 года он решил (Э4): видна первая
пятёрка по валовой прибыли, остальные раскрываются. Причина отмены: «первая пятёрка» при другом
порядке смысла не имеет. Прежний план и договор S12 не переписываются.

**Файлы:**
- Изменить: `lib/metrics/sql.ts` — удалить `ПОРЯДКИ_ТОВАРОВ`, `ПорядокТоваров`, `ПОРЯДОК_ТОВАРОВ_ПО_УМОЛЧАНИЮ`, `запросТоваров`; `MONTH_ITEMS` — один порядок; `ITEMS_FROM_ROWS` — `in_top`
- Изменить: `lib/metrics/report.ts` — `monthlyReport(month, deps)` без третьего довода; поле `inTop`
- Изменить: `app/page.tsx` — удалить `СтолбецПорядка`, `адресПорядка`, `ЗаголовокПорядка`, `ОтказПорядка`, `ПОДПИСИ_ПОРЯДКОВ`, довод `sort`, чтение `params.sort`; заголовки таблицы — обычные `<th scope="col">`
- Изменить: `__tests__/metrics/items-order.test.ts` (переписать), `__tests__/metrics/items-extra.test.ts` (дописать), `__tests__/metrics/totals-fixture.ts`
- Удалить: `__tests__/screen/items-order.test.tsx`, `__tests__/screen/sort-page.test.tsx`

**Интерфейс:** `monthlyReport(month?: string, deps?: Partial<MetricsDeps>): Promise<MonthReport>`;
`items[number].inTop?: boolean`.

- [ ] **Шаг 1: переписать `__tests__/metrics/items-order.test.ts`**

```ts
test('порядок товаров один — валовая прибыль по убыванию, при равенстве по артикулу', () => {
  expect(MONTH_ITEMS.trimEnd().endsWith('order by sum(net) - sum(cogs) desc, sku')).toBe(true)
})
```
плюс прежняя проверка «при равных значениях столбца порядок решается артикулом» на `MONTH_ITEMS`
настоящим запросом к местной базе — её тело не меняется, меняется только вызов запроса.

В `__tests__/metrics/items-extra.test.ts` дописать:

```ts
test('первая пятёрка — пять лучших по прибыли, при равенстве по артикулу', async () => {
  // Равенство прибыли приходится ровно на пятую и шестую строку: решает артикул, NP-E раньше NP-F.
  const все = await колонки([
    { sku: 'NP-F', net: '10.00', profit: '5.00' },
    { sku: 'NP-A', net: '10.00', profit: '9.00' },
    { sku: 'NP-C', net: '10.00', profit: '7.00' },
    { sku: 'NP-B', net: '10.00', profit: '8.00' },
    { sku: 'NP-E', net: '10.00', profit: '5.00' },
    { sku: 'NP-D', net: '10.00', profit: '6.00' },
  ])
  const пятёрка = все.filter((к) => к.in_top).map((к) => к.sku).sort()
  expect(пятёрка).toEqual(['NP-A', 'NP-B', 'NP-C', 'NP-D', 'NP-E'])
})

test('при пяти строках и меньше в пятёрке все', async () => {
  const все = await колонки(ЧЕТЫРЕ)
  expect(все.every((к) => к.in_top)).toBe(true)
})
```
(у типа `Колонки` дописать `in_top: boolean`).

- [ ] **Шаг 2: краснеют** — `npx vitest run __tests__/metrics/items-order.test.ts __tests__/metrics/items-extra.test.ts`.

- [ ] **Шаг 3: запрос**

```ts
/**
 * Таблица товаров в единственном порядке — кусок S13, решение владельца Э4: валовая прибыль по
 * убыванию, при равенстве — по артикулу. Шесть порядков адреса куска S12 (Ф2) отменены.
 */
export const MONTH_ITEMS = `${ТОВАРЫ_БЕЗ_ПОРЯДКА} order by sum(net) - sum(cogs) desc, sku\n`
```

В `ITEMS_FROM_ROWS` в итоговый `select` дописать:

```sql
       (row_number() over (order by k.profit desc, k.sku) <= 5)              as in_top,
```

- [ ] **Шаг 4: отчёт и страница**

`monthlyReport`: удалить третий довод и его комментарий; `client.query(MONTH_ITEMS, [dayParam])`;
в `items.map` добавить `inTop: itemsExtra.get(item.sku)?.in_top === true,`.

`app/page.tsx`: удалить перечисленные функции и константы; у `Dashboard` удалить довод `sort` и
`поПрибыли` (хвост строки над таблицей «— считая по убыванию прибыли…» уходит); в `HomePage` удалить
чтение `sortParam`, проверку порядка и `ОтказПорядка`; вызов — `monthlyReport(monthParam)`.

- [ ] **Шаг 5: удалить `__tests__/screen/items-order.test.tsx` и `__tests__/screen/sort-page.test.tsx`**; в
`__tests__/metrics/totals-fixture.ts` заменить ввоз `ПОРЯДОК_ТОВАРОВ_ПО_УМОЛЧАНИЮ` на `MONTH_ITEMS`.

- [ ] **Шаг 6: полный прогон** — `npm test`, `code=$?`; перепись экрана поправить построчно (ушли
ссылки заголовков — текст тот же; ушёл хвост про порядок); затем код 0.

- [ ] **Шаг 7: учёт** — 4 проверки `metrics/items-order.test.ts`, 5 — `screen/items-order.test.tsx`,
6 — `screen/sort-page.test.tsx`, каждая строкой; причина у всех — отмена Ф2 решением Э4.

- [ ] **Шаг 8: сломы**

```ts
  {
    id: 'top-is-six',
    claim: 'класть в первую пятёрку шесть строк',
    mustRedden: 'первая пятёрка — пять лучших по прибыли',
    file: 'lib/metrics/sql.ts',
    find: '(row_number() over (order by k.profit desc, k.sku) <= 5)',
    replace: '(row_number() over (order by k.profit desc, k.sku) <= 6)',
    tests: 'все',
  },
  {
    id: 'top-ties-by-chance',
    claim: 'решать равенство прибыли в пятёрке не артикулом',
    mustRedden: 'первая пятёрка — пять лучших по прибыли',
    file: 'lib/metrics/sql.ts',
    find: '(row_number() over (order by k.profit desc, k.sku) <= 5)',
    replace: '(row_number() over (order by k.profit desc, k.sku desc) <= 5)',
    tests: 'все',
  },
  {
    id: 'items-order-by-net',
    claim: 'упорядочить товары по чистой выручке',
    mustRedden: 'порядок товаров один — валовая прибыль по убыванию',
    file: 'lib/metrics/sql.ts',
    find: 'order by sum(net) - sum(cogs) desc, sku\\n`',
    replace: 'order by sum(net) desc, sku\\n`',
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: 'раскладка переписи идёт через отчёт, а не через запрос; строка объявлена на случай, если прогон покажет иное' }],
    tests: 'все',
  },
```
(Слом `top-ties-by-chance`: при `sku desc` из двух артикулов с прибылью 5,00 пятым встаёт NP-F, и
пятёрка становится A, B, C, D, F — проверка краснеет. Слом `top-is-six` добавляет в пятёрку NP-F.)

- [ ] **Шаг 9: коммит** — все пути задачи поимённо, сообщение «S13, задача 4: отмена порядка по адресу (Ф2 S12), первая пятёрка».

---

### Задача 5 — признак дыр у видов неполноты

**Файлы:** `lib/metrics/sql.ts` (`MONTH_GAPS`, строка `select kind, count, at`), `lib/metrics/report.ts`,
`__tests__/metrics/report.test.ts` (дописать), `__tests__/screen/fixture.ts`.

**Интерфейс:** `gaps[number].hasHoles?: boolean`.

- [ ] **Шаг 1: дописать проверку** в `__tests__/metrics/report.test.ts`:

```ts
test('у каждого вида неполноты признак «есть дыры» совпадает с ненулевым счётом', async () => {
  const отчёт = await monthlyReport()
  expect(отчёт.gaps).toHaveLength(11)
  for (const вид of отчёт.gaps) expect(вид.hasHoles).toBe(вид.count > 0)
})
```
и вторую — на подставленной строке, где дыра есть (взять раскладку файла, дающую ненулевой вид
«строки продаж без цены поставщика», и утверждать `hasHoles === true` у этого вида). Счёт `count > 0`
живёт в проверке, а не на экране.

- [ ] **Шаг 2: краснеют.** **Шаг 3:** `select kind, count, at, count > 0 as has_holes`. **Шаг 4:** в сборке `hasHoles: row.has_holes === true`; в раскладке переписи — у каждого из 11 видов свой признак, ровно у двух — `true`.

- [ ] **Шаг 5: зелёные и полный прогон**, `code=$?`, 0. **Шаг 6: доказать дописывание** — `git diff __tests__/metrics/report.test.ts | grep -c '^-[^-]'` → `0`.

- [ ] **Шаг 7: слом**

```ts
  {
    id: 'holes-on-zero',
    claim: 'ставить признак дыр и нулевому виду',
    mustRedden: 'у каждого вида неполноты признак «есть дыры» совпадает с ненулевым счётом',
    file: 'lib/metrics/sql.ts',
    find: 'select kind, count, at, count > 0 as has_holes',
    replace: 'select kind, count, at, count >= 0 as has_holes',
    tests: 'все',
  },
```

- [ ] **Шаг 8: коммит** — «S13, задача 5: признак дыр у видов неполноты».

---

### Задача 6 — полоса показателей без доли рекламы

**Файлы:** `lib/metrics/sql.ts` (`DELTAS_FROM_ROWS`, `MONTH_DELTAS`), `__tests__/metrics/deltas.test.ts`,
`__tests__/screen/kpis-view.test.tsx`, `__tests__/screen/fixture.ts`, `app/page.tsx` (`ПОДПИСИ_ПОКАЗАТЕЛЕЙ`).

- [ ] **Шаг 1: переписать проверки.** В `deltas.test.ts` проверки, называющие `ad_share` или «доля
рекламы», удаляются под учёт; дописывается:

```ts
test('показателей в полосе три: прибыль, маржа, чистая выручка', async () => {
  const отчёт = await monthlyReport()
  expect(отчёт.kpis?.items.map((п) => п.key)).toEqual(['profit', 'margin', 'net'])
})
```
В `kpis-view.test.tsx` проверки, где названо «четырёх показателей» или «доли рекламы», — под учёт,
с заменой «четырёх» на «трёх» и удалением утверждений про долю рекламы.

- [ ] **Шаг 2: краснеют. Шаг 3:** из `values` удалить строку `(4, 'ad_share', …)` целиком; удалить
`cross join cur_state cs`, CTE `cur_state` в `MONTH_DELTAS` и `has_ads` в `prev_state`; комментарий
«доля рекламы — нет» дописать абзацем «кусок S13: доли рекламы в полосе нет».

- [ ] **Шаг 4:** из `ПОДПИСИ_ПОКАЗАТЕЛЕЙ` удалить `ad_share`; из раскладки переписи — четвёртый показатель.

- [ ] **Шаг 5: полный прогон**, перепись построчно, `code=$?` → 0. **Шаг 6: учёт.**

- [ ] **Шаг 7: слом**

```ts
  {
    id: 'kpis-keep-ad-share',
    claim: 'вернуть долю рекламы в полосу показателей',
    mustRedden: 'показателей в полосе три',
    file: 'lib/metrics/sql.ts',
    find: "     (3, 'net',      'eur', true,  c.net,        c.net::numeric,        p.net::numeric)",
    replace: "     (3, 'net',      'eur', true,  c.net,        c.net::numeric,        p.net::numeric),\n     (4, 'ad_share', 'pp',  false, null::text, null::numeric, null::numeric)",
    tests: 'все',
  },
```

- [ ] **Шаг 8: коммит** — «S13, задача 6: полоса показателей без доли рекламы».

---

### Задача 7 — вывод команды метрик

**Сегодня:** 50 строк; новых величин нет.

**Станет:** прежние 50 строк не меняются; в конце — два блока.

**Файлы:** `scripts/print-metrics.ts`, `__tests__/metrics/command.test.ts` (дописать).

- [ ] **Шаг 1: дописать проверку** в `command.test.ts` — тем же приёмом, каким файл уже подставляет
отчёт в `printMetrics`:

```ts
  test('в конце вывода — выводы и средняя по дням, прежние строки на своих местах', async () => {
  const строки: string[] = []
  const сВыводами: MonthReport = {
    ...ОТЧЁТ,
    findings: { adsVerdict: 'окупается', marginIncome: '6291.44', fixedSharePct: '72.4', loss: false, approximate: true },
    daily: {
      days: [], scaleLowPct: null, scaleHighPct: null, topNet: null, bottomNet: null, hasOrders: true,
      avgNet: '557.32', avgPct: null, avgBase: 'по 31 дню месяца', hasEmptyDays: false,
    },
  }
  await printMetrics(['2026-03'], { announce: (l) => строки.push(l), report: async () => сВыводами })
  expect(строки.slice(-10)).toEqual([
    '',
    'выводы',
    '  маржинальный доход: 6 291,44 €',
    '  постоянные расходы в маржинальном доходе: 72,4 %',
    '  реклама: окупается',
    '  месяц в убытке: нет',
    '  прибыль приблизительная: да',
    '',
    'чистая выручка по дням',
    '  средняя по 31 дню месяца: 557,32 €',
  ])
})
```
Проверка ставится внутрь существующего `describe` файла; `ОТЧЁТ` и `printMetrics` уже ввезены. Прежние
проверки файла подставляют `ОТЧЁТ` без `findings` и `daily`, поэтому новых строк у них в выводе нет.

- [ ] **Шаг 2: краснеет. Шаг 3:** в конец `printReport`:

```ts
  if (report.findings !== undefined) {
    const f = report.findings
    announce('')
    announce('выводы')
    announce(`  маржинальный доход: ${money(f.marginIncome)}`)
    announce(`  постоянные расходы в маржинальном доходе: ${percent(f.fixedSharePct)}`)
    announce(`  реклама: ${f.adsVerdict ?? 'признака нет'}`)
    announce(`  месяц в убытке: ${f.loss ? 'да' : 'нет'}`)
    announce(`  прибыль приблизительная: ${f.approximate ? 'да' : 'нет'}`)
  }
  if (report.daily?.avgNet !== undefined) {
    announce('')
    announce('чистая выручка по дням')
    announce(`  средняя ${report.daily.avgBase}: ${moneyMaybe(report.daily.avgNet)}`)
  }
```

- [ ] **Шаг 4: зелёные, полный прогон**, `code=$?` → 0. **Шаг 5:** `git diff __tests__/metrics/command.test.ts | grep -c '^-[^-]'` → `0`.

- [ ] **Шаг 6: живой «после» и сверка**

Повторить шаги 1–2 задачи 0 в файл `docs/сверка/метрики-2026-03-после-s13.txt`, затем:

```bash
diff docs/сверка/метрики-2026-03-до-s13.txt docs/сверка/метрики-2026-03-после-s13.txt
```
Ожидание: дифф — только добавленные в конце строки, ни одной удалённой. В
`docs/сверка/числа-марта-s13.md` — таблица новых чисел марта **из этого вывода** рядом с «нашим
счётом» договора (6 291,44 € / 72,4 % / окупается / нет / да / 557,32 €); расхождение называется.
Затем `npm run db:reset`, `code=$?`.

- [ ] **Шаг 7: слом**

```ts
  {
    id: 'command-verdict-swapped',
    claim: 'печатать «месяц в убытке: да» у месяца в плюсе',
    mustRedden: 'в конце вывода — выводы и средняя по дням',
    file: 'scripts/print-metrics.ts',
    find: "announce(`  месяц в убытке: ${f.loss ? 'да' : 'нет'}`)",
    replace: "announce(`  месяц в убытке: ${f.loss ? 'нет' : 'да'}`)",
    tests: 'все',
  },
```

- [ ] **Шаг 8: коммит** — «S13, задача 7: выводы и средняя в выводе команды; живая сверка марта».

---

### Задача 8 — отмена вкладок (решение куска S11): один экран

**Отмена прежнего решения.** В куске S11 (шаг 8, решения В2–В4) отчёт разложен на четыре вкладки,
незнакомая вкладка — отказ. 16 сентября 2026 года владелец решил (Э2): «Один экран, который показывает,
сколько магазин заработал на самом деле за выбранный месяц». Прежний план и договор S11 не
переписываются.

**Станет:** `Dashboard({ report })` рисует всё на одной странице в порядке договора: шапка → результат
(пока — нынешняя полоса показателей) → выводы (задача 10) → каскад → по дням → товары → качество.
Карточек «Выручка», «Затраты», «Итог», блока окупаемости и полосы вкладок нет. `?tab=` и `?sort=`
не читаются.

**Файлы:**
- Изменить: `app/page.tsx` — удалить `ВКЛАДКИ`, `Вкладка`, `ПОДПИСИ_ВКЛАДОК`, `Раздел`, `ДваВРяд`, `ПолосаВкладок`, `ОтказВкладки`, `ФОРМА_МЕСЯЦА` (если после удаления не читается нигде, кроме ветки наследства S6 — оставить), разделы `revenue`, `costs`, `bottom-line`, `payback`; довод `tab`; в `HomePage` — чтение `tabParam` и отказ вкладки; тип `searchParams` — `Promise<{ m?: string | string[] }>`
- Изменить: `app/globals.css` — удалить правила `.tabs`, `.row-two`, `.revenue`, `.costs`, `.bottom-line`, `.payback`, `.payback-main`, `.payback-note` с комментариями
- Удалить: `__tests__/screen/tabs-page.test.tsx`, `__tests__/screen/tabs-view.test.tsx`, `__tests__/screen/two-in-a-row.test.tsx`, `__tests__/screen/payback-view.test.tsx`, `__tests__/screen/payback-labels.test.tsx`
- Создать: `__tests__/screen/one-screen.test.tsx`, `__tests__/screen/old-addresses.test.tsx`
- Изменить: `__tests__/screen/census.test.tsx` (одна перепись вместо четырёх), все файлы с `tab=` из «Что снято»: `__tests__/metrics/screen.test.tsx`, `honesty-bar`, `items-scroll`, `items-view`, `no-break-units`, `substituted-cost`

- [ ] **Шаг 1: проверки одного экрана** — `__tests__/screen/one-screen.test.tsx`:

```tsx
import { renderToStaticMarkup } from 'react-dom/server'
import { expect, test } from 'vitest'

import { Dashboard } from '@/app/page'
import { ПЕРЕПИСЬ } from './fixture.ts'

/**
 * Один экран — кусок S13, решение владельца Э2. Все блоки стоят на одной странице в порядке
 * договора, и полосы вкладок нет.
 */

const разметка = () => renderToStaticMarkup(<Dashboard report={ПЕРЕПИСЬ} />)

test('все блоки на одной странице, в порядке договора', () => {
  const html = разметка()
  const места = ['Куда ушли деньги', 'Чистая выручка по дням', 'Товары', 'Качество данных'].map(
    (заголовок) => html.indexOf(`>${заголовок}</h2>`),
  )
  expect(места.every((место) => место > 0)).toBe(true)
  expect([...места].sort((а, б) => а - б)).toEqual(места)
})

test('полосы вкладок и ссылок на вкладки нет', () => {
  const html = разметка()
  expect(html).not.toContain('Разделы отчёта')
  expect(html).not.toContain('tab=')
})

test('карточек «Выручка», «Затраты», «Итог» и блока окупаемости нет', () => {
  const html = разметка()
  for (const заголовок of ['Выручка', 'Затраты', 'Итог', 'Окупаемость рекламы']) {
    expect(html).not.toContain(`>${заголовок}</h2>`)
  }
})
```

Заголовок блока качества — `Качество данных` (задача 13 объединит «Честность» и «Неполноту»; до неё
в этой задаче блок `honesty` получает `<h2>Качество данных</h2>`, а `gaps` — `<h3>Неполнота данных</h3>`).

`__tests__/screen/old-addresses.test.tsx` — подставки из удаляемого `tabs-page.test.tsx` (cookie,
отчёт, кнопка, переход) переносятся **без изменений**, затем:

```tsx
const { default: HomePage } = await import('@/app/page')

test('старые адреса со вкладкой и порядком открывают тот же единственный экран', async () => {
  cookieЗапроса = начеканить(Date.now(), СЕКРЕТ).value
  const обычный = renderToStaticMarkup(await HomePage({ searchParams: Promise.resolve({ m: '2026-03' }) }))
  for (const хвост of [{ tab: 'dengi' }, { tab: 'boom' }, { sort: 'net-asc' }, { tab: 'tovary', sort: 'boom' }]) {
    const старый = renderToStaticMarkup(
      await HomePage({ searchParams: Promise.resolve({ m: '2026-03', ...хвост }) as Promise<{ m?: string }> }),
    )
    expect(старый).toBe(обычный)
    expect(старый).not.toContain('role="alert"')
  }
})
```
(`СЕКРЕТ` и `cookieЗапроса` — из перенесённых подставок; секрет заведомо ненастоящий, как в удаляемом файле.)

- [ ] **Шаг 2: краснеют** — `npx vitest run __tests__/screen/one-screen.test.tsx __tests__/screen/old-addresses.test.tsx`.

- [ ] **Шаг 3: разметка.** Удалить перечисленное. `Dashboard` получает один довод `{ report }`; каждое
`<Раздел вкладка="…" текущая={tab} className="X" …>` заменить на `<section className="X" …>`;
`{tab === 'glavnoe' && report.daily …}` — на `{report.daily …}`. Отступ строк внутри разделов не
трогать: их цитируют сломы прошлых кусков (перевод — задача 14).

- [ ] **Шаг 4: перепись** — в `census.test.tsx` одна проверка на `<Dashboard report={ПЕРЕПИСЬ} />`
со списком из четырёх прежних, склеенных в порядке договора, без повторов шапки и без текста
удалённых карточек. Во всех файлах из списка «Изменить» убрать довод `tab=` у `Dashboard`.

- [ ] **Шаг 5: полный прогон**, `code=$?` → 0.

- [ ] **Шаг 6: учёт** — 6 + 9 + 3 + 6 + 4 проверок удалённых файлов и 2 проверки переписи, каждая строкой;
причина — отмена вкладок решением Э2.

- [ ] **Шаг 7: сломы**

```ts
  {
    id: 'tabs-come-back',
    claim: 'вернуть чтение вкладки из адреса и отказ на незнакомую',
    mustRedden: 'старые адреса со вкладкой и порядком открывают тот же единственный экран',
    file: 'app/page.tsx',
    find: "if ((await проверитьДоступ()) === 'отказать') redirect('/login')",
    replace: "if ((await проверитьДоступ()) === 'отказать') redirect('/login')\n  { const п = await searchParams as { tab?: string }; if (п.tab !== undefined) return <main><p role=\"alert\">Раздела «{п.tab}» в отчёте нет.</p></main> }",
    tests: 'все',
  },
  {
    id: 'money-cards-come-back',
    claim: 'вернуть карточку «Итог»',
    mustRedden: 'карточек «Выручка», «Затраты», «Итог» и блока окупаемости нет',
    file: 'app/page.tsx',
    find: '<section className="block waterfall">',
    replace: '<section className="block bottom-line"><h2>Итог</h2></section>\n      <section className="block waterfall">',
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: ПЕРЕПИСЬ_ПРИЧИНА }],
    tests: 'все',
  },
```

- [ ] **Шаг 8: коммит** — все пути поимённо, «S13, задача 8: отмена вкладок (S11), один экран».

---

### Задача 9 — результат

**Станет:** блок `result` вместо `kpis`: прибыль — единственное число кегля `--fontSizeHero700`;
чистая выручка, маржа, окупаемость — `--fontSizeBase400` полужирным; у окупаемости — слово признака и
порог; под прибылью — доля по настоящей цене со ссылкой `#kachestvo` и, при признаке, пометка
«приблизительно»; дельты у трёх чисел, а без базы — одна строка на блок.

**Файлы:** `app/page.tsx` (вместо раздела `kpis`), `app/globals.css` (правила `.kpi*` → `.result*`),
`__tests__/screen/kpis-view.test.tsx` → переименовать в `__tests__/screen/result-view.test.tsx` и
переписать под учёт, `__tests__/screen/census.test.tsx`.

- [ ] **Шаг 1: проверки** (новые, в `result-view.test.tsx`; сохраняемые из `kpis-view` — перенос без правки тела):

```tsx
import { readFileSync } from 'node:fs'
import { join } from 'node:path'
import { renderToStaticMarkup } from 'react-dom/server'
import { expect, test } from 'vitest'

import { Dashboard } from '@/app/page'
import { percent, ratio } from '@/lib/metrics/format'
import type { MonthReport } from '@/lib/metrics/report'
import { ПЕРЕПИСЬ } from './fixture.ts'

function сОтчётом(правка: Partial<MonthReport>): string {
  return renderToStaticMarkup(<Dashboard report={{ ...ПЕРЕПИСЬ, ...правка }} />)
}

test('прибыль — единственное число крупного кегля', () => {
  const html = сОтчётом({})
  expect(html.match(/class="result-profit"/g)?.length).toBe(1)
  const стили = readFileSync(join(process.cwd(), 'app', 'globals.css'), 'utf8')
  expect(стили.match(/font-size:\s*var\(--fontSizeHero700\)/g)?.length).toBe(1)
})

test('нет базы — одна строка на блок, а не по строке на число', () => {
  const html = сОтчётом({ kpis: { ...ПЕРЕПИСЬ.kpis!, hasBase: false, prevMonth: '2026-02' } })
  expect(html.match(/заказов нет/g)?.length).toBe(1)
  expect(html).toContain('Сравнить с 2026-02 нельзя: в 2026-02 заказов нет.')
})

test('у окупаемости слово признака и порог; признака нет — слова нет', () => {
  const есть = сОтчётом({})
  expect(есть).toContain(`окупается · порог ${ratio(ПЕРЕПИСЬ.payback!.breakevenRoas)}`)
  const нет = сОтчётом({ findings: { ...ПЕРЕПИСЬ.findings!, adsVerdict: null } })
  expect(нет).not.toContain('· порог')
})

test('слово признака рекламы берётся из признака, а не из сравнения чисел', () => {
  const html = сОтчётом({ findings: { ...ПЕРЕПИСЬ.findings!, adsVerdict: 'не окупается' } })
  expect(html).toContain('не окупается · порог')
})

test('под прибылью — доля по настоящей цене со ссылкой на блок качества', () => {
  const html = сОтчётом({})
  expect(html).toContain(`Посчитано по настоящей цене поставщика: ${percent(ПЕРЕПИСЬ.honesty.sharePct)}`)
  expect(html).toContain('href="#kachestvo"')
})

test('пометка «приблизительно» — только по признаку', () => {
  expect(сОтчётом({})).toContain('приблизительно')
  expect(сОтчётом({ findings: { ...ПЕРЕПИСЬ.findings!, approximate: false } })).not.toContain('приблизительно')
})
```

- [ ] **Шаг 2: краснеют. Шаг 3: разметка** — вместо раздела `kpis`:

```tsx
      <section className="block result" aria-label="Результат месяца">
        <div className="result-main">
          <h2>Прибыль</h2>
          <p className="result-profit">{money(report.bottom.profit)}</p>
          <p className="result-accuracy">
            {report.findings?.approximate === true && <span className="result-approx">приблизительно</span>}
            {`Посчитано по настоящей цене поставщика: ${вместе(percent(report.honesty.sharePct))} · `}
            <a href="#kachestvo">что подставлено</a>
          </p>
        </div>
        <dl className="result-stats">
          <dt>Чистая выручка</dt>
          <dd>{money(report.revenue.net)}{строкаПоказателя(полоса, 'net')}</dd>
          <dt>Маржа от чистой выручки</dt>
          <dd>{percent(report.bottom.marginPct)}{строкаПоказателя(полоса, 'margin')}</dd>
          <dt>Окупаемость рекламы по обороту</dt>
          <dd>
            {ratio(report.bottom.roasByGross)}
            {report.findings?.adsVerdict != null && report.payback !== undefined && (
              <span className="result-verdict" data-verdict={report.findings.adsVerdict}>
                {report.findings.adsVerdict === 'порога нет'
                  ? 'порога нет'
                  : `${report.findings.adsVerdict} · порог ${ratio(report.payback.breakevenRoas)}`}
              </span>
            )}
          </dd>
        </dl>
        {полоса !== undefined && !полоса.hasBase && (
          <p className="result-base">
            {полоса.prevMonth === null
              ? 'Сравнить не с чем: прошлого месяца в данных нет.'
              : `Сравнить с ${полоса.prevMonth} нельзя: в ${полоса.prevMonth} заказов нет.`}
          </p>
        )}
      </section>
```

Прежняя `строкаДельты` заменяется этой; дельта прибыли ставится сразу после `result-profit` вызовом
`{строкаПоказателя(полоса, 'profit')}`. Ни одного сравнения: `hasBase` и `verdict` — признаки отчёта.

```tsx
/**
 * Дельта одного числа результата — кусок S13. Без базы строки у числа нет: о том, что сравнить не с
 * чем, говорит одна строка на весь блок. Текст дельты — прежний, куска S11.
 */
function строкаПоказателя(полоса: Полоса | undefined, ключ: string): ReactNode {
  if (полоса === undefined || !полоса.hasBase) return null
  const п = полоса.items.find((показатель) => показатель.key === ключ)
  if (п === undefined) return null
  const текст =
    п.delta === null
      ? `к ${полоса.prevMonth}: нет данных`
      : `${п.unit === 'eur' ? вместе(money(п.delta)) : points(п.delta)} к ${полоса.prevMonth} · ${п.verdict}`
  return (
    <span className="result-delta" data-verdict={п.verdict ?? undefined}>
      {текст}
    </span>
  )
}
```

- [ ] **Шаг 4: стили.** Правила `.kpi-list`, `.kpi`, `.kpi-label` удалить; `.kpi-value` переименовать в
`.result-profit`, `.kpi-delta` — в `.result-delta`, два правила цвета — в
`.result-delta[data-verdict='лучше']` и `.result-delta[data-verdict='хуже']`, **тела и комментарии без
смены величин**. Добавить (узкий медиазапрос — в тот же блок, где сейчас правило `.kpi-list`):

```css
/*
 * Результат месяца — кусок S13. Прибыль — единственное число кегля Hero700; соседи — Base400
 * полужирным. Иерархия строится уменьшением соседей, а не новым заимствованным кеглем.
 */
.result {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
  gap: var(--spacingVerticalM) var(--spacingHorizontalXXL);
  align-items: start;
}

.result h2 {
  margin: 0;
}

.result-stats {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: var(--spacingVerticalS) var(--spacingHorizontalM);
  margin: 0;
}

.result-stats dt {
  color: var(--colorNeutralForeground2);
}

.result-stats dd {
  font-size: var(--fontSizeBase400);
  font-weight: var(--fontWeightSemibold);
  font-variant-numeric: tabular-nums;
  text-align: right;
  margin: 0;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
}

.result-verdict {
  font-size: var(--fontSizeBase200);
  font-weight: var(--fontWeightRegular);
}

.result-approx {
  color: var(--colorPaletteRedForeground3);
  margin-right: var(--spacingHorizontalS);
}

.result-base {
  grid-column: 1 / -1;
  margin: 0;
  color: var(--colorNeutralForeground3);
}
```

и в узком медиазапросе `.result { grid-template-columns: minmax(0, 1fr); }`. Все токены блока объявлены
в таблице стилей — снято при составлении плана поиском по `--имя:`. Проверка «прибыль — единственное число крупного кегля» сторожит, что второй `Hero700` не завёлся.

- [ ] **Шаг 5: полный прогон**, перепись построчно, `code=$?` → 0. **Шаг 6: учёт** — проверки
`kpis-view.test.tsx`, чьё тело изменилось или удалено (про четыре строки «нет базы», про четыре
карточки). **Шаг 7: сломы:**

```ts
  {
    id: 'no-base-per-number',
    claim: 'вернуть строку «нет базы» к каждому числу',
    mustRedden: 'нет базы — одна строка на блок',
    file: 'app/page.tsx',
    find: '<dt>Чистая выручка</dt>',
    replace: '<dt>Чистая выручка</dt><dd hidden>{полоса !== undefined && !полоса.hasBase ? `в ${полоса.prevMonth} заказов нет` : null}</dd>',
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: ПЕРЕПИСЬ_ПРИЧИНА }],
    tests: 'все',
  },
  {
    id: 'approx-always',
    claim: 'ставить пометку «приблизительно» без признака',
    mustRedden: 'пометка «приблизительно» — только по признаку',
    file: 'app/page.tsx',
    find: '{report.findings?.approximate === true && <span className="result-approx">приблизительно</span>}',
    replace: '{<span className="result-approx">приблизительно</span>}',
    tests: 'все',
  },
  {
    id: 'second-hero-size',
    claim: 'завести второе число крупного кегля',
    mustRedden: 'прибыль — единственное число крупного кегля',
    file: 'app/globals.css',
    find: '.result-stats dd {\n  font-size: var(--fontSizeBase400);',
    replace: '.result-stats dd {\n  font-size: var(--fontSizeHero700);',
    tests: 'все',
  },
```

- [ ] **Шаг 8: коммит** — «S13, задача 9: результат месяца».

---

### Задача 10 — выводы

**Станет:** блок `findings` между результатом и каскадом; до трёх строк по признакам в порядке
реклама → постоянные → приблизительная; у каждой — `data-kind` «хорошо» / «тревога» / «обычно».

**Файлы:** `app/page.tsx`, `app/globals.css`, создать `__tests__/screen/findings-view.test.tsx`, перепись.

**Тексты (канон — здесь; разметка берёт их буквально):**

| Признак | Строка | Пояснение |
|---|---|---|
| реклама «окупается» | `✓ Реклама окупается: {roas} при пороге {порог}.` | `Порог — наш счёт: 100 ÷ вклад с евро оборота, {вклад}. Это итог по всей рекламе месяца, а не отдача от следующего вложенного евро.` |
| реклама «не окупается» | `⚠ Реклама не окупается: {roas} при пороге {порог}.` | то же |
| реклама «порога нет» | `⚠ Порога окупаемости нет: вклад с евро оборота не положителен, {вклад}.` | `Вклад — наш счёт: (чистая выручка − себестоимость − комиссии) ÷ оборот.` |
| убыток, доля есть | `⚠ Месяц в убытке: постоянные расходы {fixed} — {доля} маржинального дохода.` | `Маржинальный доход — наш счёт: чистая выручка − себестоимость − реклама − комиссии, {mi}. Тревога — от 100 %.` |
| убыток, доли нет | `⚠ Месяц в убытке: маржинальный доход {mi} не положителен.` | то же |
| не убыток | `Постоянные расходы {fixed} съедают {доля} маржинального дохода.` (без знака) | то же |
| приблизительная | `⚠ Прибыль приблизительная: у {skus через запятую} нет цены поставщика.` | `Их себестоимость подставлена запасными 40 %; по настоящей цене посчитано {доля честности} чистой выручки.` |

- [ ] **Шаг 1: проверки** — `findings-view.test.tsx`:

```tsx
import { renderToStaticMarkup } from 'react-dom/server'
import { expect, test } from 'vitest'

import { Dashboard } from '@/app/page'
import type { MonthReport } from '@/lib/metrics/report'
import { ПЕРЕПИСЬ } from './fixture.ts'

const с = (f: Partial<NonNullable<MonthReport['findings']>>) =>
  renderToStaticMarkup(<Dashboard report={{ ...ПЕРЕПИСЬ, findings: { ...ПЕРЕПИСЬ.findings!, ...f } }} />)

test('строк выводов столько, сколько признаков, и в порядке договора', () => {
  const html = с({ adsVerdict: 'окупается', loss: false, approximate: true })
  const виды = [...html.matchAll(/<li class="finding" data-kind="([^"]+)"/g)].map((m) => m[1])
  expect(виды).toEqual(['хорошо', 'обычно', 'тревога'])
  expect([...с({ adsVerdict: null, approximate: false }).matchAll(/class="finding"/g)]).toHaveLength(1)
})

test('реклама: каждое из трёх слов — по своему признаку', () => {
  expect(с({ adsVerdict: 'окупается' })).toContain('Реклама окупается:')
  expect(с({ adsVerdict: 'не окупается' })).toContain('Реклама не окупается:')
  expect(с({ adsVerdict: 'порога нет' })).toContain('Порога окупаемости нет:')
})

test('постоянные ниже порога тревоги — строка без знака; убыток — со знаком', () => {
  expect(с({ loss: false })).toMatch(/data-kind="обычно"[^>]*>[^⚠]*Постоянные расходы/)
  expect(с({ loss: true })).toContain('⚠ Месяц в убытке:')
  expect(с({ loss: true, fixedSharePct: null })).toContain('не положителен.')
})

test('вклад, порог и маржинальный доход помечены «наш счёт»', () => {
  const html = с({ adsVerdict: 'окупается' })
  expect(html.match(/наш счёт/g)?.length).toBeGreaterThanOrEqual(2)
})

test('совета «масштабировать» и слов о следующем евро как отдаче нет', () => {
  const html = с({ adsVerdict: 'окупается' })
  expect(html).not.toContain('масштаб')
  expect(html).toContain('а не отдача от следующего вложенного евро')
})

test('выводов нет, когда отчёт без признаков', () => {
  const html = renderToStaticMarkup(<Dashboard report={{ ...ПЕРЕПИСЬ, findings: undefined }} />)
  expect(html).not.toContain('>Выводы</h2>')
})
```

- [ ] **Шаг 2: краснеют. Шаг 3: разметка** — компонент `Выводы({ report })` в `app/page.tsx`, вызов
после блока результата; тексты — из таблицы выше буквально; деньги и доли — через `вместе(money(…))`,
`вместе(percent(…))`, `ratio(…)`. Разметка:
`<section className="block findings"><h2>Выводы</h2><ul>{строки}</ul></section>`, строка —
`<li className="finding" data-kind="…"><p>{строка}</p><p className="finding-why">{пояснение}</p></li>`.

- [ ] **Шаг 4: стили** (все токены объявлены — снято при составлении плана):

```css
/*
 * Выводы — кусок S13. Смысл строки несут знак и слово; черта слева — второй канал того же смысла.
 * Цвета — те же два смысловых, что у дельт. «Обычно» — нейтральная черта без смыслового цвета.
 */
.findings ul {
  display: grid;
  gap: var(--spacingVerticalS);
  list-style: none;
  margin: 0;
  padding: 0;
}

.finding {
  padding-left: var(--spacingHorizontalM);
  border-left: var(--strokeWidthThick) solid var(--colorNeutralStroke1);
}

.finding p {
  margin: 0;
}

.finding[data-kind='хорошо'] {
  border-left-color: var(--colorPaletteGreenForeground1);
}

.finding[data-kind='тревога'] {
  border-left-color: var(--colorPaletteRedForeground3);
}

.finding-why {
  color: var(--colorNeutralForeground3);
  font-size: var(--fontSizeBase200);
  line-height: var(--lineHeightBase200);
}
```

**Стоп-условие.** Проверка чисел таблицы стилей утверждает, что зелёный на экране — только у «лучше».
Если она покраснеет на черте «хорошо» — остановиться и к владельцу: это его правило о смысловом
зелёном, а не проверка, которую правят ради зелени.

- [ ] **Шаг 5: полный прогон**, перепись, `code=$?` → 0.

- [ ] **Шаг 6: сломы** — `find` берётся из разметки шага 3 буквально; если в написанной разметке
строка отличается, `find` правится под написанное **до** прогона, а утверждение и `mustRedden` — нет:

```ts
  {
    id: 'findings-ads-without-verdict',
    claim: 'рисовать строку рекламы без признака',
    mustRedden: 'строк выводов столько, сколько признаков',
    file: 'app/page.tsx',
    find: "report.findings.adsVerdict !== null &&",
    replace: 'true &&',
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: 'в раскладке переписи признак есть, строка та же — объявлено на случай иного' }],
    tests: 'все',
  },
  {
    id: 'findings-order',
    claim: 'поставить строку о приблизительной прибыли раньше строки о постоянных',
    mustRedden: 'строк выводов столько, сколько признаков, и в порядке договора',
    file: 'app/page.tsx',
    find: '{строкаРекламы}{строкаПостоянных}{строкаПриблизительной}',
    replace: '{строкаРекламы}{строкаПриблизительной}{строкаПостоянных}',
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: ПЕРЕПИСЬ_ПРИЧИНА }],
    tests: 'все',
  },
  {
    id: 'findings-fixed-alarm-below-hundred',
    claim: 'ставить знак тревоги у постоянных ниже 100 %',
    mustRedden: 'постоянные ниже порога тревоги — строка без знака',
    file: 'app/page.tsx',
    find: '`Постоянные расходы ${',
    replace: '`⚠ Постоянные расходы ${',
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: ПЕРЕПИСЬ_ПРИЧИНА }],
    tests: 'все',
  },
  {
    id: 'findings-drop-our-mark',
    claim: 'снять пометку «наш счёт» с пояснения порога',
    mustRedden: 'вклад, порог и маржинальный доход помечены «наш счёт»',
    file: 'app/page.tsx',
    find: '`Порог — наш счёт: 100 ÷ вклад с евро оборота, ${',
    replace: '`Порог: 100 ÷ вклад с евро оборота, ${',
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: ПЕРЕПИСЬ_ПРИЧИНА }],
    tests: 'все',
  },
  {
    id: 'findings-marginal-claim',
    claim: 'вернуть ложное утверждение об отдаче следующего евро',
    mustRedden: 'совета «масштабировать» и слов о следующем евро как отдаче нет',
    file: 'app/page.tsx',
    find: 'а не отдача от следующего вложенного евро',
    replace: 'следующий евро рекламы приносит больше, чем стоит',
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: ПЕРЕПИСЬ_ПРИЧИНА }],
    tests: 'все',
  },
```

Для слома `findings-order` компонент `Выводы` собирает три строки в переменные `строкаРекламы`,
`строкаПостоянных`, `строкаПриблизительной` и выводит их одной строкой
`<ul>{строкаРекламы}{строкаПостоянных}{строкаПриблизительной}</ul>`; условие строки рекламы пишется как
`report.findings.adsVerdict !== null && …`.

- [ ] **Шаг 7: коммит** — «S13, задача 10: выводы по признакам».

---

### Задача 11 — средняя на ряде по дням

**Файлы:** `app/page.tsx` (блок `daily`), `app/globals.css`, `__tests__/screen/daily-view.test.tsx` (дописать), перепись.

- [ ] **Шаг 1: дописать проверки**

```tsx
test('пунктир средней стоит на той же величине, что в отчёте, и подпись называет базу', () => {
  const html = renderToStaticMarkup(<Dashboard report={ПЕРЕПИСЬ} />)
  expect(html.replace(/\s/g, '')).toContain(`--mean-at:${ПЕРЕПИСЬ.daily!.avgPct}`)
  expect(html).toContain(`средняя ${money(ПЕРЕПИСЬ.daily!.avgNet!).replace(/ €$/, ' €')} ${ПЕРЕПИСЬ.daily!.avgBase}`)
})

test('строка о пустых днях — только по признаку', () => {
  const есть = renderToStaticMarkup(<Dashboard report={ПЕРЕПИСЬ} />)
  expect(есть).toContain('в среднюю такой день входит нулём')
  const нет = renderToStaticMarkup(<Dashboard report={{ ...ПЕРЕПИСЬ, daily: { ...ПЕРЕПИСЬ.daily!, hasEmptyDays: false } }} />)
  expect(нет).not.toContain('в среднюю такой день входит нулём')
})

test('средней нет — пунктира и подписи нет', () => {
  const html = renderToStaticMarkup(<Dashboard report={{ ...ПЕРЕПИСЬ, daily: { ...ПЕРЕПИСЬ.daily!, avgNet: null, avgPct: null } }} />)
  expect(html).not.toContain('daily-mean')
})
```

- [ ] **Шаг 2: краснеют. Шаг 3: разметка** — внутри `.daily-chart` после `</ol>` столбиков:

```tsx
            {report.daily.avgPct != null && report.daily.avgNet != null && (
              <div className="daily-mean" style={{ '--mean-at': report.daily.avgPct } as CSSProperties}>
                <span className="daily-mean-label">{`средняя ${вместе(moneyMaybe(report.daily.avgNet))} ${report.daily.avgBase}`}</span>
              </div>
            )}
```
`moneyMaybe`, а не `money`, — нарочно: слом «пунктир без средней» обязан краснеть на утверждении
проверки, а не падать на пустом значении — красное на чужой ошибке ничего не доказывает.

после `.daily-chart`:
```tsx
          {report.daily.hasEmptyDays === true && (
            <p className="daily-note">Пунктир у дня — заказов в этот день не было; в среднюю такой день входит нулём.</p>
          )}
```

- [ ] **Шаг 4: стили.** Пунктир не может жить внутри `<ol className="daily-bars">` (`div` в `ol`), а
переменные шкалы стоят на `ol`. Поэтому `style` с `--scale-from` и `--scale-to` переносится с `ol` на
`<div className="daily-chart">` — одна строка разметки; столбики наследуют переменные от предка, их
формула не меняется. Сломы прошлых кусков, цитирующие строку `ol` со стилем, — в перевод задачи 14.
Добавить:

```css
/*
 * Пунктир средней — кусок S13. Место — та же формула, что у края столбика дня, от той же шкалы:
 * величина приходит из отчёта готовой. Подпись на своём фоне — линия не идёт по тексту.
 */
.daily-chart {
  position: relative;
}

.daily-mean {
  position: absolute;
  left: 0;
  right: 0;
  bottom: calc((var(--mean-at) - var(--scale-from)) / (var(--scale-to) - var(--scale-from)) * 100%);
  border-top: var(--strokeWidthThin) dashed var(--colorBrandStroke1);
  pointer-events: none;
}

.daily-mean-label {
  position: absolute;
  right: 0;
  bottom: var(--spacingVerticalXS);
  padding: 0 var(--spacingHorizontalS);
  background: var(--colorNeutralBackground1);
  color: var(--colorNeutralForeground2);
  font-size: var(--fontSizeBase200);
  line-height: var(--lineHeightBase200);
}

.daily-note {
  margin: var(--spacingVerticalS) 0 0;
  color: var(--colorNeutralForeground3);
  font-size: var(--fontSizeBase200);
  line-height: var(--lineHeightBase200);
}
```

Если `.daily-chart` уже задаёт `position` или его ось стоит в той же сетке так, что пунктир уходит на
ось, — `left` пунктира равняется ширине оси той же величиной, что уже задаёт колонку оси; своей
новой величины не заводить. Где встал пунктир — видно на снимке задачи 16.

- [ ] **Шаг 5: полный прогон**, перепись, `code=$?` → 0.

- [ ] **Шаг 6: доказать дописывание**

```bash
git diff __tests__/screen/daily-view.test.tsx | grep -c '^-[^-]'
```
Ожидание: `0`.

- [ ] **Шаг 7: сломы**

```ts
  {
    id: 'mean-place-invented',
    claim: 'ставить пунктир средней не на величину отчёта',
    mustRedden: 'пунктир средней стоит на той же величине, что в отчёте',
    file: 'app/page.tsx',
    find: "style={{ '--mean-at': report.daily.avgPct } as CSSProperties}",
    replace: "style={{ '--mean-at': '50' } as CSSProperties}",
    tests: 'все',
  },
  {
    id: 'empty-days-note-always',
    claim: 'писать строку о пустых днях без признака',
    mustRedden: 'строка о пустых днях — только по признаку',
    file: 'app/page.tsx',
    find: '{report.daily.hasEmptyDays === true && (',
    replace: '{(',
    tests: 'все',
  },
  {
    id: 'mean-without-value',
    claim: 'рисовать пунктир, когда средней нет',
    mustRedden: 'средней нет — пунктира и подписи нет',
    file: 'app/page.tsx',
    find: '{report.daily.avgPct != null && report.daily.avgNet != null && (',
    replace: '{report.daily.hasOrders && (',
    tests: 'все',
  },
```

- [ ] **Шаг 8: коммит** — «S13, задача 11: средняя на ряде по дням».

---

### Задача 12 — товары: пятёрка, раскрытие, валовая прибыль, пометки

**Станет:** заголовок блока `Товары`; строка над таблицей — «80 % валовой прибыли товаров дают …»;
заголовок колонки — «Валовая прибыль», доли — «Доля в валовой прибыли товаров» с полоской; строки с
`inTop` — в первой таблице; остальные — во второй таблице внутри
`<details className="items-more"><summary>Показать все артикулы месяца — {skusTotal}</summary>…</details>`;
обе таблицы с одним `<colgroup>`. Пометка подстановки — два вида: `⚠ подставлена` и
`⚠ подставлена частью`, ссылкой на `#kachestvo`.

**Файлы:** `app/page.tsx`, `app/globals.css`, `__tests__/screen/items-view.test.tsx`,
`__tests__/screen/substituted-cost.test.tsx`, `__tests__/screen/items-scroll.test.tsx`, создать
`__tests__/screen/items-top.test.tsx`, перепись.

- [ ] **Шаг 1: проверки** — `items-top.test.tsx`:

```tsx
import { renderToStaticMarkup } from 'react-dom/server'
import { expect, test } from 'vitest'

import { Dashboard } from '@/app/page'
import type { MonthReport } from '@/lib/metrics/report'
import { ПЕРЕПИСЬ } from './fixture.ts'

const строки = (n: number) =>
  Array.from({ length: n }, (_, i) => ({
    ...ПЕРЕПИСЬ.items[0], sku: `NP-${100 + i}`, inTop: i < 5,
  }))
const с = (items: MonthReport['items'], skusTotal = items.length) =>
  renderToStaticMarkup(<Dashboard report={{ ...ПЕРЕПИСЬ, items, itemsSummary: { ...ПЕРЕПИСЬ.itemsSummary!, skusTotal } }} />)

test('строки с признаком пятёрки — в первой таблице, остальные — под раскрытием', () => {
  const html = с(строки(7))
  const [видно, скрыто] = html.split('<details')
  expect((видно.match(/NP-1\d\d/g) ?? []).length).toBe(5)
  expect(скрыто).toContain('NP-105')
  expect(скрыто).toContain('NP-106')
})

test('число в переключателе — из отчёта, а не счётом строк', () => {
  expect(с(строки(7), 12)).toContain('Показать все артикулы месяца — 12')
})

test('шесть строк — переключатель есть, пять — нет', () => {
  expect(с(строки(6))).toContain('<details')
  expect(с(строки(5))).not.toContain('<details')
})

test('у двух таблиц одно описание колонок', () => {
  const html = с(строки(7))
  const колонки = html.match(/<colgroup>.*?<\/colgroup>/g) ?? []
  expect(колонки).toHaveLength(2)
  expect(колонки[0]).toBe(колонки[1])
})

test('колонка называется «Валовая прибыль», слово «Прибыль» отдельным заголовком не стоит', () => {
  const html = с(строки(6))
  expect(html).toContain('>Валовая прибыль</th>')
  expect(html).not.toContain('>Прибыль</th>')
})
```

В `substituted-cost.test.tsx` проверка «пометка ведёт на вкладку…» переписывается под учёт: пометка
ведёт на `#kachestvo`; проверка «подставленная целиком и подставленная частью названы разными словами»
сохраняется **без правки тела**, если слова `подставлена` / `подставлена частью` в ней те же.

- [ ] **Шаг 2: краснеют. Шаг 3: разметка** — функция `СтрокаТовара({ item })` (тело — нынешний `<tr>`
с правкой пометки), функция `ТаблицаТоваров({ строки, итоги })` с `<colgroup>` из семи `<col>` и
заголовками; в блоке:

```tsx
        <ТаблицаТоваров строки={report.items.filter((item) => item.inTop !== false)} итоги={report.itemsSummary} />
        {report.items.some((item) => item.inTop === false) && report.itemsSummary !== undefined && (
          <details className="items-more">
            <summary>{`Показать все артикулы месяца — ${count(String(report.itemsSummary.skusTotal))}`}</summary>
            <ТаблицаТоваров строки={report.items.filter((item) => item.inTop === false)} итоги={report.itemsSummary} />
          </details>
        )}
```
`filter` и `some` выбирают по признаку и не сравнивают чисел. `inTop !== false` оставляет прежние
раскладки без поля целиком в первой таблице.

Описание колонок — одна константа, чтобы у двух таблиц оно не разошлось:

```tsx
/** Ширины колонок таблицы товаров — одни на обе таблицы: видимую пятёрку и раскрытую часть. */
const КОЛОНКИ_ТОВАРОВ = ['13%', '10%', '15%', '15%', '16%', '10%', '21%']
```
и в `ТаблицаТоваров`: `<colgroup>{КОЛОНКИ_ТОВАРОВ.map((ширина, i) => <col key={i} style={{ width: ширина }} />)}</colgroup>`.
Пометка подстановки в ячейке себестоимости:

```tsx
                  {item.подстановка !== undefined && (
                    <a className="substituted" href="#kachestvo">
                      {item.подстановка === 'вся' ? '⚠ подставлена' : '⚠ подставлена частью'}
                    </a>
                  )}
```

- [ ] **Шаг 4: стили** (все токены объявлены — снято при составлении плана; фокус даёт общее правило
`:focus-visible`):

```css
/*
 * Раскрытие товаров и неполноты — кусок S13. Родной элемент страницы, без клиентского кода.
 * Вид — как у кнопки без заливки: обводка, скругление, отступы из объявленных токенов.
 */
.items-more > summary,
.gaps-more > summary {
  display: inline-block;
  margin-top: var(--spacingVerticalS);
  padding: var(--spacingVerticalXS) var(--spacingHorizontalM);
  border: var(--strokeWidthThin) solid var(--colorNeutralStroke1);
  border-radius: var(--borderRadiusMedium);
  cursor: pointer;
}

.items table {
  table-layout: fixed;
}
```

- [ ] **Шаг 5: полный прогон**, перепись, `code=$?` → 0. **Шаг 6: учёт.**

- [ ] **Шаг 7: сломы**

```ts
  {
    id: 'top-ignores-flag',
    claim: 'класть все строки в видимую таблицу',
    mustRedden: 'строки с признаком пятёрки — в первой таблице',
    file: 'app/page.tsx',
    find: 'строки={report.items.filter((item) => item.inTop !== false)}',
    replace: 'строки={report.items}',
    tests: 'все',
  },
  {
    id: 'toggle-counts-rows',
    claim: 'печатать в переключателе счёт строк вместо числа из отчёта',
    mustRedden: 'число в переключателе — из отчёта',
    file: 'app/page.tsx',
    find: 'count(String(report.itemsSummary.skusTotal))',
    replace: 'count(String(report.items.length))',
    tests: 'все',
  },
  {
    id: 'toggle-always',
    claim: 'рисовать переключатель без скрытых строк',
    mustRedden: 'шесть строк — переключатель есть, пять — нет',
    file: 'app/page.tsx',
    find: '{report.items.some((item) => item.inTop === false) && report.itemsSummary !== undefined && (',
    replace: '{report.itemsSummary !== undefined && (',
    tests: 'все',
  },
  {
    id: 'column-widths-diverge',
    claim: 'развести ширины колонок у двух таблиц',
    mustRedden: 'у двух таблиц одно описание колонок',
    file: 'app/page.tsx',
    find: '<colgroup>{КОЛОНКИ_ТОВАРОВ.map((ширина, i) => <col key={i} style={{ width: ширина }} />)}</colgroup>',
    replace: '<colgroup>{КОЛОНКИ_ТОВАРОВ.map((ширина, i) => <col key={i} style={{ width: строки.every((с) => с.inTop !== false) ? ширина : `${i}%` }} />)}</colgroup>',
    tests: 'все',
  },
  {
    id: 'gross-profit-called-profit',
    claim: 'назвать колонку «Прибыль»',
    mustRedden: 'колонка называется «Валовая прибыль»',
    file: 'app/page.tsx',
    find: '<th scope="col">Валовая прибыль</th>',
    replace: '<th scope="col">Прибыль</th>',
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: ПЕРЕПИСЬ_ПРИЧИНА }],
    tests: 'все',
  },
  {
    id: 'partial-called-full',
    claim: 'назвать частичную подстановку словом полной',
    mustRedden: 'подставленная целиком и подставленная частью названы разными словами',
    file: 'app/page.tsx',
    find: "'⚠ подставлена частью'",
    replace: "'⚠ подставлена'",
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: ПЕРЕПИСЬ_ПРИЧИНА }],
    tests: 'все',
  },
```

Слом `column-widths-diverge` различает таблицы по признаку строк, а не по их числу: у видимой таблицы
ширины прежние, у раскрытой — другие. Условие по числу строк на раскладке из семи строк не сработало
бы: обе таблицы короче шести строк.

- [ ] **Шаг 8: коммит** — «S13, задача 12: товары — пятёрка и раскрытие».

---

### Задача 13 — качество данных

**Станет:** один блок `<section id="kachestvo" className="block quality">` с заголовком «Качество
данных»: доля по настоящей цене с полосой (прежняя разметка), артикулы без цены, виды неполноты с
`hasHoles`; «Дыр в данных нет.», когда таких нет; все виды — под
`<details className="gaps-more"><summary>Показать все виды неполноты</summary>`. Числа в подписи нет:
отчёт не отдаёт число видов полем, а считать длину списка на экране нельзя.

**Файлы:** `app/page.tsx`, `app/globals.css`, `__tests__/screen/census.test.tsx` (вторая проверка —
«все одиннадцать видов» — сохраняется, ищет виды внутри раскрытия), создать `__tests__/screen/quality-view.test.tsx`.

- [ ] **Шаг 1: проверки**

```tsx
import { renderToStaticMarkup } from 'react-dom/server'
import { expect, test } from 'vitest'

import { Dashboard } from '@/app/page'
import { ПЕРЕПИСЬ } from './fixture.ts'

test('видны только виды с дырами; все одиннадцать — под раскрытием', () => {
  const html = renderToStaticMarkup(<Dashboard report={ПЕРЕПИСЬ} />)
  const [видно, раскрыто] = html.split('<details class="gaps-more">')
  const сДырами = ПЕРЕПИСЬ.gaps.filter((в) => в.hasHoles)
  expect(сДырами.every((в) => видно.includes(в.kind))).toBe(true)
  expect(ПЕРЕПИСЬ.gaps.filter((в) => !в.hasHoles).some((в) => видно.includes(`>${в.kind}:`))).toBe(false)
  expect(ПЕРЕПИСЬ.gaps.every((в) => раскрыто.includes(в.kind))).toBe(true)
})

test('дыр нет — так и написано', () => {
  const gaps = ПЕРЕПИСЬ.gaps.map((в) => ({ ...в, hasHoles: false }))
  expect(renderToStaticMarkup(<Dashboard report={{ ...ПЕРЕПИСЬ, gaps }} />)).toContain('Дыр в данных нет.')
})

test('ссылка из результата и пометки товаров ведёт на этот блок', () => {
  expect(renderToStaticMarkup(<Dashboard report={ПЕРЕПИСЬ} />)).toContain('id="kachestvo"')
})
```

- [ ] **Шаг 2: краснеют** — `npx vitest run __tests__/screen/quality-view.test.tsx`.

- [ ] **Шаг 3: разметка** — разделы `honesty` и `gaps` сливаются; полоса доли и её комментарий о
`clamp` переносятся **без правки**:

```tsx
      <section id="kachestvo" className="block quality">
        <h2>Качество данных</h2>
        {/* прежнее содержимое раздела honesty без <h2>, без правки */}
        <h3>Неполнота данных</h3>
        {report.gaps.some((gap) => gap.hasHoles === true) ? (
          <ul className="gaps-holes">
            {report.gaps
              .filter((gap) => gap.hasHoles === true)
              .map((gap) => (
                <li key={gap.kind}>
                  {gap.kind}: {count(String(gap.count))}
                  {gap.at.length > 0 ? ` (${gap.at.join(', ')})` : ''}
                </li>
              ))}
          </ul>
        ) : (
          <p>Дыр в данных нет.</p>
        )}
        <details className="gaps-more">
          <summary>Показать все виды неполноты</summary>
          <ul>
            {report.gaps.map((gap) => (
              <li key={gap.kind} data-zero={gap.hasHoles === true ? undefined : 'true'}>
                {gap.kind}: {count(String(gap.count))}
                {gap.at.length > 0 ? ` (${gap.at.join(', ')})` : ''}
              </li>
            ))}
          </ul>
        </details>
      </section>
```
Прежнее `data-zero={gap.count === 0 …}` — сравнение числа на экране — заменено признаком. Правило
`.gaps-more > summary` уже заведено в задаче 12.

- [ ] **Шаг 4: полный прогон**, перепись, `code=$?` → 0; проверка «все одиннадцать видов» — зелёная без
правки тела, иначе — в учёт.

- [ ] **Шаг 5: сломы**

```ts
  {
    id: 'holes-list-unfiltered',
    claim: 'показывать на виду все виды, а не только с дырами',
    mustRedden: 'видны только виды с дырами',
    file: 'app/page.tsx',
    find: '.filter((gap) => gap.hasHoles === true)',
    replace: '.filter(() => true)',
    alsoRedden: [{ name: ПЕРЕПИСЬ, why: ПЕРЕПИСЬ_ПРИЧИНА }],
    tests: 'все',
  },
  {
    id: 'no-holes-text-lies',
    claim: 'не говорить «дыр нет», когда их нет',
    mustRedden: 'дыр нет — так и написано',
    file: 'app/page.tsx',
    find: '<p>Дыр в данных нет.</p>',
    replace: '<p></p>',
    tests: 'все',
  },
  {
    id: 'quality-anchor-lost',
    claim: 'потерять якорь блока качества',
    mustRedden: 'ссылка из результата и пометки товаров ведёт на этот блок',
    file: 'app/page.tsx',
    find: '<section id="kachestvo" className="block quality">',
    replace: '<section className="block quality">',
    tests: 'все',
  },
```

- [ ] **Шаг 6: коммит** — «S13, задача 13: качество данных одним блоком».

---

### Задача 14 — сломы прошлых кусков: перевод и вывод из списков

**Сегодня:** 56 строк в `breaks/s10-fluent.ts`, `breaks/s11-content.ts`, `breaks/s12-reading.ts` цитируют
код, который кусок удалил или переписал.

- [ ] **Шаг 1: прогнать три списка на нынешней голове и снять отказы**

```bash
out=$(mktemp)
```
```bash
npm run breaks -- s11-content > "$out" 2>&1
```
```bash
code=$?; echo "exit=$code"; cat "$out"
```
То же для `s10-fluent` и `s12-reading`, каждый в свой `mktemp`. Каждый слом с отказом «цитата не
найдена» — в работу. Вывод читается целиком, а не поиском по нему.

- [ ] **Шаг 2: разобрать каждый отказ в одну из двух граф**

| Графа | Когда | Что делать |
|---|---|---|
| Перевод | механизм жив, изменилась цитата (отступ, имя, соседняя строка) | новая `find`/`replace`, `claim` и `mustRedden` **прежние**; если имя проверки сменилось переписью под учёт — `mustRedden` на новое имя из учёта |
| Вывод из списка | механизм удалён куском (вкладки, порядок, карточки, доли статей, `largest`, окупаемость отдельным блоком) | слом удаляется из списка; строка в разделе заявки «Сломы, выведенные из списков» с доводом — какой задачей и каким решением владельца удалён механизм |

- [ ] **Шаг 3: раздел для заявки** — `docs/сверка/перевод-сломов-s13.md`:

```markdown
| Список | id | Графа | Старая цитата | Новая цитата | Утверждение | Ожидание | Довод |
|---|---|---|---|---|---|---|---|
```

- [ ] **Шаг 4: прогнать три списка снова** — каждый своей командой, `code=$?`; ожидание: код 0 у всех,
у каждого оставшегося слома покраснела своя проверка.

- [ ] **Шаг 5: коммит** — `breaks/s10-fluent.ts breaks/s11-content.ts breaks/s12-reading.ts docs/сверка/перевод-сломов-s13.md`, «S13, задача 14: перевод сломов прошлых кусков».

---

### Задача 15 — живая проверка марта

**Файлы:** `__tests__/live/march-screen.live.ts`.

- [ ] **Шаг 1: дописать утверждения** в проверку водопада марта (якорь — сверенные владельцем числа):

```ts
  expect(отчёт.waterfall?.steps.map((с) => с.key)).toEqual(['net', 'cogs', 'ads', 'fees', 'margin_income', 'fixed', 'profit'])
  expect(отчёт.findings).toEqual({
    adsVerdict: 'окупается', marginIncome: '6291.44', fixedSharePct: '72.4', loss: false, approximate: true,
  })
  expect(отчёт.daily?.avgNet).toBe('557.32')
  expect(отчёт.daily?.avgBase).toBe('по 31 дню месяца')
  expect(отчёт.items.filter((i) => i.inTop).map((i) => i.sku)).toEqual(['NP-004', 'NP-003', 'NP-009', 'NP-012', 'NP-002'])
```
Прежние утверждения про девять ступеней от оборота — под учёт.

Числа `6291.44`, `72.4`, `557.32` — не наш счёт, а вывод живой команды задачи 7; если он разошёлся со
счётом договора — ставится вывод команды, расхождение названо в `числа-марта-s13.md`.

- [ ] **Шаг 2: прогон**

```bash
npm run test:live -- __tests__/live/march-screen.live.ts
```
```bash
code=$?; echo "exit=$code"
```
Ожидание: код 0; вторая проверка файла — база вернулась в посев.

- [ ] **Шаг 3: коммит** — «S13, задача 15: живая проверка марта на новом экране».

---

### Задача 16 — снимки: одна пересборка

**Файлы:** `docs/screens/generate.tsx`, `docs/screens/fixture.ts`, `docs/screens/run.tsx`, `docs/screens/README.md`, `docs/screens/*.png`, `docs/screens/*.html`.

- [ ] **Шаг 1: состояния** — в `СОСТОЯНИЯ` удалить `dengi`, `tovary`, `kachestvo`, `neizvestnaya-vkladka`
и функцию `вкладка`; `obychnyy` и `bez-bazy` — `<Dashboard report={…} />`; добавить `raskryto` —
разметка `obychnyy`, в которой **генератор** ставит `open` у обоих `<details`:

```ts
/**
 * Раскрытый вид — кусок S13. Страница не знает состояния «раскрыто»: его держит браузер. Снимок
 * раскрытого вида получает атрибут `open` здесь, в генераторе, на готовой разметке. Приложение
 * своего пути к раскрытию для снимков не заводит: вид, который видят только снимки, — путь, которого
 * нет в бою.
 */
export function раскрыть(разметка: string): string {
  const итог = разметка.replaceAll('<details class=', '<details open="" class=')
  сверитьЕдинственность('раскрытие переключателей', (итог.match(/<details open=""/g) ?? []).length === 2 ? 1 : 0)
  return итог
}
```
(Если `сверитьЕдинственность` устроена иначе — завести в `guards.ts` свой отказ `сверитьРаскрытие(сколько)`
с ненулевым кодом и сломом; молча принять одну раскрытую из двух нельзя.)

- [ ] **Шаг 2: `КАРТИНКИ`** — удалить `08`–`13` и `15`; добавить `08-raskryto-shirokiy.png`,
`08-raskryto-shirokiy-tyomnaya.png`, `09-raskryto-uzkiy.png`, `09-raskryto-uzkiy-tyomnaya.png`. Высоты —
**замером** настоящим браузером на странице снимка, как в S11; числа замера — в комментарии с датой.
Число страниц и картинок в `run.tsx` — из вывода генератора, не из головы.

- [ ] **Шаг 3: пересборка — один раз**

```bash
npm run screens
```
```bash
code=$?; echo "exit=$code"
```

- [ ] **Шаг 4: опись** — в `docs/screens/README.md` таблица «картинка — какая задача её изменила»; у
каждой изменившейся — номер задачи. Снимок, изменившийся без задачи, — стоп, к владельцу.

- [ ] **Шаг 5: коммит — только файлы снимков**

```bash
git add docs/screens/*.png docs/screens/*.html
git commit -m "S13, задача 16: пересборка эталонов снимков — один раз"
```
Генератор, раскладка и опись — **предыдущим** коммитом этой же задачи.

---

### Задача 17 — ворота, сломы, проверка кода, заявка

- [ ] **Шаг 1: ворота на голове, которая пойдёт в слияние** — каждая своей командой:

```bash
npm test
```
```bash
code=$?; echo "test exit=$code"
```
```bash
npm run lint
```
```bash
code=$?; echo "lint exit=$code"
```
```bash
npm run build
```
```bash
code=$?; echo "build exit=$code"
```

- [ ] **Шаг 2: сплошной прогон всех списков сломов** — по списку за команду, с `git rev-parse HEAD`
до первого и после последнего; машину не усыплять; вывод — целиком в
`docs/сверка/прогон-сломов-s13.md` тем же форматом, что `прогон-сломов-s12.md`. Слом, у которого своя
проверка не покраснела, — механизм не доказан, кусок не сдаётся.

- [ ] **Шаг 3: проверка кода** — субагент с чистым контекстом. Даётся: `git diff origin/main...HEAD`
**целиком**, `.contracts/s13-one-screen.md` с ветки договора, `CLAUDE.md`. Не даётся: переписка, этот
план, доводы автора. Просьба: пройти утверждения договора 1–13 и строки приёмки по очереди, у каждого —
«прошёл» / «не прошёл» с файлом и строкой; отдельным разделом — всё ненадёжное.

- [ ] **Шаг 4: находки** — до слияния чинится всё, что делает утверждение ложным или оставляет сторожа
неспособным покраснеть; остальное — в тело заявки списком «на потом». После починки — ворота и сломы
снова, на новой голове.

- [ ] **Шаг 5: заявка** — тело по разделам: что сделано (таблица по задачам); отступления, названные
заранее (из договора, пять); две отмены прежних решений; учёт правки принятых проверок (ссылка на
`учёт-проверок-s13.md` и сводная таблица); перевод и вывод сломов (ссылка на `перевод-сломов-s13.md`);
пути, которых нет в настоящих данных (семь, из договора); живая сверка марта (до/после, новые числа);
ворота и сломы (таблица, ссылка на файл прогона); находки на потом; что осталось непроверенным.

- [ ] **Шаг 6: работа владельца руками — одним списком**

| Что | Где | Зачем |
|---|---|---|
| Один проход целиком: вход → экран марта → «Показать все артикулы месяца» → «Показать все виды неполноты» → переход на 2026-02 → «Обновить» | предпросмотр заявки на хостинге, своим входом | единственный путь, которым пользуется человек; вход агент не выполняет |
| Сверить новые числа марта | тело заявки, раздел живой сверки | якорь снаружи у маржинального дохода, доли постоянных и средней |

---

### Задача 18 — отмена решения Э5: «убыток» по показанной прибыли

**Отмена прежнего решения.** 16 сентября 2026 года владелец решил (Э5): «убыток» — доля постоянных в маржинальном
доходе от 100 % или маржинальный доход не положителен. 17 сентября 2026 года он это решение отменил. Прежний текст
плана выше не переписан: он показывает, как решение стояло до отмены. Договор куска
(`origin/contract/s13-one-screen`) не менялся: отметка об отмене — в теле заявки.

**Решение владельца, дословно:** «считать «убыток» не по разности «маржинальный доход минус постоянные», а прямо по
показанной прибыли меньше нуля».

Следствия, названные владельцем в тот же день:

- прибыль ровно 0,00 — не убыток (строго меньше нуля);
- строка пояснения на экране — «Тревога — когда месяц в убытке: прибыль меньше нуля.» вместо «Тревога — от 100 %.»;
- снимки пересобраны в третий раз в куске S13 (опись — `docs/screens/README.md`, «Третья пересборка в куске S13»).

**Причина** — находка И6 итоговой проверки куска: на краю 100,0 % прежнее правило давало «⚠ Месяц в убытке» при прибыли
+0,10 € — постоянные 199,90 € при маржинальном доходе 200,00 €, доля показана 100,0 %.

Доля постоянных в маржинальном доходе осталась, как была: показывается, пуста при маржинальном доходе не больше нуля.

**Места плана, ставшие неверными** (номера строк сняты командой `grep -n -E "Э5|убыток: ниже 100|убыток: маржинальный
доход не положителен|«убыток» — дол|решение владельца Э5|fixed_share >= 100|loss-from-unrounded|loss-misses-nonpositive-income|Тревога — от 100|порога тревоги|ниже 100 %',$"` до дописывания этого раздела):

| Строка | Что там | Что теперь |
|---|---|---|
| 49 | решение Э5 «Постоянные — ⚠ только от 100 %» | отменено этой задачей |
| 567 | проверка «убыток: ниже 100 % — нет, ровно 100 % показанных — да» | переписана: «убыток — по показанной прибыли, а не по доле: 100,0 % при прибыли +0,10 — нет»; дописаны края прибыли и расхождение с разностью «МД − постоянные» |
| 574 | проверка «убыток: маржинальный доход не положителен…» на раскладке без согласованной прибыли | утверждение то же, прибыль раскладки согласована |
| 614–615 | комментарий запроса: «убыток» — доля от 100 % или МД не положителен (Э5) | «убыток» — показанная прибыль меньше нуля |
| 638 | `(s.mi <= 0 or s.fixed_share >= 100) as loss` | `(s.profit < 0) as loss` |
| 703–717 | сломы `loss-from-unrounded`, `loss-misses-nonpositive-income` | заменены: `loss-from-fixed-share`, `loss-at-zero-profit`, `loss-from-income-minus-fixed` |
| 731 | пояснение к слому `loss-from-unrounded` | слома нет |
| 1557 | строка экрана «… Тревога — от 100 %.» | «… Тревога — когда месяц в убытке: прибыль меньше нуля.» |
| 1588 | проверка «постоянные ниже порога тревоги — строка без знака; убыток — со знаком» | переименована: «без убытка — строка постоянных без знака; убыток — со знаком; тревога названа по прибыли» |
| 1688–1689 | слом `findings-fixed-alarm-below-hundred` с прежним утверждением и именем проверки | утверждение и своё ожидание переведены |

Сделано в задаче 17 (коммиты `913856d` — запрос, проверки и сломы; `d6d25bc` и `2d2e994` — строка экрана и третья
пересборка снимков; `77b07b3` — проверка на расхождение с разностью «МД − постоянные»).

## Самопроверка плана по договору

| Требование договора | Задача |
|---|---|
| Пункт 1 — каскад от чистой выручки | 1 |
| Пункт 2 — признак рекламы | 2 |
| Пункт 3 — доля постоянных, «убыток» | 2 |
| Пункт 4 — «приблизительная» | 2 |
| Пункт 5 — средняя | 3 |
| Пункт 6 — один порядок, пятёрка | 4 |
| Пункт 7 — дыры неполноты | 5 |
| Пункт 8 — полоса без доли рекламы | 6 |
| Пункт 9 — вывод команды | 7 |
| Пункты 10–11 — шапка и результат | 8, 9 |
| Пункт 12 — выводы | 10 |
| Пункт 13 — каскад на экране, строка про оборот | 1 |
| Пункт 14 — ряд со средней | 11 |
| Пункты 15–16 — товары и два вида пометки | 12 |
| Пункт 17 — качество данных | 13 |
| Пункт 18 — старые адреса | 8 |
| Утверждения 1–13 | 1–13, у каждого слом |
| Живой заход и сверка марта | 0, 7, 15 |
| Учёт принятых проверок | 1–13, файл учёта |
| Перевод сломов | 14 |
| Одна пересборка снимков | 16 |
| Ворота, рецензент, заявка | 17 |
| Отмены прежних решений отдельными задачами | 4, 8, 18 |
