# S11 — содержание экрана: план со сломами

> Контракт: `.contracts/s11-content.md`, ветка `contract/s11-content`, `a48f4a2`. Читать с ветки.
>
> **Плану предшествует условие, а не пожелание:** S10 доведён и влит в `main`. Работа S11 идёт от
> `main` после слияния. Пока этого нет, ни одна задача ниже не начинается.
>
> План лежит на ветке контракта, потому что рабочей ветки ещё нет: она заводится от `main` после
> слияния S10, и файл переезжает туда вместе с первой задачей.

## Решения владельца, принятые вместе с контрактом

| Развилка | Решение |
|---|---|
| 1. Где считается дельта | **SQL.** Деньги ни разу не становятся числом |
| 2. Округление таблицы товаров до целых евро | **Не округляем.** Задание в этой части не исполняется — отступление с доводом |
| 3. Новые числа в команде метрик | **Команду не трогаем.** Последствие уходит отложенной задачей, а не строкой в теле pull request |
| 4. Порог безубыточности | **От вклада без рекламы и без постоянных** |
| 5. Время последнего разбора | **Показываем то, что есть, называем верно.** Миграций нет. UTC, пояс назван в строке |
| 6. Место водопада | **Сразу после полосы показателей** |
| 7. С каким месяцем сравнивать | **С предыдущим календарным** |

**Требование, добавленное при приёмке контракта — сверх выбора по развилке 4.** Человек видит на
экране маржу 10,1 %, делит сто на десять, получает 9,90 × и вывод «не окупается» — прямо
противоположный нашему. Экран обязан не дать сделать это молча:

- подпись у порога называет, **от чего** он считается;
- сам вклад стоит на экране **числом** — 57,1 % на мартовских данных, — а не остаётся у нас в
  голове;
- определение и вывод помечаются **нашими** везде, где появляются: в SQL, в коде, в плане, в теле
  pull request.

## Задача 0 — предусловие и снимок «до»

- [x] S10 влит в `main` — `8bf2e3a`, заявка №14. Ветка `feat/s11-content` заведена **от `main`**. Боевой
      адрес показывает на странице состояния ту же голову `8bf2e3a`.
- [x] **Пересчёт якорей: обязано остаться 28.** На `8bf2e3a` — 28, все единственные; задеты вкладками 2. S10 разметку не двигал, значит число не менялось;
      это дешёвая проверка моего же обязательства из контракта. Расхождение — стоп и разговор.
- [x] `npm test` на голове — код возврата и числа записываются как снимок «до». **Снимок «до»:
      61 файл, 778 проверок, 0 красных, код возврата 0, `8bf2e3a`.**
- [x] Живой заход «до»: загрузка Таблицы и папки Диска в **местную** базу, `npm run metrics -- 2026-03`
      в файл `docs/сверка/метрики-2026-03-до-s11.txt`. **Разрешён владельцем прямо**, вместе с
      заходом «после» задачи 14. Наружу не пишется ничего, боевая база не открывается. Каждый заход
      называется в теле pull request отдельной строкой с доводом.
- [x] Живой заход обязан подтвердить прежние числа загрузки: 4 файла, 279 строк сырья, 186 после
      свёртки копии. **Подтвердил:** 1699, 2814, 2908, 986 байт; 279; 186. Таблица — 519, 19, 11, 2, 5,
      62 строк: сошлось с независимым счётом владельца по всем шести листам. Цель у каждой из трёх
      команд — `local, база nordic_pet на 127.0.0.1`, названа явно в команде.
- [x] **Файл «до» становится эталоном куска только после сверки с независимым счётом владельца** —
      один раз, по пяти строкам: оборот 18 764,00 €, чистая выручка 17 277,04 €, прибыль
      1 738,53 €, маржа 10,1 %, окупаемость по обороту 4,23 ×. Сверка — это сличение двух чисел, а
      не перечисление строк в отчёте: **у каждой строки стоит «сошлось» или «не сошлось»**.
      **Итог: сошлись все пять**, сверка командой по кодам символов, код 0. Прибор проверен на
      красное: копия с прибылью, изменённой на цент, — «не сошлось» ровно у прибыли, код 1. Первая
      попытка порчи не применилась: в замене стоял обычный пробел, а в файле неразрывный; счёт замен
      показал 0, и красное снято со второй попытки.
- [x] **Не сошлось — стоп.** Не понадобилось. Расхождение показывается владельцу числами, а не сглаживается и не
      объясняется задним числом. Довод: побайтное сравнение «до» и «после» показывает лишь, что
      между двумя заходами ничего не сдвинулось; если оба захода одинаково неверны, оно остаётся
      зелёным. Якорь снаружи у этой сверки один — счёт владельца.
- [x] После снятия «до» база возвращается посевом, и это подтверждается наблюдением: вывод команды
      за март снова нули. **Пересборка — код 0; за март — 0,00 € по всем деньгам, маржа и
      окупаемость «нет данных».**
- [x] **Сверх плана, по просьбе владельца:** на настоящих данных один месяц — 2026-03. Путь «нет
      базы для сравнения» на них единственный. Разбор и моя ошибка — в контракте, раздел «Задача 0 —
      живой заход». Файл «до» S11 совпадает с файлом «до» S10 во всех строках, кроме четырёх строк
      шапки `npm`: «после» снимается той же командой, `npm run --silent metrics -- 2026-03`.

**Почему не на посеве.** Посев не содержит ни одного заказа: вывод за март на нём — нули по всем
полям, и сравнение нулей с нулями не может покраснеть ни при каких обстоятельствах. Прецедент
записан в `docs/сверка/README.md` на S10.

## Задача 1 — дельты и доля рекламы в обороте, в SQL

Новый запрос берёт месяц, считает его и предыдущий календарный, отдаёт готовые строки: значение,
дельту и признак «рост — это хорошо». Деньги и проценты — строками, `null` там, где базы нет.

- [ ] Доля рекламы в обороте: реклама ÷ оборот × 100, `nullif` на делителе.
- [ ] Дельты четырёх показателей: прибыль и чистая выручка в евро, маржа и доля рекламы в
      **процентных пунктах**.
- [ ] Признак «рост — это хорошо» приходит из слоя счёта, а не выводится разметкой из названия:
      прибыль и выручка — да, доля рекламы — нет.
- [ ] Нет предыдущего месяца или в нём нет заказов — **вся полоса дельт разом** отдаёт `null`, а не
      по одной.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Отдать ноль вместо `null`, когда предыдущего месяца нет | «нет предыдущего месяца — дельты пустые, а не нулевые» |
| Считать дельту к предыдущему месяцу **с заказами**, а не к календарному | «база сравнения — предыдущий календарный месяц, даже когда он пуст» |
| Снять `nullif` у делителя доли рекламы | «оборот ноль — доля рекламы пустая» |
| Перевернуть признак «рост — это хорошо» у доли рекламы | «у доли рекламы рост — это плохо» |
| Считать дельту маржи в процентах от прошлой, а не в процентных пунктах | «дельта маржи — в процентных пунктах» |

## Задача 2 — водопад в SQL

Восемь ступеней: оборот → скидки → возвраты → себестоимость → реклама → комиссии → постоянные →
прибыль. Первая и последняя — столбики от нуля, шесть средних — вычитания. Доли отдаются
**процентами**, готовыми.

- [ ] Ступени берут те же выражения, что уже стоят в `MONTH_TOTALS`. Второго определения прибыли
      кусок не заводит.
- [ ] Сходимость утверждается проверкой, а не комментарием: оборот − скидки − возвраты = чистая
      выручка; чистая выручка − себестоимость − реклама − комиссии − постоянные = прибыль.
- [ ] Доля каждой ступени — от оборота, в процентах, округление одно и в последнем `select`.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Изменить одну ступень так, что сумма перестаёт сходиться | «ступени водопада сходятся с прибылью» |
| Отдать доли долями единицы вместо процентов | «доли ступеней отданы процентами» |
| Считать долю от чистой выручки вместо оборота | «доли ступеней считаются от оборота» |
| Убрать ступень из набора | «в водопаде ровно восемь ступеней, в названном порядке» |
| Завести второе выражение прибыли вместо ссылки на существующее | «прибыль водопада — то же выражение, что прибыль итога» |

## Задача 3 — три окупаемости и вклад, в SQL

| Что | Определение | Наш счёт по марту |
|---|---|---|
| По обороту (есть) | оборот ÷ реклама | 4,23 × |
| По прибыли | прибыль ÷ реклама | 0,39 × |
| Вклад с евро оборота | (чистая выручка − себестоимость − комиссии) ÷ оборот × 100 | 57,1 % |
| Порог безубыточности | 100 ÷ вклад | 1,75 × |

**Все четыре определения — наши.** В справочниках их нет; это наша арифметика, и так она помечена
в тексте запроса, в плане и в теле pull request. Постоянные расходы в предельный счёт не входят
нарочно: от рекламы они не зависят.

- [ ] Прежняя окупаемость по обороту не трогается ни выражением, ни именем колонки.
- [ ] Маржа `null` или вклад `null` — порог отдаёт `null`.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Считать порог от маржи экрана (100 ÷ 10,1) вместо вклада | «порог считается от вклада, а не от маржи итога» — с числом на выдуманной раскладке |
| Включить постоянные расходы во вклад | «вклад считается без постоянных расходов» |
| Включить рекламу во вклад | «вклад считается без рекламы» |
| Снять `nullif` у делителя порога | «вклад ноль — порог пустой» |
| Тронуть выражение прежней окупаемости по обороту | принятые проверки слоя метрик прошлых кусков |

## Задача 4 — колонки товаров и строка над таблицей, в SQL

- [ ] Маржа строки: прибыль строки ÷ чистая выручка строки × 100, `nullif` на делителе.
- [ ] Доля строки в общей прибыли: прибыль строки ÷ прибыль месяца × 100, `nullif` на делителе.
- [ ] Прибыль месяца ноль или отрицательна — доля отдаёт `null` **у всех строк разом**.
- [ ] Сколько артикулов дают 80 % прибыли: накопительная сумма по убыванию прибыли.
- [ ] Сколько работают в минус: прибыль строки строго меньше нуля.
- [ ] Прежние колонки `MONTH_ITEMS` — `sku`, `units`, `net`, `cogs`, `profit` — не трогаются ни
      значением, ни местом, ни округлением.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Считать долю в прибыли при отрицательной прибыли месяца | «прибыль месяца не положительна — доли пустые» |
| Считать «80 % прибыли» без сортировки по убыванию | «счёт восьмидесяти процентов идёт по убыванию прибыли» |
| Считать убыточными строки с прибылью ровно ноль | «в минусе — строго меньше нуля» |
| Снять `nullif` у делителя маржи строки | «чистая выручка строки ноль — маржа пустая» |
| Округлить `net` до целых евро | принятая проверка `__tests__/metrics/report.test.ts` — это и есть цена развилки 2 |

## Задача 5 — время последнего разбора, в SQL готовой строкой

- [ ] Берётся `meta.fact_freshness.raw_seen_at`. Миграций кусок не заводит.
- [ ] Форматирование — в SQL: `ГГГГ-ММ-ДД ЧЧ:ММ`, пояс UTC, пояс назван прямо в строке.
- [ ] Подпись говорит то, что это на самом деле: **источники прочитаны по состоянию на …**, а не
      «последний разбор». Отступление от буквы задания названо в теле pull request.
- [ ] Отметки нет вовсе — слова, а не пустая строка и не эпоха.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Печатать в местном поясе вместо UTC | «время разбора печатается по UTC и называет пояс» |
| Форматировать дату в разметке вместо SQL | «разметка получает время готовой строкой» |
| Отдать эпоху вместо слов, когда отметки нет | «отметки нет — на экране слова» |

## Задача 6 — новые поля отчёта

- [ ] Новые поля `MonthReport` дописываются; ни одно прежнее не меняется ни именем, ни типом.
- [ ] `report.ts` не считает ничего: поля приходят из запросов готовыми строками.
- [ ] Все новые запросы читаются **тем же снимком фактов**, что и прежние: один
      `repeatable read read only` на весь экран.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Вычесть дельту в `report.ts` вместо SQL | «слой метрик не считает: дельта приходит из запроса» |
| Прочитать новые числа вторым соединением, вне снимка | «весь экран читается одним снимком фактов» |
| Превратить деньги в число хоть раз | «деньги остаются строкой на всём пути» |

## Задача 7 — перепись экрана

Правка разрешена владельцем прямо. Разрешение на правку — **не разрешение ослабить утверждение.**

- [ ] Прогон четырёх сломов **до** правки: `census-blind-to-swap`, `labels-swapped`,
      `items-order-reversed` — «покраснела своя»; `gaps-truncated` — перепись в списке заодно.
- [ ] Правится **только список ожидаемого** и раскладка `fixture.ts`. Утверждение (`toEqual` по
      всему тексту, по порядку) и способ снятия (`текстПоПорядку`) не меняются ни на символ.
- [ ] Прогон тех же четырёх сломов **после** правки — тот же итог.
- [ ] Дифф файла переписи показывается владельцу целиком, до слияния.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Заменить `toEqual` на `toContain` в переписи | «перепись сверяет весь текст целиком, а не вхождение» |
| Убрать из списка новую строку водопада | сама перепись |
| Переставить две новые подписи полосы показателей | сама перепись |

## Задачи 8–12 — разметка

Порядок на экране: полоса показателей → водопад → «Выручка» → «Затраты» → «Итог» → «Товары» →
«Честность данных» → «Неполнота данных». Строка времени разбора — под названием.

- [ ] **8. Полоса показателей.** Четыре показателя, у каждого значение, знак, подпись и дельта.
- [ ] **9. Водопад.** Доля приходит готовой и уходит в величину стиля — тем же приёмом, что у
      полосы доли честности: одно значение читается один раз и уходит и в текст, и в геометрию.
- [ ] **10. Три окупаемости и вклад** дописываются парами после существующей окупаемости.
      **Условия приёмки этой задачи — прямые слова владельца, а не мой пересказ:** на экране стоит
      и порог, и вклад **числом**; подпись у порога называет, **от чего** он считается; рядом
      сказано, что определение **наше**. Довод владельца записан здесь же, потому что он и есть
      условие: «экран, по которому человек может сам получить противоположный вывод, деля сто на
      маржу, — хуже экрана без порога вовсе». Задача не сдана, пока выполнены не все три.
- [ ] **11. Колонки товаров и строка над таблицей.**
- [ ] **12. Строка времени разбора** внутри шапки, после названия.

**Обязательство разметки, которым держится цена якорей.** Новая разметка только дописывается:
новыми соседними разделами и новыми парами внутри существующих списков. Ни один существующий блок
не оборачивается и не переотступается. Не выйдет — находка к владельцу, перевод якоря вместе с
кодом, старая и новая цитата отдельным разделом в теле pull request.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Посчитать дельту в разметке вместо печати готового поля | «разметка печатает дельту, которую ей дали» — на раскладке, где дельта нарочно расходится со значениями |
| Взять для столбика водопада не то значение, что напечатано текстом | «столбик и подпись водопада — из одного значения» |
| Оставить у дельты цвет, убрав знак и подпись | «смысл дельты назван не только цветом» |
| Красить дельту по знаку вместо признака | «рост доли рекламы показан как ухудшение» |
| Нарисовать ноль вместо слов, когда базы для сравнения нет | «нет базы — слова, а не ноль» |
| Снять строку времени разбора | перепись экрана |
| Снять любую прежнюю строку экрана | перепись экрана |

## Задача 13 — вид новых элементов

- [ ] Величины, цвета, скругления, отступы новых правил — из тех же токенов Fluent 2.
- [ ] Проверка чисел таблицы стилей остаётся зелёной **без правки её исключений**.
- [ ] Контраст новых элементов замеряется числами по парам, крупный текст и мелкий в каждой паре,
      обе темы. Замер снимается с отрисованной страницы в том же безголовом Chrome, которым
      делаются снимки, а не считается из объявленного.
- [ ] Красный остаётся у отказов; дельта «стало хуже» берёт смысловой красный **вместе со знаком и
      подписью**, а не вместо них.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Вписать в новые правила величину не из шкал Fluent | «размеры, скругления, отступы и толщины — из шкал Fluent 2» |
| Вписать в новые правила посторонний цвет | «в таблице стилей нет ни одной величины и ни одного цвета вне набора Fluent 2» |

## Задача 14 — доказательства, что прежние числа не поехали

- [ ] Живой заход «после», `npm run metrics -- 2026-03`, побайтное сравнение с файлом «до» —
      `diff` пуст, код возврата 0.
- [ ] Пять итогов **сверены** с независимым счётом владельца, а не названы поимённо: у каждой
      строки стоит «сошлось» или «не сошлось». Не сошлось — стоп и показ расхождения владельцу.
- [ ] Дифф по `supabase/migrations/` пуст.
- [ ] Дифф по `__tests__/` — **только добавленные строки, кроме файла переписи и раскладки**; по
      этим двум дифф показывается владельцу целиком.
- [ ] `npm test`, `npm run lint`, `npm run build` — коды возврата 0, снимаются отдельными строками.
- [ ] Прежние колонки `MONTH_TOTALS` и `MONTH_ITEMS` не тронуты: принятые проверки слоя метрик
      зелёные без единой правки.

## Задача 15 — сломы

- [ ] Список `breaks/s11-content.ts`: все строки таблиц выше, ни одной меньше.
- [ ] **Все прежние списки гоняются один раз, на голове, которая пойдёт в слияние:** `s3-drive`,
      `s4-facts`, `s5-metrics`, `s6-access`, `s8-holes`, `s9-screen`, `s10-fluent`.
- [ ] Вывод всех прогонов идёт в тело pull request одним списком вместе с таблицей. Недоказанный
      механизм не сдаётся.
- [ ] Прогон требует чистого дерева на входе.

## Задачи 16–19 — сдача

- [ ] **16. Снимки** полным набором в обеих темах и три пары «до / после»; опись переписывается.
- [ ] **17. Отчёт и `README.md`.** Первым шагом задачи — **проход по сданным документам**:
      `README.md`, `docs/ОТЧЁТ.md`, опись снимков. Ищутся **утверждения, верные до куска и неверные
      после**, а не опечатки. Контракт и планы в проход не входят: они историчны по устройству и
      обязаны показывать, как решение менялось.
      Правило заведено владельцем на все будущие куски по итогам S10, где такой проход дал шесть
      мест, и нашёл их не механизм, а взгляд перед отчётом. Отдельным списком записывается и то,
      **что проверено и ложью не оказалось**, — чтобы те же строки не перепроверялись на следующем
      круге. Только после прохода переписываются сами документы.
- [ ] **18. Живой прогон и проход целиком в бою.** Боевой путь — последняя задача, после
      доказательства на местной базе.
- [ ] **19. Pull request и проверка кода** тем, у кого нет моего контекста: только дифф целиком и
      требования, без переписки и без моих объяснений.

## Дополнение от 10 сентября 2026 года — изменение принятого решения

> Отдельным разделом, а не правкой задач выше: принятый план остаётся таким, каким его приняли.
> **Причина:** владелец посмотрел предпросмотр S10 и попросил три вещи — подсветку убыточных строк,
> столбики прибыли по месяцам и полосу быстрого перехода по разделам. Решения по семи развилкам
> контракта в силе все. Контракт: раздел «Дополнение от 10 сентября 2026 года», тот же файл.

**Порядок не меняется: сначала шесть задач S10 и слияние.** Задачи дополнения встают по слоям —
счёт раньше отчёта, отчёт раньше разметки, разметка раньше вида — и помечены буквой «Д».

### Пересчитанная цена якорей: ноль из двадцати восьми

Пересчитано командой: для каждого из 28 якорей разметки отчёта проверено, цитирует ли он что-то из
задеваемого дополнением. Задевают трое — `nav-links-drop-month`, `items-order-reversed`,
`gaps-only-when-nonzero`; переводится ноль, и почему — таблицей в контракте. `id` ставятся на
открывающие теги `<section>`, а их не цитирует ни один якорь.

- [ ] **Условие единственности.** Новая разметка не повторяет ни одной из 28 цитат. Доказывается
      прогоном всех прежних списков на голове слияния: неединственная цитата отказывает сама.
- [ ] Если без обёртки или переотступа не выходит — находка к владельцу, а не решение по дороге.

### Ограничение на весь кусок, найденное разведкой дополнения

- [ ] **Все новые поля `MonthReport` — необязательные**, и дополнения, и основного плана. Проверка
      типов покрывает `__tests__/`, а принятые проверки прошлых кусков собирают отчёт руками.
      Обязательное поле уронило бы `npm run build` на их раскладках. Прецедент: `устарели?`.
- [ ] Отсутствие поля экран читает как «сказать нечего» и не рисует блок.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Сделать одно из новых полей обязательным | `npm run build` — проверка типов на раскладках принятых проверок |

### Задача Д-1 — признак убыточной строки, в SQL

Решение по развилке Д1 ждёт владельца; рекомендация — **прибыль строки строго меньше нуля**.

- [ ] Одна колонка `MONTH_ITEMS`: признак «в минусе». Счётчик над таблицей и подсветка строки берут
      **этот же** признак, а не каждый свой.
- [ ] Прежние колонки `MONTH_ITEMS` не трогаются ни значением, ни местом, ни округлением.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Считать убыточной строку с прибылью ровно ноль | «в минусе — строго меньше нуля» |
| Завести подсветке свой признак, отдельный от счётчика | «подсветка и счётчик „в минусе“ берут один признак» |

### Задача Д-2 — прибыль по месяцам и доли столбиков, в SQL

- [ ] Один запрос на все месяцы, выражения прибыли — из `MONTH_TOTALS`, без переписывания.
- [ ] Доля столбика — прибыль месяца ÷ наибольшая по модулю прибыль среди месяцев с заказами,
      процентами **со знаком**; округление одно и в последнем `select`.
- [ ] Месяц без заказов — доля `null` (решение по развилке Д3 ждёт владельца; рекомендация — без
      столбика).
- [ ] Все месяцы без заказов или все с нулевой прибылью — делитель ноль, доли `null`.
- [ ] Читается тем же снимком фактов, что и весь экран.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Завести второе выражение прибыли вместо выражений `MONTH_TOTALS` | «прибыль месяца в переключателе — та же, что в отчёте этого месяца» — на мартовских данных это сверенные 1 738,53 € |
| Отдать отрицательную прибыль нулём | «отрицательная прибыль месяца — отрицательная доля, а не ноль» |
| Дать столбик месяцу без заказов | «месяц без заказов — без столбика» |
| Снять `nullif` у делителя доли | «наибольшая прибыль ноль — доли пустые» |
| Считать делитель по всем месяцам, включая пустые | «делитель — наибольшая по модулю прибыль среди месяцев с заказами» |

### Задача Д-3 — новые поля отчёта дополнения

- [ ] `months[].profitShare?` и признак «в минусе» у строк товаров — необязательные.
- [ ] `report.ts` не считает ничего.

### Задача Д-4 — подсветка убыточных строк, разметка и вид

- [ ] Строка получает атрибут из готового признака; разметка не сравнивает прибыль с нулём сама.
- [ ] **Цвет не единственный признак: рядом стоит само число маржи, по которому строка подсвечена.**
- [ ] Красный — смысловой токен Fluent, не спорящий с красным отказов.
- [ ] **Контраст текста на подсветке замеряется числами в обеих темах, крупный и мелкий**, с
      отрисованной страницы в том же безголовом Chrome, которым делаются снимки, — а не считается из
      объявленного. Урок S10: объявленные цвета отключённой кнопки до экрана не доходили вовсе.
- [ ] На мартовских данных подсвечено ничего — названо на снимке и в теле pull request.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Сравнить прибыль с нулём в разметке вместо готового признака | «разметка печатает признак, который ей дали» — на раскладке, где признак нарочно расходится со знаком прибыли |
| Подсветить строку, убрав рядом число маржи | «подсветка не единственный признак: рядом число маржи» |
| Взять для подсветки красный цвет отказа | проверка чисел таблицы стилей: смысловой токен подсветки назван отдельно от токена отказа |

### Задача Д-5 — столбики прибыли в переключателе месяцев

Решение по развилке Д2 ждёт владельца; рекомендация — внутри кнопок переключателя.

- [ ] Столбик вкладывается внутрь ссылки месяца **после** текста месяца; строка `href` не меняется.
- [ ] Высота — готовая доля из SQL, в величину стиля: приём полосы доли честности и водопада.
- [ ] Отрицательная доля — столбик под линией отсчёта.
- [ ] Столбик `aria-hidden`; имя ссылки — только месяц. Нового видимого текста нет.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Взять высоту столбика не из той доли, что дал отчёт | «высота столбика — ровно доля из отчёта» |
| Нарисовать отрицательную долю вверх или нулевой | «отрицательная доля — столбик вниз» |
| Снять `aria-hidden` со столбика | «имя ссылки месяца — только месяц» |
| Положить в столбик видимый текст | перепись экрана |
| Сломать ссылку месяца, вкладывая столбик | принятая проверка S5 про ссылки месяцев |

### Задача Д-6 — полоса быстрого перехода

- [ ] Ссылки якорные, вида `#…`, на шесть разделов: «Выручка», «Затраты», «Итог», «Товары»,
      «Честность данных», «Неполнота данных». `id` — на открывающих тегах `<section>`.
- [ ] Полоса живёт только в `Dashboard`: страница входа и страницы отказов её не получают по
      устройству.
- [ ] Плавность подматывания — только внутри `@media (prefers-reduced-motion: no-preference)`.
- [ ] Подписи полосы — новый видимый текст: список переписи дополняется ими в задаче 7.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Поставить одной ссылке полосы `href="/"` вместо якоря | принятая проверка S5: на странице отчёта нет ссылки на корень |
| Вставить якорную ссылку на страницу входа | принятая проверка S6: на странице входа нет ни одного `<a` |
| Убрать `id` у одного раздела | «у каждой ссылки полосы есть раздел с таким `id`» |
| Вынести плавность из-под `prefers-reduced-motion` | «плавность подматывания живёт только при no-preference» — **проверка текста, названа слабой** |

**Чего полоса не сторожит, и это названо.** Видимость фокуса на её ссылках держит общее правило
`:focus-visible`, у которого слома нет с S10. Снятое правило не покраснит ни одной проверки — ловит
это только глаз и проход клавиатурой в задаче 18.

### Что дополнение меняет в названных ценах и ловушках

- **Цена:** на мартовских данных подсветка не видна вовсе — все двенадцать артикулов в плюсе.
- **Цена:** у столбиков в переключателе нет числа, и читалкам экрана они не видны.
- **Пути, ни разу не встреченные в настоящих данных,** — дописываются: строка с отрицательной
  прибылью; месяц с заказами и отрицательной прибылью; все месяцы с нулевой прибылью.
- **Путь, встреченный в настоящих данных:** январь — месяц без заказов, но с расходами. Владелец
  увидит его на экране сразу.

### Работа владельца, добавленная дополнением

| Что | Когда |
|---|---|
| ответить на развилки Д1, Д2, Д3 | до задачи Д-1 |
| принять обновлённый план | до задачи Д-1 |
| пройти полосу клавиатурой и посмотреть фокус на ней | задача 18 |

## Поправка от 10 сентября 2026 года — столбики по месяцам отменены, вместо них суточный ряд

> Отдельным разделом. **Причина:** владелец принял развилку Д2 ошибочно — не передал договорённость.
> **Задачи Д-2 и Д-5 выше отменены** и остаются в плане как след решения; не исполняются. Д-1, Д-3,
> Д-4, Д-6 в силе. Проверка довода владельца и сравнение цены — в контракте, раздел «Поправка от
> 10 сентября 2026 года к дополнению».

**Развилки поправки ждут владельца:** Д2′-а — что рисует ряд (рекомендация: чистая выручка по дням);
Д2′-б — день без заказов (рекомендация: разрыв с пунктирной линией отсчёта).

### Задача Д-7 — чистая выручка по дням, в SQL

- [ ] Запрос берёт готовую цепочку `money` из `MONEY_CTES` и группирует по дню заказа. Своих
      выражений денег у него нет.
- [ ] Ряд — **все дни месяца**, а не только дни с заказами: день без заказов приходит со значением
      `null`, а не отсутствует и не ноль.
- [ ] Доля столбика — выручка дня ÷ наибольшая выручка дня в месяце, процентами; `nullif` на
      делителе. Отрицательная выручка дня возможна в грязных данных и даёт отрицательную долю.
- [ ] Подпись дня — готовой строкой из SQL (`1 марта`), форматирование даты в разметке не делается.
- [ ] Читается тем же снимком фактов, что и весь экран.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Завести своё выражение выручки вместо цепочки `money` | «сумма ряда по дням — чистая выручка месяца» — на мартовских данных это сверенные 17 277,04 € |
| Отдать день без заказов нулём | «день без заказов — `null`, а не ноль» |
| Выбросить день без заказов из ряда | «в ряду все дни месяца» |
| Снять `nullif` у делителя доли | «наибольшая выручка дня ноль — доли пустые» |
| Форматировать дату дня в разметке | «подпись дня приходит готовой строкой» |

### Задача Д-8 — блок суточного ряда, разметка и вид

- [ ] Новый соседний раздел между полосой показателей с водопадом и «Выручкой»; ни один
      существующий блок не оборачивается и не переотступается. Цена якорей — ноль, пересчитано.
- [ ] Высота столбика — готовая доля из SQL, в величину стиля: приём полосы доли честности.
- [ ] **У каждого дня доступная подпись с готовой строкой:** «5 марта: 612,40 €» либо «5 марта:
      заказов не было». Не видимый текст, переписи не касается.
- [ ] День без заказов — столбика нет, линия отсчёта под ним пунктиром.
- [ ] Месяц без заказов — блока нет, на его месте слова «нет данных за месяц».
- [ ] Подписи осей — пять строк переписи: заголовок блока, наибольшее значение, ноль, первый и
      последний день. Дифф переписи показывается владельцу в задаче 7.
- [ ] Чтение при 390 пикселях: тридцать один столбик в ширину экрана — проверяется замером, а не
      на глаз.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Взять высоту столбика не из той доли, что дал отчёт | «высота столбика — ровно доля из отчёта» |
| Снять доступную подпись у столбика | «у каждого дня доступная подпись с готовой строкой» |
| Подписать день без заказов «0,00 €» | «подпись дня без заказов — слова, а не ноль» |
| Нарисовать месяцу без заказов пустую рамку вместо слов | «месяц без заказов — слова „нет данных за месяц“» |
| Снять подпись оси | перепись экрана |

### Д1 — что записывается в отчёт, тело pull request и список путей

**Подсветка убыточных строк на настоящих данных не видна.** Все двенадцать мартовских артикулов в
плюсе. Механизм — сторож на случай, которого сегодня нет, и как «привлечение внимания к нужному» он
сегодня не работает вовсе. Так и пишется, словами владельца по смыслу, в трёх местах:

- [ ] в `docs/ОТЧЁТ.md`;
- [ ] в теле pull request, среди названных цен;
- [ ] в списке путей, ни разу не встреченных в настоящих данных: «строка товара в минусе».

### Оценка добавки к Д1: отметить строки, дающие 80 % прибыли — ждёт решения владельца

**Сначала — ошибка в принятом контракте, найденная этой оценкой.** Контракт определяет долю строки
в общей прибыли как «прибыль строки ÷ прибыль месяца × 100». Прибыль строки в таблице — чистая
выручка минус себестоимость, а прибыль месяца — после рекламы, комиссий и постоянных. **Наш счёт по
сверенному файлу «до»:** при таком делителе доли в сумме дают **647 %**, и один NP-004 — 94,5 %.
Это та же несравнимость баз, что владелец только что отверг в Д1, — и она сидела в моём принятом
определении. Единственный осмысленный делитель — **сумма прибыли строк**: 11 248,93 €, и тогда доли
дают ровно 100 %. Счётчик «сколько артикулов дают 80 % прибыли» считается от того же делителя.

**Цена отметки — не дорого:**

| Что | Сколько |
|---|---|
| Новых запросов | 0: признак берётся из того же окна, что уже считает «80 % прибыли» |
| Новых полей отчёта | 1, необязательное |
| Якорей переводится | 0: атрибут у `<tr>`, как у подсветки убыточных |
| Строк переписи | 0: видимого текста не добавляется |
| Пар контраста для замера | 1, в обеих темах, крупный и мелкий |
| Строк в таблицах сломов | 3 |

**Что она покажет на мартовских данных — наш счёт:** **8 строк из 12** — NP-004, NP-003, NP-009,
NP-012, NP-002, NP-010, NP-011, NP-006. Не отмечены четыре с наименьшей прибылью: NP-008, NP-001,
NP-007, NP-005. Отмечено две трети таблицы: прибыль у магазина распределена ровно, и отметка
говорит «важно почти всё» — видна, но как «внимание к нужному» слаба.

**Условие «не цветом одним» здесь слабее, чем у убыточных, — и это надо знать до решения.** У
убыточной строки рядом стоит маржа со знаком минус — второй признак читается без цвета. У строки
из «80 %» рядом стоит её доля, но принадлежность к набору по одной доле не видна: её пришлось бы
складывать в уме. Значит второй признак нужен **формой** — например, утолщённая левая граница строки
из шкалы толщин Fluent. Отметки двух видов не пересекаются по устройству: строка в минусе не может
входить в набор, дающий 80 % положительной прибыли.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Взять признак «80 %» не из того окна, что счётчик | «отметка строк и счётчик „80 % прибыли“ считаются одним окном» |
| Делить долю строки на прибыль месяца | «доли строк в сумме — ровно 100 %» |
| Оставить отметку одним цветом, без признака формы | проверка чисел таблицы стилей: у отметки есть толщина из шкалы Fluent |

## Решения владельца по поправке — 10 сентября 2026 года

> Отдельным разделом. Решения записаны в контракте, раздел с тем же названием; здесь — что они
> меняют в задачах.

| Вопрос | Решение |
|---|---|
| Д2′-а — что рисует ряд | **чистая выручка по дням** — задачи Д-7 и Д-8 исполняются |
| Д2′-б — день без заказов | **разрыв, линия отсчёта пунктиром**, подпись «заказов не было» |
| Делитель доли строки в прибыли | **сумма прибыли строк** — меняет задачу 4 |
| Отметка строк «80 %» | **не делается** — оценка выше остаётся как след решения |

### Что меняется в задаче 4 основного плана

- [ ] Доля строки в прибыли: прибыль строки ÷ **сумма прибыли строк** × 100, а не ÷ прибыль месяца.
      Сумма прибыли строк — новое поле отчёта, готовой строкой из SQL; на мартовских данных
      11 248,93 €.
- [ ] Сумма прибыли строк не положительна — доли `null` у всех строк разом (прежде условие стояло
      на прибыли месяца).
- [ ] «Сколько артикулов дают 80 % прибыли» — от **той же** суммы прибыли строк.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Делить долю строки на прибыль месяца | «доли строк в сумме — ровно 100 %» |
| Считать «80 %» от прибыли месяца | «80 % считаются от суммы прибыли строк, той же, что у долей» |

### Условия владельца, которые переезжают в задачи разметки

**Подпись доли называет свою базу.** Человек, поделивший прибыль строки на прибыль месяца сам,
обязан увидеть из подписи, почему у него не сходится. Та же оговорка, что у порога окупаемости:
экран, по которому можно самому получить другой ответ, хуже экрана без числа.

- [ ] **Задача 11.** Заголовок колонки называет базу — долю в **прибыли товаров**, а не в прибыли.
      Строка над таблицей называет базу и её число: 80 % считаются от прибыли товаров — выручки
      минус себестоимость, **11 248,93 €**, — а не от прибыли месяца, 1 738,53 €.

**Блок суточного ряда называется выручкой и только выручкой.** Кривая на экране про прибыль
прочтётся как прибыль по дням, если подпись это позволит.

- [ ] **Задача Д-8.** Заголовок блока и подписи осей называют выручку; слова «прибыль» в блоке нет
      ни одного.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Убрать из строки над таблицей базу и её число | «строка над таблицей называет базу долей и её сумму» |
| Назвать колонку «Доля в прибыли», без слова «товаров» | перепись экрана и «заголовок доли называет базу» |
| Назвать блок ряда «Прибыль по дням» | «в блоке ряда слово „выручка“ есть, а слова „прибыль“ нет» |

### Отметка строк «80 %» — не делается

Оценка выше остаётся в плане как след решения, её три строки сломов не исполняются. Довод
владельца: на настоящих данных она отмечает восемь строк из двенадцати и говорит «важно почти всё»,
а второй способ подсветки в одной таблице при таком результате — шум. Строка словами над таблицей
говорит то же самое и не занимает таблицу.

## Названные цены — то, что уйдёт в тело pull request строками

| Цена | Довод |
|---|---|
| **Задание в части «округление до целых евро в таблице» не исполняется** | Округление в существующих колонках красит принятую проверку прошлого куска — `__tests__/metrics/report.test.ts`, строки 270–271. Править принятую проверку ради нового куска запрещено. Параллельные округлённые колонки завели бы одно число дважды |
| **Подпись времени говорит не то, что просит буква задания** | В базе нет времени разбора; есть время самого свежего сырья. Показываем то, что есть, и называем верно — миграция ради подписи не заводится |
| **Команда метрик показывает меньше, чем экран** | Решение владельца по развилке 3: побайтная сверка «до / после» остаётся настоящим сторожем старых чисел. Отдельная отложенная задача — ниже |
| **Дельт по товарам к прошлому месяцу нет** | Задание их не просит; это ещё один запрос и ещё две колонки |

## Отложенные задачи

**Команда метрик и экран показывают разное, и это разойдётся дальше.** Кусок добавляет на экран
дельты, водопад, две окупаемости, вклад, две колонки товаров и время разбора. `npm run metrics`
ничего из этого не печатает. Расхождение сознательное — иначе побайтная сверка «до / после»
перестала бы быть сторожем старых чисел, — но оставлять его без записи нельзя:

- **что расходится:** всё новое содержание куска;
- **почему так вышло:** вывод команды служит эталоном сверки старых чисел, и добавление строк
  сделало бы сравнение невозможным ровно в том куске, где оно нужнее всего;
- **чем грозит:** два места, считающие одно и то же, однажды разъезжаются, и замечает это не тот,
  кто заводил. Боевой прогон читает именно команду — человек у терминала увидит меньше, чем
  человек у экрана, и не будет знать, что чего-то не видит;
- **чем закрывается:** отдельный кусок, который дописывает новые числа в команду и заново
  закрепляет эталон сверки — новым файлом «до», снятым уже после слияния S11.

## Известные ловушки, которые выстрелят потом

- **Перепись экрана правится всякий раз, когда на экран сознательно добавляется видимый текст.** В
  этом куске цена названа и оплачена диффом; в следующем будет снова.
- **Пути, ни разу не встреченные в настоящих данных.** Месяц без рекламы; неположительная прибыль
  месяца; артикул с отрицательной прибылью; `null`-маржа; месяц без предыдущего вовсе. На
  мартовских данных ни один из них не виден — проверяются они выдуманными раскладками, и это
  сказано вслух.
- **Порог безубыточности спорен по устройству.** Три защитимых определения дают 1,75, 2,80 и 9,90,
  и два из них говорят «окупается», третье — «нет». Выбранное определение и вывод помечены нашими
  везде, где появляются. Любая правка определения обязана начинаться с этого абзаца.
- **Признак «рост — это хорошо» живёт в слое счёта.** Новый показатель, заведённый без него,
  получит цвет по знаку молча.

## Работа, которую делает владелец руками — одним списком заранее

| Что | Когда |
|---|---|
| довести шесть оставшихся задач S10 и влить ветку в `main` | до задачи 0 |
| разрешить живой заход для снятия «до» | задача 0 |
| посмотреть дифф переписи экрана и прогон четырёх сломов до и после | задача 7 |
| посмотреть три пары «до / после» и сказать, принят ли вид | задача 16 |
| разрешить живой заход для снятия «после» | задача 14 |
| пройти экран глазами по боевому адресу и сказать «принято» | задача 18, обязательный шаг |

Ни ключей, ни паролей, ни новых переменных среды кусок не заводит. Ни одной миграции и ни одного
ручного запроса в боевую базу он не требует.

---

## Вкладки вместо полосы быстрого перехода — 10 сентября 2026 года

> Отдельным разделом, а не правкой задач выше: принятый план остаётся таким, каким его приняли.
> **Причина:** владелец прислал макет экрана и решил развилку — **вкладки**, полоса быстрого
> перехода отменена. Контракт: раздел «Макет и вкладки — 10 сентября 2026 года», тот же файл, —
> там что из макета берётся, что нет, развилки В0–В7, просьбы П1–П3 и цена.
>
> **Задачи ниже ждут ответов владельца** на развилки и просьбы. Пока ответа нет, в задачах стоят
> рекомендованные варианты, и это помечено у каждой задачи, которую ответ меняет.

### Что отменено и что изменено в прежних задачах

| Прежнее | Что с ним |
|---|---|
| Задача Д-6 — полоса быстрого перехода, 4 строки сломов | **отменена**, остаётся как след |
| «Порядок на экране» в задачах 8–12 | заменён раскладкой по вкладкам ниже |
| Задача 10 — окупаемости «парами после существующей» внутри «Итога» | заменена задачей В-5: свой блок (развилка В7). Условия владельца к задаче 10 — порог и вклад числом, база в подписи, пометка «наше» — **в силе все три** |
| Задача 7 — перепись одним списком | заменена задачей В-4 |
| Задача 16 — снимки | заменена задачей В-7 |
| Задача 2 — «ровно восемь ступеней» | при варианте А развилки В6 заменяется задачей В-9 |
| Обязательство «ноль якорей из двадцати восьми» | **два** — задачи В-1 и В-2 |
| Задача Д-8 — блок ряда «между полосой показателей с водопадом и «Выручкой»» | в силе: на вкладке «Главное» третьим |

### Раскладка по вкладкам

| Где | Что, по порядку |
|---|---|
| Шапка, на каждой вкладке | название, месяцы, строка «источники прочитаны по состоянию на …», «Обновить данные», «Выйти»; пометка устаревания над всем |
| Полоса вкладок, на каждой вкладке | «Главное», «Деньги», «Товары», «Качество данных» |
| «Главное» — адрес без `tab` | полоса показателей → водопад → суточный ряд |
| «Деньги» — `tab=dengi` | «Выручка» и «Затраты» рядом → «Итог» → «Окупаемость» |
| «Товары» — `tab=tovary` | строка над таблицей → таблица |
| «Качество данных» — `tab=kachestvo` | «Честность данных» → «Неполнота данных» |

Первой вкладке — `tab=glavnoe`, если имя написано явно. Порядок разделов в отчёте совпадает с
порядком вкладок.

**Что крупное на каждой вкладке.** «Главное» — четыре значения полосы показателей, одной ступенью
шкалы. «Деньги» — прибыль и маржа итога и окупаемость по обороту в своём блоке: три, как принято в
S10. «Товары» и «Качество данных» — крупных нет.

### Задача В-0 — пересчёт якорей на голове слияния

- [ ] Тем же перебором, что в контракте: в `app/page.tsx` и `app/refresh-panel.tsx` — **28 цитат**,
      задевают строки, которые трогают вкладки, — **две**: `nav-links-drop-month` и
      `button-not-on-screen`. Любое другое число — стоп и разговор до первой строки кода.

### Задача В-1 — вкладка из адреса

- [ ] Страница читает `tab` так же, как `m`: два значения — берётся первое; нет — «Главное»; одно
      из четырёх имён — оно; иное — отказ словами (развилка В4, рекомендация А).
- [ ] Вкладка передаётся разметке. **Перевод слома `button-not-on-screen`**: в `find` и `replace`
      строка `<Dashboard report={report} />` становится `<Dashboard report={report} tab={вкладка} />`;
      `claim` и `mustRedden` — слово в слово; прогон — «покраснела своя».
- [ ] После П1: обвязка двух принятых проверок — `tab: 'tovary'` в трёх строках вызова (S5 — две,
      S6 — одна). Утверждения не трогаются. Прогоны: `month-param-ignored` (S5) — «покраснела
      своя»; новый слом «страница не читает вкладку» — «покраснела своя», S5 и S6 — заодно. Дифф
      обоих файлов — только эти три строки, вывод сравнения в заявку.
- [ ] **Допущение, не сверенное с документацией Next:** после нажатия «Обновить данные» страница
      перерисовывается по тому же адресу, вместе с вкладкой. Проверяется живым проходом в задаче 18;
      не подтвердится — находка к владельцу.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Не читать `tab` — всегда первая вкладка | «вкладка из адреса определяет, какие разделы на экране»; заодно S5 «месяц из адреса определяет, что видно на экране…» и S6 «страница отчёта с годной cookie печатает числа» — после П1 обе просят «Товары» |
| Адрес без `tab` отдать весь отчёт вместо первой вкладки | «адрес без вкладки — первая вкладка, и только она» |
| Неизвестную вкладку молча показать первой | «неизвестная вкладка — отказ словами, а не первая вкладка» |
| Из двух значений `tab` взять последнее | «из двух значений вкладки берётся первое, как у месяца» |

### Задача В-2 — разделы по вкладкам и полоса вкладок

- [ ] Маленький компонент раздела: на своей вкладке рисует `<section>` с прежним классом, на чужой —
      ничего, без вкладки — рисует (развилка В2, рекомендация А: без вкладки — весь отчёт).
- [ ] У шести существующих разделов меняются **только строки открывающего и закрывающего тега** —
      двенадцать строк. Строки внутри разделов не сдвигаются ни на байт; дифф этих разделов
      показывается в заявке. Это отступление от буквы прежнего обязательства «не оборачивается» —
      названо в контракте.
- [ ] Полоса вкладок — `<nav>` с подписью для читалок после шапки. Ссылки `/?m=<месяц>&tab=<имя>`;
      при отчёте без месяца — `/?tab=<имя>`; ни одна не ведёт на корень. У текущей
      `aria-current="page"`, у прочих — нет. Ролей `tablist` и `tab` нет — довод в контракте.
- [ ] Без вкладки полосы нет: в виде «весь отчёт» текущей вкладки не бывает.
- [ ] Ссылки месяцев несут текущую вкладку (развилка В3, рекомендация А). **Перевод слома
      `nav-links-drop-month`**: `` href={`/?m=${m.month}`} `` → `` href={`/?m=${m.month}${хвостВкладки}`} ``;
      без вкладки хвост пуст, и принятая проверка S5 видит прежние `href="/?m=2026-01"`. Прогон —
      «покраснела своя».
- [ ] Новая разметка не повторяет ни одной из 28 цитат.
- [ ] Ни клиентского кода, ни зависимостей: дифф `package.json` и `package-lock.json` пуст, вывод
      сравнения в заявку.

**Проверка «каждый раздел ровно на одной вкладке»** сравнивает текст разделов в виде «весь отчёт» с
текстами четырёх вкладок, сложенными по порядку, без шапки и полосы. Это сверка двух отрисовок
одного кода между собой, **и якоря снаружи у неё нет** — якорь у разбиения один: буквальные списки
переписи по вкладкам (задача В-4). Названо, чтобы её зелень не читали как доказательство сверх этого.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Отдать один раздел двум вкладкам | «каждый раздел — ровно на одной вкладке, порядок вкладок — порядок отчёта» |
| Не отдать раздел ни одной вкладке | «каждый раздел — ровно на одной вкладке, порядок вкладок — порядок отчёта» |
| Ссылке вкладки потерять месяц | «ссылки вкладок сохраняют месяц» |
| Ссылке месяца потерять вкладку | «ссылки месяцев сохраняют вкладку» |
| Ссылке первой вкладки дать `href="/"` | «ни одна ссылка вкладок не ведёт на корень» |
| Снять `aria-current` у текущей | «у текущей вкладки признак текущей в разметке, у прочих его нет» |
| Поставить `aria-current` всем вкладкам | «у текущей вкладки признак текущей в разметке, у прочих его нет» |
| Дописать пятую вкладку | «в полосе вкладок ровно четыре ссылки, в названном порядке» |
| Рисовать шапку только на первой вкладке | перепись экрана: списки трёх других вкладок начинаются с шапки |
| Сделать полосу клиентским компонентом | «полоса вкладок без клиентского кода» — **проверка текста** (`'use client'` в файле полосы), названа слабой |

### Задача В-3 — вид полосы вкладок: рецепт Fluent 2

- [ ] Рецепт вкладки Fluent 2 перечитывается **на коммите выписки S10 `43665d5`** и сверяется с
      нашей таблицей стилей **по каждому свойству блока**, а не по нужной строке. Расхождение с тем,
      что выписано в контракте с `master`, называется в заявке.
- [ ] После П3: `colorCompoundBrandStroke` (обе темы) и `strokeWidthThicker` дописываются в выписку
      S10, каждая с цепочкой звеньев «файл:строка» до значения. Дифф файла выписки — только
      добавленные строки, вывод сравнения в заявку.
- [ ] Контраст замеряется с отрисованной страницы, в том же безголовом Chrome, что снимки, обе темы:
      текст прочих вкладок к фону, текст текущей к фону (мелкий текст, 4,5 : 1), указатель к фону
      (не текст, 3 : 1). Решение — по худшей стороне.
- [ ] Видимость фокуса держит общее правило `:focus-visible`, **у которого слома нет с S10**. Снятое
      правило не покраснит ничего — ловит его проход клавиатурой в задаче 18. Там же записывается
      порядок остановок Tab: он идёт по разметке, а не по виду.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Убрать у текущей вкладки полосу-указатель | «у текущей вкладки полоса-указатель толщиной strokeWidthThicker» — **проверка текста таблицы стилей**, названа слабой |
| Убрать у текущей вкладки насыщенность | «у текущей вкладки насыщенность fontWeightSemibold» — **проверка текста**, названа слабой |
| Вписать указателю цвет вне выписки | принятая проверка S10 «в таблице стилей нет ни одной величины и ни одного цвета вне набора Fluent 2» |

### Задача В-4 — перепись экрана по вкладкам (вместо задачи 7)

Правка разрешена владельцем, и разрешение шире прежнего: меняется не только список, но и строка
вызова. **Разрешение на правку — не разрешение ослабить утверждение.**

- [ ] Прогон четырёх сломов S9 **до** правки: `census-blind-to-swap`, `labels-swapped`,
      `items-order-reversed` — «покраснела своя»; `gaps-truncated` — перепись в списке заодно.
- [ ] Правка: четыре списка ожидаемого; строка вызова рисует четыре вкладки по списку имён,
      **выписанному в проверке буквально**. Имя проверки, тело `текстПоПорядку`, `toEqual` — ни на
      символ. Вторая проверка файла не трогается вовсе.
- [ ] Раскладка `fixture.ts` получает новые поля отчёта.
- [ ] Прогон тех же четырёх сломов **после** — тот же итог.
- [ ] Дифф файла переписи — владельцу целиком. В теле заявки отдельной строкой: разрешение шире
      прежнего, и в чём именно.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Заменить `toEqual` на `toContain` в переписи | «перепись экрана: весь видимый текст, по порядку и без единой потери» — сама |
| Убрать вкладку из приложения | перепись экрана: список убранной вкладки больше не совпадёт с отрисованным |

### Задача В-5 — «Выручка» и «Затраты» рядом, окупаемость своим блоком

- [ ] Две колонки на широком экране — правилом таблицы стилей у общего родителя, без обёртки
      разделов; на узком — одна под другой. Величины — из шкал Fluent либо названным исключением.
- [ ] Блок «Окупаемость» после «Итога» (развилка В7, рекомендация А): окупаемость по обороту
      переезжает из «Итога» крупным числом; по прибыли, вклад и порог — ступенью ниже. Условия
      владельца из задачи 10 в силе все три.
- [ ] **Раскладку в две колонки сторожат только снимки** — проверки у неё нет, и это названо.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Вернуть окупаемость по обороту в «Итог» | перепись экрана |

### Задача В-6 — доли статей затрат в обороте

Новая по просьбе владельца. Доли — **те же готовые доли ступеней водопада** из задачи 2: второго
выражения нет. Каждое значение уходит один раз и в число, и в длину полоски — приём полосы доли
честности. На мартовских данных, наш счёт: себестоимость 32,1 %, реклама 23,6 %, комиссии 2,8 %,
постоянные 24,3 %.

- [ ] Полоска и число у каждой из четырёх статей блока «Затраты».
- [ ] Оборот ноль — доли `null`, на экране слова, полосок нет.
- [ ] Доля рекламы полосы показателей (задача 1) равна доле ступени рекламы (задача 2) — проверкой;
      на мартовских данных обе обязаны дать 23,6 %, наш счёт по сверенным числам.
- [ ] Четыре строки переписи на вкладке «Деньги». Якорей — 0.
- [ ] Контраст: заполнение к дорожке (3 : 1) и число доли к фону (4,5 : 1), обе темы.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Взять длину полоски не из того значения, что напечатано числом | «полоска доли затрат и её число — из одного значения» |
| Считать долю статьи от чистой выручки, а не от оборота | «доля рекламы в полосе показателей равна доле ступени рекламы в водопаде» — и на марте 23,6 % |
| Оборот ноль — нарисовать 0 % | «оборот ноль — у долей затрат слова, а не ноль» |
| Завести блоку затрат своё поле доли вместо долей ступеней | «блок затрат и водопад печатают одну и ту же долю» — на раскладке, где поля нарочно различны |

### Задача В-7 — снимки (вместо задачи 16)

- [ ] 26 картинок, 18 страниц (развилки В4 и В5, рекомендация А у обеих). Первая вкладка — прежние
      номера `01`–`03`; прежние состояния рисуются с первой вкладкой.
- [ ] После П2: проверка генератора S10 — имя и два числа; имя переводится в 9 строках прежних
      списков (8 в S9, 1 в S10); прогон S9 и S10 — у каждого из девяти прежний итог.
- [ ] Пары «до / после» названы как есть: «до» — весь экран S10, «после» — первая вкладка.
- [ ] Опись снимков переписывается.
- [ ] Каждый показ снимков владельцу называет, что на них починено и что осталось.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Выронить состояние вкладки из описи | принятая проверка S10 после П2 — «генератор собирает полный набор и сверяет все восемнадцать страниц» |

### Задача В-8 — подписи водопада не наезжают на числа

Дефект макета, который не повторяется.

- [ ] Подписи ступеней стоят своей строкой **вне области столбиков**; число — у столбика. Наложение
      исключено устройством, а не подбором отступов.
- [ ] Замер с отрисованной страницы, широкий и узкий экран: пересечений прямоугольников подписи и
      числа — 0. Вывод в заявку. **Замер живёт вне `npm test`, и строки в списке сломов у него нет —
      названо.**

| Что ломаем | Что обязано покраснеть |
|---|---|
| Положить подпись ступени внутрь области столбиков | «подписи ступеней водопада — отдельной строкой вне области столбиков» — **проверка устройства разметки**, названа слабой |

### Задача В-9 — чистая выручка промежуточным итогом (при варианте А развилки В6)

Меняет принятое определение задачи 2: девять ступеней вместо восьми, три из них — итоги от нуля.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Считать промежуточный итог своим выражением | «промежуточный итог водопада — та же чистая выручка, что в блоке «Выручка»» — на марте 17 277,04 € |
| Нарисовать промежуточный итог висящим | «три итога водопада — от нуля: оборот, чистая выручка, прибыль» |
| Убрать ступень из набора | «в водопаде ровно девять ступеней, в названном порядке» — заменяет строку «ровно восемь» задачи 2 |

### Правило для всех новых проверок куска

**Новая проверка не имеет права краснеть на прежнем сломе.** Списки S9 и S10 гоняют весь набор
проверок. Новая проверка, покрасневшая на прежнем сломе, даст ему итог «ломает больше, чем
объявлено», а починить это можно только правкой прежнего списка. Значит новые проверки строятся так,
чтобы прежние сломы их не задевали: например, проверка подсветки строки товара не опирается на
порядок строк — его ломает `items-order-reversed`. Доказательство — прогон всех прежних списков на
голове слияния (задача 15).

### Что вкладки дописывают в названные цены

| Цена | Довод |
|---|---|
| **Честность данных не видна с первой вкладки** | на марте прибыль посчитана по настоящей цене поставщика для 80,3 % чистой выручки. Строка на первой вкладке — только по слову владельца |
| **Вид «весь отчёт» видят только проверки** (при В2-А) | принятые проверки S5 и S9 рисуют разметку без вкладки; разбиение сторожат перепись по вкладкам и проверка «ровно на одной вкладке» |
| **Четыре остановки Tab вместо одной со стрелками** | стрелки — клиентский код, условие 1 его запрещает |
| **Макет делит одну статью на две базы** | полоски затрат на макете — от чистой выручки, плашка над водопадом — от оборота. У нас база одна — оборот |
| **Пары «до / после» не одно с одним** | «до» — весь экран, «после» — первая вкладка |
| **Отступление от буквы «не оборачивается»** | у шести разделов меняются строки тегов; внутри — ни байта |

### Известные ловушки, которые выстрелят потом

- **Новая вкладка в следующем куске** меняет разом: перепись (пятый список), проверку «ровно четыре
  ссылки», число страниц и картинок в проверке генератора — а с ним имя в девяти строках прежних
  списков. Узнать: генератор и перепись краснеют вместе. Делать: называть цену числом до кода.
- **Разметка без вкладки рисует весь отчёт.** Если следующий кусок позовёт её без вкладки на
  настоящей странице, человек молча получит длинный экран. Сторожит слом «адрес без `tab` отдать
  весь отчёт».

### Работа владельца — полный список после вкладок

| Что | Когда |
|---|---|
| ответить на развилки В0–В7 и просьбы П1–П3 | до задачи В-0 |
| положить макет файлом в ветку контракта | до проверки кода, задача 19 |
| довести шесть оставшихся задач S10 и влить ветку в `main` | до задачи 0 |
| разрешить живой заход для снятия «до» | задача 0 |
| посмотреть дифф обвязки S5 и S6 и прогоны их сломов | задача В-1 |
| посмотреть дифф переписи и прогон четырёх сломов S9 до и после | задача В-4 |
| посмотреть 26 снимков и пары «до / после» и сказать, принят ли вид | задача В-7 |
| разрешить живой заход для снятия «после» | задача 14 |
| пройти полосу вкладок клавиатурой и посмотреть фокус и признак текущей | задача 18 |
| пройти экран глазами по боевому адресу и сказать «принято» | задача 18, обязательный шаг |

## Решения владельца по вкладкам — 10 сентября 2026 года: что они меняют в задачах

> Отдельным разделом. Решения и замер — в контракте, раздел «Решения владельца по макету и вкладкам
> — 10 сентября 2026 года». Здесь — что меняется в задачах В-0 — В-9. **Открыт вопрос Г** (семь
> проверок генератора S10); задачи ниже написаны по рекомендации А и помечены там, где ответ их
> меняет.

### Что отменено в задачах вкладок

| Прежнее | Что с ним |
|---|---|
| В-2: «без вкладки — рисует весь отчёт»; «без вкладки полосы нет» | **отменено решением В2**: без вкладки — «Главное» |
| В-2: проверка «ровно на одной вкладке» сравнивает вкладки с видом «весь отчёт» | сравнивает заголовки разделов четырёх вкладок с **буквальным списком десяти заголовков** в самой проверке |
| В-4: вторая проверка файла переписи «не трогается вовсе» | получает обвязку: вкладка «Качество данных» |
| Ловушка «разметка без вкладки рисует весь отчёт» | снята: такого вида нет |
| В-1: «после «Обновить данные» остаёмся на вкладке» — допущение | **требование со сломом** — задача В-1 ниже |

### Порядок работы — по рекомендации А вопроса Г

Переключение на вкладки идёт **одним шагом** и **после** блоков «Главного»: сначала задачи счёта и
отчёта (1–6, Д-1, Д-3, Д-7, В-9 в части SQL), затем блоки «Главного» (8, 9, Д-8) и остальная разметка
на прежнем одностраничном экране — это настоящий экран до шага вкладок, а не вид для проверок, —
**затем** шаг вкладок: В-1 и В-2 вместе с обвязкой пятнадцати
проверок, переписью по вкладкам (В-4) и пересборкой эталонов снимков (В-7). Довод: прибор сломов не
работает на красном исходном состоянии, а эталон пустого «Главного» не сторожит ничего. При варианте
Б вопроса Г порядок другой, и план переписывается отдельным разделом.

### Задача В-1 — дописывается

- [ ] Обвязка **пятнадцати** принятых проверок — только строки вызова, утверждения не трогаются:

| Файл проверок | Проверок | Вкладка в строке вызова |
|---|---|---|
| проверки экрана S5 | 9 | 8 — одна вкладка; «экран печатает поля отчёта, а не свои числа» — три вкладки, сложенные |
| полоса доли честности S9 | 3 | «Качество данных» |
| перепись S9, вторая проверка | 1 | «Качество данных» |
| месяц из адреса S5 | 1 | «Товары» — две строки вызова |
| закрытые пути S6 | 1 | «Товары» |

- [ ] Дифф каждого из пяти файлов — только строки вызова; вывод сравнения с числом изменённых и
      удалённых строк по каждому — в заявку.
- [ ] **Доказательство каждой прежним сломом** — после того как набор зелён целиком. У 11 из 15
      прежний слом есть: `gaps-truncated`, `share-bar-fixed-width`, `share-bar-other-value`,
      `share-bar-without-value` (S9); `month-param-ignored`, `screen-computes-profit`,
      `screen-diverges-from-report`, `roas-label-removed`, `honest-label-removed`,
      `gaps-only-when-nonzero`, `gaps-no-address` (S5); `items-order-reversed` (S9) — заодно. У четырёх
      прежнего слома нет — их доказывают новые сломы S11 из таблицы ниже.
- [ ] Отказ на незнакомую вкладку называет четыре вкладки ссылками, первой — «Главное», сохраняет
      правильный месяц, оставляет кнопку выхода.
- [ ] **Требование:** после «Обновить данные» страница остаётся на той же вкладке. Наш код сторожит
      то, что действие не уводит со страницы; сохранение адреса при перерисовке — поведение Next
      (цитата с условием — в контракте), наблюдается в задаче 18 на каждой из четырёх вкладок.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Дописать действию кнопки переход на `/` после работы | «после «Обновить данные» действие не уводит со страницы»; заодно S6 «действие кнопки с годной cookie работу делает» — оно ждёт исход, а не переход |
| Отдать незнакомой вкладке отказ без ссылок | «отказ на незнакомую вкладку называет разделы ссылками, и первая — «Главное»» |
| Печатать окупаемость по обороту форматом процента | S5 «окупаемость печатается отношением, а не процентами» — прежнего слома у неё нет; заодно S5 «экран печатает поля отчёта, а не свои числа» |
| Печатать маржу сырым значением, мимо формата | S5 «нет данных печатается словами, а не как 0 и не как NaN» — прежнего слома у неё нет |
| Снять строку «Без цены поставщика» | S5 «экран называет товары без цены поставщика» — прежнего слома у неё нет; заодно перепись экрана |

Четвёртую проверку без прежнего слома — S6 «страница отчёта с годной cookie печатает числа» —
доказывает уже вписанный слом «не читать `tab`».

### Задача В-6 — дописывается: база названа в подписи

- [ ] Подпись доли у каждой статьи затрат называет базу — «% оборота», — как у ступеней водопада и у
      доли рекламы в полосе показателей. Несходимость макета названа в подписи к нему.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Убрать из подписи доли затрат базу «оборота» | перепись экрана |

### Задача В-5 — дописывается: база порога

- [ ] Порог стоит рядом с окупаемостью по обороту, крупной; в подписи порога — база: от вклада, а не
      от маржи экрана; вклад рядом числом. Сломы задачи 10 про порог и вклад в силе.

### Задача В-7 — дописывается

- [ ] После перевода имени проверки генератора в девяти строках — **все семь прежних списков
      гоняются целиком** на голове, которая пойдёт в слияние, вывод с кодом возврата — в заявку.
- [ ] Эталоны снимков пересобираются **один раз**, на шаге вкладок; утверждения семи проверок
      генератора не трогаются. Потребует правки хоть одно — стоп и к владельцу.

### Прибор замера

Местная ветка замера от головы S10 — **не для слияния и не выложена**. Её дифф показан владельцу
целиком. Работа S11 идёт по-прежнему от `main` после слияния S10, а изменение замера переносится туда
в шаге вкладок и там же доказывается.

### Работа владельца, добавленная решениями

| Что | Когда |
|---|---|
| ответить на вопрос Г — семь проверок генератора S10 | до шага вкладок |
| посмотреть дифф обвязки пятнадцати проверок и прогоны их сломов | шаг вкладок |
| посмотреть, осталась ли страница на вкладке после «Обновить данные», на каждой из четырёх | задача 18 |

## Вопрос Г решён — 10 сентября 2026 года: вариант А

> Решение и довод владельца — в контракте, раздел «Решение владельца по вопросу Г». Порядок работы
> из раздела выше («по рекомендации А вопроса Г») становится действующим.

### Шаг вкладок — условия владельца как пункты проверки

- [ ] Эталоны снимков пересобраны **один раз**, на экран с готовыми блоками «Главного». Нужен
      промежуточный пересбор — стоп и к владельцу.
- [ ] Утверждения семи проверок генератора S10 — ни одной правки. Дифф их файла — только имя и два
      числа по П2; вывод сравнения в заявку. Потребовало правки утверждение — стоп и к владельцу.
- [ ] Четыре проверки без прежнего слома — S6 «страница отчёта с годной cookie печатает числа», S5
      «окупаемость печатается отношением, а не процентами», S5 «нет данных печатается словами, а не
      как 0 и не как NaN», S5 «экран называет товары без цены поставщика» — сначала получают новый
      слом S11, и его прогон показывает «покраснела своя» **именно у этой проверки**; только после
      этого их обвязка считается доказанной. Слом красит соседнюю — слом сужается.
- [ ] Все доказательства — на полностью зелёном наборе, код возврата 0 снят отдельной строкой до
      первого прогона сломов.

### Что идёт следом — до всякой задачи S11

| Шаг | Что |
|---|---|
| 1 | заявка №14 — сдача S10 |
| 2 | слияние S10 владельцем |
| 3 | задача 0 и В-0 этого плана: ветка S11 от `main`, пересчёт якорей — 28 цитат, задеты 2 |

## После задачи 0 — 10 сентября 2026 года: на настоящих данных один месяц

> Отдельным разделом. Разбор — в контракте, раздел «Задача 0 — живой заход, 10 сентября 2026 года».
> Здесь — что это меняет в задачах.

- [x] **Задача В-0 выполнена на `8bf2e3a`:** 28 цитат, все единственные, задеты две —
      `button-not-on-screen` и `nav-links-drop-month`.
- **Неверная строка этого плана**, раздел «Что дополнение меняет в названных ценах и ловушках»:
  «Путь, встреченный в настоящих данных: январь — месяц без заказов, но с расходами.» Январь — из
  раскладки снимков S10, в данных его нет. Строка оставлена как след.

### Пути — пересобранный список

| Путь | Где он теперь |
|---|---|
| Нет базы для сравнения — вся полоса дельт словами | **единственный на настоящих данных** |
| Дельты с числами, цвет по признаку «рост — это хорошо» | ни разу не встречен; только выдуманные раскладки |
| Месяц без заказов — «нет данных за месяц» (Д3) | ни разу не встречен |
| Прошлый месяц без рекламы | ни разу не встречен |
| Суточный ряд за месяц с заказами | встречен: март, 31 день; дни без заказов внутри марта этим заходом не проверены |

Проверка задачи 1 «нет предыдущего месяца — дельты пустые, а не нулевые» сторожит **боевой** путь,
а не редкий: её слом — первый, на который смотреть на приёмке.

### Вопрос Е1 ждёт владельца

Снимки «Главного» на пути настоящих данных — рекомендация: +2 снимка, 26 → 28. До ответа задача
В-7 считается по 26.

## Новый порядок задач — 10 сентября 2026 года

> Отдельным разделом: прежний порядок остаётся как след. **Причина — решение владельца после
> задачи 0:** на настоящих данных один месяц, и полоса дельт сегодня не отвечает ни на что, а всё
> остальное отвечает. Дельты — в конец, механизм делается полностью: работодатель подтвердил, что
> месяцы будут добавляться. Контракт: раздел «Решения владельца после задачи 0».

### Порядок

| Шаг | Задачи | На что отвечает на одном месяце |
|---|---|---|
| 1 | **Водопад:** 2 и В-9 (SQL, девять ступеней, доли от оборота), поле отчёта, 9 (разметка), В-8 (подписи вне области столбиков) | куда ушли деньги |
| 2 | **Суточный ряд:** Д-7, Д-8. Живой заход этого шага проверяет, есть ли в марте дни без заказов — разрешён владельцем | как шёл месяц |
| 3 | **Окупаемости:** 3, 10. Блок «Окупаемость» после «Итога» с тремя новыми числами; окупаемость по обороту стоит в «Итоге» до шага вкладок | окупается ли реклама |
| 4 | **Товары:** 4 с делителем «сумма прибыли строк», Д-1, Д-3, 11, Д-4 | какие товары тянут вниз |
| 5 | **Доли статей затрат:** В-6 | из чего состоят затраты |
| 6 | **Время чтения источников:** 5, 12 | насколько свежи числа |
| 7 | **Дельты и доля рекламы:** 1, 8 — полоса показателей | стало ли лучше; на бою — путь «нет базы» |
| 8 | **Шаг вкладок:** В-1 — В-5, переезд окупаемости по обороту, перепись по вкладкам, 28 снимков, пересборка эталонов **один раз** | — |
| 9 | 13 — вид, 14 — доказательства, 15 — сломы на голове слияния, 16–19 — сдача | — |

### Правила работы до шага вкладок

- [ ] **Разметка существующих блоков не меняется ни на байт.** Новые блоки рисуются только при
      наличии своих полей отчёта. Тогда принятые проверки и генератор снимков остаются зелёными, и
      эталоны пересобираются один раз — на шаге вкладок. Понадобилась правка существующего блока
      раньше — стоп и к владельцу.
- [ ] **Каждый шаг заканчивается на полностью зелёном наборе**: код возврата 0 снят отдельной
      строкой, затем прогон сломов этого шага — «покраснела своя» у каждой строки.
- [ ] Перепись получает новые поля в раскладку и новые строки в список по шагам — это правка,
      разрешённая владельцем. Прогон четырёх сломов S9 до и после — на шаге вкладок, когда список
      принимает окончательный вид, и на голове слияния.
- [ ] Проверка «доля рекламы в полосе показателей равна доле ступени рекламы» из задачи В-6
      переезжает в шаг 7: долю рекламы полосы считает запрос дельт.

### Шаг 7 — дописывается: вид полосы и переход путей

- [ ] **Значение показателя стоит всегда** — прибыль, маржа, чистая выручка, доля рекламы числами.
      Когда базы нет, пуста только дельта: одна короткая строка на карточку.
- [ ] **Переход между путями — сам, от данных.** Ни флага, ни переменной среды, ни настройки:
      появился предыдущий месяц с заказами — дельты считаются; исчез — снова слова.
- [ ] Путь «нет базы для сравнения» — **боевой**: снимок в обеих темах, свой слом наравне с путём с
      числами.

| Что ломаем | Что обязано покраснеть |
|---|---|
| Скрыть значение показателя, когда базы нет | «нет базы для сравнения — значения четырёх показателей стоят числами, пуста только дельта» |
| Запереть дельты постоянным «нет базы» | «появился предыдущий месяц — дельты считаются; исчез — снова слова» — на местной базе, одним и тем же запросом до и после добавления месяца |
| Включать путь с числами переменной среды | «появился предыдущий месяц — дельты считаются; исчез — снова слова» — проверка идёт без переменной |

### Шаг 8 — дописывается

- [ ] Снимков **28**: к 26 — «Главное» на пути «нет базы для сравнения», светлая и тёмная, широкий
      экран. Страниц генератора — 20. В проверке генератора по П2 — 20 страниц и 28 картинок.

### Работа владельца — дописывается

| Что | Когда |
|---|---|
| **когда в Таблице появится второй месяц — зайти на боевой адрес и проверить дельты глазами**; до того дня путь с числами живьём не видел никто | после слияния S11, в день появления второго месяца |

## Шаг 1 — водопад: ход работы

### Сделано — счёт и поле отчёта

- [x] Запрос водопада: девять ступеней раскладывают строку итогов месяца целиком, своих выражений
      денег нет; доли и края — процентами от оборота, одно округление; оборот ноль — пусто.
- [x] Поле отчёта `waterfall` — необязательное, читается тем же снимком фактов.
- [x] Проверок — 8, из них одна настоящим путём, через отчёт на местной базе. Дифф по `__tests__/` —
      один новый файл, 181 добавленная строка, 0 удалённых.
- [x] Ворота: `npm test` — 62 файла, 786 проверок, код 0; `npm run lint` — код 0; проверка типов —
      код 0.
- [x] Сломы шага: 9 строк в `breaks/s11-content.ts`, у каждого набор — весь `npm test`. Прогон на
      чистом дереве: **9 из 9 — «покраснела своя», код возврата 0**, дерево на выходе чистое.

### Ждёт развилки Ж1 — разметка водопада

Вертикальному водопаду, как на макете, нужна высота области столбиков, а такой длины нет ни в одной
шкале Fluent. Разметка, вид, перепись и сломы разметки шага 1 — после ответа владельца.

### Сделано — разметка и вид водопада (решение Ж1: горизонтальный)

- [x] Блок «Куда ушли деньги» сразу после шапки, рисуется только при своём поле. Подписи полные.
      Итоги — сплошные от нуля, подпись и сумма полужирные; вычитания — светлая заливка с обводкой
      внутренней тенью, висят от остатка. У каждой ступени сумма и доля «… % оборота».
- [x] Узкий экран: подпись и сумма строкой, дорожка и доля — второй строкой.
- [x] Перепись: в раскладку — водопад с числами, не повторяющими ни одного числа раскладки, все доли
      числами; в список — 28 строк. Дифф: 28 и 18 добавленных строк, 0 удалённых.
- [x] Проверок вида — 7, из них две слабые и так названные: текст таблицы стилей и устройство
      разметки. Живая проверка якоря — оборот 18 764,00, чистая выручка 17 277,04, прибыль 1 738,53
      и прибыль ступени = прибыль итога — написана, запускается в живом заходе шага 2.
- [x] Ворота: `npm test` — 63 файла, 793 проверки, код 0; линтер — код 0, четыре предупреждения в
      принятой проверке S6 были до куска; проверка типов — код 0.
- [x] Цитаты всех восьми списков сломов — 284, все единственные: 28 якорей разметки отчёта целы.
- [x] Сломы шага: 15 строк; прогон на чистом дереве — **15 из 15 «покраснела своя», код 0**.
- [x] Черновой снимок — вне репозитория, на мартовских итогах: подписи не наезжают, ни широкий, ни
      узкий экран. Настоящие снимки — на шаге вкладок.

### Найдено по ходу

- **Проверка чисел таблицы стилей не ловит ссылку на необъявленную величину.** Я сослался на
  `spacingVerticalXXS`, которого в таблице нет, и проверка осталась зелёной: значение «ссылка» она
  засчитывает, не спрашивая, объявлена ли величина. Исправлено; вся таблица проверена командой —
  88 ссылок, необъявленных 0. Сама проверка принадлежит S10 и не открывается — находка в список
  отложенных.
- **Цент округления** — развилка Ж2 в контракте: цепочка показанных сумм на марте даёт 1 738,54 при
  прибыли 1 738,53.

### Условие 5 владельца — к шагу вкладок

- [ ] Высота снимков «Главного» подбирается по замеру, и генератор отказывает, если нижний край
      водопада или суточного ряда ниже края кадра. Новый сторож в генераторе S10 — со своей
      проверкой и сломом.

### Сделано — Ж2 и заливка вычитаний

- [x] **Ж2, вариант А с утверждением владельца:** напечатанное расхождение равно фактической
      разнице показанных сумм. SQL отдаёт два расхождения — до чистой выручки и до прибыли, ноль —
      пусто; под водопадом строка у каждого, только когда оно есть. Проверка считает разницу сама,
      в целых центах, на четырёх наборах: сходящемся, с центом до прибыли, с центом до чистой
      выручки, несходящемся. Порога нет. Мой прежний «не больше двух центов» — число из головы,
      названо в контракте.
- [x] Живая проверка дополнена: на марте расхождение с прибылью — 0,01, с чистой выручкой — нет.
- [x] Вычитания — светлая заливка `colorBrandStroke2` вместо обводки. Величина дописана в выписку
      S10: три добавленные строки, ноль удалённых — ждёт подтверждения П4.
- [x] Перепись: +1 строка о расхождении; раскладка — расхождение с прибылью 0,07.
- [x] Ворота: `npm test` — 63 файла, 795 проверок, код 0; линтер — код 0; проверка типов — код 0.
      Цитаты всех списков — 288, все единственные.
- [x] Сломы шага: 18 строк. Полный прогон нашёл 4 недоказанных — все «ломает больше, чем объявлено»:
      три слома ступеней красят и проверку расхождения, слом строки о расхождении роняет настоящую
      страницу и красит принятую проверку S5 про кнопку. Объявлено «заодно» с причинами; перепрогон
      этих четырёх — «покраснела своя», коды 0. **Итого 18 из 18.** Единым прогоном всех восьми
      списков это подтверждается на голове слияния.

### Отложенные задачи — дописывается

- **Проверка чисел таблицы стилей S10 не ловит ссылку на необъявленную величину.** Засчитывает её
  как «ссылку». Найдено на шаге 1; таблица стилей проверена командой — необъявленных ссылок 0.
  Проверку не трогаем — решение владельца.

### Шаг 1 — строка «съедает больше всего» — сделано

- [x] Признак самого большого вычитания ставит запрос водопада, в той же строке, что и ступень. При
      равенстве до цента помечены все равные, строка называет их «поровну»; все вычитания ноль — не
      помечено ничего; без доли строки нет. Обычный текст, без своего цвета.
- [x] Сломов — 7, в том числе на равенство. Полный прогон 25 строк: 23 — «покраснела своя»; слом
      равенства был построен неверно (недопустимая оконная запись роняла весь запрос) — переписан;
      у слома «строка без доли» объявлено «заодно». Перепрогон двух — «покраснела своя».
- [x] **Путь, ни разу не встреченный в настоящих данных:** два вычитания равны до цента.

## Шаг 2 — чистая выручка по дням: ход работы

### Сделано — счёт, поле отчёта, живой заход

- [x] Запрос ряда: готовая цепочка `money` по дню заказа; все дни месяца; день без заказов — пусто;
      ноль — честный ноль; доля — от наибольшей по модулю выручки дня; подпись дня готовой строкой.
- [x] Поле отчёта `daily` — необязательное, тем же снимком.
- [x] Проверок — 7, одна настоящим путём. Сломов — 7: 4 — «покраснела своя» с первого раза, у трёх
      объявлено «заодно» с причинами (свой расчёт выручки меняет и доли; снятый `nullif` роняет весь
      отчёт на дне с нулевой выручкой — это ловит принятая проверка S5 про деление на ноль; сырая
      подпись видна проверке настоящего пути) — перепрогон — «покраснела своя».
- [x] Ворота: `npm test` — 64 файла, 809 проверок, код 0; линтер и типы — коды 0.
- [x] **Живой заход, разрешённый владельцем** — только живая проверка марта, цель — местная база:
      итоги ступеней 18 764,00 / 17 277,04 / 1 738,53, прибыль ступени = прибыль итога,
      расхождение с прибылью 0,01, с чистой выручкой — нет, в ряду 31 день, сумма ряда — 17 277,04.
      Код возврата 0.
- [x] **Наблюдение: в марте один день без заказов — 9 марта.** Путь «день без заказов — разрыв,
      линия отсчёта пунктиром» встречен в настоящих данных.

### Найдено в живом заходе — дефект моей живой проверки, исправлен

- **Уход возвращал только сырьё.** Проверка строит факты, а посев заменяет лишь сырые строки: факты
  с совпавшими номерами пережили замену, и за март на «посеве» стояло 179,00 € оборота вместо нуля.
  База возвращена полной пересборкой — код 0, за март нули, фактов 0. Уход переписан: опустошает
  факты и отметку свежести, затем кладёт посев; вторая проверка утверждает состояние посева. Уборка
  проверена без похода наружу: факты из сырья посева — 4 заказа и отметка, после уборки — 0, 0, 0.
- **Заход запущен дважды.** Первый прошёл и всё утвердил, но строку наблюдения среда проверок
  спрятала: вывод прошедших проверок по умолчанию скрыт. Второй — тот же заход с `--silent=false`.
  Живая проверка марта запускается так:
  `NORDIC_PET_DB_TARGET=local npm run test:live -- __tests__/live/march-screen.live.ts --silent=false`.

### Ждёт развилки Ж3 — высота области столбиков ряда

Разметка ряда (Д-8) — после ответа владельца.

### Сделано — ряд на экране и П5 (решение Ж3: высота исключением)

- [x] Блок «Чистая выручка по дням» после водопада. Столбик на день; у каждого дня доступная подпись
      («5 марта: 612,40 €», «9 марта: заказов не было»); видимые подписи — у каждого пятого дня,
      начиная с первого, шаг решает SQL; у оси — края шкалы деньгами. День без заказов — столбика нет,
      линия отсчёта пунктиром. Месяц без заказов — блока нет, на его месте слова. Слова «прибыль» в
      блоке нет.
- [x] **П5, четыре условия владельца:** у строки исключения `8rem` — довод прямо в ней (шкалы высот
      графика у Fluent нет, наибольшая длина шкал — 92px, `global/fonts.ts:28` на `43665d5`); число
      в шапке проверки снято командой — ключей в списке 5, «их четыре» → «их пять»; слом «чужая
      длина на месте 8rem» — «покраснела своя»; назвать исключение в теле заявки и в отчёте отдельной
      строкой — пункт задачи 17 ниже. Дифф выписки: одна новая запись (5 физических строк) и одна
      правленая строка шапки.
- [x] Перепись: раскладка — семь дней, второй без заказов; в список — 5 строк.
- [x] Проверок: запроса — 10, вида — 8, из них одна слабая и названная (текст пунктира).
- [x] Ворота: `npm test` — 65 файлов, 820 проверок, код 0; линтер и типы — коды 0. Цитат сломов
      всех списков — 310, все единственные.
- [x] Сломы шага 2: 15 строк — все «покраснела своя». Из них четыре — после объявлений «заодно»
      (новые проверки оси законно краснеют при сломах запроса) и перепрогона.
- [x] Черновик вида — вне репозитория, числа выдуманы: широкий и узкий экран держат 31 столбик,
      пунктир и подписи с шагом.

### Задача 17 — дописывается по П5

- [ ] В теле заявки и в `docs/ОТЧЁТ.md` — исключение длины `8rem` отдельной строкой, как прежние
      четыре: что это, почему шкалы Fluent его не дают, решение владельца по Ж3, слом.

### Задача 14 — дописывается

- [ ] После живого захода «после» — возврат базы в состояние посева подтверждается наблюдением ещё
      раз: у переписанной уборки живой проверки прогона на настоящих данных пока нет.

## Шаг 3 — окупаемость рекламы: ход работы

- [x] Запрос: строка итогов месяца целиком, своих выражений денег нет. По прибыли — прибыль ÷
      реклама; вклад — (чистая выручка − себестоимость − комиссии) ÷ оборот × 100; порог — 100 ÷
      вклад; вклад не положителен — порога нет, словами «вклад не положителен». Все определения —
      наши. На марте, наш счёт по сверенным числам: 0,39 ×, 57,1 %, 1,75 ×.
- [x] Блок «Окупаемость рекламы» после «Итога»: подпись порога называет базу — «от вклада, а не от
      маржи»; вклад числом рядом с порогом; строка «Вклад и порог — наши определения…». Окупаемость
      по обороту стоит в «Итоге» до шага вкладок — переезд там.
- [x] Перепись: +8 строк. Проверок: запроса — 8, вида — 5.
- [x] Ворота: `npm test` — 67 файлов, 833 проверки, код 0; линтер и типы — коды 0. Цитат сломов —
      319, все единственные.
- [x] Сломы шага: 9. С первого раза — 6; у двух объявлено «заодно» (порог от маржи есть и при
      нулевом обороте; снятая защита порога роняет отчёт — это ловит принятая проверка S5); слом
      «снять nullif у окупаемости по прибыли» ронял каждый отчёт без рекламы — переписан на
      настоящий риск, «ноль вместо пусто». Перепрогон трёх — «покраснела своя». **9 из 9.**
- [x] Моя проверка водопада «оборот ноль — слова» искала слово «оборота» по всей странице и
      покраснела от пояснения окупаемости; сужена до блока водопада. Её два слома перепрогнаны —
      «покраснела своя».

**Пути, ни разу не встреченные в настоящих данных — дописываются:** вклад не положителен — порога
нет; реклама ноль — окупаемость по прибыли пустая.

## Правило владельца на остаток куска — 10 сентября 2026 года: отношение называет свою базу

> **Любое отношение на экране называет свою базу рядом с собой** — не в отчёте и не в контракте, а
> на экране, рядом с числом. Повод: окупаемость по прибыли 0,39 × рядом с порогом 1,75 × читалась как
> противоречие.

### Проверка сделанного и оставшегося

| Отношение | База на экране | Где |
|---|---|---|
| Доли ступеней водопада | «… % оборота» у каждой | шаг 1 ✓ |
| «Съедает больше всего» | «… % оборота» | шаг 1 ✓ |
| Окупаемость по обороту | «(по обороту)» в подписи | S5 ✓ |
| Окупаемость по прибыли | фраза под числом: после постоянных, окупаемость показывает порог | шаг 3 ✓, дописано по правилу |
| Вклад | «с евро оборота» | шаг 3 ✓ |
| Порог | «от вклада, а не от маржи» | шаг 3 ✓ |
| Доля честности | «(доля от чистой выручки)» | S5 ✓ |
| Ряд по дням | отношений текстом нет; высота столбика — от края шкалы, край подписан деньгами | шаг 2 ✓ |
| Маржа строки товара | заголовок «Маржа от чистой выручки» | шаг 4 ✓ |
| Доля строки | заголовок «Доля в прибыли товаров», строка над таблицей называет базу числом | шаг 4 ✓ |
| **Маржа в «Итоге»** | **базы нет: подпись — «Маржа»** | **S5 — просьба П7** |
| Доли статей затрат | «… % оборота» — обязательно | шаг 5 |
| Доля рекламы в полосе показателей | «от оборота» — обязательно | шаг 7 |
| Маржа в полосе показателей | «от чистой выручки» — обязательно | шаг 7 |
| Дельты маржи и доли рекламы | «п. п.» — пункты, а не проценты от прошлого | шаг 7 |

## Шаг 4 — товары: ход работы

- [x] Запрос берёт готовые строки таблицы товаров, текст `MONTH_ITEMS` не тронут: маржа строки,
      доля в прибыли товаров (от суммы прибыли строк), сколько артикулов дают 80 %, сколько в минусе.
      Признак «в минусе» — один на подсветку и счётчик.
- [x] Таблица: строка над таблицей называет базу долей числом и отличие от прибыли месяца; колонки
      «Маржа от чистой выручки» и «Доля в прибыли товаров»; у строки в минусе — признак из данных,
      рядом — число маржи. Всё рисуется только при своих полях: у прежних раскладок отрисованная
      таблица не меняется ни на байт.
- [x] Проверок: запроса — 6, вида — 6. Живая проверка марта дополнена якорями: 11 248,93 €, 8 из 12,
      в минусе 0; NP-004 — маржа 59,1 %, доля 14,6 % (наш счёт по сверенным строкам).
- [x] Ворота: `npm test` — 69 файлов, 846 проверок, код 0; линтер и типы — коды 0. Цитат — 332, все
      единственные.
- [x] Сломы шага: 11 — все «покраснела своя» с первого раза.
- [ ] **Цвет подсветки строки в минусе — ждёт просьбы П6.** Признак в разметке есть, правила вида нет.

**Путь, ни разу не встреченный в настоящих данных:** сумма прибыли товаров не положительна — строка
над таблицей говорит словами.

## П6, П7, изменение правила о красном и шаг 5 — 10 сентября 2026 года

### П6 — подсветка строки в минусе: сделано по трём условиям владельца

- [x] Заливка `colorPaletteRedBackground2` на ячейках строки в минусе; в выписке S10 — три добавленные
      строки с якорем на `43665d5`, удалённых ноль.
- [x] Условие 1 — в подсвеченной строке нет приглушённого текста: основной цвет ячеек задан явно;
      слом «оставить приглушённый текст» — «покраснела своя». Проверка — текста таблицы стилей,
      слабая и названная; контраст замером — перед сдачей.
- [x] Условие 2 — рядом число маржи со знаком минус: сделано на шаге 4, слом есть.
- [x] Условие 3 — слом «взять для подсветки токен отказа» — «покраснела своя».
- [x] **На мартовских данных подсветка не сработает ни на одной строке.** Подтверждено счётом по
      файлу «до», снятому живым заходом задачи 0: строк с отрицательной прибылью — 0 из 12. Живая
      проверка марта утверждает это ещё раз в задаче 14 (`negativeCount: 0`). Механизм есть,
      показать его на настоящих данных нечем.

### П7 — «Маржа (от чистой выручки)» в «Итоге» — на шаге вкладок

- [ ] Подпись меняется на шаге вкладок, вместе с пересборкой эталонов; перепись — строка списка.
      Владелец пересчитал: 1 738,53 ÷ 17 277,04 = 10,1 %, база названа верно.

### Изменение правила о красном — принято владельцем

- [x] Шапка таблицы стилей переписана: прежние формулировки названы дословно.
- [ ] **Задача 17:** `README.md`, раздел об ориентирах вида, — «один смысловой цвет, а красный только
      для отказов» становится неверным; переписать на новое правило. Проходом по сданным документам
      проверить, где ещё стоит прежняя формулировка.

### Шаг 5 — доли статей затрат: сделано

- [x] У каждой статьи «… % оборота» и полоска — та же готовая доля, что у ступени водопада, одно
      поле; разметка берёт её по ключу статьи. Рецепт полосы — как у доли честности: к дорожке
      4,31 и 4,50 : 1 (наш счёт; совпадает с принятым в S10). Светлый оттенок бренда дал бы 1,21 и
      1,31 : 1 — не взят.
- [x] Мой класс полоски содержал подстроку `share-fill` и покрасил принятую проверку S9 полосы
      честности; проверку не трогал — переименовал свои классы.
- [x] Перепись — 4 строки; проверок вида — 5; сломов — 4, все «покраснела своя» (один — после
      объявления «заодно» и перепрогона).
- [x] Ворота: `npm test` — 70 файлов, 853 проверки, код 0; линтер и типы — коды 0. Цитат — 338, все
      единственные.

## После шага 5 — 11 сентября 2026 года: замечание владельца к подписи черновика

**Замечание владельца.** Черновик шага 5 был подписан «на мартовских итогах», хотя мартовскими на
нём были только «Затраты» и «Итог». Новые колонки товаров, строка над таблицей и подсветка брали
значения из черновой раскладки, а там у всех строк стояло одно значение. **Правило к черновикам:**
подпись называет происхождение каждого элемента, а не снимка целиком.

- [x] **Откуда взялось одно значение.** Его дал мой временный скрипт черновика, раскладки в
      репозитории ни при чём: скрипт ставил каждой строке одни и те же 59,1 % и 14,6 %. Счёт того же
      запроса по мартовским строкам даёт у NP-012 60,0 % и 11,1 %, у NP-004 59,1 % и 14,6 % —
      совпадает со счётом владельца. Черновики с шага 6 собираются из запросов по мартовским строкам.
- [x] **Проверка раскладки переписи на поля с одним значением у всех строк.** Скрипт по строкам
      товаров, ступеней водопада и дней ряда нашёл:
      - край столбика у шести дней с заказами — везде «0.0». **Дыра:** разметка, взявшая край соседнего
        дня, осталась бы зелёной;
      - пределы шкалы ряда совпадают с пределами водопада («0.0» и «100.0»), и ни одна проверка вида
        пределов не утверждала. **Дыра:** разметка, взявшая чужие пределы или постоянные, осталась бы
        зелёной;
      - вид ступени и признак «съедает больше всего» повторяются по смыслу, это перечисления;
      - край «0.0» у трёх итогов водопада — по устройству: итоги идут от нуля, края вычитаний
        различны;
      - в раскладке снимков S10 у NP-004 и NP-008 одинаково 58 штук — так в мартовских данных.
- [x] Обе дыры закрыты двумя дописанными проверками вида на различных значениях. Дифф в файлах
      проверок — только добавленные строки. Три слома — все «покраснела своя».
- [x] Отдельно, по просьбе владельца, называется в отчёте: у строки с отрицательной прибылью доля
      «нет данных» — доля от положительной суммы для отрицательной строки не определена.

## Шаг 6 — время чтения источников: ход работы

- [x] Запрос отдаёт готовую строку `ГГГГ-ММ-ДД ЧЧ:ММ UTC` от `meta.fact_freshness.raw_seen_at`;
      перевод в UTC — в самом запросе, пояс назван в строке, секунды отбрасываются, а не
      округляются. Отметки нет — пусто. Эпоха — тоже пусто: её пишет запись отметки при пустом
      сырье, и это не время чтения. Миграций нет.
- [x] Отчёт читает время тем же снимком, что и числа. Поле необязательное: у прежних раскладок шапка
      не меняется ни на байт.
- [x] На экране — «Источники прочитаны по состоянию на …», при пустоте — «Время чтения источников
      неизвестно: отметки о чтении нет.» Мелко и приглушённо, своей строкой во всю ширину шапки.
- [x] **Отступление от буквы задачи 12 («после названия»).** В разметке строка стоит после
      переключателя месяцев, на экране — под строкой названия и месяцев. Так название ничем не
      оборачивается, месяцы остаются на своём месте, а порядок чтения совпадает с видом. На шаге
      вкладок шапка общая, и её вид пересматривается там.
- [x] Проверок: запроса — 3, настоящим путём — 1, вида — 3; перепись — одна строка.
- [x] Сломы шага: 7, все «покраснела своя». Один после объявления «заодно»: у слома «пояс сеанса
      вместо UTC» краснеет и проверка настоящим путём, потому что сеанс местной базы работает в поясе
      Europe/Minsk (снято `current_setting('TimeZone')`). Без перевода в UTC настоящий отчёт на
      местной базе напечатал бы время на три часа позже.

**Пути, ни разу не встреченные в настоящих данных:** отметки нет; отметка — эпоха.

### Найдено по ходу — к владельцу: удаление строки в источнике не двигает отметку свежести

Отметка — самое свежее `updated_at` сырья. Правка и дописывание строки его двигают, а **удаление
последней строки листа** — нет. Проверено на местной базе в откатанной транзакции: отметка поставлена,
«числа отстали» — нет; из четырёх строк `raw.orders` снята последняя; строк стало три, отметка та же,
«числа отстали» — по-прежнему нет.

Для шага 6 это значит, что после такого удаления строка времени покажет более раннее время. Это не
ложь: «по состоянию на» остаётся нижней границей. Главное последствие не в шаге 6, а в пометке
«числа отстали» S8: после удаления она не встанет. Починка требует миграции — например, отметки о
любой записи снимка, а не только об изменённой строке. Значит это решение владельца, в куске не
чинится.

### Решение владельца по находке — 11 сентября 2026 года: в отложенные и в «Где не уверен»

**Решение:** в отложенные задачи, миграцией в этом куске не чинится. **Довод владельца:** это
механизм прошлого куска, а не этого, и починка требует наката в бой — самой тяжёлой нашей операции.

**Условие владельца:** запись не одной строкой, а с наблюдением и последствием, и ещё в раздел «Где не
уверен» отчёта, а не только в отложенные. Пометка устаревания — одно из пяти обещаний, по которым нас
принимают. То, что она ловит правку и дописывание, но не удаление, работодатель обязан прочитать у
нас, а не обнаружить сам.

- **Наблюдение:** строк заказов было 4, стало 3, отметка свежести не двинулась, пометка «числа
  отстали» не встала. Местная база, транзакция с откатом.
- **Последствие, прямо:** после удаления строки в источнике экран покажет устаревшие числа как свежие,
  и человек об этом не узнает.
- **Когда именно (наш разбор кода кнопки):** когда загрузка с удалением прошла, а пересборка фактов
  после неё — нет. Это отказ разбора или папки при нажатии кнопки либо загрузка из терминала без
  разбора. Нажавший кнопку увидит отказ шага на панели; тот, кто откроет страницу после него, пометки
  не увидит. Если пересборка прошла, числа верны.
- [x] Записано в `docs/ОТЧЁТ.md`: строка в «Где не уверен» и строка в «Отложенные задачи → Счёт и
      экран».
- [ ] **Задача 17 — дописывается:** эти две строки уже стоят в отчёте. Проход по документам их не
      дублирует, а переносит в новую редакцию отчёта как есть.

## Отложенные задачи — дописывается 11 сентября 2026 года

**Пометка «числа отстали» не видит удаления строки в источнике.** Наблюдение, последствие и условие —
в разделе «Решение владельца по находке» выше. **Чем закрывается:** миграция, после которой запись
снимка сырья двигает отметку при любом изменении, включая удаление. Отдельный кусок, накат в бой —
с прямого слова владельца.

## Шаг 7 — полоса показателей и дельты: ход работы

- [x] **Счёт.** Запрос берёт две готовые строки итогов — месяца и предыдущего календарного — и отдаёт
      четыре показателя: прибыль, маржу, чистую выручку, долю рекламы в обороте. Дельта — разность
      **показанных** значений: прибыль и выручка в евро, маржа и доля рекламы в процентных пунктах.
      Знак плюс и смысл — «лучше», «хуже», «без изменений» — ставит запрос по признаку «рост — это
      хорошо» (у доли рекламы — нет). Разметка знак с нулём не сравнивает.
- [x] **Итоги прошлого месяца — тем же запросом итогов.** Текст `MONTH_TOTALS` не тронут: у его копии
      граница месяца сдвинута на месяц назад. Параметр месяца в тексте стоит ровно дважды, оба раза в
      границах; равенство сдвинутых итогов прямым итогам за прошлый месяц утверждает проверка на
      фактах, где прошлый месяц не пуст. Есть ли у прошлого месяца заказы — по определению
      переключателя месяцев, второго определения нет.
- [x] **Нет базы.** У прошлого месяца нет заказов — дельты пусты у всех четырёх разом, значения стоят.
      На карточке одна строка: «нет базы: в 2026-02 заказов нет». Первая редакция — «нет базы для
      сравнения: в 2026-02 заказов нет» — на черновике широкого экрана переносилась на две строки и
      сокращена. На узком экране (390) строка переносится и сейчас; там же карточка с подписью в две
      строки ставит значение ниже соседнего — к виду «Главного» на шаге вкладок.
- [x] **Правило «нет данных — словами» контракта:** у прошлого месяца нет ни одной строки рекламы —
      пуста дельта доли рекламы, остальные считаются; оборот ноль — доля рекламы пуста; маржа прошлого
      месяца пуста — пуста дельта маржи.
- [x] **Доля рекламы полосы — то же выражение, что у ступени рекламы** (перенесено сюда из задачи В-6):
      проверка сличает их на одной строке итогов; живая проверка марта утверждает 23,6 % у обеих.
- [x] **Переход между путями — от данных.** Проверка идёт настоящим отчётом без единой подставки: у
      февраля в местной базе нет заказов — «нет базы»; записан февральский заказ — дельты считаются;
      убран — снова слова. Ни флага, ни переменной среды. **Устройство проверки, названное вслух:** она
      пишет в местную базу одну строку сырья и одну строку фактов и убирает их в `finally`. Транзакция с
      откатом здесь невозможна: снимок отчёта начинается с `begin isolation level repeatable read`, и
      Postgres отказывает ему внутри начатой транзакции (код 25001) — снято прогоном.
- [x] **Разметка:** четыре карточки после шапки, перед водопадом; подписи называют базу — «Маржа от
      чистой выручки», «Доля рекламы от оборота»; дельты маржи и доли рекламы — «п. п.». Значение
      набрано Subtitle 2 (16 / 22, 600): крупных чисел на экране по-прежнему три, в «Итоге»; вес полосы
      на «Главном» решается на шаге вкладок по снимку. На узком экране — по две карточки в ряд.
- [x] **Слома «разметка сама считает дельту» нет — по устройству:** прошлых значений в отчёте нет,
      считать разметке не из чего.
- [x] Живая проверка марта дописана: путь «нет базы», значения — числа счёта владельца, доля рекламы
      23,6 % у полосы и у ступени. Запускается в задаче 14.
- [ ] **Цвет дельты — ждёт развилки Ж4.** Смысл сейчас несут знак и слово; признак на карточке есть,
      правила цвета нет.

**Пути, ни разу не встреченные в настоящих данных:** дельты с числами (весь путь с базой); у прошлого
месяца нет рекламы; маржа прошлого месяца пуста; оборот ноль.

### Развилка Ж4 — цвет дельты. Решает владелец

Смысл дельты сейчас несут знак и слово; признак «лучше / хуже / без изменений» стоит на карточке, а
правила цвета нет. Два текста расходятся, и решение за владельцем:

- контракт, раздел «Цвет дельты определяется смыслом», дословно: «У каждого показателя заводится
  признак „рост — это хорошо“: рост прибыли зелёный, рост доли рекламы красный, снижение возвратов
  зелёное.»;
- ориентиры владельца в шапке таблицы стилей, дословно: «один цвет для смысловых пометок; красный —
  отказы и значения, которые плохи по смыслу, больше нигде, чтобы он не обесценился». **Наш вывод:**
  зелёный был бы вторым смысловым цветом.

| Вариант | «Хуже» | «Лучше», «без изменений» | Цена |
|---|---|---|---|
| **А (рекомендую)** | `colorPaletteRedForeground2` — свой красный, не токен отказа, как у П6: #751d1f / #f1bbbc, к карточке 10,76 и 8,71 : 1 | без цвета — приглушённый текст, как сейчас | 1 строка выписки с якорем, 1 правило, 1 слом «взять токен отказа» |
| Б | то же | «лучше» — зелёный `colorPaletteGreenForeground1`: #0e700e / #54b054, 6,28 и 5,34 : 1 | + вторая строка выписки, изменение ориентира о палитре записью |
| В | `colorPaletteRedForeground1` — красный отказов: #bc2f32 / #e37d80, 5,85 и 5,20 : 1 | без цвета | как А, но слома «не токен отказа» нет — токен тот же |

Значения — `packages/tokens/src/global/colors.ts` на `43665d5`: `red.shade30`, `red.tint40`,
`red.shade10`, `red.tint30`, `green.shade10`, `green.tint30`; формулы токенов — `lightColorPalette.ts`
и `darkColorPalette.ts` там же. Контраст — **наш счёт** по формуле WCAG к карточке `#ffffff` и
`#292929`; замер с отрисованной страницы — в задаче 13.

**На настоящих данных выбор сегодня не виден:** у марта нет базы, дельт нет. Увидят его снимки из
выдуманной раскладки и — первым на бою — владелец, когда в Таблице появится второй месяц.

## Решение владельца по Ж4 — 11 сентября 2026 года: вариант Б, и изменение принятого ориентира

**Решение:** «лучше» — зелёный, «хуже» — свой красный, не цвет отказа, «без изменений» — без цвета.

**Изменение принятого ориентира о палитре — записано как изменение, прежнее оставлено для следа.**

- **Дата:** 11 сентября 2026 года. Решение владельца.
- **Прежнее, словами владельца:** «палитра сдержанная, один цвет для смысловых пометок».
- **Прежнее, дословно в шапке таблицы стилей:** «один цвет для смысловых пометок; красный — отказы и
  значения, которые плохи по смыслу, больше нигде, чтобы он не обесценился». В `README.md`, дословно:
  «один смысловой цвет, а красный только для отказов» — переписывается в задаче 17.
- **Действующее, словами владельца:** «два смысловых цвета: красный — отказы и значения, которые плохи
  по смыслу; зелёный — только вердикт „лучше“ в полосе показателей».
- **Довод владельца:** весь смысл полосы — ответить «стало лучше или хуже»; серая целиком, она
  заставляет собирать ответ чтением четырёх слов. Цвет отвечает за секунду, а слово и знак остаются
  вторым каналом — правило «смысл не кодируется одним признаком» не нарушено ни в одну сторону.
- **Граница владельца:** зелёный живёт только в строке дельты полосы показателей, больше нигде на
  экране. Закрыта утверждением в проверке чисел таблицы стилей и сломом.

### Сделано

- [x] Две строки выписки с якорями на `43665d5`: `colorPaletteGreenForeground1` (green.shade10 /
      green.tint30, #0e700e / #54b054) и `colorPaletteRedForeground2` (red.shade30 / red.tint40,
      #751d1f / #f1bbbc). В файле проверки чисел — только добавленные строки: 55 добавлено, 0 удалено.
- [x] Утверждение «зелёный — только в строке дельты «лучше»: ровно одно правило». Зелёным считается
      величина с `Green` или `Success` в имени, величина с тем же значением и величина, ссылающаяся на
      зелёную через `var()` в любое число звеньев. Чего разбор не понимает, названо в его тексте:
      зелёный Fluent под другим именем палитры (`Forest`, `Lime`, `Seafoam`).
- [x] Сломы: «зелёный во втором месте», «зелёный во втором месте под другим именем», «взят цвет
      отказа», «снят зелёный у „лучше“» — все «покраснела своя».
- [x] Шапка таблицы стилей переписана по новому ориентиру, прежние формулировки названы дословно.
- [x] **Моё упущение по П6, исправлено здесь.** В разделе «Пометки и отказы» таблицы стилей с 10
      сентября стояло неверное «Отказ — красный, и красный больше нигде»: правило о красном я тогда
      переписал в шапке, а это место пропустил. Переписано, прежнее названо дословно.

### Вопрос к владельцу

Новая формулировка называет два смысловых цвета — красный и зелёный — и не называет синий бренда.
Синий на экране остаётся: выбранный месяц, карточка «Итог», столбики водопада и полос. **Наш
вывод:** синий — цвет пометок, а не смысла, и формулировку стоит дописать, иначе через кусок её
прочтут как «синего быть не должно». Как именно — решает владелец.

### Шаг 8 — дописывается

- [ ] **Правило «крупных чисел ровно три».** Полоса уезжает на «Главное». Если «Итог» окажется на той
      же вкладке, крупных чисел станет семь. Посчитать на собранной вкладке и назвать владельцу числом.

## Решение владельца по Ж5 — 11 сентября 2026 года: «хуже» ярче, и что из этого следует

**Решение:** вариант Б — «хуже» берёт `colorPaletteRedForeground3` (red.primary #d13438 в светлой теме,
заплатка Fluent red.tint30 #e37d80 в тёмной). **Довод владельца:** прежний тёмный красный к обычному
тексту — 1,44 : 1, глазом он не красный, а чёрный; цвет, который не отличается, цветом не является, а
замер к карточке это пропускает, потому что меряет не ту пару.

**Что из этого следует — записано так, как есть, по требованию владельца.** «Дельта „хуже“ — не цвет
отказа» верно по имени токена и в светлой теме по значению (#d13438 против #bc2f32). **В тёмной теме
значение совпадает с цветом текста отказа (#e37d80), и различает их только форма:** отказ всегда
плашка с фоном и рамкой, дельта всегда простой текст внутри карточки. Записано в правиле таблицы
стилей; уходит в отчёт и в тело заявки.

- [x] Строка выписки заменена: `colorPaletteRedForeground3` с якорями на `43665d5` (светлая —
      `lightColorPalette.ts:19` формула Foreground3 → `colors.ts:195` red.primary; тёмная —
      `darkColorPalette.ts:31` заплатка → `colors.ts:198` red.tint30). `colorPaletteRedForeground2` из
      таблицы стилей и выписки убран — нигде не используется.
- [x] Утверждение формы: у всех правил строки дельты нет фона, рамки, обводки и тени. Слом «дельта
      плашкой» — «покраснела своя» после того, как слом перестал брать токены отказа (с ними он
      краснил заодно проверку «не токен отказа» и был переписан, а не объявлен).
- [x] Имя утверждения поправлено по правде: «дельта „хуже“ — свой токен красного, а не токен отказа».
- [x] В ориентиры дописан синий, словами владельца: смысловых цветов два — красный и зелёный, они несут
      вердикт; синий бренда вердикта не несёт, он помечает выбранное, главное действие и рисует графику.

**Наш счёт по формуле контраста, светлая / тёмная:**

| Пара | Светлая | Тёмная |
|---|---|---|
| «хуже» к карточке | 4,93 | 5,20 |
| «хуже» к основному тексту | 3,15 | 2,80 |
| «лучше» к карточке | 6,28 | 5,34 |
| «лучше» к основному тексту | 2,47 | 2,72 |
| «хуже» к тексту отказа | 1,19 | **1,00** — одно значение |

**Оговорка к прибору, снятая этим же счётом.** Формула меряет светлоту, а не оттенок: зелёный к
приглушённому серому даёт 1,01 : 1, а глазом отличается сразу. Число пары «к соседнему тексту»
называется, но отличается ли цвет, решает снимок.

### Задача 13 — дописывается

- [ ] Замер с отрисованной страницы, обе темы, пары выше — **включая «дельта „хуже“ против текста
      отказа»**: у нас она не мерилась нигде, а после Ж5 стала осмысленной.

### Задача 17 — дописывается: приём прохода по документам

- [ ] Искать не новую формулировку, а **прежнюю** — дословно и устойчивыми кусками: она и показывает
      недописанные места. Прецедент куска: правило о красном переписано в шапке таблицы стилей 10
      сентября, а прежнее «красный больше нигде» в разделе «Пометки и отказы» нашлось только поиском
      прежних слов 11 сентября. Прежние формулировки этого куска для поиска: «красный только для
      отказов», «Красный — только отказы», «красный больше нигде», «один смысловой цвет», «один цвет
      для смысловых пометок», «палитра сдержанная».

### Тело заявки — дописывается

- [ ] Отдельной строкой: «хуже» в тёмной теме совпадает по значению с текстом отказа; роли различает
      форма; довод — у правила таблицы стилей и в этом разделе.

## Шаг вкладок — ход работы, 11 сентября 2026 года

### Решения владельца по черновикам шага

| Вопрос | Решение | Довод или условие владельца |
|---|---|---|
| Вес значений полосы на «Главном» | **крупной ступенью, как «Итог» (Title 2)** | на вкладке, которая отвечает за десять секунд, при среднем весе нет ни одного крупного числа, и глаз садится на водопад |
| Ряд по дням в раскладке снимков | **живой заход разрешён**: чтение в местную базу, наружу ничего, после — возврат посевом с наблюдением | выдуманный ряд в эталонах — то, по чему принимается вид: на нём не видно ни провалов, ни разрывов, а 9 марта в настоящих данных пустое |
| Комментарий над проверкой генератора | **заменить числа** | условие П2 писалось, чтобы не трогали утверждений; комментарий, который врёт о числе страниц, хуже правки |

### Изменение принятого ориентира о крупных числах — 11 сентября 2026 года

**Прежнее, дословно** (шапка таблицы стилей): «крупных чисел единицы — прибыль, маржа, окупаемость;
остальное ровным весом». У правила «Итога»: «Три крупных числа — Title 2 (28 / 36, 600). Их ровно
три, и больше на экране крупного нет.»

**Действующее, словами владельца:** счёт крупных чисел ведётся **на вкладку**, а не на весь экран; на
«Главном» их четыре, на «Деньгах» три, на остальных ноль. **Довод владельца:** правило заводилось
против экрана, где выделено всё, — при четырёх на вкладке оно соблюдено по смыслу.

**Проверено на собранных страницах** — настоящим браузером, по вычисленному размеру шрифта (28
пикселей) у элементов с цифрами: «Главное» — 4 (1 738,53 €, 10,1 %, 17 277,04 €, 23,6 %); «Деньги» —
3 (1 738,53 €, 10,1 %, 4,23 ×); «Товары» — 0; «Качество данных» — 0.

### Сделано

- [x] В-1: вкладка из адреса, из двух значений — первая; без вкладки — «Главное»; незнакомая — отказ
      словами до похода в базу: четыре ссылки, первой «Главное», месяц сохранён, если он правильной
      формы, «Выйти» на месте. Отказ — компонент приложения, снимок берёт его же.
- [x] В-2: разделы — компонент, заменяющий тег `<section>`; строки внутри разделов не сдвинуты. Полоса
      вкладок — ссылки в `<nav>`, без клиентского кода. Месяцы помнят вкладку; у «Главного» хвоста нет.
- [x] В-3: рецепт вкладки Fluent 2 перечитан на `43665d5` целиком; две величины по П3 с якорями.
      Сверка по свойствам — в `docs/сверка/рецепты-fluent.md`, раздел 8 (дописывается в задаче 13).
- [x] В-4: перепись — четыре списка; сложенные подряд, они совпали с прежним единым списком, кроме
      двух названных перемен (подпись маржи, переезд окупаемости по обороту). Прогон четырёх сломов S9
      до правки — 4 из 4 «покраснела своя»; после — ниже.
- [x] В-5: «Выручка» и «Затраты» рядом строчными блоками; окупаемость по обороту — в своём блоке
      крупным числом, блок есть всегда. П7 — «Маржа (от чистой выручки)».
- [x] Обвязка 15 принятых проверок — только строки вызова; состав покрасневших совпал с замером.
- [x] В-7: генератор — 10 состояний, 20 страниц, 28 картинок; П2 — имя и два числа, имя переведено в
      9 строках прежних списков; четыре числа комментария — по разрешению.
- [x] Условие 5 к Ж1: сторож кадра — со своей проверкой и сломами.
- [x] **Эталоны пересобраны один раз**, после ответов владельца: страницы записаны из тех же
      состояний генератора, затем `npm run screens` настоящим браузером — код 0. Набор после —
      909 из 909, код 0.

### Живой заход раскладки снимков

- Заход: местная база, чтение Таблицы и папки рекламы, сборка фактов, отчёт марта в файл; после —
  возврат посевом. **Наблюдение после:** фактов 0 во всех семи таблицах, отметки свежести 0, строк
  сырья заказов 4 — посев.
- Снято: ряд по дням за 31 день (9 марта — заказов нет) и время чтения источников `2026-09-11 07:47
  UTC`. **Тем же заходом сверено** всё, что раскладка получила раньше запросами по сверенным числам:
  13 полей из 13 совпали — итоги, честность, неполнота, водопад, окупаемость, строка над таблицей,
  колонки товаров, ряд, время, полоса пути «нет базы», список месяцев пути «нет базы».

### Высоты картинок — по замеру

Замер настоящим браузером на страницах снимков, широкий / узкий экран: «Главное» — край ряда 905 /
1292, страница 953 / 1332; «Деньги» — 821 / 1304; «Товары» — 771 / 881; «Качество данных» — 623 /
877. **Широкий кадр «Главного» — 1300, а не 1000, по устройству:** подставной браузер принятых
проверок генератора отвечает на замер кадра шириной окна (1280), и кадр ниже покрасил бы их.

### Найдено черновиком и починено до пересборки

- Сетка у разметки страницы сжала все разделы на ширину плавающей кнопки «Обновить данные» — снята;
  «Выручка» и «Затраты» рядом строчными блоками. Цена: карточки рядом не выравниваются по высоте.
- Фраза под окупаемостью по прибыли уезжала за край — ей по наследству запрещался перенос.
- Моя первая редакция оставляла окупаемость по обороту в «Итоге» без поля окупаемости — путь, который
  видели бы только проверки; переделано: блок окупаемости есть всегда.

### Переписанное утверждение моей проверки шага 3

Прежнее, дословно: «блок окупаемости рисуется только тогда, когда в отчёте есть его поле» — после В7
неверно по устройству. Новое: «окупаемость по обороту стоит в блоке всегда, а три числа шага 3 — только
при своём поле». Ни один слом на прежнее не ссылался.

### Моя ошибка в сообщении коммита

В сообщении коммита сломов шага написано «тридцать сломов», а их 29 — посчитано командой по списку
после коммита. История не переписывается; верное число — здесь и в отчёте.

### Сломы шага и доказательства обвязки — прогон

Все прогоны — на полностью зелёном наборе (909 из 909, код 0, снят до первого прогона), каждый слом
по имени, весь `npm test`. **29 сломов шага** — все «покраснела своя»; у двадцати двух с первого раза
вышло «ломает больше, чем объявлено» или отказ самого слома, и после объявления «заодно» с причиной у
каждой строки и двух поправок — перепрогон. Большая часть «заодно» — проверки генератора снимков: он
сверяет разметку каждой страницы с эталоном побайтно, и слом, меняющий разметку страниц снимков,
красит их. Две поправки: имя проверки окупаемости уточнено разделом «экран /» — такое же имя есть у
проверки команды метрик; слом строки «Без цены поставщика» заменяет её пустым фрагментом — без неё
файл проверок переставал разбираться целиком, и не краснела ни одна проверка по имени.

**16 прежних сломов S5 и S9** — все «покраснела своя»: 11 доказывают обвязку одиннадцати из пятнадцати
принятых проверок, `items-order-reversed` — заодно; четыре слома переписи S9 — прогон «после» (итог
тот же, что «до»); `nav-links-drop-month` и `button-not-on-screen` — перевод якорей вместе с кодом.
**Четыре проверки без прежнего слома** доказаны новыми: S5 «окупаемость печатается отношением» —
`roas-as-percent`; S5 «нет данных печатается словами» — `margin-raw`; S5 «экран называет товары без цены
поставщика» — `no-price-line-dropped`; S6 «страница отчёта с годной cookie печатает числа» —
`tab-not-read-s6`. У последнего заодно краснеет S5 «месяц из адреса…»: обе проверки рисуют одну
страницу на одной вкладке, и разделить их сломом кода нельзя — названо в строке слома.

- [ ] **Семь прежних списков целиком** — в задаче 15, на голове, которая пойдёт в слияние (правило
      владельца: прежние приборы гоняются один раз, на голове слияния).

### Находка к владельцу: знак евро отрывается от числа в строках текста

На узком снимке «Товаров» в строке над таблицей «11 248,93 €» переносится между числом и знаком. Это
отложенная задача S5: формат денег ставит перед знаком обычный пробел; в ячейках значений перенос
запрещён правилом `dd`, а в строках текста — строка над таблицей, строка о центах, «Съедает больше
всего» — нет. Починка — неразрывный пробел в формате денег; она меняет каждую денежную строку экрана,
перепись и принятые проверки S5, где знак записан через обычный пробел. Правка принятых проверок —
решение владельца.

## Решение владельца по знаку евро — 11 сентября 2026 года: обход в своих строках, со сторожем

**Решение:** вариант А (неразрывный пробел в общем формате денег) отклонён. **Довод владельца:** он меняет
каждую денежную строку экрана, перепись целиком и ожидания принятых проверок нескольких прошлых кусков —
самая дорогая правка, какая у нас бывает, ради трёх предложений. Чинить так, как в S9, — в собственных
строках куска; общий формат денег и принятые проверки — нетронутыми.

**Условие владельца:** это обход, а не починка, и он обязан быть сторожёным — проверка «денежное значение
внутри предложения не бывает без защиты от переноса» и слом к ней: снять защиту в любой из трёх строк —
покраснеть должна именно она. Тогда четвёртое такое место краснеет само.

- [x] Три предложения — строка над таблицей, строка о центах, «Съедает больше всего» — держат число со
      знаком неразрывным пробелом в самой строке; сверх трёх — денежная дельта полосы показателей: это
      тоже строка текста этого куска.
- [x] **Сторож нашёл сверх названного**, обойдя разметку всех вкладок: значения полосы, суммы и доли
      ступеней, края шкалы ряда — строки этого куска — и долю честности S9 «80,3 %» внутри предложения.
      Они защищены тем же способом, что ячейки значений, — запретом переноса в таблице стилей, без правки
      текста; принятые проверки S9 не тронуты.
- [x] Сторож обходит всю разметку четырёх вкладок на шести раскладках (все ветви трёх предложений) и у
      каждого числа со знаком через обычный пробел требует защиты; разбор, который ничего не нашёл, не
      проходит — чисел со знаком обязано найтись больше ста. Вторая проверка — слабая, текстом таблицы
      стилей: у защищённых элементов перенос запрещён.
- [x] Правка ожиданий — только в проверках этого куска: шесть ожидаемых строк в трёх файлах и пять строк
      переписи; у проверок прошлых кусков — ни одной.
- [x] Пять сломов: снять защиту в каждом из трёх предложений и в строке дельты — краснеет сторож; снять
      запрет переноса у доли честности — краснеет его проверка таблицы стилей. Три цитаты прежних сломов
      куска переведены вместе с кодом.
- [x] Отложенная задача переписана словами владельца — в `docs/ОТЧЁТ.md`, из «Закрыто позже» обратно в
      отложенные: прежняя строка называла её закрытой стилями в S9, это было верно только для ячеек.
- [ ] **Эталоны снимков — вторая пересборка, ждёт слова владельца.** Правка меняет разметку страниц
      снимков, а условие шага вкладок — эталоны один раз, повторная пересборка — к владельцу.
      Предложение: пересобрать один раз после задачи 13, чтобы в ту же пересборку легли её правки, если
      они будут. До пересборки красны десять проверок генератора, и сломы этого решения не гонятся.

## Задача 13 — вид и контраст новых элементов: сделано, 11 сентября 2026 года

- [x] Замер с отрисованных страниц в том же безголовом Chrome, обе темы и погашенный вид — таблица в
      `docs/сверка/контраст.md`, раздел «Кусок S11». Пар 27 и пять пар цвета к цвету; у одной пары на
      разных страницах расхождений 0.
- [x] Текст — все пары выше 4,5 : 1, худшая сторона — 4,91 (дельта «лучше», погашенный вид, тёмная тема)
      и 4,93 (дельта «хуже», светлая). Крупные — выше 3 : 1. Мелкий и крупный названы у каждой
      поверхности с крупным числом: полоса показателей — 10,05 / 15,52, окупаемость — 6,19 / 15,52.
- [x] Вкладки (В-3): прочая — 8,82 и 13,62; текущая — 13,62 и 19,80; указатель — 4,72 и 7,05.
- [x] Доли затрат (В-6): заливка к дорожке — 4,31 и 4,50; число — 6,19 и 6,48.
- [x] Строка в минусе (Д-4): текст к заливке — 9,29 и 10,76; строка выдумана для замера.
- [x] Пара «хуже» к тексту отказа — условие владельца к Ж5: 1,19 и **1,00** — в тёмной теме одно значение;
      различает форма, записано у правила и в сверке.
- [x] Ниже порога — две графические пары, обе названы с доводом: столбик вычитания водопада к карточке
      1,51 (решение П4; наш вывод по условию 1.4.11 — сумма и доля напечатаны числом) и дорожка доли
      статьи 1,25 / 1,15 (цена рецепта полосы прогресса, принятая в S10).
- [x] Проверка чисел таблицы стилей зелёная, её исключения не правились.
- [x] **Правок стилей по итогам замера нет** — вторая пересборка эталонов нужна только для правки со
      знаком евро.

## Вторая пересборка эталонов — разрешение владельца, 11 сентября 2026 года

**Довод владельца:** правило «один раз» ставилось против промежуточного эталона на полупустом экране;
здесь содержание закончено, правок стилей задача 13 не дала — одна пересборка покрывает всё.

| Условие владельца | Чем исполнено |
|---|---|
| 1. Утверждения семи проверок генератора и трёх проверок сторожа кадра не трогаются | дифф файлов проверок генератора и сторожа кадра после пересборки пуст |
| 2. После пересборки набор зелёный целиком, и только тогда пять сломов правки со знаком | набор — 911 из 911, код 0, линтер и типы — код 0; затем пять сломов — все «покраснела своя» (два — после объявления «заодно» проверки порчи эталона «Главного» с причиной) |
| 3. Эта пересборка — последняя; третья — находка к владельцу | записано в описи снимков |

- [x] Из 28 картинок изменились 2 — узкие «Товары» в обеих темах, где знак отрывался от числа; 26
      побайтно те же. Страницы изменились все 20: в них вклеена таблица стилей.
- [x] Сверка контраста: две графические пары ниже порога записаны числом, порогом и условием 1.4.11
      дословно, полной цитатой; строкой рядом — полнота списка пар держится на глазе, а не на механизме.

## Задача 14 — доказательства, что прежние числа не поехали: сделано, 11 сентября 2026 года

- [x] Живой заход «после» на голове `bab037a`: `npm run --silent metrics -- 2026-03` в
      `docs/сверка/метрики-2026-03-после-s11.txt`; с файлом «до» — `cmp`, код 0, по 2251 байту.
      Сверка с независимым счётом владельца сделана на файле «до» (пять из пяти сошлись); файл
      «после» побайтно тот же.
- [x] Числа загрузки прежние: Таблица 519, 19, 11, 2, 5, 62; папка 1699, 2814, 2908, 986 байт, 279 → 186.
- [x] Живая проверка марта куска — 2 из 2, в том числе новые якоря полосы «нет базы» (23,6 % у полосы и у
      ступени рекламы). **Возврат посевом подтверждён повторно:** своей проверкой и наблюдением
      командой метрик — за март нули.
- [x] Дифф `supabase/migrations/` с main — 0 строк.
- [x] `lib/metrics/sql.ts` — 362 добавлено, 0 удалено: тексты `MONTH_TOTALS` и `MONTH_ITEMS` не тронуты;
      принятые проверки слоя метрик зелёные без правки.
- [x] Дифф `__tests__/` с main: 18 файлов — только добавленные строки; 8 файлов с изменёнными строками,
      каждый — по разрешению владельца: перепись (169 / 62) и её раскладка (57 / 2) — правка списков;
      обвязка пятнадцати проверок — `screen.test.tsx` 9 / 9, `screen-month.test.tsx` 2 / 2,
      `honesty-bar.test.tsx` 1 / 1, `guarded-paths.test.tsx` 1 / 1; проверка генератора 5 / 5 — имя, два
      числа (П2) и две строки комментария; проверка чисел таблицы стилей 72 / 1 — одна строка «Их пять»
      (П5). Дифф переписи и раскладки — владельцу целиком в заявке.
- [x] `npm test` — 911 из 911, код 0; `npm run lint` — 0; `npm run build` — 0.

## Задача 15 — первый прогон остановлен на S9: новая проверка краснела на прежнем сломе, 11 сентября 2026 года

**Что вышло.** Один заход на голове слияния `01e50f6`, дерево чистое на входе и на выходе — снято
командой. S3, S4, S5, S6, S8 — код 0 (6, 85, 87, 26, 35 из стольких же). **S9 — код 1, 8 из 9:** слом
«убрать признак устаревания с обёртки чисел, оставив один класс» заодно покрасил три проверки сторожа
кадра этого куска. Прогон остановлен по условию владельца; S10 и S11 не гонялись.

**Причина — наша.** Проверки сторожа кадра собирали снимки против закоммиченных эталонов; слом S9
меняет разметку, генератор первым отказывает «разметка разошлась», до сторожа кадра дело не доходит.
Правило «новая проверка не краснеет на прежнем сломе» я проверил на шаге вкладок выборкой сломов S9 и
назвал доказанным.

**Правило владельца, записанное правилом, а не воспоминанием:** «новая проверка не краснеет на
прежнем сломе» доказывается только полным прогоном всех прежних списков; выборка этого не доказывает и
доказать не может. Вписано и в отчёт — раздел ловушек следующего куска.

**Решение владельца — вариант А.** Вариант Б — дописать «заодно» в список S9 — отпал по правилу: это
подгонка чужого прибора под нашу работу; прибор сказал правду.

- [x] Правится только обвязка трёх проверок сторожа кадра: эталоны берутся свежими, из тех же
      состояний генератора, во временный каталог; утверждения — ни на символ: строк с `expect` в диффе
      0, изменены ввоз и три строки вызова.
- [x] Временные эталоны — только во временный каталог: помощник отказывает, если каталог не временный;
      в `docs/screens` проверка не пишет.
- [ ] Сверку с закоммиченными эталонами по-прежнему держат проверки генератора S10 — подтверждается
      сплошным прогоном: слом S9 о классе устаревших чисел обязан красить объявленные проверки
      генератора и «покраснела своя» у своей.
- [x] Из общей постоянной моего списка «генератор краснеет заодно» убраны три имени сторожа кадра —
      объявление должно быть правдой.
- [ ] Сплошной прогон всех восьми списков на новой голове, без выборок.

## Задача 15 — сплошной прогон №1 на `afc53c2`: S9 чист, S10 и S11 — код 1; решения владельца

**Итог прогона** (20:35:45–21:39:47, дерево чистое на входе и выходе, снято командой): S3 6/6, S4 85/85,
S5 87/87, S6 26/26, S8 35/35, **S9 9/9** — правка обвязки сработала; **S10 14/16**, **S11 100/131**.
Сверку с закоммиченными эталонами держат проверки генератора S10: слом S9 о классе устаревших чисел
дал «покраснела своя» вместе с объявленными проверками генератора.

**Находка 1 — два слома S10 красили наши проверки сторожа кадра.**

- «Разрешить генератору продолжать при неудавшемся замере ширины»: замер кадра отказывал через тот же
  сторож замера S10. **Решение владельца:** у замера кадра свой сторож и **свой** вид отказа — «кадр не
  замерен», — проверка утверждает именно его, свой слом `frame-measure-guard-body`. Сторож с тем же
  видом, что у S10, владелец отклонил: проверка не отличила бы свой отказ от чужого (правило с S8).
  Утверждение нашей проверки поменялось — это наша проверка этого куска, ещё не сданная. **Сторож замера
  S10 остаётся на своём месте** — у замера ширины, строка вызова в генераторе прежняя.
- «Завести в таблице стилей третий блок величин»: наши проверки разбирали настоящую таблицу стилей.
  **Обвязка:** своя маленькая таблица стилей во временном каталоге, из неё же — свежие эталоны.
  Утверждения не двигались.
- Мой новый сторож повторил строку, которую цитирует слом S10 `measure-check-off`, и сделал его
  двусмысленным; найдено счётом цитат до прогона. Список S10 не тронут — переименован параметр в моём
  стороже.

**Находка 2 — 31 слом нашего списка «ломает больше».** 27 из них красят проверки генератора: доказаны до
пересборки эталонов на шаге вкладок, а с пересборки раскладка снимков несёт поля куска. Объявлены
«заодно» с причиной. Отдельно, по требованию владельца:

- **Верная поимка:** «печатать дельту маржи и доли рекламы процентами» покрасил сторожа знака —
  процент в строке дельты идёт через обычный пробел. Сторож работает не вхолостую.
- **Рвущиеся связи между нашими проверками:** «доли ступеней долями единицы» и «доля ступени от чистой
  выручки» рвут связь «доля рекламы полосы = доля ступени рекламы» — выражение доли ступени сменилось, а
  доля полосы считается прежним; «ноль вместо пустоты при нулевом обороте» и «не прочитать водопад»
  рвут связь «доля рекламы полосы = доля ступени в отчёте»; «назвать ряд прибылью по дням» рвёт связь
  «заголовки разделов вкладки = буквальный список» в трёх проверках вкладок.

**Правило, дописанное владельцем:** полный прогон нужен не только после чужих перемен, но и после
своих, если переменилось что-то общее — эталоны, сторож, раскладка. Моё упущение: после пересборки
эталонов я прогнал только сломы шага вкладок, а весь свой список — нет.

**Наблюдение владельца:** прогон занял час, а не шесть — у старых списков узкие наборы проверок; это
подтверждает отложенную задачу S10 про их узкий охват: их зелень говорит меньше, чем выглядит.

- [ ] Сплошной прогон №2 всех восьми списков на новой голове, без выборок.

## Задача 15 — сплошной прогон №2 на `133bb55`: все восемь списков — код 0

22:10:18–23:15:15, дерево чистое на входе и выходе, снято командой. S3 6/6, S4 85/85, S5 87/87,
S6 26/26, S8 35/35, S9 9/9, S10 16/16, S11 132/132 — 396 из 396, исходов, кроме «покраснела своя», 0.
Принято владельцем 11 сентября 2026 года.

## Задача 17 — README, отчёт, проход по прежним формулировкам

**Три правила владельца, те же, что в S10:** ни строки о том, чего мы не делали; у каждой отложенной
задачи причина и то, чем грозит; проход по документам ищет **прежние** формулировки.

**Список владельца «что обязано попасть в отчёт» — где стоит:**

| Что | Где в отчёте |
|---|---|
| красный получил вторую роль, зелёный — ровно в одном правиле; прежние формулировки дословно, с датой | «Изменённые ориентиры владельца» — прежнее как в `main` после S10 |
| крупные числа — на вкладку | там же |
| путь «нет базы» — боевой; путь с числами живьём не видел никто, первым увидит владелец | две строки в разделе S11 и строка путей в «Где не уверен» |
| подсветка убыточных строк на настоящих данных не видна ни в одной строке | строка раздела S11; 0 из 12 — утверждение живой проверки; в телах 20 страниц снимков признака строки в минусе 0 — счёт командой |
| знак евро — обход, сторожёный | строка раздела S11 и отложенная задача формата |
| две графические пары ниже 3 : 1 | таблица с числами, условие 1.4.11 целиком дословно, рядом — строка о полноте списка пар |
| удаление строки не двигает отметку | «Где не уверен» и отложенные — как решено |
| правило полного прогона и ловушка выборки | «Ловушки» — с уточнением о своих переменах общего |
| узкий охват трёх прежних списков | отложенная задача помечена «унаследовано из S10, в S11 не закрыто»; «из шестидесяти одного» → «из семидесяти восьми» |
| **не доказано: после «Обновить данные» страница остаётся на вкладке** | отдельная строка в «Где не уверен» |

Сверх списка владельца — «порог окупаемости спорен по устройству» (ловушка из плана) и «хуже» к тексту
отказа в тёмной теме — в «Где не уверен».

**Найдено проходом:**

- Прежние формулировки — поиском слов, а не новых: `README.md` (ориентиры, «семь длин», «одна из шести»,
  «Живых проверок четыре», погашенная полоса «кадр обрывается выше неё»), `docs/ОТЧЁТ.md` (число кусков,
  «семь длин», «одна карточка из шести», 778 / 264 / семь списков, «из шестидесяти одного», «по 14»),
  комментарии `app/globals.css` о трёх крупных числах и шести карточках, комментарий настроек живого набора.
- Опись снимков не знала отказа «кадр не замерен», заведённого в задаче 15, — дописан; фраза «утверждения
  сторожа кадра не тронуты» уточнена: не тронуты пересборкой, изменены позже решением владельца.
- Полоса управления перемерена на страницах снимков, окно 1280: 98 и 172 пикселя вместо 74 и 148 из S10.
- **Моя ошибка в сверке контраста:** «худшая сторона по тексту — 4,91 и 4,93» — неверно; в той же таблице
  стоит 4,73 (дельта «хуже», погашенный вид, тёмная тема). Найдено счётом по таблице командой. Вывод «все
  выше 4,5» не меняется; строка сверки исправлена с пометкой. Запись задачи 13 выше оставлена как была.
- Живой набор: в S11 прогнан один файл из пяти — строка отчёта говорит это, а не прежнее «6 проверок».
- Таблица стилей в 20 страницах снимков — копия на день второй пересборки, с прежними комментариями;
  названо в описи. Обновить — третья пересборка, вопрос к владельцу.

### Решения владельца по задаче 17 — 12 сентября 2026 года

- Отчёт принят. Копия таблицы стилей в двадцати страницах снимков остаётся; строка описи уточнена его
  словами. Комментарий «Живых проверок четыре» правится: это настройка, а не утверждение.
- **Числа прозы — механикой, а не привычкой.** Третье за кусок число, написанное на глаз, — значит
  правило «считать до текста» не работает. Заведена команда `node scripts/prose-numbers.ts --pr <тело>`:
  числа прозы отчёта, сверки контраста, описи снимков и тела заявки вычисляются из источника — таблицы,
  файла, `git`, вставленного в тело вывода прогонов — и сверяются с каждым местом; расхождение, пропавший
  или неоднозначный якорь — код 1. Где команда невозможна — рядом строка-источник. Прозу с числами
  прописью в зарегистрированных местах перевёл в цифры, иначе сверять нечего.
- Команда в набор проверок не включена: новая проверка в наборе потребовала бы сплошного прогона восьми
  списков заново — правило владельца о прежних сломах. Её вывод и код идут в тело заявки как ворота.
- Правило номера: открыв заявку, её номер ставится в счёт заявок с отложенными задачами и в «Где
  смотреть след работы»; до этого отчёт не уезжает.
- **Тело заявки ограничено 65 536 знаками** — так записано в заявке № 14 (документации GitHub я не
  проверял). Полный вывод восьми списков — 74 304 знака, отчётные части — 52 643. В тело идут все восемь
  отчётных таблиц целиком; строки хода «слом: …» опущены — командой проверено, что во всех восьми
  списках они один к одному, по порядку, повторяют первую колонку таблицы.
