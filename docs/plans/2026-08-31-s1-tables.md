# S1 — база: две схемы, запись снимка, посев. План работ

> **Для исполнителя:** план выполняется задача за задачей. Шаги отмечаются галочками (`- [ ]`).
> Каждая задача заканчивается коммитом и проверяемым результатом.

**Цель:** одна команда пересоздаёт базу с нуля — схемы `raw` и `fact`, по семь таблиц в каждой,
семь функций записи снимка источника и посев демонстрационных строк, — а проверки в `ci`
краснеют, если это перестало быть правдой.

**Устройство:** сырой слой повторяет источник строками текста и опознаёт строку адресом в
источнике; слой фактов повторяет его состав, но с типами, явной валютой и внешним ключом на
сырую строку с каскадным удалением. Запись снимка (вставить новое, обновить изменившееся,
удалить исчезнувшее) живёт функциями в базе, а не в загрузчиках, поэтому S2 и S3 не смогут
написать её каждый по-своему.

**Инструменты:** PostgreSQL 15 (Homebrew локально, служба раннера в `ci`), Supabase CLI без
Docker, vitest с драйвером `pg`, GitHub Actions.

**Контракт:** `.contracts/s1-tables.md` на ветке `contract/s1-tables` (коммит `f0aca79`).
План спорит с контрактом, а не заменяет его: исполнитель читает оба.

## Общие ограничения

- Проверка отчитывается под именем ровно `ci`. Новые шаги дописываются в существующую работу
  `ci`. Работу не переименовывать и не делить надвое.
- Docker не участвует ни локально, ни в `ci`.
- Команда пересоздания — одна и та же локально и в `ci`:
  `supabase db reset --yes --db-url "$DATABASE_URL"`, где по умолчанию
  `postgresql://postgres@127.0.0.1:5432/nordic_pet?sslmode=disable`. Оба хвоста обязательны:
  без `--yes` команда ждёт клавиатуры, без `sslmode=disable` отвечает `tls error`.
- Загрузчиков нет: ни клиента Google, ни чтения `.env.local`, ни обращения к сети.
- Правил счёта нет: разбор грязных значений — работа S4.
- В схеме `raw` все колонки источника имеют тип `text`. Исключение — адресные колонки `row_no`,
  `file_name` и служебная `updated_at`.
- Ни одна колонка, хранящая число источника, не объявлена `not null` и не имеет умолчания.
  Адресные колонки — наоборот, обязаны быть заполнены.
- Функция записи снимка отвергает пустой снимок ошибкой.
- Строка переписывается только при отличии содержимого: `where старое is distinct from нового`.
- Посев пишет сырьё только через `raw.replace_*`, не собственными вставками, и живёт в
  `supabase/seed.sql` — ни одной вставки данных в миграциях.
- Миграции обязаны применяться и к PostgreSQL 15 локально, и к PostgreSQL 17 в облаке; опираться
  на схемы и расширения платформы Supabase (`auth`, `storage`, `vault` и прочие) они не вправе.
- `supabase db push` не выполняется: миграции в облачный проект в этом куске не уезжают.
- Каждая проверка, меняющая данные, выполняется в транзакции и откатывается — иначе соседние
  проверки поедут на чужих строках.

---

### Задача 1: база проекта, драйвер, защита от чужой базы и опора для проверок

**Файлы:**
- Создать: `lib/db-url.ts`, `scripts/db-reset.ts`, `__tests__/db-url.test.ts`,
  `__tests__/db/support.ts`
- Изменить: `package.json`, `tsconfig.json`

Адрес базы берётся из переменной окружения, а на сервере разработчика рядом живут двенадцать
чужих баз. Опечатки в переменной хватило бы, чтобы пересоздание снесло чужую работу, поэтому
адрес проходит проверку принадлежности проекту — и в проверках, и в команде пересоздания, одним
и тем же путём. Отказ, а не предупреждение.

**Интерфейсы:**
- Отдаёт наружу: `databaseUrl` — строка подключения; `pool` — пул соединений;
  `inRollback(fn)` — выполняет `fn(client)` в транзакции и всегда откатывает её;
  `rows(sql, params?)` — короткий запрос через пул.

- [ ] **Шаг 1: завести базу проекта**

```bash
createdb -h 127.0.0.1 -U postgres nordic_pet
```

Если база уже есть, команда скажет об этом — это не ошибка.

- [ ] **Шаг 2: поставить драйвер**

```bash
npm install --save-dev pg @types/pg
```

- [ ] **Шаг 3: добавить команду пересоздания в `package.json`**

В раздел `scripts`:

```json
"db:reset": "supabase db reset --yes --db-url \"${DATABASE_URL:-postgresql://postgres@127.0.0.1:5432/nordic_pet?sslmode=disable}\""
```

- [ ] **Шаг 4: написать опору для проверок**

Создать `__tests__/db/support.ts`:

```ts
import { Pool, type PoolClient } from 'pg'

export const databaseUrl =
  process.env.DATABASE_URL ??
  'postgresql://postgres@127.0.0.1:5432/nordic_pet?sslmode=disable'

export const pool = new Pool({ connectionString: databaseUrl })

/** Выполняет запрос через пул и возвращает строки. */
export async function rows<T = Record<string, unknown>>(
  sql: string,
  params: unknown[] = [],
): Promise<T[]> {
  const result = await pool.query(sql, params)
  return result.rows as T[]
}

/**
 * Выполняет fn в транзакции и всегда откатывает её.
 * Проверки, которые пишут в таблицы, обязаны идти через это:
 * иначе следующая проверка поедет на чужих строках.
 */
export async function inRollback<T>(fn: (client: PoolClient) => Promise<T>): Promise<T> {
  const client = await pool.connect()
  try {
    await client.query('begin')
    return await fn(client)
  } finally {
    await client.query('rollback')
    client.release()
  }
}
```

- [ ] **Шаг 5: убедиться, что опора соединяется с базой**

```bash
npm test
```

Ожидается: прежние проверки страницы `/health` зелены, новых пока нет.

- [ ] **Шаг 6: коммит**

```bash
git add package.json package-lock.json __tests__/db/support.ts
git commit -m "chore: база проекта, драйвер pg и опора для проверок базы"
```

---

### Задача 2: роли, схемы и сырой слой

**Файлы:**
- Создать: `supabase/migrations/<время>_roles.sql`
- Создать: `supabase/migrations/<время>_schemas_and_raw.sql`
- Создать: `__tests__/db/structure.test.ts`

**Интерфейсы:**
- Отдаёт наружу: роли `anon`, `authenticated`, `service_role`; схемы `raw` и `fact`; таблицы
  `raw.orders`, `raw.refunds`, `raw.costs`, `raw.fees`, `raw.opex`, `raw.fx` с ключом
  `row_no integer` и `raw.ads` с составным ключом `(file_name text, row_no integer)`. У каждой
  колонка `updated_at timestamptz not null default now()`.

- [ ] **Шаг 1: написать падающую проверку**

Создать `__tests__/db/structure.test.ts`:

```ts
import { afterAll, expect, test } from 'vitest'
import { pool, rows } from './support'

afterAll(() => pool.end())

test('роли Supabase заведены — без них на S6 нечем закрыть доступ', async () => {
  const found = await rows<{ rolname: string }>(
    `select rolname from pg_roles
      where rolname in ('anon', 'authenticated', 'service_role')
      order by rolname`,
  )
  expect(found.map((r) => r.rolname)).toEqual(['anon', 'authenticated', 'service_role'])
})

test('схемы raw и fact существуют, схемы dev не существует', async () => {
  const found = await rows<{ nspname: string }>(
    `select nspname from pg_namespace
      where nspname in ('raw', 'fact', 'dev') order by nspname`,
  )
  expect(found.map((r) => r.nspname)).toEqual(['fact', 'raw'])
})

test('в схеме raw ровно семь таблиц источника', async () => {
  const found = await rows<{ tablename: string }>(
    `select tablename from pg_tables where schemaname = 'raw' order by tablename`,
  )
  expect(found.map((r) => r.tablename)).toEqual([
    'ads', 'costs', 'fees', 'fx', 'opex', 'orders', 'refunds',
  ])
})

test('сырой слой не чинит источник: все его колонки — текст', async () => {
  const wrong = await rows<{ место: string; тип: string }>(
    `select table_name || '.' || column_name as "место", data_type as "тип"
       from information_schema.columns
      where table_schema = 'raw'
        and column_name not in ('row_no', 'file_name', 'updated_at')
        and data_type <> 'text'
      order by 1`,
  )
  expect(wrong).toEqual([])
})

test('адресные колонки обязательны — адрес не бывает пустым', async () => {
  const nullable = await rows(
    `select table_name || '.' || column_name as "место"
       from information_schema.columns
      where table_schema = 'raw'
        and column_name in ('row_no', 'file_name')
        and is_nullable = 'YES'`,
  )
  expect(nullable).toEqual([])
})
```

- [ ] **Шаг 2: убедиться, что проверка падает**

```bash
npm test
```

Ожидается: пять новых проверок красные — ролей нет, схем нет, таблиц нет.

- [ ] **Шаг 3: написать миграцию ролей**

```bash
supabase migration new roles
```

```sql
-- В облачном проекте эти роли уже есть и миграция там ничего не делает.
-- Локально их нет, а без них на S6 нечем закрыть доступ.
do $$
begin
  if not exists (select 1 from pg_roles where rolname = 'anon') then
    create role anon nologin noinherit;
  end if;
  if not exists (select 1 from pg_roles where rolname = 'authenticated') then
    create role authenticated nologin noinherit;
  end if;
  if not exists (select 1 from pg_roles where rolname = 'service_role') then
    create role service_role nologin noinherit bypassrls;
  end if;
end
$$;
```

- [ ] **Шаг 4: написать миграцию схем и сырых таблиц**

```bash
supabase migration new schemas_and_raw
```

```sql
create schema raw;
create schema fact;

revoke all on schema raw from public;
revoke all on schema fact from public;

create table raw.orders (
  row_no       integer primary key,
  date         text,
  order_id     text,
  sku          text,
  units        text,
  gross_eur    text,
  discount_eur text,
  gateway      text,
  updated_at   timestamptz not null default now()
);

create table raw.refunds (
  row_no      integer primary key,
  refund_date text,
  order_id    text,
  sku         text,
  units       text,
  amount_eur  text,
  updated_at  timestamptz not null default now()
);

create table raw.costs (
  row_no     integer primary key,
  sku        text,
  cost_eur   text,
  valid_from text,
  updated_at timestamptz not null default now()
);

create table raw.fees (
  row_no     integer primary key,
  gateway    text,
  percent    text,
  fixed_eur  text,
  updated_at timestamptz not null default now()
);

create table raw.opex (
  row_no     integer primary key,
  month      text,
  category   text,
  amount_eur text,
  updated_at timestamptz not null default now()
);

create table raw.fx (
  row_no      integer primary key,
  date        text,
  usd_per_eur text,
  updated_at  timestamptz not null default now()
);

create table raw.ads (
  file_name  text    not null,
  row_no     integer not null,
  date       text,
  campaign   text,
  spend_usd  text,
  updated_at timestamptz not null default now(),
  primary key (file_name, row_no)
);
```

- [ ] **Шаг 5: пересоздать базу и убедиться, что проверка зелёная**

```bash
npm run db:reset && npm test
```

- [ ] **Шаг 6: коммит**

```bash
git add supabase/migrations __tests__/db
git commit -m "feat: роли Supabase, схемы raw и fact, сырые таблицы обоих источников"
```

---

### Задача 3: запись снимка для листа `orders`

Здесь появляется механизм, ради которого весь кусок и затевался. Остальные шесть функций —
задачи 4 и 5 — повторяют его в точности, поэтому здесь он покрывается проверками полностью.

**Файлы:**
- Создать: `supabase/migrations/<время>_replace_orders.sql`
- Создать: `__tests__/db/replace-orders.test.ts`

**Интерфейсы:**
- Отдаёт наружу: `raw.replace_orders(p_rows jsonb) returns void`. Вход — массив объектов с
  ключами `row_no`, `date`, `order_id`, `sku`, `units`, `gross_eur`, `discount_eur`, `gateway`.
  Лишние ключи игнорируются. Пустой массив, `null` и не-массив приводят к ошибке.

- [ ] **Шаг 1: написать падающие проверки**

Создать `__tests__/db/replace-orders.test.ts`:

```ts
import { afterAll, expect, test } from 'vitest'
import { inRollback, pool } from './support'

afterAll(() => pool.end())

const пятьСтрок = [
  { row_no: 1, date: '01.03.2026', order_id: 'A-1', sku: 'NP-001', units: '1',
    gross_eur: '10,00', discount_eur: '', gateway: 'stripe' },
  { row_no: 2, date: '2026-03-01', order_id: 'A-2', sku: 'np-002 ', units: '2',
    gross_eur: '20,00', discount_eur: '1,00', gateway: 'paypal' },
  { row_no: 3, date: '02.03.2026', order_id: 'A-3', sku: 'NP-003', units: '1',
    gross_eur: '30,00', discount_eur: '', gateway: 'stripe' },
  { row_no: 4, date: '2026-03-02', order_id: 'A-4', sku: 'NP-001', units: '3',
    gross_eur: '40,00', discount_eur: '', gateway: 'stripe' },
  { row_no: 5, date: '03.03.2026', order_id: 'A-5', sku: 'NP-002', units: '1',
    gross_eur: '50,00', discount_eur: '', gateway: 'paypal' },
]

test('снимок записывается, а хвостовой пробел в артикуле переживает запись', async () => {
  await inRollback(async (c) => {
    await c.query('select raw.replace_orders($1::jsonb)', [JSON.stringify(пятьСтрок)])
    const { rows } = await c.query('select row_no, sku from raw.orders order by row_no')
    expect(rows).toHaveLength(5)
    expect(rows[1].sku).toBe('np-002 ')
  })
})

test('тот же снимок дважды подряд не меняет ни одного байта', async () => {
  await inRollback(async (c) => {
    await c.query('select raw.replace_orders($1::jsonb)', [JSON.stringify(пятьСтрок)])
    const до = (await c.query('select * from raw.orders order by row_no')).rows

    await c.query('select raw.replace_orders($1::jsonb)', [JSON.stringify(пятьСтрок)])
    const после = (await c.query('select * from raw.orders order by row_no')).rows

    // сравнение целиком, включая updated_at: ни одна колонка не исключена
    expect(после).toEqual(до)
  })
})

test('исчезнувший из источника адрес исчезает из таблицы', async () => {
  await inRollback(async (c) => {
    await c.query('select raw.replace_orders($1::jsonb)', [JSON.stringify(пятьСтрок)])
    await c.query('select raw.replace_orders($1::jsonb)', [JSON.stringify(пятьСтрок.slice(0, 3))])
    const { rows } = await c.query('select row_no from raw.orders order by row_no')
    expect(rows.map((r) => r.row_no)).toEqual([1, 2, 3])
  })
})

test('изменившееся значение всё-таки записывается', async () => {
  await inRollback(async (c) => {
    await c.query('select raw.replace_orders($1::jsonb)', [JSON.stringify(пятьСтрок)])
    const изменённый = пятьСтрок.map((r) => (r.row_no === 1 ? { ...r, units: '9' } : r))
    await c.query('select raw.replace_orders($1::jsonb)', [JSON.stringify(изменённый)])
    const { rows } = await c.query('select units from raw.orders where row_no = 1')
    expect(rows[0].units).toBe('9')
  })
})

test('пустой снимок отвергается, а не вычищает таблицу', async () => {
  await inRollback(async (c) => {
    await c.query('select raw.replace_orders($1::jsonb)', [JSON.stringify(пятьСтрок)])
    await expect(c.query(`select raw.replace_orders('[]'::jsonb)`)).rejects.toThrow(
      /пустой снимок/,
    )
  })
})
```

- [ ] **Шаг 2: убедиться, что проверки падают**

```bash
npm test
```

Ожидается: красные с сообщением о том, что функции `raw.replace_orders` не существует.

- [ ] **Шаг 3: написать функцию**

```bash
supabase migration new replace_orders
```

```sql
create function raw.replace_orders(p_rows jsonb) returns void
language plpgsql
as $$
begin
  if p_rows is null
     or jsonb_typeof(p_rows) <> 'array'
     or jsonb_array_length(p_rows) = 0 then
    raise exception 'пустой снимок источника для raw.orders: загрузка отменена'
      using hint = 'ноль строк почти всегда означает сбой чтения источника, а не опустевший лист';
  end if;

  insert into raw.orders as t
         (row_no, date, order_id, sku, units, gross_eur, discount_eur, gateway)
  select (r->>'row_no')::integer, r->>'date', r->>'order_id', r->>'sku', r->>'units',
         r->>'gross_eur', r->>'discount_eur', r->>'gateway'
    from jsonb_array_elements(p_rows) as r
      on conflict (row_no) do update
         set date         = excluded.date,
             order_id     = excluded.order_id,
             sku          = excluded.sku,
             units        = excluded.units,
             gross_eur    = excluded.gross_eur,
             discount_eur = excluded.discount_eur,
             gateway      = excluded.gateway,
             updated_at   = now()
       where (t.date, t.order_id, t.sku, t.units, t.gross_eur, t.discount_eur, t.gateway)
          is distinct from
             (excluded.date, excluded.order_id, excluded.sku, excluded.units,
              excluded.gross_eur, excluded.discount_eur, excluded.gateway);

  delete from raw.orders t
   where not exists (
     select 1 from jsonb_array_elements(p_rows) as r
      where (r->>'row_no')::integer = t.row_no
   );
end;
$$;
```

- [ ] **Шаг 4: убедиться, что проверки зелёные**

```bash
npm run db:reset && npm test
```

- [ ] **Шаг 5: коммит**

```bash
git add supabase/migrations __tests__/db
git commit -m "feat: запись снимка листа orders с подчисткой исчезнувших адресов"
```

---

### Задача 4: запись снимка для остальных пяти листов

**Файлы:**
- Создать: `supabase/migrations/<время>_replace_sheets.sql`
- Создать: `__tests__/db/replace-sheets.test.ts`

**Интерфейсы:**
- Отдаёт наружу: `raw.replace_refunds`, `raw.replace_costs`, `raw.replace_fees`,
  `raw.replace_opex`, `raw.replace_fx` — все с сигнатурой `(p_rows jsonb) returns void`.

Тело каждой функции — копия `raw.replace_orders` из задачи 3, в которой заменены имя функции,
имя таблицы и список колонок. Условие `is distinct from`, проверка пустого снимка и
`delete … where not exists` остаются как есть.

| Функция | Таблица | Колонки |
|---|---|---|
| `raw.replace_refunds` | `raw.refunds` | `refund_date, order_id, sku, units, amount_eur` |
| `raw.replace_costs` | `raw.costs` | `sku, cost_eur, valid_from` |
| `raw.replace_fees` | `raw.fees` | `gateway, percent, fixed_eur` |
| `raw.replace_opex` | `raw.opex` | `month, category, amount_eur` |
| `raw.replace_fx` | `raw.fx` | `date, usd_per_eur` |

- [ ] **Шаг 1: написать падающие проверки**

Создать `__tests__/db/replace-sheets.test.ts`. Пять источников проверяются одним перебором —
у каждого три утверждения: снимок записан, повторный снимок ничего не изменил, пустой снимок
отвергнут.

```ts
import { afterAll, describe, expect, test } from 'vitest'
import { inRollback, pool } from './support'

afterAll(() => pool.end())

const источники = [
  {
    функция: 'raw.replace_refunds',
    таблица: 'raw.refunds',
    строки: [
      { row_no: 1, refund_date: '05.03.2026', order_id: 'A-1', sku: 'NP-001',
        units: '1', amount_eur: '10,00' },
      { row_no: 2, refund_date: '2026-03-05', order_id: 'A-2', sku: 'NP-002',
        units: '1', amount_eur: '5,00' },
    ],
  },
  {
    функция: 'raw.replace_costs',
    таблица: 'raw.costs',
    строки: [
      { row_no: 1, sku: 'NP-001', cost_eur: '4,00', valid_from: '01.01.2026' },
      { row_no: 2, sku: 'NP-002', cost_eur: '6,50', valid_from: '2026-02-01' },
    ],
  },
  {
    функция: 'raw.replace_fees',
    таблица: 'raw.fees',
    строки: [
      { row_no: 1, gateway: 'stripe', percent: '1,4', fixed_eur: '0,25' },
      { row_no: 2, gateway: 'paypal', percent: '2,49', fixed_eur: '0,35' },
    ],
  },
  {
    функция: 'raw.replace_opex',
    таблица: 'raw.opex',
    строки: [
      { row_no: 1, month: '2026-03', category: 'аренда', amount_eur: '1 234,50' },
      { row_no: 2, month: '03.2026', category: 'связь', amount_eur: '89,00' },
    ],
  },
  {
    функция: 'raw.replace_fx',
    таблица: 'raw.fx',
    строки: [
      { row_no: 1, date: '01.03.2026', usd_per_eur: '1,0850' },
      { row_no: 2, date: '2026-03-02', usd_per_eur: '1,0871' },
    ],
  },
]

describe.each(источники)('$функция', ({ функция, таблица, строки }) => {
  test('снимок записывается', async () => {
    await inRollback(async (c) => {
      await c.query(`select ${функция}($1::jsonb)`, [JSON.stringify(строки)])
      const { rows } = await c.query(`select * from ${таблица}`)
      expect(rows).toHaveLength(строки.length)
    })
  })

  test('тот же снимок дважды не меняет ни одного байта', async () => {
    await inRollback(async (c) => {
      await c.query(`select ${функция}($1::jsonb)`, [JSON.stringify(строки)])
      const до = (await c.query(`select * from ${таблица} order by row_no`)).rows
      await c.query(`select ${функция}($1::jsonb)`, [JSON.stringify(строки)])
      const после = (await c.query(`select * from ${таблица} order by row_no`)).rows
      expect(после).toEqual(до)
    })
  })

  test('пустой снимок отвергается', async () => {
    await inRollback(async (c) => {
      await expect(c.query(`select ${функция}('[]'::jsonb)`)).rejects.toThrow(/пустой снимок/)
    })
  })
})
```

- [ ] **Шаг 2: убедиться, что проверки падают**

```bash
npm test
```

- [ ] **Шаг 3: написать миграцию с пятью функциями**

```bash
supabase migration new replace_sheets
```

- [ ] **Шаг 4: убедиться, что проверки зелёные**

```bash
npm run db:reset && npm test
```

- [ ] **Шаг 5: коммит**

```bash
git add supabase/migrations __tests__/db
git commit -m "feat: запись снимка для остальных пяти листов таблицы"
```

---

### Задача 5: запись снимка рекламы — областью в целую папку

Отличие от листов одно, и оно существенное: снимок приходит по всей папке сразу, а адрес
составной. Иначе исчезнувший из папки файл остался бы в базе навсегда — удалять его строки было
бы некому.

**Файлы:**
- Создать: `supabase/migrations/<время>_replace_ads.sql`
- Создать: `__tests__/db/replace-ads.test.ts`

**Интерфейсы:**
- Отдаёт наружу: `raw.replace_ads(p_rows jsonb) returns void`. Вход — строки **всех** файлов
  папки разом, ключи `file_name`, `row_no`, `date`, `campaign`, `spend_usd`.

- [ ] **Шаг 1: написать падающие проверки**

Создать `__tests__/db/replace-ads.test.ts`:

```ts
import { afterAll, expect, test } from 'vitest'
import { inRollback, pool } from './support'

afterAll(() => pool.end())

const папка = [
  { file_name: 'meta_2026-03.csv', row_no: 1, date: '2026-03-01',
    campaign: 'spring', spend_usd: '10.00' },
  { file_name: 'meta_2026-03 (1).csv', row_no: 1, date: '2026-03-01',
    campaign: 'spring', spend_usd: '10.00' },
  { file_name: 'google_2026-03.csv', row_no: 1, date: '2026-03-01',
    campaign: 'search', spend_usd: '20.00' },
]

test('дубликат файла принят отдельными строками — ничего не потеряно', async () => {
  await inRollback(async (c) => {
    await c.query('select raw.replace_ads($1::jsonb)', [JSON.stringify(папка)])
    const { rows } = await c.query('select file_name from raw.ads order by file_name')
    expect(rows).toHaveLength(3)
  })
})

test('тот же снимок папки дважды не меняет ни одного байта', async () => {
  await inRollback(async (c) => {
    await c.query('select raw.replace_ads($1::jsonb)', [JSON.stringify(папка)])
    const до = (await c.query('select * from raw.ads order by file_name, row_no')).rows
    await c.query('select raw.replace_ads($1::jsonb)', [JSON.stringify(папка)])
    const после = (await c.query('select * from raw.ads order by file_name, row_no')).rows
    expect(после).toEqual(до)
  })
})

test('исчезнувший из папки файл уносит с собой все свои строки', async () => {
  await inRollback(async (c) => {
    await c.query('select raw.replace_ads($1::jsonb)', [JSON.stringify(папка)])
    const безДубликата = папка.filter((r) => r.file_name !== 'meta_2026-03 (1).csv')
    await c.query('select raw.replace_ads($1::jsonb)', [JSON.stringify(безДубликата)])
    const { rows } = await c.query(
      `select 1 from raw.ads where file_name = 'meta_2026-03 (1).csv'`,
    )
    expect(rows).toEqual([])
  })
})

test('пустой снимок папки отвергается', async () => {
  await inRollback(async (c) => {
    await expect(c.query(`select raw.replace_ads('[]'::jsonb)`)).rejects.toThrow(/пустой снимок/)
  })
})
```

- [ ] **Шаг 2: убедиться, что проверки падают**

```bash
npm test
```

- [ ] **Шаг 3: написать функцию**

```bash
supabase migration new replace_ads
```

```sql
create function raw.replace_ads(p_rows jsonb) returns void
language plpgsql
as $$
begin
  if p_rows is null
     or jsonb_typeof(p_rows) <> 'array'
     or jsonb_array_length(p_rows) = 0 then
    raise exception 'пустой снимок источника для raw.ads: загрузка отменена'
      using hint = 'снимок приходит по всей папке сразу; ноль строк означает сбой чтения папки';
  end if;

  insert into raw.ads as t (file_name, row_no, date, campaign, spend_usd)
  select r->>'file_name', (r->>'row_no')::integer, r->>'date', r->>'campaign', r->>'spend_usd'
    from jsonb_array_elements(p_rows) as r
      on conflict (file_name, row_no) do update
         set date       = excluded.date,
             campaign   = excluded.campaign,
             spend_usd  = excluded.spend_usd,
             updated_at = now()
       where (t.date, t.campaign, t.spend_usd)
          is distinct from
             (excluded.date, excluded.campaign, excluded.spend_usd);

  delete from raw.ads t
   where not exists (
     select 1 from jsonb_array_elements(p_rows) as r
      where r->>'file_name' = t.file_name
        and (r->>'row_no')::integer = t.row_no
   );
end;
$$;
```

- [ ] **Шаг 4: убедиться, что проверки зелёные**

```bash
npm run db:reset && npm test
```

- [ ] **Шаг 5: коммит**

```bash
git add supabase/migrations __tests__/db
git commit -m "feat: запись снимка папки рекламы, подчистка областью в целую папку"
```

---

### Задача 6: слой фактов

**Файлы:**
- Создать: `supabase/migrations/<время>_fact.sql`
- Создать: `__tests__/db/fact.test.ts`

**Интерфейсы:**
- Потребляет: сырые таблицы из задачи 2 — на них ссылаются внешние ключи.
- Отдаёт наружу: `fact.orders`, `fact.refunds`, `fact.costs`, `fact.fees`, `fact.opex`,
  `fact.fx`, `fact.ads`. Ключ каждой — адресные колонки соответствующей сырой таблицы, они же
  внешний ключ с `on delete cascade`.

- [ ] **Шаг 1: написать падающие проверки**

Создать `__tests__/db/fact.test.ts`:

```ts
import { afterAll, expect, test } from 'vitest'
import { inRollback, pool, rows } from './support'

afterAll(() => pool.end())

const дваЗаказа = [
  { row_no: 1, date: '01.03.2026', order_id: 'A-1', sku: 'NP-001', units: '1',
    gross_eur: '10,00', discount_eur: '', gateway: 'stripe' },
  { row_no: 2, date: '2026-03-01', order_id: 'A-2', sku: 'NP-002', units: '1',
    gross_eur: '20,00', discount_eur: '', gateway: 'stripe' },
]

test('в схеме fact ровно семь таблиц', async () => {
  const найдено = await rows<{ tablename: string }>(
    `select tablename from pg_tables where schemaname = 'fact' order by tablename`,
  )
  expect(найдено.map((r) => r.tablename)).toEqual([
    'ads', 'costs', 'fees', 'fx', 'opex', 'orders', 'refunds',
  ])
})

test('ноль не выдаётся за отсутствие данных', async () => {
  const нарушения = await rows(
    `select table_schema || '.' || table_name || '.' || column_name as "место"
       from information_schema.columns
      where table_schema in ('raw', 'fact')
        and column_name not in ('row_no', 'file_name', 'updated_at')
        and (column_default is not null or is_nullable = 'NO')
      order by 1`,
  )
  expect(нарушения).toEqual([])
})

test('исчезнувшая сырая строка уносит свой факт', async () => {
  await inRollback(async (c) => {
    await c.query('select raw.replace_orders($1::jsonb)', [JSON.stringify(дваЗаказа)])
    await c.query(
      `insert into fact.orders (row_no, date, order_id, sku, units, gross, currency, gateway)
       values (1, date '2026-03-01', 'A-1', 'NP-001', 1, 10.00, 'EUR', 'stripe'),
              (2, date '2026-03-01', 'A-2', 'NP-002', 1, 20.00, 'EUR', 'stripe')`,
    )
    await c.query('select raw.replace_orders($1::jsonb)', [
      JSON.stringify(дваЗаказа.slice(0, 1)),
    ])
    const { rows: остаток } = await c.query('select row_no from fact.orders order by row_no')
    expect(остаток.map((r) => r.row_no)).toEqual([1])
  })
})

test('сумма без валюты запрещена: число без валюты — не деньги', async () => {
  await inRollback(async (c) => {
    await c.query('select raw.replace_orders($1::jsonb)', [JSON.stringify(дваЗаказа)])
    await expect(
      c.query(`insert into fact.orders (row_no, gross, currency) values (1, 10.00, null)`),
    ).rejects.toThrow(/orders_currency_required/)
  })
})
```

- [ ] **Шаг 2: убедиться, что проверки падают**

```bash
npm test
```

- [ ] **Шаг 3: написать миграцию**

```bash
supabase migration new fact
```

```sql
create table fact.orders (
  row_no   integer primary key references raw.orders(row_no) on delete cascade,
  date     date,
  order_id text,
  sku      text,
  units    integer,
  gross    numeric(14,2),
  discount numeric(14,2),
  currency char(3),
  gateway  text,
  constraint orders_currency_required
    check (num_nulls(gross, discount) = 2 or currency is not null)
);

create table fact.refunds (
  row_no      integer primary key references raw.refunds(row_no) on delete cascade,
  refund_date date,
  order_id    text,
  sku         text,
  units       integer,
  amount      numeric(14,2),
  currency    char(3),
  constraint refunds_currency_required check (amount is null or currency is not null)
);

create table fact.costs (
  row_no     integer primary key references raw.costs(row_no) on delete cascade,
  sku        text,
  cost       numeric(14,2),
  currency   char(3),
  valid_from date,
  constraint costs_currency_required check (cost is null or currency is not null)
);

create table fact.fees (
  row_no   integer primary key references raw.fees(row_no) on delete cascade,
  gateway  text,
  percent  numeric(6,4),
  fixed    numeric(14,2),
  currency char(3),
  constraint fees_currency_required check (fixed is null or currency is not null)
);

create table fact.opex (
  row_no   integer primary key references raw.opex(row_no) on delete cascade,
  month    date,
  category text,
  amount   numeric(14,2),
  currency char(3),
  constraint opex_currency_required check (amount is null or currency is not null)
);

create table fact.fx (
  row_no      integer primary key references raw.fx(row_no) on delete cascade,
  date        date,
  usd_per_eur numeric(12,6)
);

create table fact.ads (
  file_name text    not null,
  row_no    integer not null,
  date      date,
  campaign  text,
  platform  text,
  spend     numeric(14,2),
  currency  char(3),
  primary key (file_name, row_no),
  foreign key (file_name, row_no) references raw.ads(file_name, row_no) on delete cascade,
  constraint ads_currency_required check (spend is null or currency is not null)
);
```

- [ ] **Шаг 4: убедиться, что проверки зелёные**

```bash
npm run db:reset && npm test
```

- [ ] **Шаг 5: коммит**

```bash
git add supabase/migrations __tests__/db
git commit -m "feat: слой фактов, связанный с сырьём каскадным внешним ключом"
```

---

### Задача 7: посев

Посев живёт обычным SQL в `supabase/seed.sql`. Ни функции, ни схемы `dev`: этого файла нет в
облаке, поэтому и защищать нечего. В сырьё он пишет функциями записи снимка, в факты — своими
вставками с `on conflict`, потому что разбора в S1 ещё нет.

**Файлы:**
- Создать: `supabase/seed.sql`
- Изменить: `supabase/config.toml`
- Создать: `__tests__/db/seed.test.ts`

- [ ] **Шаг 1: написать падающие проверки**

Создать `__tests__/db/seed.test.ts`:

```ts
import { readdirSync, readFileSync } from 'node:fs'
import { join } from 'node:path'
import { afterAll, expect, test } from 'vitest'
import { pool, rows } from './support'

afterAll(() => pool.end())

test('посев положил строки в оба слоя', async () => {
  const сырьё = await rows<{ n: string }>('select count(*)::int as n from raw.orders')
  const факты = await rows<{ n: string }>('select count(*)::int as n from fact.orders')
  expect(Number(сырьё[0].n)).toBeGreaterThan(0)
  expect(Number(факты[0].n)).toBeGreaterThan(0)
})

test('обе выгрузки meta на месте — дубликат различается только адресом', async () => {
  const найдено = await rows<{ file_name: string; campaign: string }>(
    `select file_name, campaign from raw.ads where file_name like 'meta%' order by file_name`,
  )
  expect(найдено).toHaveLength(2)
  expect(найдено[0].campaign).toBe(найдено[1].campaign)
})

test('кривизна источника доехала до сырого слоя нетронутой', async () => {
  const артикулы = await rows<{ sku: string }>('select sku from raw.orders order by row_no')
  expect(артикулы.map((r) => r.sku)).toContain('np-003 ')
  const суммы = await rows<{ amount_eur: string }>(
    `select amount_eur from raw.opex where category = 'аренда'`,
  )
  expect(суммы[0].amount_eur).toBe('1 234,50')
})

test('двойной посев не создал дублей: количества ровно те, что заложены', async () => {
  const количества = await rows<{ таблица: string; n: string }>(
    `select 'raw.orders' as "таблица", count(*)::int as n from raw.orders
     union all select 'raw.ads', count(*)::int from raw.ads
     union all select 'fact.orders', count(*)::int from fact.orders
     order by 1`,
  )
  expect(количества).toEqual([
    { таблица: 'fact.orders', n: 3 },
    { таблица: 'raw.ads', n: 3 },
    { таблица: 'raw.orders', n: 3 },
  ])
})

test('демонстрационных строк нет там, куда уезжают миграции', async () => {
  const каталог = join(process.cwd(), 'supabase', 'migrations')
  const виновные = readdirSync(каталог)
    .filter((f) => f.endsWith('.sql'))
    .filter((f) => /insert\s+into|\bdev\b/i.test(readFileSync(join(каталог, f), 'utf8')))
  expect(виновные).toEqual([])
})
```

- [ ] **Шаг 2: убедиться, что проверки падают**

```bash
npm test
```

- [ ] **Шаг 3: написать `supabase/seed.sql`**

Артикулы и суммы пишутся ровно так, как в источнике: `NP-001` с обычным дефисом, `np-003 ` со
строчными буквами и хвостовым пробелом, `NP‑003` с неразрывным дефисом U+2011, аренда
`1 234,50` с неразрывным пробелом U+00A0. Это не описки, а содержание проверки.

```sql
-- Демонстрационные строки. Файл выполняется только при локальном пересоздании базы
-- и никогда не уезжает в облачный проект вместе с миграциями.
-- В сырьё пишем функциями записи снимка: механизм идемпотентности в проекте один.

select raw.replace_orders($json$[
  {"row_no": 1, "date": "01.03.2026", "order_id": "A-1", "sku": "NP-001", "units": "1",
   "gross_eur": "10,00", "discount_eur": "", "gateway": "stripe"},
  {"row_no": 2, "date": "2026-03-01", "order_id": "A-2", "sku": "np-003 ", "units": "2",
   "gross_eur": "20,00", "discount_eur": "1,00", "gateway": "paypal"},
  {"row_no": 3, "date": "02.03.2026", "order_id": "A-3", "sku": "NP‑003", "units": "1",
   "gross_eur": "30,00", "discount_eur": "", "gateway": "stripe"}
]$json$::jsonb);

-- далее так же: replace_refunds, replace_costs, replace_fees, replace_opex, replace_fx
-- и replace_ads с двумя файлами meta и одним google

insert into fact.orders as t
       (row_no, date, order_id, sku, units, gross, discount, currency, gateway)
values (1, date '2026-03-01', 'A-1', 'NP-001', 1, 10.00, null, 'EUR', 'stripe')
    on conflict (row_no) do update
       set date = excluded.date, order_id = excluded.order_id, sku = excluded.sku,
           units = excluded.units, gross = excluded.gross, discount = excluded.discount,
           currency = excluded.currency, gateway = excluded.gateway
     where (t.date, t.order_id, t.sku, t.units, t.gross, t.discount, t.currency, t.gateway)
        is distinct from
           (excluded.date, excluded.order_id, excluded.sku, excluded.units,
            excluded.gross, excluded.discount, excluded.currency, excluded.gateway);

-- то же для остальных шести таблиц фактов
```

- [ ] **Шаг 4: заставить посев выполняться дважды при каждом пересоздании**

В `supabase/config.toml`, раздел `[db.seed]`:

```toml
# Файл указан дважды намеренно: тогда неидемпотентный посев ломает саму команду
# пересоздания базы, а не только проверку, и ломает её сразу у всех.
sql_paths = ["./seed.sql", "./seed.sql"]
```

- [ ] **Шаг 5: выяснить фактом, выполняется ли посев дважды**

```bash
npm run db:reset
```

Ожидается: команда проходит, а количества строк остаются теми же, что при одиночном посеве.
Если CLI схлопнул повторяющиеся записи — количества это не покажут, поэтому проверяется прямо:

```bash
psql "postgresql://postgres@127.0.0.1:5432/nordic_pet" -c "select count(*) from raw.orders"
```

Если окажется, что список схлопывается, запасной способ — флаг `--sql-paths`, повторённый
дважды в команде `db:reset`: CLI документирует его как «may be repeated». Что из двух верно —
записать в контракт, в раздел про проверенные допущения, фактом.

- [ ] **Шаг 6: убедиться, что проверки зелёные**

```bash
npm run db:reset && npm test
```

- [ ] **Шаг 7: коммит**

```bash
git add supabase/seed.sql supabase/config.toml __tests__/db
git commit -m "feat: посев демонстрационных строк, выполняемый дважды при каждом пересоздании"
```

---

### Задача 8: проверки в `ci` и записи в документах

**Файлы:**
- Изменить: `.github/workflows/ci.yml`
- Изменить: `README.md`
- Изменить: `CLAUDE.md`

- [ ] **Шаг 1: переписать работу `ci`**

Имя работы остаётся `ci`. К ней добавляется служба базы и три шага; шаг `test` переезжает ниже
пересоздания базы, потому что теперь без неё не работает.

```yaml
jobs:
  ci:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/nordic_pet?sslmode=disable

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: npm

      - name: install
        run: npm ci

      - name: lint
        run: npm run lint

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: создать базу проекта
        run: psql "postgresql://postgres:postgres@127.0.0.1:5432/postgres" -c "create database nordic_pet"

      - name: пересоздать базу той же командой, что и разработчик
        run: npm run db:reset

      - name: test
        run: npm test

      - name: build
        run: npm run build
```

- [ ] **Шаг 2: дописать в README раздел о локальной базе**

```markdown
## Локальная база

Нужен PostgreSQL 15 и Supabase CLI. Docker не нужен.

    createdb nordic_pet          # один раз
    npm run db:reset             # пересоздать из миграций и посева
    npm test                     # проверки, включая проверки базы
```

- [ ] **Шаг 3: поправить команду в `CLAUDE.md`**

В блоке команд `supabase db reset` заменяется на `npm run db:reset` — прежняя команда без адреса
базы на этой машине не работает, а правила должны описывать то, что есть.

- [ ] **Шаг 4: убедиться, что всё зелено локально**

```bash
npm run lint && npm run db:reset && npm test && npm run build
```

- [ ] **Шаг 5: коммит**

```bash
git add .github/workflows/ci.yml README.md CLAUDE.md
git commit -m "ci: база службой раннера, пересоздание и проверки в работе ci"
```

- [ ] **Шаг 6: доказать, что проверка краснеет от сломанного кода**

Сломать одно утверждение — убрать `where … is distinct from …` из `raw.replace_orders`, —
запушить, дождаться красного прогона `ci`, вернуть обратно, дождаться зелёного. Ссылки на оба
прогона идут в тело pull request: этого требует критерий 10 контракта.

---

## Отложено на потом — в тело pull request списком задач

- Пересоздание базы печатает предупреждение `failed to cache migrations catalog … docker`: CLI
  не смог сложить служебный кэш, потому что Docker выключен. На поведение не влияет, но шумит в
  выводе `ci`. Гасится отдельно, кругом на это не тратимся.

## Порядок сдачи

1. Задачи 1–8 выполнены, каждая своим коммитом.
2. `npm run db:reset`, `npm test`, `npm run lint`, `npm run build` зелены на срезе ветки, вывод
   вставлен в тело pull request.
3. Pull request открыт из `feat/s1-tables` в `main`, в теле — проход по всем двенадцати
   критериям приёмки контракта с указанием файла и строки, ссылки на красный и зелёный прогоны
   `ci`, и отдельной строкой — всякое отступление от буквы контракта с причиной.
4. Проверка кода тем, кто его не писал: рецензенту передаются только диапазон коммитов, контракт
   и правила проекта.
5. После исправления замечаний — вторая, узкая проверка на дельту между уже проверенным коммитом
   и новой головой ветки. Сливается только оценённый коммит.
