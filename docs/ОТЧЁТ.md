# Финальный отчёт — дашборд прибыли Nordic Pet

Дашборд работает на `nordic-pet.vercel.app`, закрыт входом по паролю, показывает прибыль
магазина за выбранный месяц. Работа разрезана на десять кусков; каждый прошёл контракт, план,
код и проверку кода тем, у кого не было контекста автора.

Отчёт написан по трём вопросам приёмки: **что сделал, чего не сделал, где не уверен.** Ниже них —
полный список отложенных задач и указание, где лежит след работы.

---

## Что сделал

| Кусок | Что появилось |
|---|---|
| S0 | Приложение и проверка сборки; страница состояния называет номер коммита, из которого собрана |
| S1 | База: сырой слой, слой фактов, запись снимка, посев; пересоздание одной командой |
| S2 | Загрузка Google Таблицы в сырой слой; повторный запуск не меняет ни строки |
| S3 | Загрузка папки `ads-exports` с Google Диска; папка читается целиком и за один раз |
| S4 | Разбор сырых строк в слой фактов: свёртка копий, пересчёт в евро по курсу дня, одиннадцать видов неполноты |
| S5 | Семь правил счёта, экран и кнопка «Обновить данные» |
| S6 | Вход по паролю, закрытие всего кроме названных исключений, публикация в бой |
| S7 | Папка рекламы читается и с общего диска |
| S8 | Пять дыр перед сдачей: замок от подбора пароля, выход, вид отказа, пометка «числа отстали», замок от двух одновременных обновлений |
| S9 | Вид экрана |

### Пять проверок работодателя

| Проверка | Чем закрыта |
|---|---|
| Числа за март сходятся | пять итогов сверены с независимым счётом, который владелец вёл отдельно от кода дашборда, прямо из источников; расхождений нет. **Эталон работодателя нам неизвестен, и сверка с ним — его шаг** |
| Повторная загрузка ничего не меняет | живая проверка: два нажатия подряд, отчёт до и после совпадает целиком |
| Живой источник работает | боевой путь ходит в Google по-настоящему. Проверено: кнопка перечитывает оба источника; второй прогон подряд не меняет ни одного числа; исправленная человеком ячейка сырья пересчитывается, а соседняя строка факта физически не переписывается. **Дописанную строку работодатель добавит сам на приёмке — этого мы не проверяли** |
| Доступ закрыт | закрыто всё, кроме названных исключений; проверено из окна инкогнито и прогоном по боевому адресу |
| Экран сознаётся в неполноте | доля выручки, посчитанной по настоящей цене, названа числом и полосой; одиннадцать видов дыр печатаются, включая нулевые |

### Числа за март 2026

| Итог | Значение |
|---|---|
| Оборот | 18 764,00 € |
| Скидки | 427,50 € |
| Возвраты | 1 059,46 € |
| **Чистая выручка** | **17 277,04 €** |
| **Себестоимость проданного** | **6 028,11 €** |
| **Реклама** | **4 431,37 €** |
| **Комиссии** | **526,12 €** |
| Постоянные расходы | 4 552,90 € |
| **Прибыль** | **1 738,53 €** |
| Маржа | 10,1 % |
| Окупаемость рекламы | 4,23 × по обороту |
| Посчитано по настоящей цене | 80,3 % от чистой выручки |

### Чем это доказано

| Прибор | Ответ |
|---|---|
| Набор проверок | 743 проверки в 58 файлах, код возврата 0 |
| Приборы сломов, шесть списков | 248 сломов, 248 доказанных механизмов, код возврата 0 у каждого |
| Живой набор | 6 проверок, включая настоящий поход в Google и закрытие боевого адреса снаружи |
| Типы, линтер, сборка | код возврата 0 |
| Проверка сборки на GitHub | зелёная на каждом слитом коммите |

Слом — это утверждение о механизме, записанное так, чтобы его можно было запустить: ломаем
механизм, смотрим, покраснела ли **его собственная** проверка, возвращаем файл. Красное вообще
ничего не доказывает; доказывает только красное у той проверки, которая сторожит именно это
утверждение.

---

## Чего не сделал

| Что | Почему |
|---|---|
| Знак магазина на экране | решение владельца: нынешнего вида достаточно |
| Сортировка таблицы нажатием, свёртки блоков, графики | это поведение, а не вид; любое требует счёта на экране или новых полей отчёта |
| Роль базы с правом только на вызов функций записи | смена модели безопасности базы и миграция в бой — отдельный кусок работы |
| Живая проверка на настоящем общем диске | общего диска у нас нет; поведение Google на нём никем не наблюдалось |
| Сломы для таблицы стилей | вид проверяется глазом и снимком; проверка, читающая исходный текст, доказывала бы наличие строки, а не наличие вида |
| Несколько магазинов, несколько валют, права пользователей | в задании этого нет |
| Показ строк-близнецов на экране | они видны в выводе команды разбора; вынести их на экран — это «что», а не «как» |

---

## Где не уверен

| Место | В чём неуверенность |
|---|---|
| **Развилка правил 4 и 5** | буква правила 5 снимает себестоимость возвращённых штук, буква правила 4 определяет себестоимость товара без цены как 40 % чистой выручки, из которой возвраты уже вычтены. Исполнить обе дословно значит снять возвраты дважды. Наше прочтение даёт себестоимость 6 028,11 € против 5 894,31 € — **2,22 % при пороге приёмки 0,5 %**, по прибыли разрыв 7,70 %. Направление важно: у нас себестоимость **больше**, прибыль **меньше** |
| **Заказ без ставки комиссии** | даёт ноль комиссии молча — единственное место, где ноль встал вместо «нет данных» без называния. Путь закрыт двумя отказами разбора и на мартовские числа не влияет |
| **Поведение Google на общем диске** | доказано составом наших запросов и цитатами справочника, а не живой папкой |
| **Доля честности вне 0…100** | арифметически возможна, в настоящих данных не встречалась ни разу |
| **Вид в других движках** | проверен в одном; Safari и Firefox не смотрел |
| **Месяцы, кроме марта** | живьём считался только март; остальные месяцы проходили через код, но их числа никто не сверял |
| **Переменные `SUPABASE_*` при пересоздании базы** | снимаются только `PG*`; влияют ли `SUPABASE_*` на то, какие миграции применятся, не проверял никто |

---

## Отложенные задачи — полный список

Собран из тел двенадцати pull request. У каждой строки — причина, по которой она не сделана, и
чем она грозит на деле. Доказательства по каждой лежат в её pull request.

### Права и защита базы

| Задача | Причина | Чем грозит |
|---|---|---|
| Замок на разрушающие операторы | требует своей роли, смены модели безопасности базы и миграции в бой | «загрузчик ничего не разрушает» — обещание кода, а не запрет базы |
| Хост и пользователь переносятся из входа, а не из констант | дыры нет, работа наполовину | «адрес собран заново» верно лишь отчасти |
| Мёртвая строка в списке написаний локального хоста | недостижима сегодня | сработай она — собранный адрес вышел бы поломанным |
| Проверка непростого имени пользователя ловит слишком широко | подмениться сегодня нечем | зубы у проверки тупее, чем выглядят |
| Проверка с подставной командой пересоздания держится на её коде возврата | сегодня код ненулевой | станет нулевым — проверка молча пойдёт применять посев к живой базе |
| Два списка локальных адресов в двух файлах | второй заведён, чтобы не открывать запертый файл | со временем разойдутся |

### Загрузка из Google

| Задача | Причина | Чем грозит |
|---|---|---|
| Снимок папки не атомарен | пакетного снимка Drive API не даёт | файл, добавленный между списком и скачиванием, приедет следующим прогоном |
| Отказ на документе Google проверен только с нашей стороны | подставку для него не ставили нарочно | поведение Google живьём не наблюдалось |
| Защита обхода страниц не ловит бесконечный ряд разных меток | потолка числа страниц нет в справочнике, а выдумывать его нельзя | теоретический бесконечный обход |
| Проверка команды зовёт файл сценария, а не строку из описания команд | тот же изъян у двух команд | опечатка в строке запуска не ловится |
| Две загрузки разом не испытывались | руки не дошли | поведение при одновременных прогонах неизвестно |
| Живая проверка на настоящем общем диске | общего диска нет | единственное непроверенное место S7 |
| Ловушка сети для загрузок на боевом пути | их боевой путь ходит в Google по-настоящему | у загрузок нет боевого двойника под перекрытой сетью |
| У доступа к Диску есть шов ради проверки, у Таблицы нет | неясно, какой приём лучше | два разных приёма для одного дела |
| Защиты S3 в приборе сломов не дописаны | решение владельца: не раздувать маленький кусок ради полноты чужого списка | семь защит S3 показаны сломами руками, но в приборе их нет |
| Нет слома на половину «что делать» в отказе про неполный список | один слом снимает обе половины разом | половинная правка отказа не покраснеет своим сломом |

### Разбор в факты

| Задача | Причина | Чем грозит |
|---|---|---|
| Отказы идут двумя волнами | вторая волна требует полностью разобранных данных | лист с обеими бедами потребует двух кругов правки |
| Долю вместо процентных пунктов не ловит ничто | порога, ниже которого ставок не бывает, нет ни в задании, ни в документации | `0.0190` пройдёт как ставка и даст комиссию в сто раз меньше |
| Строки-близнецы видны только в выводе команды | показать их на экране — это «что», а не «как» | человек у экрана о них не узнает |
| Код состояния и подлинный текст отказа базы теряются при переводе | перевод на человеческий язык съедает подробности | отлаживать редкий отказ будет нечем |
| `40P01` живьём не наблюдался | воспроизвести взаимную блокировку нарочно дорого | перевод этого кода проверен подставкой, а не настоящим случаем |
| «Боевая проверка возвращает базу такой, какой нашла» сторожится случайно | сторожит порядок файлов прогона, а он берётся из кэша длительностей | снятая уборка ловится через раз, и красным чужим, по которому причину не найти |

### Счёт и экран

| Задача | Причина | Чем грозит |
|---|---|---|
| Заказ без ставки комиссии даёт ноль молча | путь закрыт двумя отказами разбора, на мартовские числа не влияет; починка ввела бы два новых вида дыры | единственное место, где ноль встал вместо «нет данных» без называния |
| `money('')` тихо отдаёт « €» | путь сегодня недостижим | пустая строка вместо числа прошла бы незамеченной |
| Боевой вызов команды метрик в списке не повторяет сценарий целиком | та же асимметрия у трёх прежних команд | кто позовёт запись списка вместо сценария, получит не то, что ожидал |
| Проверка «слой метрик не превращает деньги в число» читает исходный текст | заменить её механизмом нечем: механизм здесь и есть система типов | проверка честно слабая и названа слабой |
| Замок обновления стоит только на пути кнопки | команды из терминала его не берут | одновременный прогон из терминала и кнопки не разведён |
| Наследственная ветка на странице отчёта | живёт ради принятой проверки S6 | убрать её значит покрасить чужую принятую проверку |
| Сторожа у серверных действий входа и выхода нет | они ничего не отдают и ничего не пишут постороннему | доказательство им одно — проход руками |
| Прибор сломов хрупок на длинном прогоне | за кусок обрывался четыре раза, оставляя правку в дереве | оборванный прогон легко принять за красное по существу |

### Вид экрана

| Задача | Причина | Чем грозит |
|---|---|---|
| Защиты, живущие в таблице стилей, без сломов | их две: приведение длины полосы к пределам и гашение устаревших чисел. Сломать их проверкой нельзя — нужен прогон в настоящем браузере | удаление одной строки вернёт ложь «полоса во всю дорожку при отрицательной доле», и ни один слом не увидит |
| Липкая шапка таблицы не работает уже 768 пикселей | уже с этой ширины таблица становится своим контейнером прокрутки, а внутри него прилипание считается от него, а не от страницы | на узком экране числа читаются без подписей колонок |
| `display: block` у таблицы на узком экране | иначе за край уезжает вся страница | снимает табличную семантику в части читалок экрана |
| Снимки делаются раскладкой, а не живым экраном | снимок с живого экрана потребовал бы входа и пароля | снимок и бой могут разойтись, и заметит это только глаз |
| Цитата слома `stale-class-dropped` — подстрока цитаты соседнего слома | сегодня обе однозначны | одна будущая правка этой строки потребует перевода в двух местах |
| Вид проверен в одном движке | других под рукой нет | поведение в Safari и Firefox неизвестно |

### Хостинг и окружение

| Задача | Причина | Чем грозит |
|---|---|---|
| Вернуть защиту развёртывания Vercel на превью | снималась ради прохода снаружи | предпросмотры веток открыты |
| Предупреждение про Docker при пересоздании базы | CLI не смог сложить служебный кэш | шумит в выводе, на поведение не влияет |
| Версия среды исполнения задана в трёх местах | сначала надо решить, какую считать главной | обычный способ получить «в проверке зелено, в бою не собирается» |
| Запасной путь определения коммита не привязан к каталогу приложения | в бою отвечает переменная хоста, и этот путь не используется | внутри чужого репозитория страница напечатала бы чужой правдоподобный номер |
| Проверки определителя коммита зависят от рабочего каталога | очевидная починка заменила бы честную ошибку молчаливым расхождением | запуск из другого каталога роняет три проверки не по существу |

### Закрыто позже — здесь для полноты

| Что | Где закрыто |
|---|---|
| Перебор пароля не ограничен | S8 |
| Выхода не было | S8 |
| Отказ отчёта не называл свой вид, и в текст попадал адрес базы | S8 |
| Экран не знал о щели при холодном открытии | S8 |
| Два одновременных нажатия кнопки не разведены | S8 |
| Папка на общем диске не читалась | S7 |
| Знак евро мог уехать от числа на другую строку | S9, стилями — без правки формата |
| Комментарий в настройках живого набора врал о числе живых проверок | проверено: исправлен |

---

## Где смотреть след работы

| След | Где лежит |
|---|---|
| Контракты — по одному на кусок, каждый с развилками и решениями владельца | ветки `contract/s0-skeleton` … `contract/s9-screen`, файл `.contracts/<кусок>.md` |
| Планы со списками сломов | `docs/plans/` |
| Тела двенадцати pull request: отступления, находки проверки кода, «чего этим не доказано», отложенные задачи | pull request с 1 по 12 |
| Вопросы, заданные до начала | развилками внутри контрактов: каждая названа явно, с рекомендацией и с решением владельца, и решение записано его словами, а не пересказом |
| Снимки экрана и опись к ним | `docs/screens/` |
| Как устроен каждый механизм | README |

Порядок чтения, если время ограничено: контракт куска → тело его pull request → таблица сломов
в нём. Контракт говорит, что обещано; тело pull request — что вышло и чем доказано; таблица
сломов — что из этого проверено красным, а что осталось словами.

---

## Как запустить и посмотреть

| Что | Как |
|---|---|
| Адрес дашборда | `https://nordic-pet.vercel.app` |
| Вход | по логину и паролю; они передаются отдельно от этого отчёта и в репозитории не лежат |
| Что видно без входа | ничего: ни экрана, ни чисел, ни адресов, по которым страница берёт числа |
| Обновление данных | кнопка «Обновить данные» на самом экране: перечитывает оба источника и пересобирает факты |
| Месяц | берётся из адреса (`?m=2026-03`); без него показан последний месяц, за который есть заказы |

**Дашборд только читает Google и никогда туда не пишет.** У служебного аккаунта доступ
«Просмотр», и у Google запрашиваются области **только на чтение** — отдельная для Таблицы,
отдельная для Диска.

**Замок от подбора пароля запирает и нас.** Десять неверных попыток подряд закроют дашборд на
пятнадцать минут для того, кто ошибался. Это исправная работа механизма, а не поломка.

Запустить у себя: нужны PostgreSQL 15 и Supabase CLI, дальше — `npm run db:reset`, `npm test`,
`npm run dev`. Полностью это описано в README.
