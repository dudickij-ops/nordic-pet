# Числа марта: кусок S13

Дней без заказов в марте: 1

День заказа — по тому же правилу, что в `MONEY_CTES`: самая ранняя дата строк заказа.

Команда:

```bash
node --input-type=module -e "import pg from 'pg'; import { projectDatabaseUrl } from './lib/db-url.ts'; const p = new pg.Pool({ connectionString: projectDatabaseUrl() }); const r = await p.query(\"select count(*)::int as n from generate_series('2026-03-01'::date, '2026-03-31'::date, interval '1 day') d where d::date not in (select min(o.date) from fact.orders o group by o.order_id)\"); console.log('days_without_orders', r.rows[0].n); await p.end()"
```

Вывод: `days_without_orders 1`, код возврата 0. Команда из задания сработала как есть, без
запасного `.mjs`-скрипта.

## Задача 7: живой «после» — выводы и средняя по дням

Команда: `NORDIC_PET_DB_TARGET=local npm run --silent metrics -- 2026-03 > docs/сверка/метрики-2026-03-после-s13.txt`, код возврата 0. Числа ниже — из этого вывода.

| Величина | Наш счёт (договор) | Живой вывод команды | Совпало / расхождение |
|---|---|---|---|
| маржинальный доход | 6 291,44 € | 6 291,44 € | совпало |
| постоянные расходы в маржинальном доходе | 72,4 % | 72,4 % | совпало |
| реклама | окупается | окупается | совпало |
| месяц в убытке | нет | нет | совпало |
| прибыль приблизительная | да | да | совпало |
| средняя чистая выручка по дням | 557,32 € (по 31 дню месяца) | 557,32 € (по 31 дню месяца) | совпало |

`diff docs/сверка/метрики-2026-03-до-s13.txt docs/сверка/метрики-2026-03-после-s13.txt` — только
десять добавленных в конце строк, ни одной удалённой (`50a51,60`).
