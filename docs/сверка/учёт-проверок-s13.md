# Учёт правки принятых проверок — кусок S13

Решение владельца Э6 (16 сентября 2026 года): проверки прошлых кусков, которые один экран делает
неверными, удаляются или переписываются под учёт. Дописанное рядом правкой не считается.

| Задача | Файл | Проверка | Что утверждала | Почему больше не верно | Что утверждает теперь |
|---|---|---|---|---|---|
| 1 | `__tests__/metrics/waterfall.test.ts` | в водопаде ровно девять ступеней, в названном порядке | 9 ступеней: оборот→скидки→возвраты→чистая выручка→себестоимость→реклама→комиссии→постоянные→прибыль | каскад начинается с чистой выручки, ступеней оборота/скидок/возвратов нет, добавлен маржинальный доход | «ступеней семь, от чистой выручки до прибыли, маржинальный доход пятым» — 7 ключей `net,cogs,ads,fees,margin_income,fixed,profit` |
| 1 | `__tests__/metrics/waterfall.test.ts` | каждая ступень печатает свою колонку итогов, прибыль — та же колонка, что прибыль итога | все 9 полей строки итогов (включая gross/discounts/refunds) совпадают со своими ступенями | gross/discounts/refunds больше не ступени | свойство «своя колонка, не пересчёт» покрыто проверкой маржинального дохода и проверкой расхождений цепочки |
| 1 | `__tests__/metrics/waterfall.test.ts` | доли ступеней отданы процентами, а не долями единицы | доли — проценты, на примере cogs/gross | база — чистая выручка, gross не ступень | «доли и края ступеней — от чистой выручки» |
| 1 | `__tests__/metrics/waterfall.test.ts` | доли ступеней считаются от оборота, а не от чистой выручки | база доли — оборот | задача 1 меняет базу на чистую выручку — это и есть цель задачи | «доли и края ступеней — от чистой выручки» |
| 1 | `__tests__/metrics/waterfall.test.ts` | ступени водопада сходятся: вычитания висят от остатка и кончаются на итоге | цепочка от оборота через gross/discounts/refunds/net/… | этих ступеней нет, база другая | сходимость проверяется через `base_pct` cogs/ads/fixed в «доли и края ступеней — от чистой выручки» |
| 1 | `__tests__/metrics/waterfall.test.ts` | три итога водопада — от нуля; отрицательная прибыль идёт вниз, и шкала опускается под ноль | сценарий на ключах gross/net/profit, база — оборот | ключа gross нет, база доли — чистая выручка | тот же сценарий (fixed=400, profit=−100) на ключах net/margin_income/profit, числа −11,8 вместо −10,0 |
| 1 | `__tests__/metrics/waterfall.test.ts` | оборот ноль — доли, края и шкала пусты, а не нули | пустота долей при обороте = 0 | условие пустоты доли — чистая выручка не положительна, а не оборот | «чистая выручка не положительна — долей и краёв нет, суммы стоят», net='0.00' и '-10.00' |
| 1 | `__tests__/metrics/waterfall.test.ts` | самое большое вычитание помечено среди вычитаний, итоги не помечаются | признак `largest` в ответе запроса | строка «Съедает больше всего» и признак `largest` убраны решением владельца | проверки нет — функциональность удалена |
| 1 | `__tests__/metrics/waterfall.test.ts` | при равенстве двух вычитаний до цента помечены оба | признак `largest` при равенстве | то же | проверки нет |
| 1 | `__tests__/metrics/waterfall.test.ts` | все вычитания ноль — не помечено ничего | признак `largest` при нулевых вычитаниях | то же | проверки нет |
| 1 | `__tests__/metrics/waterfall.test.ts` | месяц без строк рекламы: у ступени «Реклама» пусты сумма и доля, «съедает больше всего» её не берёт | пустота суммы/доли рекламы и что признак `largest` её не выбирает | признак `largest` убран; появился маржинальный доход, который считается и без рекламы | «без строк рекламы ступень рекламы пуста, маржинальный доход считается с нулём» |
| 1 | `__tests__/metrics/waterfall.test.ts` | напечатанное расхождение равно фактической разнице между суммой показанных ступеней и показанным итогом | центы gross/discounts/refunds/net для net_gap брались из ответа запроса (`с.gross` и т. п.) — там раньше были ступени с этими ключами | gross/discounts/refunds больше не ступени, `с.gross` стал бы `undefined` | та же формула и то же ожидаемое значение net_gap; центы gross/discounts/refunds/net берутся прямо из подставленной строки итогов (`итоги.gross` и т. п.), profit_gap по-прежнему из ступеней |
| 1 | `__tests__/metrics/waterfall.test.ts` | отчёт несёт водопад из девяти ступеней, прочитанный тем же снимком | 9 ключей, сверка `revenue.gross` со ступенью `gross` | 7 ступеней, `gross` не ступень | «…из семи ступеней…», сверка `revenue.net` со ступенью `net` |
| 1 | `__tests__/screen/waterfall-view.test.tsx` | столбик ступени берёт ту же долю и тот же край, что напечатаны | текст доли одной ступени — `'37,5 % оборота'` | база доли — чистая выручка, а не оборот | тот же тест, ожидание — `'37,5 % чистой выручки'` |
| 1 | `__tests__/screen/waterfall-view.test.tsx` | у каждой ступени названа база доли — оборот | 9 раз «% оборота» | база доли — чистая выручка, ступеней 7 | «…чистая выручка» — 7 раз «% чистой выручки», плюс проверка отсутствия «% оборота» |
| 1 | `__tests__/screen/waterfall-view.test.tsx` | итог и вычитание различены признаком в разметке, а не только цветом | 3 итога / 6 вычитаний | ступеней стало 7 (было 9) | 3 итога / 4 вычитания |
| 1 | `__tests__/screen/waterfall-view.test.tsx` | оборот ноль — у ступеней слова, столбиков нет | 9 пустых долей, отсутствие слова «оборота» во всём блоке | ступеней 7; слово «оборот» легитимно появляется в новом пояснении про скидки и возвраты, которое показывается всегда | «чистая выручка не положительна — у ступеней слова, столбиков нет» — 7 пустых долей, проверка сужена до отсутствия «% чистой выручки» |
| 1 | `__tests__/screen/waterfall-view.test.tsx` | нет строк рекламы — у ступени «Реклама» слова и в сумме, и в доле, а столбика нет | то же плюс поле `largest: false` в объекте ступени | поля `largest` в типе больше нет | тот же тест без поля `largest` |
| 1 | `__tests__/screen/waterfall-view.test.tsx` | подписи ступеней водопада — отдельной ячейкой вне дорожки столбика | 9 дорожек | ступеней 7 | 7 дорожек |
| 1 | `__tests__/screen/waterfall-view.test.tsx` | строка «съедает больше всего» называет помеченную ступень и берёт её же долю | текст и поведение строки «Съедает больше всего» | строка убрана решением владельца | проверки нет |
| 1 | `__tests__/screen/waterfall-view.test.tsx` | при равенстве строка называет обе ступени поровну | то же | то же | проверки нет |
| 1 | `__tests__/screen/waterfall-view.test.tsx` | без доли строки «съедает больше всего» нет | то же | то же | проверки нет |
| 1 | `__tests__/screen/waterfall-view.test.tsx` | у строки «съедает больше всего» нет своего цвета — это подпись, а не тревога | свойство CSS-правила `.waterfall-largest` | правило и строка удалены | проверки нет |
| 1 | `__tests__/screen/waterfall-view.test.tsx` | строка о расхождении — только когда оно есть, и с тем числом, что дал отчёт (вторая часть) | отсутствие класса `waterfall-gap` при отсутствии расхождений | класс `waterfall-gap` теперь несёт ещё и постоянное пояснение про скидки и возвраты | (вторая часть) отсутствие текста «округлены до цента» |
| 1 | `__tests__/screen/cost-shares-view.test.tsx` (файл удалён целиком) | доли статей рисуются только тогда, когда в отчёте есть водопад | существование `cost-share` в карточке «Затраты» | доли статей затрат убраны решением владельца | функциональности и файла нет |
| 1 | `__tests__/screen/cost-shares-view.test.tsx` (файл удалён целиком) | у каждой статьи затрат доля названа от оборота | то же | то же | — |
| 1 | `__tests__/screen/cost-shares-view.test.tsx` (файл удалён целиком) | доля статьи — та же, что у её ступени водопада | то же | то же | — |
| 1 | `__tests__/screen/cost-shares-view.test.tsx` (файл удалён целиком) | полоска статьи берёт ту же долю, что напечатана | то же | то же | — |
| 1 | `__tests__/screen/cost-shares-view.test.tsx` (файл удалён целиком) | оборот ноль — у статей слова, полосок нет | то же | то же | — |
| 1 | `__tests__/screen/cost-shares-view.test.tsx` (файл удалён целиком) | месяц без выгрузки рекламы: сумма рекламы — слова, а не ноль | то же | то же | — |
| 1 | `__tests__/screen/census.test.tsx` | перепись экрана (блок «Главное», строки каскада) | подписи/суммы/доли 9 ступеней с базой «оборот» и строку «Съедает больше всего: постоянные расходы · 18,8 % оборота» | каскад — 7 ступеней от чистой выручки, строки «Съедает больше всего» нет, добавлена ступень «Маржинальный доход» и пояснение про скидки и возвраты | 7 строк каскада «% чистой выручки», без «Съедает больше всего», с новой строкой «Скидки 2 222,22 € и возвраты 3 333,33 € сняты с оборота 11 111,11 € до этой шкалы.» |
| 1 | `__tests__/screen/census.test.tsx` | перепись экрана (блок «Деньги», карточка «Затраты») | долю каждой статьи затрат («% оборота») под её суммой | доли статей затрат убраны решением владельца | только сумма статьи, без строки доли |
| 1 | `__tests__/metrics/deltas.test.ts` | доля рекламы полосы — та же, что у ступени рекламы в водопаде | инвариант «доля рекламы полосы (от оборота) = доля ступени рекламы (от оборота)» | база каскада — чистая выручка (договор куска, пункт 1); показатель доли рекламы полосы сам уходит с экрана в задаче 6 (договор, пункт 8) | ничего — показатель уходит в задаче 6, сравнивать больше нечего |
| 4 | `__tests__/metrics/items-order.test.ts` | порядок по умолчанию — прибыль по убыванию | `ПОРЯДОК_ТОВАРОВ_ПО_УМОЛЧАНИЮ === 'profit-desc'`, `itemsOn(...)` без порядка отдаёт строки по нему | решение владельца Э4 отменяет шесть порядков адреса (S12, Ф2): порядок один и вшит в запрос, умолчания больше нет | «порядок товаров один — валовая прибыль по убыванию, при равенстве по артикулу» — `MONTH_ITEMS` |
| 4 | `__tests__/metrics/items-order.test.ts` | шесть порядков дают шесть таблиц, и каждая упорядочена своим столбцом | шесть значений `ПОРЯДКИ_ТОВАРОВ` дают шесть разных раскладок строк | шести порядков больше нет — решение владельца Э4 | проверки нет — функциональность отменена |
| 4 | `__tests__/metrics/items-order.test.ts` | порядков ровно шесть, и другого текста в запрос не попадает | `Object.keys(ПОРЯДКИ_ТОВАРОВ)` длиной 6, каждый хвост — `order by ... (asc\|desc), sku` | списка `ПОРЯДКИ_ТОВАРОВ` больше нет: порядок один и вшит в `MONTH_ITEMS` | форма единственного порядка утверждается той же новой проверкой «порядок товаров один…» |
| 4 | `__tests__/metrics/items-order.test.ts` | при равных значениях столбца порядок решается артикулом, а не случаем | при равной прибыли двух строк порядок `'profit-desc'` (третий довод `itemsOn`) решает по артикулу | `itemsOn` больше не принимает порядок доводом: запрос всегда один | тот же сценарий и то же ожидание; тело проверки не менялось, изменился только вызов — `itemsOn` без последнего довода |
| 4 | `__tests__/screen/items-order.test.tsx` (файл удалён целиком) | разметка отдаёт строки в том порядке, в каком получила | цикл по шести значениям `ПОРЯДКИ_ТОВАРОВ`, каждый раз `sort={порядок}` не переставляет строки | доводов `sort` и `ПОРЯДКИ_ТОВАРОВ` в разметке больше нет — порядок один | — |
| 4 | `__tests__/screen/items-order.test.tsx` (файл удалён целиком) | заголовки трёх столбцов — ссылки, и каждая ведёт на свой порядок | `href` заголовков `net`/`cogs`/`profit` несёт `&sort=...` | `ЗаголовокПорядка` и `адресПорядка` удалены, заголовки — обычные `<th scope="col">` | — |
| 4 | `__tests__/screen/items-order.test.tsx` (файл удалён целиком) | нынешний столбец назван признаком, а не только видом | `aria-sort` стоит у столбца, по которому упорядочено | сортировки по адресу нет, признак `aria-sort` удалён вместе с `ЗаголовокПорядка` | — |
| 4 | `__tests__/screen/items-order.test.tsx` (файл удалён целиком) | адрес порядка по умолчанию хвоста не несёт: одно состояние — одно написание | `адресПорядка(месяц, порядок)` строит `/?...&sort=...` или без хвоста для умолчания | функция `адресПорядка` удалена вместе с адресом порядка | — |
| 4 | `__tests__/screen/items-order.test.tsx` (файл удалён целиком) | отказ на незнакомый порядок называет имя и перечисляет все шесть | `ОтказПорядка` печатает `role="alert"` с именем и шесть ссылок | `ОтказПорядка` удалён: незнакомого порядка больше не бывает, отказывать не на что | — |
| 4 | `__tests__/screen/sort-page.test.tsx` (файл удалён целиком) | порядок из адреса доезжает до слоя метрик | `sortParam` уходит третьим доводом в `monthlyReport` | `monthlyReport` лишилась третьего довода; страница не читает `params.sort` | — |
| 4 | `__tests__/screen/sort-page.test.tsx` (файл удалён целиком) | адрес без порядка — прибыль по убыванию | без `sort` в адресе `monthlyReport` зовётся с `'profit-desc'` | то же — третьего довода нет вовсе | — |
| 4 | `__tests__/screen/sort-page.test.tsx` (файл удалён целиком) | из двух значений порядка берётся первое, как у месяца и вкладки | `sort: ['net-asc', 'cogs-desc']` — берётся первое | `sortParam` в странице больше не читается | — |
| 4 | `__tests__/screen/sort-page.test.tsx` (файл удалён целиком) | незнакомый порядок — отказ словами, а не молчаливое умолчание | незнакомый `sort` — страница `ОтказПорядка`, `monthlyReport` не вызывается | отказа на порядок нет: чтение `sortParam` и проверка порядка удалены со страницы | — |
| 4 | `__tests__/screen/sort-page.test.tsx` (файл удалён целиком) | отказ называет все шесть порядков ссылками и сохраняет месяц | список из шести `href` на отказе плюс сохранённый месяц | то же — страницы отказа на порядок больше нет | — |
| 4 | `__tests__/screen/sort-page.test.tsx` (файл удалён целиком) | утверждение над таблицей говорит, что счёт идёт по прибыли, когда порядок другой | хвост «— считая по убыванию прибыли, а таблица сейчас упорядочена иначе» появляется при порядке, отличном от умолчания | порядок всегда по прибыли (решение владельца Э4): «первая пятёрка» при другом порядке смысла не имеет, хвост и сам условный довод `sort`/`поПрибыли` убраны | строка над таблицей текста не несёт условного хвоста — проверки нет |
| 6 | `__tests__/metrics/deltas.test.ts` | четыре показателя в названном порядке, значения — готовые колонки итогов | `Object.keys(п)` равен `['profit', 'margin', 'net', 'ad_share']` | доля рекламы уходит из полосы решением владельца (договор, пункт 8): показателей три, не четыре | «три показателя в названном порядке…» — `Object.keys(п)` равен `['profit', 'margin', 'net']` |
| 6 | `__tests__/metrics/deltas.test.ts` | доля рекламы — реклама ÷ оборот, дельта — в пунктах | значение, дельта и единица показателя `ad_share` | показателя `ad_share` в `values` `DELTAS_FROM_ROWS` больше нет | проверки нет — показатель отменён |
| 6 | `__tests__/metrics/deltas.test.ts` | месяц без строк рекламы: пусто и в полосе, и у ступени водопада — обе стороны разом | `п.ad_share.value` совпадает с долей ступени рекламы водопада, обе `null` | то же: `ad_share` из полосы убран целиком, сравнивать нечего (вторая половина инварианта, первая убрана в задаче 1) | проверки нет; вспомогательная функция `доляСтупениРекламы` и импорт `WATERFALL_FROM_TOTALS` в файле тоже убраны как ставшие мёртвым кодом |
| 6 | `__tests__/metrics/deltas.test.ts` | у доли рекламы рост — это плохо, у прибыли — хорошо | `good_when_up`/`verdict` показателя `ad_share` в контрасте с `profit` | показателя `ad_share` нет | проверки нет |
| 6 | `__tests__/metrics/deltas.test.ts` | нет предыдущего месяца с заказами — дельты пустые, а не нулевые, у всех четырёх разом | массивы дельт/вердиктов/значений длиной 4, включая `ad_share` | показателей три | «…у всех трёх разом» — те же массивы длиной 3, без `ad_share` |
| 6 | `__tests__/metrics/deltas.test.ts` | в прошлом месяце нет рекламы — дельта доли рекламы пустая, остальные считаются | `п.ad_share.delta`/`verdict` пусты, `profit.delta` считается | показателя `ad_share` нет | проверки нет |
| 6 | `__tests__/metrics/deltas.test.ts` | у текущего месяца нет ни одной строки рекламы — доля рекламы пустая, а не ноль | `ad_share.value`/`delta`/`verdict` пусты при отсутствии рекламы за месяц | показателя `ad_share` нет | проверки нет |
| 6 | `__tests__/metrics/deltas.test.ts` | оборот ноль — доля рекламы пустая | `ad_share.value`/`delta` пусты при нулевом обороте | показателя `ad_share` нет; ни один из оставшихся трёх показателей от оборота не зависит | проверки нет |
| 6 | `__tests__/metrics/deltas.test.ts` | база сравнения — предыдущий календарный месяц, даже когда он пуст | массив `[delta, has_base, prev_month]` по четырём показателям | показателей три | тот же сценарий, массив по трём показателям |
| 6 | `__tests__/metrics/deltas.test.ts` | в месяце нет строк рекламы — боевой запрос отдаёт долю рекламы пустой | `MONTH_DELTAS` отдаёт `ad_share` пустым, когда за месяц нет строк рекламы | показателя `ad_share` в `MONTH_DELTAS` нет | проверки нет |
| 6 | `__tests__/metrics/deltas.test.ts` | появился предыдущий месяц — дельты считаются; исчез — снова слова | `items.map(к => к.delta)` — массив из четырёх `null` до/после появления базы | показателей три | тот же сценарий, массив из трёх `null` |
| 6 | `__tests__/metrics/deltas.test.ts` | отчёт несёт полосу показателей: значения — те же, что в итогах и в водопаде | ожидаемый объект значений несёт четвёртое поле `ad_share`, сверенное с долей ступени рекламы водопада | показателя `ad_share` в `отчёт.kpis.items` нет | тот же тест без поля `ad_share` и без вспомогательной переменной `доляРекламы` |
| 6 | `__tests__/screen/kpis-view.test.tsx` | нет базы для сравнения — значения четырёх показателей стоят числами, пуста только дельта | четыре значения и `Array(4)` дельт | показателей три | «…значения трёх показателей…» — три значения и `Array(3)` дельт |
| 6 | `__tests__/screen/kpis-view.test.tsx` | месяца для сравнения нет вовсе — «нет базы для сравнения» без месяца | `Array(4).fill('нет базы для сравнения')` | показателей три | тот же тест, `Array(3)` |
| 6 | `__tests__/screen/kpis-view.test.tsx` | разметка печатает дельту и её смысл, которые ей дали, а не выводит смысл из знака | вторая строка полосы — выдуманная карточка с ключом `ad_share` | ключа `ad_share` в реальных данных больше нет | тот же тест и то же ожидание, вторая строка — с ключом `margin` (тот же случай «рост — плохо» демонстрирует любой `pp`-показатель) |
| 6 | `__tests__/screen/kpis-view.test.tsx` | подписи называют базу отношений, дельты маржи и доли рекламы — в процентных пунктах | подписи четырёх карточек, включая «Доля рекламы от оборота»; строк в процентных пунктах — 2 | показателей три, «доли рекламы» нет | «…дельта маржи — в процентных пунктах» — подписи трёх карточек, строк в процентных пунктах — 1 |
| 6 | `__tests__/screen/kpis-view.test.tsx` | база есть, а дельты нет — «нет данных» у этой карточки, остальные с числами | пустая дельта у карточки `ad_share`, индекс строки — 3 | ключа `ad_share` нет | тот же сценарий на карточке `net`, индекс строки — 2 (последний из трёх) |
| 6 | `__tests__/screen/census.test.tsx` | перепись экрана (блок «Главное», строки полосы показателей) | 4 карточки полосы, включая «Доля рекламы от оборота» / «25,6 %» / дельту | показателей три | 3 карточки полосы, без строк доли рекламы |
| 8 | `__tests__/screen/tabs-page.test.tsx` (файл удалён целиком) | вкладка из адреса определяет, какие разделы на экране | `?tab=` отбирает разделы: «Деньги» — четыре карточки, «Товары», «Качество» — два блока, «Главное» — три | отмена вкладок решением Э2: отчёт — один экран; `?tab=` не читается — это утверждает «старые адреса со вкладкой и порядком открывают тот же единственный экран» | — |
| 8 | `__tests__/screen/tabs-page.test.tsx` (файл удалён целиком) | адрес без вкладки — первая вкладка, и только она | без `tab` — разделы «Главного» и ссылка `tab=glavnoe` с `aria-current` | отмена вкладок решением Э2: отчёт — один экран; полосы вкладок нет | — |
| 8 | `__tests__/screen/tabs-page.test.tsx` (файл удалён целиком) | из двух значений вкладки берётся первое, как у месяца | `tab=[tovary, dengi]` — вкладка «Товары» | отмена вкладок решением Э2: отчёт — один экран; вкладка не читается вовсе | — |
| 8 | `__tests__/screen/tabs-page.test.tsx` (файл удалён целиком) | неизвестная вкладка — отказ словами, а не первая вкладка | `tab=boom` — `role="alert"` до похода в базу | отмена вкладок решением Э2: отчёт — один экран; отказа нет — старый адрес открывает единственный экран (пункт 18 договора) | — |
| 8 | `__tests__/screen/tabs-page.test.tsx` (файл удалён целиком) | отказ на незнакомую вкладку называет разделы ссылками, и первая — «Главное» | ссылки четырёх вкладок на отказе, месяц сохранён, кнопка выхода на месте | отмена вкладок решением Э2: отчёт — один экран; отказа и полосы вкладок нет | — |
| 8 | `__tests__/screen/tabs-page.test.tsx` (файл удалён целиком) | после «Обновить данные» действие не уводит со страницы | `refreshAction` не зовёт переход, возвращает `{ ok: true }`, перечитывает `/` | отмена вкладок решением Э2: отчёт — один экран; файл вкладок удалён, а проверка не про вкладки | перенесена в old-addresses.test.tsx, тело прежнее (`__tests__/screen/old-addresses.test.tsx`, с той же подставкой перехода и тем же `beforeEach`) |
| 8 | `__tests__/screen/tabs-view.test.tsx` (файл удалён целиком) | каждый раздел — ровно на одной вкладке, порядок вкладок — порядок отчёта | разделы четырёх вкладок не повторяются и идут в порядке отчёта | отмена вкладок решением Э2: отчёт — один экран | — |
| 8 | `__tests__/screen/tabs-view.test.tsx` (файл удалён целиком) | в полосе вкладок ровно четыре ссылки, в названном порядке | четыре ссылки «Главное», «Деньги», «Товары», «Качество данных» | отмена вкладок решением Э2: отчёт — один экран | — |
| 8 | `__tests__/screen/tabs-view.test.tsx` (файл удалён целиком) | ссылки вкладок сохраняют месяц | `href` вкладок несёт `m=` | отмена вкладок решением Э2: отчёт — один экран | — |
| 8 | `__tests__/screen/tabs-view.test.tsx` (файл удалён целиком) | ни одна ссылка вкладок не ведёт на корень | ни одного `href="/"` в полосе | отмена вкладок решением Э2: отчёт — один экран | — |
| 8 | `__tests__/screen/tabs-view.test.tsx` (файл удалён целиком) | у текущей вкладки признак текущей в разметке, у прочих его нет | `aria-current="page"` только у текущей | отмена вкладок решением Э2: отчёт — один экран | — |
| 8 | `__tests__/screen/tabs-view.test.tsx` (файл удалён целиком) | ссылки месяцев сохраняют вкладку | ссылка месяца несёт `&tab=` | отмена вкладок решением Э2: отчёт — один экран | — |
| 8 | `__tests__/screen/tabs-view.test.tsx` (файл удалён целиком) | полоса вкладок без клиентского кода | полоса — ссылки в `<nav>`, без ролей `tablist`/`tab` | отмена вкладок решением Э2: отчёт — один экран | — |
| 8 | `__tests__/screen/tabs-view.test.tsx` (файл удалён целиком) | у текущей вкладки полоса-указатель толщиной strokeWidthThicker | правило `.tabs a[aria-current='page']::after` в таблице стилей | отмена вкладок решением Э2: отчёт — один экран | — |
| 8 | `__tests__/screen/tabs-view.test.tsx` (файл удалён целиком) | у текущей вкладки насыщенность fontWeightSemibold | правило `.tabs a[aria-current='page']` в таблице стилей | отмена вкладок решением Э2: отчёт — один экран | — |
| 8 | `__tests__/screen/two-in-a-row.test.tsx` (файл удалён целиком) | «Выручка» и «Затраты» лежат в одном ряду, и в нём только они | обёртка `.row-two` содержит ровно две карточки | отмена вкладок решением Э2: отчёт — один экран; карточек «Выручка» и «Затраты» и ряда `.row-two` больше нет | — |
| 8 | `__tests__/screen/two-in-a-row.test.tsx` (файл удалён целиком) | ряд стоит после шапки и полосы вкладок, а не вровень с плавающей полосой действия | `.row-two` не первый элемент отчёта | отмена вкладок решением Э2: отчёт — один экран; карточек «Выручка» и «Затраты» и ряда `.row-two` больше нет | — |
| 8 | `__tests__/screen/two-in-a-row.test.tsx` (файл удалён целиком) | на чужой вкладке пустой обёртки в разметке не остаётся | на прочих вкладках `row-two` нет | отмена вкладок решением Э2: отчёт — один экран; карточек «Выручка» и «Затраты» и ряда `.row-two` больше нет | — |
| 8 | `__tests__/screen/payback-view.test.tsx` (файл удалён целиком) | окупаемость по обороту стоит в блоке всегда, а три числа шага 3 — только при своём поле | блок окупаемости: главное число всегда, три числа при `payback` | отмена вкладок решением Э2: отчёт — один экран; блока «Окупаемость рекламы» на экране нет | — |
| 8 | `__tests__/screen/payback-view.test.tsx` (файл удалён целиком) | подпись порога называет его базу — вклад, а не маржу экрана | подпись «Порог от вклада — наш счёт» | отмена вкладок решением Э2: отчёт — один экран; блока «Окупаемость рекламы» на экране нет | — |
| 8 | `__tests__/screen/payback-view.test.tsx` (файл удалён целиком) | вклад стоит числом рядом с порогом | «Вклад с евро оборота» числом в блоке | отмена вкладок решением Э2: отчёт — один экран; блока «Окупаемость рекламы» на экране нет | — |
| 8 | `__tests__/screen/payback-view.test.tsx` (файл удалён целиком) | рядом с порогом сказано, что определения наши | пояснение «Вклад и порог — наши определения» | отмена вкладок решением Э2: отчёт — один экран; блока «Окупаемость рекламы» на экране нет | — |
| 8 | `__tests__/screen/payback-view.test.tsx` (файл удалён целиком) | порога нет — сказано словами, а не «нет данных» и не отрицательным порогом | `breakevenNote` печатается словами «порога нет: …» | отмена вкладок решением Э2: отчёт — один экран; блока «Окупаемость рекламы» на экране нет | — |
| 8 | `__tests__/screen/payback-view.test.tsx` (файл удалён целиком) | у окупаемости по прибыли названа база — после постоянных расходов, и сказано, чем она не порог | пояснение про окупаемость по прибыли | отмена вкладок решением Э2: отчёт — один экран; блока «Окупаемость рекламы» на экране нет | — |
| 8 | `__tests__/screen/payback-labels.test.tsx` (файл удалён целиком) | ни одна подпись не длиннее предела | подписи блока окупаемости не длиннее предела | отмена вкладок решением Э2: отчёт — один экран; блока «Окупаемость рекламы» на экране нет | — |
| 8 | `__tests__/screen/payback-labels.test.tsx` (файл удалён целиком) | в ячейке значения лежит значение, а не предложение | `dd` блока окупаемости — число | отмена вкладок решением Э2: отчёт — один экран; блока «Окупаемость рекламы» на экране нет | — |
| 8 | `__tests__/screen/payback-labels.test.tsx` (файл удалён целиком) | пометка «наш счёт» стоит в подписях наших же величин, а не только в пояснении | «наш счёт» в подписях вклада и порога | отмена вкладок решением Э2: отчёт — один экран; блока «Окупаемость рекламы» на экране нет | — |
| 8 | `__tests__/screen/payback-labels.test.tsx` (файл удалён целиком) | подпись порога называет свою базу, а противопоставление ушло в пояснение | подпись порога и пояснение блока | отмена вкладок решением Э2: отчёт — один экран; блока «Окупаемость рекламы» на экране нет | — |
| 8 | `__tests__/screen/census.test.tsx` | перепись экрана: весь видимый текст, по порядку и без единой потери | четыре списка — по одному на вкладку, у каждого шапка и подписи полосы вкладок; «Деньги» — текст карточек «Выручка», «Затраты», «Итог» и блока окупаемости; заголовок «Честность данных» | отмена вкладок решением Э2: отчёт — один экран; карточки и окупаемость удалены; блок честности назван «Качество данных» (задача 8) | один список единственного экрана: шапка один раз, «Главное», «Товары», «Качество» подряд в порядке договора; без подписей вкладок и текста удалённых карточек; «Качество данных» вместо «Честность данных» |
| 8 | `__tests__/screen/census.test.tsx` | на экране все одиннадцать видов неполноты, каждый со своим числом | то же на `tab="kachestvo"` | отмена вкладок решением Э2: отчёт — один экран; довода `tab` у `Dashboard` нет | то же на единственном экране — снят только довод `tab` |
| 8 | `__tests__/metrics/screen.test.tsx` | экран печатает поля отчёта, а не свои числа | на склейке «Деньги»+«Товары»+«Качество»: поля выручки, затрат, прибыли, маржи (9,9 %), окупаемость «1,23 ×», товары, доля 80,3 %, штуки | отмена вкладок решением Э2: отчёт — один экран; поля выручки, затрат, итога и окупаемость печатали только удалённые карточки и блок окупаемости | на единственном экране: числа товаров, доля 80,3 %, штуки 101; утверждения о полях удалённых карточек сняты, довод `tab` снят |
| 8 | `__tests__/metrics/screen.test.tsx` | окупаемость печатается отношением, а не процентами | «1,23 ×» и не «1,23 %» на «Деньгах» | отмена вкладок решением Э2: отчёт — один экран; окупаемость по обороту печатал только удалённый блок окупаемости | — (удалена) |
| 8 | `__tests__/metrics/screen.test.tsx` | окупаемость подписана словом «по обороту» | подпись «Окупаемость рекламы (по обороту)» | отмена вкладок решением Э2: отчёт — один экран; блока окупаемости нет | — (удалена) |
| 8 | `__tests__/metrics/screen.test.tsx` | нет данных печатается словами, а не как 0 и не как NaN | пустая маржа `baseReport` — «нет данных» на «Деньгах» | отмена вкладок решением Э2: отчёт — один экран; единственное пустое поле раскладки печатала удалённая карточка «Итог»; у раскладки нет полосы показателей и каскада, где «нет данных» живёт теперь | — (удалена); «нет данных» словами на экране сторожат проверки полосы показателей и каскада |
| 8 | `__tests__/metrics/screen.test.tsx` | доля подписана словами «от чистой выручки» | то же на своей вкладке (`tab=`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` |
| 8 | `__tests__/metrics/screen.test.tsx` | блок неполноты виден, даже когда все нули | то же на своей вкладке (`tab=`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` |
| 8 | `__tests__/metrics/screen.test.tsx` | таблица товаров идёт в порядке отчёта | то же на своей вкладке (`tab=`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` |
| 8 | `__tests__/metrics/screen.test.tsx` | блок неполноты называет адрес у каждой ненулевой дыры | то же на своей вкладке (`tab=`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` |
| 8 | `__tests__/metrics/screen.test.tsx` | экран называет товары без цены поставщика | то же на своей вкладке (`tab=`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` |
| 8 | `__tests__/screen/honesty-bar.test.tsx` | полоса берёт ту же величину, что и напечатанное рядом число | то же на `tab="kachestvo"` (через общую отрисовку `сДолей`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/screen/honesty-bar.test.tsx` | доля вне пределов уходит в полосу как есть, а не подправленной разметкой | то же на `tab="kachestvo"` (через общую отрисовку `сДолей`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/screen/honesty-bar.test.tsx` | нет доли — нет полосы, а на экране слова | то же на `tab="kachestvo"` (через общую отрисовку `сДолей`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/screen/items-view.test.tsx` | новые колонки и строка над таблицей рисуются только при своих полях | то же на `tab="tovary"` (через общую отрисовку `сОтчётом`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/screen/items-view.test.tsx` | заголовок доли называет базу — прибыль товаров, а заголовок маржи — чистую выручку | то же на `tab="tovary"` (через общую отрисовку `сОтчётом`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/screen/items-view.test.tsx` | строка над таблицей называет базу долей и её сумму, и прибыль месяца рядом | то же на `tab="tovary"` (через общую отрисовку `сОтчётом`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/screen/items-view.test.tsx` | разметка печатает признак «в минусе», который ей дали, а не сравнивает прибыль с нулём | то же на `tab="tovary"` (через общую отрисовку `сОтчётом`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/screen/items-view.test.tsx` | у строки в минусе рядом стоит само число маржи — цвет не единственный признак | то же на `tab="tovary"` (через общую отрисовку `сОтчётом`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/screen/items-view.test.tsx` | сумма прибыли товаров не положительна — строка над таблицей говорит словами | то же на `tab="tovary"` (через общую отрисовку `сОтчётом`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/screen/items-scroll.test.tsx` | строка стоит в разметке таблицы товаров | то же на `tab="tovary"` | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` |
| 8 | `__tests__/screen/items-scroll.test.tsx` | без товаров и без новых колонок строки нет: сказать было бы нечего | то же на `tab="tovary"` | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` |
| 8 | `__tests__/screen/no-break-units.test.tsx` | денежное значение внутри предложения не бывает без защиты от переноса | обход разметки каждой раскладки на четырёх вкладках; в сообщении о находке — имя вкладки | отмена вкладок решением Э2: отчёт — один экран; вкладок нет | обход разметки каждой раскладки на единственном экране; цикл по вкладкам и имя вкладки в сообщении сняты, порог «больше 100 чисел со знаком» и список защит не тронуты |
| 8 | `__tests__/screen/substituted-cost.test.tsx` | пометка ведёт на вкладку, где лежит её размер, и несёт месяц отчёта | `href` пометки — `/?m=2026-03&amp;tab=kachestvo` | отмена вкладок решением Э2: отчёт — один экран; вкладки «Качество данных» нет, блок качества стоит на том же экране (решение контролёра по задаче 8) | переименована в «пометка ведёт на блок качества на том же экране, где лежит её размер»: `href` пометки — `#kachestvo`, и на том же экране ровно один элемент с `id="kachestvo"` |
| 8 | `__tests__/screen/substituted-cost.test.tsx` | строка с настоящей ценой не помечена ничем | то же на `tab="tovary"` (через общую отрисовку `разметка`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/screen/substituted-cost.test.tsx` | подставленная целиком и подставленная частью названы разными словами | то же на `tab="tovary"` (через общую отрисовку `разметка`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/screen/substituted-cost.test.tsx` | помечена ровно та строка, у которой подстановка, а не соседняя | то же на `tab="tovary"` (через общую отрисовку `разметка`) | отмена вкладок решением Э2: отчёт — один экран; довода `tab` нет | то же на единственном экране — снят только довод `tab` в общей отрисовке, тело проверки не тронуто |
| 8 | `__tests__/styles/narrow-gap.test.ts` | зазор на узком экране больше обычного | в узком запросе есть `margin-bottom` и `gap` на `--spacingVerticalXXXL` | отмена вкладок решением Э2: отчёт — один экран; `gap: var(--spacingVerticalXXXL)` было правилом удалённого ряда `.row-two` | в узком запросе есть `margin-bottom: var(--spacingVerticalXXXL)`; утверждение о `gap` снято, прочие не тронуты |
| 8 | `__tests__/metrics/screen-month.test.tsx` | месяц из адреса определяет, что видно на экране, а не месяц по умолчанию | то же на адресах `{ m: МЕСЯЦ_Б, tab: 'tovary' }` и `{ tab: 'tovary' }` | отмена вкладок решением Э2: отчёт — один экран; `tab` в адресе не читается, и тип `searchParams` его больше не знает (проверка типов) | то же на адресах `{ m: МЕСЯЦ_Б }` и `{}` — снят только `tab` |
| 8 | `__tests__/auth/guarded-paths.test.tsx` | страница отчёта с годной cookie печатает числа | то же на адресе `{ tab: 'tovary' }` | отмена вкладок решением Э2: отчёт — один экран; `tab` в адресе не читается, и тип `searchParams` его больше не знает (проверка типов) | то же на адресе `{}` — снят только `tab` |
| 9 | `__tests__/screen/kpis-view.test.tsx` | весь файл | файл `kpis-view.test.tsx` — вид полосы показателей | замена полосы показателей блоком «Результат месяца» (задача 9): прибыль — единственное крупное число, чистая выручка, маржа и окупаемость — мельче; без базы — одна строка на блок | файл переименован в `result-view.test.tsx` (`git mv`); три проверки токенов цвета дельты («хуже», «простой текст», «лучше») и их вспомогательная функция перенесены без правки |
| 9 | `__tests__/screen/result-view.test.tsx` | полоса рисуется только тогда, когда в отчёте есть её поле | без поля `kpis` нет `kpi-list`, с полем — есть | замена полосы показателей блоком «Результат месяца» (задача 9): прибыль — единственное крупное число, чистая выручка, маржа и окупаемость — мельче; без базы — одна строка на блок; блок результата стоит всегда, списка `kpi-list` нет | переименована в «дельты рисуются только тогда, когда в отчёте есть поле полосы»: без поля `kpis` нет `kpi-delta`, с полем — есть |
| 9 | `__tests__/screen/result-view.test.tsx` | нет базы для сравнения — значения трёх показателей стоят числами, пуста только дельта | три значения полосы в `kpi-value` и три строки «нет базы: в 2026-02 заказов нет» | замена полосы показателей блоком «Результат месяца» (задача 9): прибыль — единственное крупное число, чистая выручка, маржа и окупаемость — мельче; без базы — одна строка на блок; числа блока — поля отчёта, а не значения полосы; строки «нет базы» у числа нет | переименована в «нет базы для сравнения — три числа результата стоят числами, строк дельты у чисел нет»: прибыль в `result-profit`, чистая выручка, маржа и окупаемость в `dd`; строк `kpi-delta` нет |
| 9 | `__tests__/screen/result-view.test.tsx` | нет базы — слова, а не ноль | в полосе нет `0,0` в дельте и нет `data-verdict` | замена полосы показателей блоком «Результат месяца» (задача 9): прибыль — единственное крупное число, чистая выручка, маржа и окупаемость — мельче; без базы — одна строка на блок; `data-verdict` законно несёт слово окупаемости | в блоке нет `0,0` в дельте и нет обёртки `class="kpi"`; есть строка «Сравнить с 2026-02 нельзя: в 2026-02 заказов нет.» |
| 9 | `__tests__/screen/result-view.test.tsx` | месяца для сравнения нет вовсе — «нет базы для сравнения» без месяца | три строки «нет базы для сравнения» | замена полосы показателей блоком «Результат месяца» (задача 9): прибыль — единственное крупное число, чистая выручка, маржа и окупаемость — мельче; без базы — одна строка на блок | переименована в «месяца для сравнения нет вовсе — одна строка без месяца»: строк дельты нет, строка «Сравнить не с чем: прошлого месяца в данных нет.» — ровно одна |
| 9 | `__tests__/screen/result-view.test.tsx` | разметка печатает дельту и её смысл, которые ей дали, а не выводит смысл из знака | признаки — все `data-verdict` полосы | замена полосы показателей блоком «Результат месяца» (задача 9): прибыль — единственное крупное число, чистая выручка, маржа и окупаемость — мельче; без базы — одна строка на блок; `data-verdict` в блоке несёт и слово окупаемости | признаки — `data-verdict` у обёртки `class="kpi"`; ожидания те же |
| 9 | `__tests__/screen/result-view.test.tsx` | смысл дельты назван не только цветом: у каждой строки знак и слово | то же в разметке `section.block.kpis` (через общую функцию `блок`) | замена полосы показателей блоком «Результат месяца» (задача 9): прибыль — единственное крупное число, чистая выручка, маржа и окупаемость — мельче; без базы — одна строка на блок | то же в разметке `section.block.result` — изменён только якорь общей функции `блок`, тело проверки не тронуто |
| 9 | `__tests__/screen/result-view.test.tsx` | подписи называют базу отношений, дельта маржи — в процентных пунктах | подписи `kpi-label`: «Прибыль», «Маржа от чистой выручки», «Чистая выручка» | замена полосы показателей блоком «Результат месяца» (задача 9): прибыль — единственное крупное число, чистая выручка, маржа и окупаемость — мельче; без базы — одна строка на блок; подписей `kpi-label` нет | подписи — `h2` и `dt` блока: «Прибыль», «Чистая выручка», «Маржа от чистой выручки», «Окупаемость рекламы по обороту»; строк в процентных пунктах — 1, как было |
| 9 | `__tests__/screen/result-view.test.tsx` | база есть, а дельты нет — «нет данных» у этой карточки, остальные с числами | строка дельты с индексом 2 (карточка `net`) — «к 2026-02: нет данных» | замена полосы показателей блоком «Результат месяца» (задача 9): прибыль — единственное крупное число, чистая выручка, маржа и окупаемость — мельче; без базы — одна строка на блок; порядок чисел — прибыль, чистая выручка, маржа | переименована в «…у этого числа, остальные с числами»: все три строки дельты по порядку — прибыль числом, чистая выручка «к 2026-02: нет данных», маржа числом |
| 9 | `__tests__/screen/census.test.tsx` | перепись экрана: весь видимый текст, по порядку и без единой потери | после шапки — три карточки полосы: подпись, значение полосы, дельта | замена полосы показателей блоком «Результат месяца» (задача 9): прибыль — единственное крупное число, чистая выручка, маржа и окупаемость — мельче; без базы — одна строка на блок | после шапки — «Прибыль», прибыль отчёта, её дельта, «приблизительно», строка доли по настоящей цене, «что подставлено»; затем чистая выручка, маржа — с дельтами; окупаемость со словом признака и порогом. Прочие строки не тронуты |
| 9 | `__tests__/screen/no-break-units.test.tsx` | денежное значение внутри предложения не бывает без защиты от переноса | в списке защищённых элементов — `span.kpi-value` | замена полосы показателей блоком «Результат месяца» (задача 9): класс крупного числа `kpi-value` переведён в `result-profit` (решение контролёра 2), элемент — `p` вместо `span` | утверждение прежнее; в списке защищённых элементов переведено только имя элемента: `p.result-profit` (решение контролёра по задаче 9, вариант А) |
| 9 | `__tests__/screen/no-break-units.test.tsx` | элементам под защитой таблица стилей запрещает перенос | среди селекторов запрета переноса — `.kpi-value` | замена полосы показателей блоком «Результат месяца» (задача 9): класс крупного числа `kpi-value` переведён в `result-profit` (решение контролёра 2), элемент — `p` вместо `span` | утверждение прежнее; переведено только имя класса: `.result-profit` (решение контролёра по задаче 9, вариант А) |
| 10 | `__tests__/screen/census.test.tsx` | перепись экрана: весь видимый текст, по порядку и без единой потери | после блока результата сразу «Куда ушли деньги» | блок «Выводы» между результатом и каскадом (задача 10): до трёх строк по признакам отчёта | после строки окупаемости с порогом — «Выводы» и три строки раскладки с пояснениями: реклама окупается, постоянные расходы без знака, прибыль приблизительная; одна строка комментария. Прочие строки не тронуты |
| 10 | `__tests__/screen/substituted-cost.test.tsx` | строка с настоящей ценой не помечена ничем | во всём видимом тексте экрана нет строк со словом «подставлена» | блок «Выводы» (задача 10) стоит выше таблицы товаров и каскада, и его тексты содержат искомые слова: «подставлена» в пояснении приблизительной прибыли, «Реклама» в строке о рекламе | обвязка: поиск внутри своего раздела (текст снимается с блока товаров, без блока — отказ); утверждение прежнее, доказано пробным сломом `substituted-markup-always` (S12): `expected [ …(2) ] to deeply equal []` |
| 10 | `__tests__/screen/substituted-cost.test.tsx` | подставленная целиком и подставленная частью названы разными словами | строки со словом «подставлена» — ровно две пометки, «подставлена» и «подставлена частью» | блок «Выводы» (задача 10) стоит выше таблицы товаров и каскада, и его тексты содержат искомые слова: «подставлена» в пояснении приблизительной прибыли, «Реклама» в строке о рекламе | обвязка: поиск внутри своего раздела (текст снимается с блока товаров, без блока — отказ); утверждение прежнее, доказано пробным сломом `substituted-markup-one-word` (S12): `expected [ …(2) ] to deeply equal [ …(2) ]` |
| 10 | `__tests__/screen/substituted-cost.test.tsx` | помечена ровно та строка, у которой подстановка, а не соседняя | первая строка со словом «подставлена» стоит после второго артикула | блок «Выводы» (задача 10) стоит выше таблицы товаров и каскада, и его тексты содержат искомые слова: «подставлена» в пояснении приблизительной прибыли, «Реклама» в строке о рекламе | обвязка: поиск внутри своего раздела (текст снимается с блока товаров, без блока — отказ); утверждение прежнее, доказано пробным сломом «пометка по признаку соседней строки» (однострочная замена условия пометки, в списки сломов не внесена): `expected 14 to be greater than 18` |
| 10 | `__tests__/screen/waterfall-view.test.tsx` | нет строк рекламы — у ступени «Реклама» слова и в сумме, и в доле, а столбика нет | первый элемент списка со словом «Реклама» во всей разметке — ступень со словами вместо суммы и доли | блок «Выводы» (задача 10) стоит выше таблицы товаров и каскада, и его тексты содержат искомые слова: «подставлена» в пояснении приблизительной прибыли, «Реклама» в строке о рекламе | обвязка: поиск внутри своего раздела (строки режутся внутри блока водопада, без блока — отказ); утверждение прежнее, доказано пробным сломом `waterfall-ads-zero-on-screen` (S11): `expected 'data-kind="вычитание"><span class="wa…' to contain '<span class="waterfall-amount">нет да…'` |
| 11 | `__tests__/screen/census.test.tsx` | перепись экрана: весь видимый текст, по порядку и без единой потери | в ряду по дням после подписей оси сразу видимые подписи дней «1», «6», за ними «Товары» | пунктир средней на ряде по дням (задача 11): подпись средней называет сумму и базу; при пустых днях — строка о них | после подписей оси — «средняя 271,83 € по 31 дню месяца»; после подписей дней — строка о пустых днях раскладки; одна строка комментария. Прочие строки не тронуты |
| 12 | `__tests__/screen/items-view.test.tsx` | новые колонки и строка над таблицей рисуются только при своих полях | без итогов нет заголовка «Доля в прибыли товаров»; с итогами есть `<th>Доля в прибыли товаров</th>` | задача 12 меняет видимые слова блока товаров: колонка «Валовая прибыль», заголовок доли «Доля в валовой прибыли товаров», строка над таблицей «80 % валовой прибыли товаров дают …», пометка подстановки короткая «⚠ подставлена» / «⚠ подставлена частью» | то же с новым заголовком «Доля в валовой прибыли товаров» (изменены только две строки ожидания); доказано пробными сломами «заголовок доли без базы» (`expected '<section class="block items"><h2>Това…' to contain '<th>Доля в валовой прибыли товаров</t…'`) и «заголовок доли без условия итогов» (`… not to contain 'Доля в валовой прибыли товаров'`), в списки сломов не внесены |
| 12 | `__tests__/screen/items-view.test.tsx` | заголовок доли называет базу — прибыль товаров, а заголовок маржи — чистую выручку | `<th>Доля в прибыли товаров</th>` и `<th>Маржа от чистой выручки</th>` | задача 12 меняет видимые слова блока товаров: колонка «Валовая прибыль», заголовок доли «Доля в валовой прибыли товаров», строка над таблицей «80 % валовой прибыли товаров дают …», пометка подстановки короткая «⚠ подставлена» / «⚠ подставлена частью» | `<th>Доля в валовой прибыли товаров</th>` и прежний заголовок маржи (изменена только строка ожидания); доказано пробным сломом «заголовок доли без базы»: `expected '<section class="block items"><h2>Това…' to contain '<th>Доля в валовой прибыли товаров</t…'` |
| 12 | `__tests__/screen/items-view.test.tsx` | строка над таблицей называет базу долей и её сумму, и прибыль месяца рядом | «80 % прибыли товаров дают 2 из 2 артикулов» и прежние части строки | задача 12 меняет видимые слова блока товаров: колонка «Валовая прибыль», заголовок доли «Доля в валовой прибыли товаров», строка над таблицей «80 % валовой прибыли товаров дают …», пометка подстановки короткая «⚠ подставлена» / «⚠ подставлена частью» | «80 % валовой прибыли товаров дают 2 из 2 артикулов», прочие ожидания прежние; доказано пробным сломом «строка без слова „валовой“»: `expected '<section class="block items"><h2>Това…' to contain '80 % валовой прибыли товаров дают 2 и…'` |
| 12 | `__tests__/screen/substituted-cost.test.tsx` | подставленная целиком и подставленная частью названы разными словами | строки со словом «подставлена» — «подставлена — см. «Качество данных»» и «подставлена частью — см. «Качество данных»» | задача 12 меняет видимые слова блока товаров: колонка «Валовая прибыль», заголовок доли «Доля в валовой прибыли товаров», строка над таблицей «80 % валовой прибыли товаров дают …», пометка подстановки короткая «⚠ подставлена» / «⚠ подставлена частью» | те же две строки в коротком виде: «⚠ подставлена», «⚠ подставлена частью»; обвязка задачи 10 не тронута; доказано сломом `partial-called-full`: `expected [ '⚠ подставлена', '⚠ подставлена' ] to deeply equal [ '⚠ подставлена', …(1) ]` |
| 12 | `__tests__/screen/census.test.tsx` | перепись экрана: весь видимый текст, по порядку и без единой потери | строка над таблицей «80 % прибыли товаров …», заголовки «Прибыль» и «Доля в прибыли товаров», пометки «… — см. «Качество данных»» | задача 12 меняет видимые слова блока товаров: колонка «Валовая прибыль», заголовок доли «Доля в валовой прибыли товаров», строка над таблицей «80 % валовой прибыли товаров дают …», пометка подстановки короткая «⚠ подставлена» / «⚠ подставлена частью» | те же позиции с новыми словами (5 строк ожидания), одна строка комментария; доказано сломами `gross-profit-called-profit`, `partial-called-full`, `toggle-always`: `expected [ …(99) ] to deeply equal [ …(99) ]` |
| 12 | `__tests__/screen/items-view.test.tsx` | строка над таблицей называет базу долей и её сумму, и прибыль месяца рядом | «Прибыль товаров — выручка минус себестоимость, 9 090,90 €» | решение контролёра к задаче 12: второе упоминание в строке над таблицей тоже называется валовой прибылью — «Валовая прибыль товаров — выручка минус себестоимость, …» в обеих ветках строки | «Валовая прибыль товаров — выручка минус себестоимость, 9 090,90 €» (изменена только эта строка ожидания); доказано пробным сломом «второе упоминание без слова „валовая“»: `expected '<section class="block items"><h2>Това…' to contain 'Валовая прибыль товаров — выручка мин…'` |
| 12 | `__tests__/screen/census.test.tsx` | перепись экрана: весь видимый текст, по порядку и без единой потери | строка над таблицей «… в минусе — 0. Прибыль товаров — выручка минус себестоимость, …» | решение контролёра к задаче 12: второе упоминание в строке над таблицей тоже называется валовой прибылью — «Валовая прибыль товаров — выручка минус себестоимость, …» в обеих ветках строки | «… в минусе — 0. Валовая прибыль товаров — выручка минус себестоимость, …» (одна строка ожидания) и уточнённая строка комментария; доказано тем же пробным сломом: `expected [ …(99) ] to deeply equal [ …(99) ]` |
| 13 | `__tests__/screen/census.test.tsx` | перепись экрана: весь видимый текст, по порядку и без единой потери | после «Неполнота данных» и пояснения — одиннадцать строк видов один раз | задача 13 сливает блоки «Качество данных» и «Неполнота данных» в один: на виду — виды с признаком «есть дыры», все одиннадцать — под раскрытием «Показать все виды неполноты» | те же строки видов на виду (в раскладке у всех признак), затем «Показать все виды неполноты» и те же одиннадцать строк ещё раз (добавлены 12 строк ожидания и строка комментария); доказано пробными сломами «список под раскрытием урезан до трёх» (`expected [ …(103) ] to deeply equal [ …(111) ]`) и «подпись раскрытия без слов „виды неполноты“» (`expected [ …(111) ] to deeply equal [ …(111) ]`), в списки не внесены |
| 13 | `__tests__/screen/census.test.tsx` | на экране все одиннадцать видов неполноты, каждый со своим числом | каждая строка «вид: число» есть где-либо на странице | задача 13 сливает блоки «Качество данных» и «Неполнота данных» в один: на виду — виды с признаком «есть дыры», все одиннадцать — под раскрытием «Показать все виды неполноты»; виды на виду зависят от признака, полный список — только под раскрытием | та же строка «вид: число» внутри `<details class="gaps-more">`; нет раскрытия — отказ (изменена только обвязка поиска, утверждение прежнее); доказано пробным сломом «список под раскрытием урезан до трёх» при нетронутом списке на виду: `вид «возвраты, не попавшие в счёт» пропал с экрана: expected '<details class="gaps-more"><summary>П…' to contain 'возвраты, не попавшие в счёт: 4'`, в списки не внесён |
| 15 | `__tests__/live/march-screen.live.ts` | три итога водопада — числа владельца, мимо нашего кода | ступень водопада с ключом `gross` равна обороту 18 764,00 €, наравне со ступенями `net`/`profit` | каскад — семь ступеней от чистой выручки (задача 1); `gross` больше не ступень водопада | оборот берётся из `отчёт.revenue.gross`, а не из ступеней; добавлена проверка порядка семи ключей ступеней `net,cogs,ads,fees,margin_income,fixed,profit` |
| 15 | `__tests__/live/march-screen.live.ts` | полоса показателей несёт четыре показателя, включая долю рекламы | `items.map(к => [к.key, к.value, к.delta])` равен `[['profit','1738.53',null],['margin','10.1',null],['net','17277.04',null],['ad_share','23.6',null]]` | показатель доли рекламы отменён решением владельца (задача 6): показателей три, не четыре | тот же список из трёх показателей, без `ad_share` |
| 15 | `__tests__/live/march-screen.live.ts` | доля ступени рекламы в водопаде равна доле рекламы полосы показателей | `отчёт.waterfall.steps.find(ads).sharePct` равна `'23.6'`, той же, что доля рекламы полосы | показатель доли рекламы полосы отменён (задача 6) — сравнивать больше не с чем; сама база доли ступени сменилась с оборота на чистую выручку (задача 1), число другое | проверки нет — сравнивать нечего; вместо неё — новые утверждения о признаках выводов, средней по дням и первой пятёрке товаров |
| 16 | `__tests__/screens/generator.test.ts` | генератор собирает полный набор и сверяет все двадцать страниц → **новое имя:** генератор собирает полный набор и сверяет все четырнадцать страниц | 20 страниц, 30 картинок | вкладки отменены (задача 8): состояний «Деньги», «Товары», «Качество данных» и отказа на незнакомую вкладку нет, добавлен раскрытый вид | 14 страниц, 20 картинок; число в имени сменено вместе с числом страниц — сломы прежних кусков, цитирующие прежнее имя, переводит задача 14 |
| 16 | `__tests__/screens/generator.test.ts` | генератор отказывает, когда разметка разошлась с эталоном | порча эталона заменой `'18\u00A0764,00 €'` и та же строка в тексте отказа | на новой странице оборот печатается с неразрывным пробелом и перед знаком евро: прежняя строка на ней не находится (счёт командой — 0), замена не портила бы эталон | изменена только обвязка: строка `'18\u00A0764,00\u00A0€'` (на новой странице — 1 раз); утверждения прежние — вид «разметка разошлась» и оборот в тексте отказа; слом тела сторожа разметки красит проверку |
| 16 | `docs/screens/run.tsx` | снимки собраны: двадцать страниц и тридцать картинок → **новое имя:** снимки собраны: четырнадцать страниц и двадцать картинок | 20 страниц, 30 картинок | то же, что у проверки полного набора выше | 14 страниц, 20 картинок |
| 14 | `__tests__/metrics/screen.test.tsx` | доля подписана словами «от чистой выручки» | слова «от чистой выручки» есть где-либо в разметке экрана | блок результата (задача 9) всегда печатает «Маржа от чистой выручки», и поиск по всему экрану зеленеет без подписи доли: слом `honest-label-removed` (S5) не мог покраснеть — решение контролёра к задаче 14 | обвязка: поиск внутри блока качества (`id="kachestvo"`), без блока — отказ; утверждение прежнее, строка `expect(` не тронута; доказано пробным сломом `honest-label-removed` |
| 17 | `__tests__/screen/no-break-units.test.tsx` | денежное значение внутри предложения не бывает без защиты от переноса | сообщение первого утверждения — «чисел со знаком в разметке всех вкладок» | вкладок нет с задачи 8: сообщение называло то, чего нет (итоговая проверка, И7) | утверждения прежние — `toBeGreaterThan(100)` и пустой список открытых; изменено только сообщение: «чисел со знаком в разметке всего экрана» |
| 17 | `__tests__/screen/items-top.test.tsx` (дописана рядом; удалённая — строка задачи 4 выше) | разметка отдаёт строки в том порядке, в каком получила | — | утверждение было снято в задаче 4 вместе с файлом `items-order.test.tsx` без замены, хотя оставалось верным (итоговая проверка, И1) | **утверждение вернулось:** восемь строк вразброс, признак пятёрки не у первых пяти по индексу; последовательность артикулов в первой таблице и под раскрытием равна полученной. Слом — `items-markup-sorts-by-sku` (сортировка разметкой по артикулу); заодно — у `top-ignores-flag`, `top-slice-by-index` и S9 `items-order-reversed` |
| 17 | `__tests__/metrics/waterfall.test.ts` (дописана рядом; прежняя — строка задачи 1 «оборот ноль — доли, края и шкала пусты, а не нули») | чистая выручка не положительна — пределов шкалы нет | — | при переписи в задаче 1 утверждение о пустых пределах шкалы (`scale_low_pct`, `scale_high_pct`) не перенесено, хотя осталось верным (итоговая проверка, М6) | **утверждение вернулось** при новом условии пустоты: чистая выручка 0,00 и −10,00 — оба предела пусты у всех ступеней. Слом — `waterfall-scale-without-base` |

Строк учёта по задаче 1 (счёт `grep -c` по имени файла в этой таблице): `waterfall.test.ts` — 13,
`waterfall-view.test.tsx` — 11, `cost-shares-view.test.tsx` — 6, `census.test.tsx` — 2,
`deltas.test.ts` — 1; всего — 33.

Строк учёта по задаче 4 (счёт `grep -F '| 4 |' … | grep -F -c <путь>` — строки задачи 4, по
пути файла целиком, а не голому имени: `items-order.test.ts` и `items-order.test.tsx` иначе
считались бы одной строкой, а сама эта сводка — лишним совпадением):
`__tests__/metrics/items-order.test.ts` — 4, `__tests__/screen/items-order.test.tsx` — 5,
`__tests__/screen/sort-page.test.tsx` — 6; всего — 15.

Строк учёта по задаче 6 (счёт `grep -c '^| 6 |' …` — якорь на начало строки, а не `grep -F`:
без якоря счёт находит сам себя в тексте этого абзаца; по пути файла целиком — `grep '^| 6 |' … |
grep -F -c <путь>`): `__tests__/metrics/deltas.test.ts` — 12, `__tests__/screen/kpis-view.test.tsx`
— 5, `__tests__/screen/census.test.tsx` — 1; всего — 18.

Строк учёта по задаче 8 (счёт `grep -c '^| 8 |' …`; по пути файла целиком — `grep '^| 8 |' … |
grep -F -c '`<путь>`'`): `__tests__/screen/tabs-page.test.tsx` — 6, `__tests__/screen/tabs-view.test.tsx`
— 9, `__tests__/screen/two-in-a-row.test.tsx` — 3, `__tests__/screen/payback-view.test.tsx` — 6,
`__tests__/screen/payback-labels.test.tsx` — 4, `__tests__/screen/census.test.tsx` — 2,
`__tests__/metrics/screen.test.tsx` — 9, `__tests__/screen/honesty-bar.test.tsx` — 3,
`__tests__/screen/items-view.test.tsx` — 6, `__tests__/screen/items-scroll.test.tsx` — 2,
`__tests__/screen/no-break-units.test.tsx` — 1, `__tests__/screen/substituted-cost.test.tsx` — 4,
`__tests__/styles/narrow-gap.test.ts` — 1, `__tests__/metrics/screen-month.test.tsx` — 1,
`__tests__/auth/guarded-paths.test.tsx` — 1; всего — 58. Из них 12 — довод `tab` снят только в общей
отрисовке файла (`grep '^| 8 |' … | grep -F -c 'в общей отрисовке'`), 1 — проверка перенесена в
`__tests__/screen/old-addresses.test.tsx` с прежним телом.

Строк учёта по задаче 9 (счёт `grep -c '^| 9 |' …`; по пути файла целиком — `grep '^| 9 |' … |
grep -F -c '`<путь>`'`): `__tests__/screen/kpis-view.test.tsx` — 1 (сам файл переименован),
`__tests__/screen/result-view.test.tsx` — 8, `__tests__/screen/census.test.tsx` — 1,
`__tests__/screen/no-break-units.test.tsx` — 2; всего — 12. Из них 1 — изменён только якорь общей функции
`блок`, тело проверки не тронуто; 2 — переведено только имя класса защищённого элемента.

Строк учёта по задаче 10 (счёт `grep -c '^| 10 |' …`; по пути файла целиком — `grep '^| 10 |' … |
grep -F -c '`<путь>`'`): `__tests__/screen/census.test.tsx` — 1,
`__tests__/screen/substituted-cost.test.tsx` — 3, `__tests__/screen/waterfall-view.test.tsx` — 1; всего — 5. Из них 4 —
изменена только обвязка поиска, утверждения прежние.

Строк учёта по задаче 11 (счёт `grep -c '^| 11 |' …`): `__tests__/screen/census.test.tsx` — 1; всего — 1.
`__tests__/screen/daily-view.test.tsx` — только дописаны три проверки и ввоз `money`, удалённых строк в диффе нет; строки учёта не требует.

Строк учёта по задаче 12 (счёт `grep -c '^| 12 |' …`): `__tests__/screen/items-view.test.tsx` — 4,
`__tests__/screen/substituted-cost.test.tsx` — 1, `__tests__/screen/census.test.tsx` — 2; всего — 7. Во всех изменены
только строки ожиданий под новые видимые слова; по 1 строке у items-view и census — правка по решению контролёра
(второе упоминание валовой прибыли). `__tests__/screen/items-scroll.test.tsx` не тронут.

Строк учёта по задаче 13 (счёт `grep -c '^| 13 |' …`): `__tests__/screen/census.test.tsx` — 2; всего — 2. Из них 1 —
изменена только обвязка поиска (виды ищутся внутри раскрытия неполноты), утверждение прежнее.

Строк учёта по задаче 15 (счёт `grep -c '^| 15 |' …`): `__tests__/live/march-screen.live.ts` — 3; всего — 3.

Строк учёта по задаче 16 (счёт `grep -c '^| 16 |' …`): `__tests__/screens/generator.test.ts` — 2, `docs/screens/run.tsx` — 1;
всего — 3. Из них 1 — изменена только обвязка (строка порчи эталона). `__tests__/screens/guards.test.ts` — только дописана
проверка отказа раскрытия и ввоз, удалённых строк в диффе нет; строки учёта не требует.

Строк учёта по задаче 16b (счёт `grep -c '^| 16b |' …`): 0. Принятые проверки не тронуты:
`git diff --name-only --diff-filter=MD 712a5e6..HEAD -- __tests__` → пустой вывод. Добавлен один новый файл:
`git diff --name-only --diff-filter=A 712a5e6..HEAD -- __tests__` → `__tests__/styles/one-screen-layout.test.ts`.
В `breaks/s13-one-screen.ts` только дописаны сломы: `git diff -U0 712a5e6..HEAD -- breaks/s13-one-screen.ts | grep -c '^-[^-]'` → `0`,
`… | grep -c '^+    id: '` → `6`.
Правка задачи 16b по решению контролёра: в собственном файле задачи `__tests__/styles/one-screen-layout.test.ts` (принят не был)
переписана проверка узкой таблицы (`min-width` → `table-layout: auto` и запрет переноса у `td`) и утверждение о фоне подписи
средней; слом `items-table-shrinks` заменён на `items-table-fixed-narrow`, дописан `mean-label-no-background`. Принятые проверки
по-прежнему не тронуты.

Строк учёта по задаче 14 (счёт `grep -c '^| 14 |' …`): `__tests__/metrics/screen.test.tsx` — 1; всего — 1. Изменена только
обвязка поиска: `git diff -U0 -- __tests__/metrics/screen.test.tsx | grep -c '^[-+].*expect('` → `0`.

Строк учёта по задаче 17 (правка по итоговой проверке; счёт `grep -c '^| 17 |' …`): 3 — `__tests__/screen/no-break-units.test.tsx`
(изменено только сообщение утверждения), `__tests__/screen/items-top.test.tsx` и `__tests__/metrics/waterfall.test.ts` (утверждения
вернулись дописанными проверками). Прочие проверки задачи 17 только дописаны: `__tests__/metrics/findings.test.ts` (две),
`__tests__/metrics/command.test.ts` (одна); удалённых строк в их диффе нет, кроме заголовочного комментария `findings.test.ts`.
Правки только комментариев в
файлах проверок строк учёта не получают: `__tests__/screen/old-addresses.test.tsx` (комментарий внутри проверки «после
«Обновить данные» действие не уводит со страницы»), `__tests__/screen/substituted-cost.test.tsx` (комментарий файла),
`__tests__/metrics/deltas.test.ts` (комментарий обвязки раскладки).
