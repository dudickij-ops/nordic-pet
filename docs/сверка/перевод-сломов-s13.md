# Перевод сломов прошлых кусков — кусок S13, задача 14

Сломы прошлых кусков цитируют код дословно (`find`) и называют проверку, которая обязана покраснеть
(`mustRedden`, `alsoRedden`). Кусок S13 переписал запросы и собрал отчёт в один экран, и часть цитат и
имён разошлась с кодом. Здесь — каждая такая строка: переведена, выведена из списка или оставлена без
правки, и почему.

**Как отобраны строки.** Проблемой считается слом, у которого на голове `aa1af2f`: (а) `find` встречается
в своём файле не ровно один раз — так, как считает прибор сломов (`readFileSync(file).split(find).length - 1`);
(б) `mustRedden` подходит не ровно к одной проверке набора; (в) имя из `alsoRedden` не подходит ни к одной;
(г) `find` встречается один раз, но окрестность совпадения отличается от слияния S12 (`b94066b`) — такие
просмотрены глазами. Имена проверок сняты из отчёта `vitest run --reporter=json` на той же голове.
Прибор сломов не запускался: все списки гоняются один раз на голове слияния (задача 17).

**Графы.** «Перевод» — механизм жив, новая цитата отрицает то же утверждение. «Перевод: только имя
проверки заодно» — цитата верна, переименована объявленная заодно проверка. «Без правки» — находка вида
(г) оказалась той же цитатой на том же механизме. «Вывод из списка» — механизм снят договором куска.
Утверждение (`claim`) ни у одного оставшегося слома не менялось — это сверено командой при сборке таблицы.
Цитаты длиннее 420 знаков приведены началом и концом с пометкой; целиком они в диффе коммита.

## Часть A: `s5-metrics`, `s9-screen`, `s12-reading` и одна строка `s10-fluent`

| Список | id | Графа | Старая цитата | Новая цитата | Утверждение | Ожидание | Довод |
|---|---|---|---|---|---|---|---|
| s5-metrics | `missing-refund-narrow` | Без правки: цитата на прежнем механизме | `missing_refunds as (\n  select distinct mr.order_id, mr.sku\n    from month_refunds mr\n    left join lines l on l.order_id = mr.order_id and l.sku = mr.sku\n   where l.order_id is null\n)` | та же | считать не попавшим в счёт только возврат, чьей пары нет в заказах | прежнее — своё: «возврат к паре, выпавшей из счёта из-за пустой суммы, тоже назван» | рядом с цитатой в запросе неполноты добавлен признак `has_holes` (задача 5); сама цитата — тот же отбор возвратов без пары, что в b94066b |
| s5-metrics | `roas-label-removed` | Перевод | `<dt>Окупаемость рекламы (по обороту)</dt>` | `<dt>Окупаемость рекламы по обороту</dt>` | убрать подпись «по обороту» | было своё: «окупаемость подписана словом «по обороту»»; стало своё: «подписи называют базу отношений, дельта маржи — в процентных пунктах» | блок окупаемости снят (Э2, задача 8), подпись с базой «по обороту» стоит в блоке результата (договор, п. 11). Проверка «окупаемость подписана словом «по обороту»» удалена (учёт задачи 8); подпись дословно ждёт проверка, переписанная в задаче 9 (учёт: «подписи называют базу отношений…»). Набор проверок — её файл. Прежняя своя проверка удалена под учёт задачи 8, не переименована; то же утверждение — подпись окупаемости называет базу «по обороту» — сейчас утверждает «подписи называют базу отношений, дельта маржи — в процентных пунктах» (`__tests__/screen/result-view.test.tsx`), решение контролёра к задаче 14 |
| s5-metrics | `honest-label-removed` | Без правки: цитата на прежнем механизме | `Посчитано по настоящей цене поставщика (доля от чистой выручки):{' '}` | та же | убрать подпись «от чистой выручки» | прежнее — своё: «доля подписана словами «от чистой выручки»» | цитата стоит в блоке «Качество данных» — то же предложение, что на вкладке «Честность данных» в b94066b. Находка: проверка искала «от чистой выручки» во всей разметке, а блок результата (задача 9) печатает «Маржа от чистой выручки» всегда — слом не мог покраснеть. По решению контролёра в задаче 14 изменена обвязка проверки: поиск внутри блока качества, строка `expect(` прежняя (учёт задачи 14); доказано пробным прогоном этого слома |
| s5-metrics | `gap-discount-blind` | Без правки: цитата на прежнем механизме | `    select 1 as ord, 'скидки' as kind,\n           (select count(*) from month_orders where discount is null)::int as count,\n           array(select row_no::text from month_orders where discount is null order by row_no) as at` | та же | подставить ноль вместо пустой скидки без называния | прежнее — своё: «счётчик «скидки» срабатывает и называет адрес»; заодно: «каждая найденная дыра называет адрес» | в окрестности добавлен признак `has_holes` (задача 5); ветка «скидки» запроса неполноты та же |
| s5-metrics | `gaps-only-when-nonzero` | Перевод | `        <ul>\n          {report.gaps.map((gap) => (` | `          <ul>\n            {report.gaps.map((gap) => (` | печатать блок неполноты, только когда дыры есть | прежнее — своё: «блок неполноты виден, даже когда все нули» | блок неполноты слит с качеством (задача 13): виды с дырами на виду, полный список под раскрытием; условие ставится на полный список — единственный, где печатаются нулевые виды |
| s5-metrics | `gaps-no-address` | Перевод | `` {gap.at.length > 0 ? ` (${gap.at.join(', ')})` : ''} `` | ``                   {gap.kind}: {count(String(gap.count))}\n                  {gap.at.length > 0 ? ` (${gap.at.join(', ')})` : ''} ``; andThen ``                 {gap.kind}: {count(String(gap.count))}\n                {gap.at.length > 0 ? ` (${gap.at.join(', ')})` : ''} `` | убрать адреса из блока неполноты | прежнее — своё: «блок неполноты называет адрес у каждой ненулевой дыры» | адреса печатаются в двух списках блока (задача 13), прежняя цитата встречалась 2 раза; адреса убираются из обоих — второй правкой (`andThen`), иначе блок называл бы их по-прежнему |
| s5-metrics | `screen-computes-profit` | Перевод | `          <dt>Прибыль</dt>\n          <dd>{money(report.bottom.profit)}</dd>` | `      <td>{money(item.profit)}</td>` | посчитать что-нибудь в разметке страницы | прежнее — своё: «экран печатает поля отчёта, а не свои числа» | карточки «Итог» нет (Э2); проверка после задачи 8 сверяет числа таблицы товаров (учёт задачи 8). Счёт в разметке ставится на прибыль строки товара — её проверка ждёт |
| s5-metrics | `screen-diverges-from-report` | Перевод | `          <dt>Скидки</dt>\n          <dd>{money(report.revenue.discounts)}</dd>` | `      <td>{money(item.net)}</td>` | разойтись числами экрана и отчёта | прежнее — своё: «экран печатает поля отчёта, а не свои числа» | карточки «Выручка» нет (Э2); проверка сверяет числа таблицы товаров (учёт задачи 8). Чужое поле — в ячейке выручки строки товара |
| s5-metrics | `button-not-on-screen` | Перевод | `  return (\n    <>\n      <LogoutButton />\n      {/*\n        Пометка` … `} tab={вкладка} sort={порядок} />\n      </RefreshPanel>\n    </>\n  )` (многострочная, 20 строк; целиком — в диффе) | `  return (\n    <>\n      <LogoutButton />\n      {/*\n        Пометка` … `    <Dashboard report={report} />\n      </RefreshPanel>\n    </>\n  )` (многострочная, 20 строк; целиком — в диффе) | не подключить кнопку к экрану | прежнее — своё: «экран содержит кнопку и оборачивает ею отчёт» | у разметки отчёта нет доводов вкладки и порядка (Э2, Э4, задачи 4 и 8); отличие цитаты только в строке `<Dashboard …>` |
| s5-metrics | `default-month-any` | Без правки: цитата на прежнем механизме | `const resolvedMonth = month ?? months.find((m) => m.hasOrders)?.month ?? null` | та же | взять месяц по умолчанию не последним с заказами | прежнее — своё: «месяц по умолчанию — последний, за который есть заказы» | рядом запрос товаров без довода порядка (задача 4); выбор месяца по умолчанию тот же |
| s5-metrics | `items-sort-by-sku` | Перевод | `  'profit-desc': 'order by sum(net) - sum(cogs) desc, sku',` | `} order by sum(net) - sum(cogs) desc, sku` | отсортировать таблицу товаров не по прибыли | прежнее — своё: «товары отсортированы по прибыли убыванием, штуки — за вычетом возвращённых» | списка шести порядков нет — порядок один и вшит в запрос товаров (Э4, задача 4) |
| s5-metrics | `month-param-ignored` | Перевод | `report = await monthlyReport(monthParam, {}, порядок)` | `report = await monthlyReport(monthParam)` | не передавать месяц из адреса дальше отчёта | прежнее — своё: «месяц из адреса определяет, что видно на экране, а не месяц по умолчанию»; заодно: «кривой месяц в адресе показывает наш текст, а не падает» | у вызова отчёта нет третьего довода — порядка (Э4, задача 4) |
| s5-metrics | `nav-links-drop-month` | Перевод | `` href={`/?m=${m.month}${хвостВкладки}`} `` | `` href={`/?m=${m.month}`} `` | увести ссылки переключателя месяца на / вместо своего месяца | прежнее — своё: «ссылки переключателя месяца ведут на /?m=<месяц>, а не на /» | у ссылки месяца нет хвоста вкладки (Э2, задача 8) |
| s5-metrics | `bad-month-crashes-page` | Перевод | `  let report: MonthReport\n  try {\n    report = await monthlyReport(m` … `.message}</p>\n        </main>\n      )\n    }\n\n    throw error\n  }` (многострочная, 48 строк; целиком — в диффе) | `  let report: MonthReport\n  try {\n    report = await monthlyReport(m` … `.message}</p>\n        </main>\n      )\n    }\n\n    throw error\n  }` (многострочная, 48 строк; целиком — в диффе) | убрать перехват отказа кривого месяца на экране | прежнее — своё: «кривой месяц в адресе показывает наш текст, а не падает» | у вызова отчёта нет третьего довода — порядка (Э4, задача 4); прочий текст перехвата тот же |
| s9-screen | `census-blind-to-swap` | Перевод | `          <dt>Скидки</dt>\n          <dd>{money(report.revenue.discounts)}</dd>\n          <dt>Возвраты</dt>\n          <dd>{money(report.revenue.refunds)}</dd>` | `Скидки ${вместе(money(report.revenue.discounts))} и возвраты ${вместе(money(report.revenue.refunds))}` | переставить местами два денежных значения на экране | было своё: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «под каскадом — скидки и возвраты, снятые с оборота, тремя суммами отчёта» | карточки «Выручка» нет (Э2, задача 8); те же два значения — скидки и возвраты — стоят в строке под каскадом (задача 1). Заодно объявлена проверка этой строки, заведённая куском S13; проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); в причине заодно число страниц — 14 полного набора против 2 режима пар, 12 вне пар (`docs/screens/run.tsx`, `__tests__/screens/generator.test.ts`) |
| s9-screen | `labels-swapped` | Перевод | `          <dt>Скидки</dt>\n          <dd>{money(report.revenue.discounts)}</dd>\n          <dt>Возвраты</dt>\n          <dd>{money(report.revenue.refunds)}</dd>` | `Скидки ${вместе(money(report.revenue.discounts))} и возвраты ${вместе(money(report.revenue.refunds))}` | переставить местами две подписи, оставив значения на местах | было своё: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «под каскадом — скидки и возвраты, снятые с оборота, тремя суммами отчёта» | карточки «Выручка» нет (Э2); подписи «Скидки» и «возвраты» с суммами стоят в строке под каскадом (задача 1). Заодно объявлена проверка этой строки (кусок S13); проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); в причине заодно число страниц — 14 полного набора против 2 режима пар, 12 вне пар (`docs/screens/run.tsx`, `__tests__/screens/generator.test.ts`) |
| s9-screen | `items-order-reversed` | Перевод | `            {report.items.map((item) => (` | `        {строки.map((item) => (` | перевернуть порядок товаров в разметке | было своё: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «таблица товаров идёт в порядке отчёта»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «разметка отдаёт строки в том порядке, в каком получила»; заодно: «подставленная целиком и подставленная частью названы разными словами»; заодно: «помечена ровно та строка, у которой подстановка, а не соседняя»; стало своё: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «таблица товаров идёт в порядке отчёта»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «подставленная целиком и подставленная частью названы разными словами»; заодно: «помечена ровно та строка, у которой подстановка, а не соседняя» | строки печатает общая таблица товаров (задача 12). Из заодно снята «разметка отдаёт строки в том порядке, в каком получила» — удалена в задаче 4 (учёт); проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); в причине заодно число страниц — 14 полного набора против 2 режима пар, 12 вне пар (`docs/screens/run.tsx`, `__tests__/screens/generator.test.ts`) |
| s9-screen | `gaps-truncated` | Перевод: только имя проверки заодно | `          {report.gaps.map((gap) => (` | та же | печатать не все виды неполноты, а первые три | было своё: «на экране все одиннадцать видов неполноты, каждый со своим числом»; заодно: «перепись экрана: весь видимый текст»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «на экране все одиннадцать видов неполноты, каждый со своим числом»; заодно: «перепись экрана: весь видимый текст»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | цитата попадает на полный список видов под раскрытием (задача 13) — на него же после задачи 13 смотрит своя проверка (учёт: «на экране все одиннадцать видов…» ищет внутри раскрытия); проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); в причине заодно число страниц — 14 полного набора против 2 режима пар, 12 вне пар (`docs/screens/run.tsx`, `__tests__/screens/generator.test.ts`) |
| s9-screen | `share-bar-fixed-width` | Перевод: только имя проверки заодно | `` <div className="share-fill" style={{ '--share': `${доля}%` } as CSSProperties} /> `` | та же | задать полосе доли постоянную ширину | было своё: «полоса берёт ту же величину, что и напечатанное рядом число»; заодно: «доля вне пределов уходит в полосу как есть, а не подправленной разметкой»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «полоса берёт ту же величину, что и напечатанное рядом число»; заодно: «доля вне пределов уходит в полосу как есть, а не подправленной разметкой»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); в причине заодно число страниц — 14 полного набора против 2 режима пар, 12 вне пар (`docs/screens/run.tsx`, `__tests__/screens/generator.test.ts`) |
| s9-screen | `share-bar-other-value` | Перевод: только имя проверки заодно | `` <div className="share-fill" style={{ '--share': `${доля}%` } as CSSProperties} /> `` | та же | взять ширину полосы не из доли, а из соседнего числа отчёта | было своё: «полоса берёт ту же величину, что и напечатанное рядом число»; заодно: «доля вне пределов уходит в полосу как есть, а не подправленной разметкой»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «полоса берёт ту же величину, что и напечатанное рядом число»; заодно: «доля вне пределов уходит в полосу как есть, а не подправленной разметкой»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); в причине заодно число страниц — 14 полного набора против 2 режима пар, 12 вне пар (`docs/screens/run.tsx`, `__tests__/screens/generator.test.ts`) |
| s9-screen | `share-bar-without-value` | Без правки: цитата на прежнем механизме | `        {доля !== null && (` | та же | рисовать полосу и тогда, когда доли нет | прежнее — своё: «нет доли — нет полосы, а на экране слова» | цитата стоит в блоке «Качество данных» у той же полосы доли, что на вкладке «Честность данных» в b94066b |
| s9-screen | `stale-attribute-dropped` | Перевод: только имя проверки заодно | `      <div data-stale={stale ? 'true' : 'false'} className={stale ? 'numbers numbers-stale' : 'numbers'}>` | та же | убрать признак устаревания с обёртки чисел, оставив один класс | было своё: «отказ со щелью: и текст, и изменённый вид чисел»; заодно: «при удаче вид не помечен устаревшим и отказа не печатает»; заодно: «отказ без щели: текст есть, вид чисел прежний»; заодно: «экран содержит кнопку и оборачивает ею отчёт»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «отказ со щелью: и текст, и изменённый вид чисел»; заодно: «при удаче вид не помечен устаревшим и отказа не печатает»; заодно: «отказ без щели: текст есть, вид чисел прежний»; заодно: «экран содержит кнопку и оборачивает ею отчёт»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); в причине заодно число страниц — 14 полного набора против 2 режима пар, 12 вне пар (`docs/screens/run.tsx`, `__tests__/screens/generator.test.ts`) |
| s9-screen | `stale-class-dropped` | Перевод: только имя проверки заодно | `className={stale ? 'numbers numbers-stale' : 'numbers'}` | та же | не менять класс обёртки, когда числа устарели | было своё: «устаревшие числа получают свой класс, свежие — не получают»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; стало своё: «устаревшие числа получают свой класс, свежие — не получают»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц») |
| s12-reading | `items-default-by-revenue` | Перевод | `export const ПОРЯДОК_ТОВАРОВ_ПО_УМОЛЧАНИЮ: ПорядокТоваров = 'profit-desc'` | `} order by sum(net) - sum(cogs) desc, sku` | вернуть порядок по умолчанию к выручке, как было до куска | было своё: «порядок по умолчанию — прибыль по убыванию»; заодно: «нынешний столбец назван признаком, а не только видом»; заодно: «заголовки трёх столбцов — ссылки, и каждая ведёт на свой порядок»; заодно: «адрес порядка по умолчанию хвоста не несёт: одно состояние — одно написание»; заодно: «отказ на незнакомый порядок называет имя и перечисляет все шесть»; заодно: «адрес без порядка — прибыль по убыванию»; заодно: «отказ называет все шесть порядков ссылками и сохраняет месяц»; заодно: «товары отсортированы по прибыли убыванием, штуки — за вычетом возвращённых»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; стало своё: «товары отсортированы по прибыли убыванием, штуки — за вычетом возвращённых»; заодно: «порядок товаров один — валовая прибыль по убыванию, при равенстве по артикулу»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц» | умолчания среди шести порядков нет: порядок один и вшит в запрос (Э4, задача 4). Проверка умолчания удалена под учёт задачи 4; своей стала проверка поведения «товары отсортированы по прибыли убыванием, штуки — за вычетом возвращённых» (`__tests__/metrics/report.test.ts`), проверка текста запроса «порядок товаров один — …» — заодно (решение проверки задачи 14). Из заодно сняты шесть проверок порядков в адресе — удалены в задаче 4. Дубль: `find` и `replace` совпадают со сломом `items-order-by-net` списка S13 — оставлен по решению контролёра к задаче 14 |
| s12-reading | `items-direction-ignored` | Вывод из списка | `  'profit-asc': 'order by sum(net) - sum(cogs) asc, sku',` | — | не читать направление: любой порядок отдавать по убыванию | было своё: «шесть порядков дают шесть таблиц, и каждая упорядочена своим столбцом» | шесть порядков в адресе отменены (Э4, задача 4; договор: «Два прежних решения, которые кусок отменяет»); проверка «шесть порядков дают шесть таблиц…» удалена (учёт задачи 4). Единственный порядок сторожат «порядок товаров один — …» и сломы `items-order-by-net` (S13), `items-default-by-revenue`, `items-second-key-off` |
| s12-reading | `items-column-ignored` | Вывод из списка | `  'cogs-desc': 'order by sum(cogs) desc, sku',` | — | не читать столбец: себестоимость упорядочивать по выручке | было своё: «шесть порядков дают шесть таблиц, и каждая упорядочена своим столбцом» | то же: столбцов порядка в адресе нет (Э4, задача 4); проверка удалена (учёт задачи 4); замена — те же проверка и сломы единственного порядка |
| s12-reading | `items-second-key-off` | Перевод | `  'profit-desc': 'order by sum(net) - sum(cogs) desc, sku',` | `} order by sum(net) - sum(cogs) desc, sku` | снять второй ключ порядка — артикул | было своё: «порядков ровно шесть, и другого текста в запрос не попадает»; заодно: «порядков ровно шесть, и другого текста в запрос не попадает»; стало своё: «порядок товаров один — валовая прибыль по убыванию, при равенстве по артикулу»; заодно: «порядок товаров один — валовая прибыль по убыванию, при равенстве по артикулу» | второй ключ — артикул — остался в единственном порядке; проверку «порядков ровно шесть…» переписали в «порядок товаров один — …, при равенстве по артикулу», которая утверждает форму хвоста (учёт задачи 4) |
| s12-reading | `items-unknown-order-silent` | Вывод из списка | `  if (порядок === undefined) {\n` | — | молча подставлять умолчание вместо отказа на незнакомый порядок | было своё: «незнакомый порядок — отказ словами, а не молчаливое умолчание»; заодно: «отказ называет все шесть порядков ссылками и сохраняет месяц» | отказа на незнакомый порядок нет (Э4, договор п. 18: старые адреса открывают единственный экран); проверки отказа удалены (учёт задачи 4). Замена сторожится проверкой «старые адреса со вкладкой и порядком открывают тот же единственный экран» и сломом `tabs-come-back` (S13) |
| s12-reading | `items-markup-sorts` | Перевод | `            {report.items.map((item) => (` | `        {строки.map((item) => (` | дать разметке сортировать строки самой | было своё: «разметка отдаёт строки в том порядке, в каком получила»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «parseFloat и Number(...) не встречаются в тексте слоя метрик и экрана»; заодно: «подставленная целиком и подставленная частью названы разными словами»; заодно: «помечена ровно та строка, у которой подстановка, а не соседняя»; стало своё: «таблица товаров идёт в порядке отчёта»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «parseFloat и Number(...) не встречаются в тексте слоя метрик и экрана»; заодно: «подставленная целиком и подставленная частью названы разными словами»; заодно: «помечена ровно та строка, у которой подстановка, а не соседняя»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | разметка по-прежнему не переставляет строки, но проверка «разметка отдаёт строки…» удалена (учёт задачи 4). При одном порядке прежняя порча — сортировка по прибыли — неотличима от честной печати, поэтому порча сортирует по штукам (то же отрицание утверждения), прежняя своя проверка удалена под учёт задачи 4, не переименована; то же утверждение — разметка печатает строки в порядке отчёта — сейчас утверждает принятая «таблица товаров идёт в порядке отчёта» (S5, `__tests__/metrics/screen.test.tsx`), решение контролёра к задаче 14. Заодно объявлены проверки генератора снимков: штуки раскладки снимков идут не по возрастанию, и порча меняет разметку страниц (проверка задачи 14) |
| s12-reading | `substituted-marks-everyone` | Без правки: цитата на прежнем механизме | `  if (всего === 0 \|\| подставлено === 0) return undefined` | та же | помечать подставленной каждую строку товара | прежнее — своё: «слово выводится из двух чисел, а не из ровной маржи»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «слово выводится из двух чисел, а не из ровной маржи»; заодно: «три состояния различаются на настоящих строках продаж» | рядом у `monthlyReport` снят третий довод (задача 4); функция слова подстановки та же |
| s12-reading | `substituted-part-as-whole` | Без правки: цитата на прежнем механизме | `  return подставлено === всего ? 'вся' : 'часть'` | та же | выдавать частичную подстановку за полную | прежнее — своё: «три состояния различаются на настоящих строках продаж»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «слово выводится из двух чисел, а не из ровной маржи»; заодно: «три состояния различаются на настоящих строках продаж» | то же |
| s12-reading | `substituted-counts-skus` | Без правки: цитата на прежнем механизме | `       count(*) filter (where price is null)::int    as rows_substituted,\n       count(*)::int                                 as rows_counted` | та же | считать подстановку по артикулам, а не по строкам продаж | прежнее — своё: «частичная подстановка не выдаётся за полную: у строки считаются продажи, а не артикулы»; заодно: «три состояния различаются на настоящих строках продаж» | ниже цитаты вместо шести порядков стоит единственный запрос товаров (задача 4); счёт строк подстановки тот же |
| s12-reading | `substituted-no-address` | Перевод | ``                       href={месяц === null ? '/?tab=kachestvo' : `/?m=${месяц}&tab=kachestvo`} `` | `<a className="substituted" href="#kachestvo">` | не говорить в пометке, где искать её размер | было своё: «пометка ведёт на вкладку, где лежит её размер, и несёт месяц отчёта»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «пометка ведёт на блок качества на том же экране, где лежит её размер»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | вкладок нет (Э2, задача 8), пометка ведёт на блок качества того же экрана; проверка переименована в «пометка ведёт на блок качества на том же экране, где лежит её размер» (учёт задачи 8); проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц») |
| s12-reading | `substituted-markup-always` | Перевод | `                  {item.подстановка !== undefined && (` | `        {item.подстановка !== undefined && (` | рисовать пометку в каждой строке, не спрашивая слова | было своё: «строка с настоящей ценой не помечена ничем»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «помечена ровно та строка, у которой подстановка, а не соседняя»; заодно: «пометка ведёт на вкладку, где лежит её размер, и несёт месяц отчёта»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «строка с настоящей ценой не помечена ничем»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «помечена ровно та строка, у которой подстановка, а не соседняя»; заодно: «пометка ведёт на блок качества на том же экране, где лежит её размер»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | строка товара вынесена в функцию (задача 12), сменился отступ; заодно-проверка адреса пометки переименована (учёт задачи 8); проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц») |
| s12-reading | `substituted-markup-one-word` | Перевод | `                      {item.подстановка === 'вся' ? 'подставлена' : 'подставлена частью'} — см.` | `{item.подстановка === 'вся' ? '⚠ подставлена' : '⚠ подставлена частью'}` | печатать в разметке одно слово на оба состояния подстановки | было своё: «подставленная целиком и подставленная частью названы разными словами»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «подставленная целиком и подставленная частью названы разными словами»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | пометка короткая: «⚠ подставлена» / «⚠ подставлена частью» (задача 12, учёт задачи 12); проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц») |
| s12-reading | `items-summary-silent-on-other-order` | Вывод из списка | `артикулов${поПрибыли ? '' : ' — считая по убыванию прибыли, а таблица сейчас упорядочена иначе'}` | — | молчать о том, что счёт идёт по прибыли, когда таблица упорядочена иначе | было своё: «утверждение над таблицей говорит, что счёт идёт по прибыли, когда порядок другой» | хвоста «таблица сейчас упорядочена иначе» нет: порядок всегда по прибыли (Э4, задача 4); проверка удалена (учёт задачи 4). Замены нет — сказать нечего |
| s12-reading | `payback-label-explains-again` | Вывод из списка | `<dt>Порог от вклада — наш счёт</dt>` | — | вернуть подписи порога работу пояснения | было своё: «ни одна подпись не длиннее предела»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «подпись порога называет свою базу, а противопоставление ушло в пояснение»; заодно: «пометка «наш счёт» стоит в подписях наших же величин, а не только в пояснении»; заодно: «подпись порога называет его базу — вклад, а не маржу экрана»; заодно: «вклад стоит числом рядом с порогом»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | блока окупаемости нет (Э2, задача 8; договор, «Что уходит с экрана вместе с вкладками»: окупаемость по прибыли и вклад отдельными строками); проверки подписей блока удалены (учёт задачи 8). Порог и вклад живут в выводах: пометку «наш счёт» сторожат «вклад, порог и маржинальный доход помечены «наш счёт»» и слом `findings-drop-our-mark` (S13) |
| s12-reading | `payback-sentence-back-in-cell` | Вывод из списка | `            <dd>{ratio(report.payback.roasByProfit)}</dd>` | — | вернуть пояснение в ячейку значения | было своё: «в ячейке значения лежит значение, а не предложение»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «у окупаемости по прибыли названа база — после постоянных расходов, и сказано, чем она не порог»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | ячейки окупаемости по прибыли нет на экране (договор, «Что уходит»: окупаемость по прибыли остаётся в слое метрик); проверки удалены (учёт задачи 8). Замены на экране нет |
| s12-reading | `payback-ours-off-the-label` | Вывод из списка | `<dt>Вклад с евро оборота — наш счёт</dt>` | — | снять пометку «наш счёт» с подписей, оставив её только в пояснении | было своё: «пометка «наш счёт» стоит в подписях наших же величин, а не только в пояснении»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «вклад стоит числом рядом с порогом»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | подписи вклада нет — вклад переехал в пояснение вывода (договор, «Что уходит»); проверки удалены (учёт задачи 8). Пометку «наш счёт» у вклада сторожат «вклад, порог и маржинальный доход помечены «наш счёт»» и слом `findings-drop-our-mark` (S13) |
| s12-reading | `row-two-removed` | Вывод из списка | `  return вкладка === текущая ? <div className="row-two">{children}</div> : null` | — | снять ряд: вернуть карточки в общий поток по одной | было своё: ««Выручка» и «Затраты» лежат в одном ряду, и в нём только они»; заодно: «ряд стоит после шапки и полосы вкладок, а не вровень с плавающей полосой действия»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | ряда из двух карточек нет (договор, «Что уходит»: ряд S12, Ф4 — «карточек больше нет»; задача 8); проверки ряда удалены (учёт задачи 8). Отсутствие карточек сторожат «карточек «Выручка», «Затраты», «Итог» и блока окупаемости нет» и слом `money-cards-come-back` (S13) |
| s12-reading | `row-two-at-the-top` | Вывод из списка | `    <main className="report">\n      <header className="report-head">` | — | поставить ряд первым элементом отчёта — вровень с плавающей полосой действия | было своё: «ряд стоит после шапки и полосы вкладок, а не вровень с плавающей полосой действия»; заодно: «на чужой вкладке пустой обёртки в разметке не остаётся»; заодно: ««Выручка» и «Затраты» лежат в одном ряду, и в нём только они»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | то же; цитата (начало разметки отчёта) на HEAD встречается один раз, но ряда, о котором утверждение, нет — прежняя находка вида (d) |
| s12-reading | `narrow-gap-back` | Перевод | `    margin-bottom: var(--spacingVerticalXXXL);\n  }\n\n  .row-two {\n    gap: var(--spacingVerticalXXXL);` | `    margin-bottom: var(--spacingVerticalXXXL);\n  }` | вернуть зазор на узком экране к обычному | прежнее — своё: «зазор на узком экране больше обычного»; заодно: «зазор взят из выписанной шкалы, а не своим числом» | правило `.row-two` удалено вместе с рядом (Э2, задача 8); проверка больше не ждёт в нём зазор (учёт задачи 8), зазор блоков на узком экране остался; прежняя цитата захватывала оба правила |
| s12-reading | `narrow-gap-own-number` | Перевод | `  .row-two {\n    gap: var(--spacingVerticalXXXL);` | `    margin-bottom: var(--spacingVerticalXXXL);\n  }` | задать зазор своим числом вместо величины шкалы | прежнее — своё: «зазор взят из выписанной шкалы, а не своим числом»; заодно: «зазор на узком экране больше обычного»; заодно: «в таблице стилей нет ни одной величины и ни одного цвета вне набора Fluent 2» | правила `.row-two` нет; своё число ставится в оставшийся зазор блоков на узком экране, который та же проверка читает |
| s12-reading | `items-scroll-hint-off` | Перевод: только имя проверки заодно | `          Таблица прокручивается вбок: за правым краем есть ещё колонки.\n` | та же | убрать строку о скрытых колонках | было своё: «строка стоит в разметке таблицы товаров»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «строка стоит в разметке таблицы товаров»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | в окрестности строки таблица товаров вынесена в общую функцию (задача 12); строка та же; проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц») |
| s12-reading | `items-scroll-hint-always` | Без правки: цитата на прежнем механизме | `.items-scroll {\n  display: none;\n}` | та же | показывать строку о скрытых колонках и на широком экране, где колонки видны все | прежнее — своё: «по умолчанию строки не видно: на широком экране колонки видны все» | в узком запросе после цитаты добавлены правила задачи 16b; правило «по умолчанию строки не видно» то же |
| s12-reading | `items-scroll-hint-unconditional` | Без правки: цитата на прежнем механизме | `        {report.items.length > 0 && report.itemsSummary !== undefined && (` | та же | говорить о скрытых колонках и там, где их нет: без товаров и без новых колонок | прежнее — своё: «без товаров и без новых колонок строки нет: сказать было бы нечего» | рядом строка над таблицей и пятёрка (задачи 12); условие строки о прокрутке то же |
| s12-reading | `narrow-bez-bazy-snapshot-off` | Перевод | `  { файл: '14-bez-bazy-uzkiy.png', состояние: 'bez-bazy', тёмная: false, ширина: 500, высота: 1400, кадр: true },\n  { файл: '14-bez-bazy-uzkiy-tyomnaya.png', состояние: 'bez-bazy', тёмная: true, ширина: 500, высота: 1400, кадр: true },\n` | `  { файл: '14-bez-bazy-uzkiy.png', состояние: 'bez-bazy', тёмная: false, ширина: 500, высота: 2600, кадр: true },\n  { файл: '14-bez-bazy-uzkiy-tyomnaya.png', состояние: 'bez-bazy', тёмная: true, ширина: 500, высота: 2600, кадр: true },\n` | убрать из описи узкий снимок пути «нет базы для сравнения» | было своё: «генератор собирает полный набор и сверяет все двадцать страниц»; стало своё: «генератор собирает полный набор и сверяет все четырнадцать страниц» | высота узкого снимка «нет базы» — 2600 после пересборки (задача 16); проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц») |
| s10-fluent | `third-values-block` | Перевод: только имя проверки заодно | `/* ——— Основа ——— */` | та же | завести в таблице стилей третий блок величин | было своё: «в таблице стилей ровно два блока величин и один блок тёмной темы»; заодно: «в таблице стилей нет ни одной величины и ни одного цвета вне набора Fluent 2»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда эталона нет»; заодно: «генератор отказывает, когда разметка разошлась с эталоном»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «в таблице стилей ровно два блока величин и один блок тёмной темы»; заодно: «в таблице стилей нет ни одной величины и ни одного цвета вне набора Fluent 2»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда эталона нет»; заодно: «генератор отказывает, когда разметка разошлась с эталоном»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); найдено проверкой списка вне части A, переведено по решению контролёра к задаче 14 |

Строк по графам (вывод сборки таблицы): «Перевод» — 22, «Перевод: только имя проверки заодно» — 7,
«Без правки: цитата на прежнем механизме» — 10, «Вывод из списка» — 9; всего — 48. Сломов в списках
до и после: `s5-metrics` 87 → 87, `s9-screen` 9 → 9, `s12-reading` 25 → 16, `s10-fluent` 16 → 16.

### Слом `honest-label-removed` (S5): сторож не мог покраснеть — починен

Цитата на месте и попадает на то же предложение, что в S12. Но своя проверка «доля подписана словами
«от чистой выручки»» искала эти слова во всей разметке, а блок результата (задача 9) всегда печатает
`<dt>Маржа от чистой выручки</dt>`. По решению контролёра изменена обвязка проверки: текст снимается с
блока качества (`id="kachestvo"`), без блока — отказ; строка `expect(` прежняя. Строка учёта — в
`docs/сверка/учёт-проверок-s13.md` (задача 14). Вердикт пробного прогона одного этого слома — ниже.

Пробный прогон на голове `fb4937c`: `npm run breaks -- s5-metrics honest-label-removed` → код 0; дерево после — чисто
(`git status --porcelain | wc -l` → 0). Вывод прибора:

```
# Прогон сломов: s5-metrics

Начало 2026-09-17 10:46:01, конец 2026-09-17 10:46:28.

Разрывов времени прибор не нашёл: самый долгий слом — 1 с, середина — 1 с.

| № | Что ломаем | Что обязано покраснеть | Итог |
|---|---|---|---|
| 1 | убрать подпись «от чистой выручки» | доля подписана словами «от чистой выручки» | покраснела своя |

Всего сломов: 1. Проверено механизмов: 1.
```

## Часть B: `s11-content`

**Как отобраны строки.** Тем же счётом, что в части A, на голове `47f52f6`: (а)–(в) и кандидаты (г). Имена
проверок — из отчёта `vitest run --reporter=json` на той же голове (958 проверок). До правки у 114 из 149
сломов списка была хотя бы одна находка. Прибор сломов не запускался.

**Граница перевода и вывода.** Выведены только сломы механизмов, снятых договором куска: вкладки и отказ
незнакомой вкладки, карточки «Затраты» и «Итог», блок окупаемости и его подписи, доли статей затрат,
строка «Съедает больше всего» и признак `largest`, доля рекламы в полосе показателей, доля ступени от
оборота. Сломы живых механизмов каскада (набор ступеней, своя колонка, доли процентами, итоги от нуля,
пустая доля без базы) переведены на семь ступеней от чистой выручки; ряд по дням, полоса дельт и её
переход «нет базы → есть база», числа результата, перепись, граница зелёного — переведены.

**Новая графа.** «Без правки цитаты: объявлена проверка куска S13 заодно» — цитата верна, но проверка,
дописанная куском S13, краснеет от того же слома; она объявлена в `alsoRedden` с причиной. Там, где
объявленная прежде проверка удалена или больше не читает сломанное место, она из `alsoRedden` снята —
это названо в доводе строки.

| Список | id | Графа | Старая цитата | Новая цитата | Утверждение | Ожидание | Довод |
|---|---|---|---|---|---|---|---|
| s11-content | `waterfall-chain-broken` | Перевод | `- t.fees::numeric),` | `- t.ads::numeric),` | изменить одну ступень так, что остаток после вычитаний перестаёт сходиться с итогом | было своё: «ступени водопада сходятся: вычитания висят от остатка и кончаются на итоге»; стало своё: «доли и края ступеней — от чистой выручки» | сходимость краёв жива в семиступенчатом каскаде. Прежняя своя проверка «ступени водопада сходятся…» переписана под учёт задачи 1: сходимость утверждается краями cogs/ads/fixed в «доли и края ступеней — от чистой выручки». Прежняя цитата снимала последнее вычитание с края ступени; та же порча — у края рекламы, чей край проверка ждёт (41,2), а не у края комиссий, которого она не читает |
| s11-content | `waterfall-fractions` | Перевод | `round(s.amount / nullif(b.gross, 0) * 100, 1)::text` | `round(s.amount / b.denom * 100, 1)::text` | отдать доли ступеней долями единицы вместо процентов | было своё: «доли ступеней отданы процентами, а не долями единицы»; заодно: «отчёт несёт полосу показателей: значения — те же, что в итогах и в водопаде»; заодно: «доли ступеней считаются от оборота, а не от чистой выручки»; заодно: «ступени водопада сходятся: вычитания висят от остатка и кончаются на итоге»; заодно: «три итога водопада — от нуля; отрицательная прибыль идёт вниз, и шкала опускается под ноль»; заодно: «доля рекламы полосы — та же, что у ступени рекламы в водопаде»; стало своё: «доли и края ступеней — от чистой выручки»; заодно: «три итога водопада — от нуля; отрицательная прибыль идёт вниз, и шкала опускается под ноль» | доли процентами живы; выражение доли — от `b.denom` (задача 1). Своя проверка «доли ступеней отданы процентами…» переписана под учёт задачи 1 в «доли и края ступеней — от чистой выручки» (ждёт 35.3). Заодно снято: «…от оборота…», «…сходятся…» (переписаны в ту же), «доля рекламы полосы…» (учёт задачи 1: удалена), «отчёт несёт полосу показателей…» (после задачи 6 водопада не читает — `grep -c waterfall` по файлу проверок дельт → 0) |
| s11-content | `waterfall-share-of-net` | Вывод из списка | `round(s.amount / nullif(b.gross, 0) * 100, 1)::text` | — | считать долю ступени от чистой выручки, а не от оборота | было своё: «доли ступеней считаются от оборота, а не от чистой выручки»; заодно: «ступени водопада сходятся: вычитания висят от остатка и кончаются на итоге»; заодно: «три итога водопада — от нуля; отрицательная прибыль идёт вниз, и шкала опускается под ноль»; заодно: «доля рекламы полосы — та же, что у ступени рекламы в водопаде» | утверждение «доля от оборота, а не от чистой выручки» снято договором: каскад и его доли — от чистой выручки (договор, «Слой метрик», пункт 1; задача 1). Обратное утверждение сторожит слом `waterfall-base-gross` списка S13 |
| s11-content | `waterfall-step-dropped` | Перевод | `     (4, 'net',       'итог',      t.net::numeric,       0::numeric,\n                                   t.net::numeric),\n` | `     (1, 'net',           'итог',      t.net::numeric,   0::numeric,      t.net::numeric),\n` | убрать ступень «чистая выручка» из набора | было своё: «в водопаде ровно девять ступеней, в названном порядке»; заодно: «каждая ступень печатает свою колонку итогов, прибыль — та же колонка, что прибыль итога»; заодно: «ступени водопада сходятся: вычитания висят от остатка и кончаются на итоге»; заодно: «три итога водопада — от нуля; отрицательная прибыль идёт вниз, и шкала опускается под ноль»; заодно: «отчёт несёт водопад из девяти ступеней, прочитанный тем же снимком»; заодно: «напечатанное расхождение равно фактической разнице между суммой показанных ступеней и показанным итогом»; стало своё: «ступеней семь, от чистой выручки до прибыли, маржинальный доход пятым»; заодно: «доли и края ступеней — от чистой выручки»; заодно: «чистая выручка не положительна — долей и краёв нет, суммы стоят»; заодно: «три итога водопада — от нуля; отрицательная прибыль идёт вниз, и шкала опускается под ноль»; заодно: «отчёт несёт водопад из семи ступеней, прочитанный тем же снимком»; заодно: «напечатанное расхождение равно фактической разнице между суммой показанных ступеней и показанным итогом» | набор ступеней жив, ступень «чистая выручка» — первая из семи. Своя проверка «в водопаде ровно девять ступеней…» переписана под учёт задачи 1 в «ступеней семь, от чистой выручки до прибыли, маржинальный доход пятым». Заодно: прежние «каждая ступень печатает свою колонку…» и «…сходятся…» удалены/переписаны (учёт задачи 1); теперь ступень ищут по ключу «доли и края…» и «чистая выручка не положительна…» — обе названы; «отчёт несёт водопад из семи ступеней…» — переименована под учёт задачи 1 |
| s11-content | `waterfall-second-profit` | Перевод | `(9, 'profit',    'итог',      t.profit::numeric,` | `(7, 'profit',        'итог',      t.profit::numeric, 0::numeric,` | завести прибыли ступени второе выражение из слагаемых вместо колонки прибыли итога | было своё: «каждая ступень печатает свою колонку итогов, прибыль — та же колонка, что прибыль итога»; заодно: «напечатанное расхождение равно фактической разнице между суммой показанных ступеней и показанным итогом»; стало своё: «напечатанное расхождение равно фактической разнице между суммой показанных ступеней и показанным итогом» | своя колонка прибыли у ступени жива. Своя проверка «каждая ступень печатает свою колонку итогов…» удалена под учёт задачи 1 с доводом «покрыто проверкой маржинального дохода и проверкой расхождений цепочки»; из двух краснеет вторая — «напечатанное расхождение равно фактической разнице…» сличает расхождение строки итогов с суммами, показанными ступенями (в прежнем списке она уже стояла заодно). Замена прежняя — то же выражение из слагаемых |
| s11-content | `waterfall-second-net` | Перевод | `(4, 'net',       'итог',      t.net::numeric,       0::numeric,` | `(1, 'net',           'итог',      t.net::numeric,   0::numeric,      t.net::numeric),` | посчитать промежуточный итог своим выражением вместо колонки чистой выручки итога | было своё: «каждая ступень печатает свою колонку итогов, прибыль — та же колонка, что прибыль итога»; заодно: «напечатанное расхождение равно фактической разнице между суммой показанных ступеней и показанным итогом»; стало своё: «напечатанное расхождение равно фактической разнице между суммой показанных ступеней и показанным итогом»; заодно: «чистая выручка не положительна — долей и краёв нет, суммы стоят» | своя колонка чистой выручки у ступени жива. Своя проверка — как у `waterfall-second-profit`: «напечатанное расхождение…» берёт сумму ступени «чистая выручка» в расхождение с прибылью. Замена прежняя (оборот − скидки − возвраты). Заодно дописана «чистая выручка не положительна…»: она ждёт у ступени сумму «0.00» и «-10.00» из строки итогов |
| s11-content | `waterfall-total-floats` | Перевод | `(4, 'net',       'итог',      t.net::numeric,       0::numeric,` | `(5, 'margin_income', 'итог',      m.mi,             0::numeric,      m.mi),` | нарисовать промежуточный итог висящим от остатка, а не от нуля | было своё: «три итога водопада — от нуля; отрицательная прибыль идёт вниз, и шкала опускается под ноль»; заодно: «ступени водопада сходятся: вычитания висят от остатка и кончаются на итоге»; стало своё: «три итога водопада — от нуля; отрицательная прибыль идёт вниз, и шкала опускается под ноль» | итоги от нуля живы. Промежуточный итог каскада теперь — маржинальный доход (договор, пункт 1); чистая выручка — первая ступень, остатка над ней нет. Порча — край «от» маржинального дохода берёт остаток после комиссий. Своя проверка прежняя (ждёт край маржинального дохода 0.0); «…сходятся…» заодно снята — переписана под учёт задачи 1 |
| s11-content | `waterfall-zero-share` | Перевод | `round(s.amount / nullif(b.gross, 0) * 100, 1)::text` | `round(s.amount / b.denom * 100, 1)::text` | при нулевом обороте отдать долю ступени нулём, а не пустотой | было своё: «оборот ноль — доли, края и шкала пусты, а не нули»; заодно: «месяц без строк рекламы: пусто и в полосе, и у ступени водопада — обе стороны разом»; заодно: «месяц без строк рекламы: у ступени «Реклама» пусты сумма и доля, «съедает больше всего» её не берёт»; заодно: «отчёт несёт полосу показателей: значения — те же, что в итогах и в водопаде»; стало своё: «чистая выручка не положительна — долей и краёв нет, суммы стоят» | пустая доля при неположительной базе жива; база — чистая выручка (договор, пункт 1), утверждение строки прежнее. Своя проверка «оборот ноль — доли, края и шкала пусты…» переписана под учёт задачи 1 в «чистая выручка не положительна — долей и краёв нет, суммы стоят». Заодно сняты: «месяц без строк рекламы: пусто и в полосе…» (учёт задачи 6: удалена), «…«съедает больше всего» её не берёт» (учёт задачи 1: переписана, долей не читает), «отчёт несёт полосу показателей…» (водопада не читает) |
| s11-content | `waterfall-not-in-report` | Перевод | `    const waterfallResult = await client.query(MONTH_WATERFALL, [dayParam])\n` | та же | не прочитать водопад в отчёте | было своё: «отчёт несёт водопад из девяти ступеней, прочитанный тем же снимком»; заодно: «отчёт несёт полосу показателей: значения — те же, что в итогах и в водопаде»; стало своё: «отчёт несёт водопад из семи ступеней, прочитанный тем же снимком» | чтение водопада отчётом живо, цитата та же. Своя проверка переименована под учёт задачи 1 («…девяти…» → «…семи…»). Заодно снята «отчёт несёт полосу показателей…»: после задачи 6 она водопада не читает |
| s11-content | `waterfall-bar-other-value` | Перевод: только имя проверки заодно | `'--step-size': ступень.sharePct,` | та же | взять для длины столбика не ту долю, что напечатана текстом | было своё: «столбик ступени берёт ту же долю и тот же край, что напечатаны»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «столбик ступени берёт ту же долю и тот же край, что напечатаны»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `waterfall-base-unnamed` | Перевод | `` `${percent(ступень.sharePct)} оборота` `` | `` `${percent(ступень.sharePct)} чистой выручки` `` | печатать долю ступени без базы — без слова «оборота» | было своё: «у каждой ступени названа база доли — оборот»; заодно: «столбик ступени берёт ту же долю и тот же край, что напечатаны»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «у каждой ступени названа база доли — чистая выручка»; заодно: «столбик ступени берёт ту же долю и тот же край, что напечатаны»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | база доли у ступени названа и сейчас, словами «чистой выручки» (договор, пункт 1); утверждение строки прежнее — печатать долю без базы. Своя проверка переименована под учёт задачи 1 («…— оборот» → «…— чистая выручка»); ожидание соседней проверки — «37,5 % чистой выручки» (учёт задачи 1). Цитата совпадает с цитатой слома `waterfall-share-says-gross` списка S13; утверждения разные |
| s11-content | `waterfall-kind-dropped` | Перевод: только имя проверки заодно | `<li key={ступень.key} data-kind={ступень.kind}>` | та же | снять со ступени признак итога или вычитания | было своё: «итог и вычитание различены признаком в разметке, а не только цветом»; заодно: «нет строк рекламы — у ступени «Реклама» слова и в сумме, и в доле, а столбика нет»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «итог и вычитание различены признаком в разметке, а не только цветом»; заодно: «нет строк рекламы — у ступени «Реклама» слова и в сумме, и в доле, а столбика нет»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `waterfall-zero-bar` | Перевод | `{ступень.sharePct !== null && ступень.basePct !== null && (` | та же | рисовать столбик ступени и тогда, когда доли нет | было своё: «оборот ноль — у ступеней слова, столбиков нет»; заодно: «нет строк рекламы — у ступени «Реклама» слова и в сумме, и в доле, а столбика нет»; стало своё: «чистая выручка не положительна — у ступеней слова, столбиков нет»; заодно: «нет строк рекламы — у ступени «Реклама» слова и в сумме, и в доле, а столбика нет» | цитата та же. Своя проверка переименована под учёт задачи 1 («оборот ноль — у ступеней слова…» → «чистая выручка не положительна — у ступеней слова, столбиков нет») |
| s11-content | `waterfall-label-in-track` | Перевод: только имя проверки заодно | `                <span className="waterfall-label">\n                  {ПОДПИСИ_СТУПЕНЕЙ[ступень.key] ?? ступень.key}\n                </span>\n                <span className="waterfall-track" aria-hidden="true">\n` | та же | положить подпись ступени внутрь дорожки столбика | было своё: «подписи ступеней водопада — отдельной ячейкой вне дорожки столбика»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «подписи ступеней водопада — отдельной ячейкой вне дорожки столбика»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `waterfall-gap-not-actual` | Без правки цитаты: объявлена проверка куска S13 заодно | `                  - t.fixed::numeric - t.profit::numeric, 0)` | та же | отдать расхождение с прибылью, не равное разнице показанных сумм — без одного слагаемого | было своё: «напечатанное расхождение равно фактической разнице между суммой показанных ступеней и показанным итогом»; стало своё: «напечатанное расхождение равно фактической разнице между суммой показанных ступеней и показанным итогом»; заодно: «расхождения цепочки остаются у чистой выручки и у прибыли» | цитата стоит на расхождении с прибылью. Задача 1 куска S13 дописала проверку «расхождения цепочки остаются у чистой выручки и у прибыли», которая ждёт на несходящейся строке 215,59 — без постоянных расходов расхождение другое. Объявлена заодно |
| s11-content | `waterfall-gap-zero-given` | Без правки: цитата на прежнем механизме | `nullif(t.net::numeric - t.cogs::numeric - t.ads::numeric - t.fees::numeric` + andThen `                  - t.fixed::numeric - t.profit::numeric, 0)` | та же; andThen `                  - t.fixed::numeric - t.profit::numeric, 0)` | отдать нулевое расхождение нулём, а не пустотой | прежнее — своё: «напечатанное расхождение равно фактической разнице между суммой показанных ступеней и показанным итогом» | обе цитаты — в том же выражении расхождения с прибылью. Проверка «расхождения цепочки остаются…» задачи 1 смотрит несходящуюся строку, где расхождение не ноль, — снятый `nullif` её не трогает |
| s11-content | `largest-among-all` | Вывод из списка | `(s.kind = 'вычитание'\n        and s.amount > 0\n        and s.amount = max(s.amount) filter (where s.kind = 'вычитание') over ())` | — | искать самое большое среди всех ступеней, а не среди вычитаний | было своё: «самое большое вычитание помечено среди вычитаний, итоги не помечаются»; заодно: «при равенстве двух вычитаний до цента помечены оба»; заодно: «все вычитания ноль — не помечено ничего» | строка «Съедает больше всего» и признак `largest` сняты (договор, «Что уходит с экрана»: «эту работу берут выводы»; задача 1): признака в запросе нет, проверки удалены под учёт задачи 1 |
| s11-content | `largest-tie-first-only` | Вывод из списка | `and s.amount = max(s.amount) filter (where s.kind = 'вычитание') over ())` | — | при равенстве двух вычитаний до цента пометить только первое по порядку | было своё: «при равенстве двух вычитаний до цента помечены оба» | строка «Съедает больше всего» и признак `largest` сняты (договор, «Что уходит с экрана»: «эту работу берут выводы»; задача 1): признака в запросе нет, проверка удалена под учёт задачи 1 |
| s11-content | `largest-zero-marked` | Вывод из списка | `        and s.amount > 0\n` | — | пометить самым большим нулевое вычитание, когда все вычитания ноль | было своё: «все вычитания ноль — не помечено ничего» | строка «Съедает больше всего» и признак `largest` сняты (договор, «Что уходит с экрана»: «эту работу берут выводы»; задача 1): признака в запросе нет, проверка удалена под учёт задачи 1 |
| s11-content | `largest-share-other-step` | Вывод из списка | `` · ${вместе(percent(съедает[0].sharePct))} оборота` `` | — | назвать в строке одну ступень, а долю взять у другой | было своё: «строка «съедает больше всего» называет помеченную ступень и берёт её же долю»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «генератор отказывает, когда разметка разошлась с эталоном» | строка «Съедает больше всего» и признак `largest` сняты (договор, «Что уходит с экрана»: «эту работу берут выводы»; задача 1): строки на экране нет, проверка удалена под учёт задачи 1; отсутствие строки сторожит проверка «строки «съедает больше всего» нет» |
| s11-content | `largest-tie-one-name` | Вывод из списка | `${съедает.map(подписьСтроки).join(' и ')}` | — | при равенстве назвать в строке только одну ступень | было своё: «при равенстве строка называет обе ступени поровну» | строка «Съедает больше всего» и признак `largest` сняты (договор, «Что уходит с экрана»: «эту работу берут выводы»; задача 1): строки нет, проверка удалена под учёт задачи 1 |
| s11-content | `largest-without-share` | Вывод из списка | `с.largest === true && с.sharePct !== null` | — | печатать строку «съедает больше всего» и тогда, когда доли нет | было своё: «без доли строки «съедает больше всего» нет»; заодно: «оборот ноль — у ступеней слова, столбиков нет» | строка «Съедает больше всего» и признак `largest` сняты (договор, «Что уходит с экрана»: «эту работу берут выводы»; задача 1): строки нет, проверка удалена под учёт задачи 1 |
| s11-content | `largest-signal-colour` | Вывод из списка | `.waterfall-largest {\n  margin: 0 0 var(--spacingVerticalS);\n}` | — | покрасить строку «съедает больше всего» сигнальным цветом | было своё: «у строки «съедает больше всего» нет своего цвета — это подпись, а не тревога» | строка «Съедает больше всего» и признак `largest` сняты (договор, «Что уходит с экрана»: «эту работу берут выводы»; задача 1): правила `.waterfall-largest` в таблице стилей нет, проверка удалена под учёт задачи 1 |
| s11-content | `daily-own-expression` | Без правки цитаты: объявлена проверка куска S13 заодно | `  select m.sold_on as day, sum(m.net) as net\n` | та же | посчитать выручку дня своим выражением вместо цепочки money | было своё: «сумма ряда по дням — чистая выручка месяца, из той же цепочки money»; заодно: «доля дня — от наибольшей по модулю выручки дня, процентами со знаком»; заодно: «края шкалы и подписи оси — ноль и наибольшая выручка дня; столбик дня — от нуля»; стало своё: «сумма ряда по дням — чистая выручка месяца, из той же цепочки money»; заодно: «доля дня — от наибольшей по модулю выручки дня, процентами со знаком»; заодно: «края шкалы и подписи оси — ноль и наибольшая выручка дня; столбик дня — от нуля»; заодно: «средняя — чистая выручка ÷ все дни месяца, дни без заказов в делителе» | цитата стоит на ряде по дням. Задача 3 куска S13 дописала проверку «средняя — чистая выручка ÷ все дни месяца…»: у её строк оборот 1,00 при выручке 100,00 и 200,01, и своё выражение даёт другую среднюю. Объявлена заодно |
| s11-content | `daily-empty-day-dropped` | Без правки цитаты: объявлена проверка куска S13 заодно | `  from days d\n  left join by_day bd on bd.day = d.day\n` | та же | выбросить день без заказов из ряда | было своё: «в ряду все дни месяца, по порядку»; заодно: «день без заказов — пусто, а не ноль»; заодно: «подпись дня приходит готовой строкой»; заодно: «отчёт несёт ряд из всех дней месяца, прочитанный тем же снимком»; заодно: «видимые подписи — у каждого пятого дня, начиная с первого»; заодно: «месяц без заказов назван признаком, а не пустым графиком»; стало своё: «в ряду все дни месяца, по порядку»; заодно: «день без заказов — пусто, а не ноль»; заодно: «подпись дня приходит готовой строкой»; заодно: «отчёт несёт ряд из всех дней месяца, прочитанный тем же снимком»; заодно: «видимые подписи — у каждого пятого дня, начиная с первого»; заодно: «месяц без заказов назван признаком, а не пустым графиком»; заодно: «месяц без заказов — средней нет» | цитата стоит на последнем соединении дней с рядом (у средней своё соединение с другим отступом — цитата его не задевает). Задача 3 дописала «месяц без заказов — средней нет»: она читает первую строку ряда месяца без заказов, а строк не остаётся. Объявлена заодно |
| s11-content | `daily-no-nullif` | Без правки: цитата на прежнем механизме | `nullif(max(abs(net)), 0) as top` | та же | снять nullif у делителя доли дня | прежнее — своё: «наибольшая выручка дня ноль — доли пусты, а выручка дня — честный ноль»; заодно: «нулевая чистая выручка от реальных строк не роняет отчёт ошибкой деления» | цитата — делитель долей ряда. Средняя (задача 3) делится на тот же делитель, но единственная раскладка с нулевым делителем — та же своя проверка; прочие проверки средней делитель не нулевой |
| s11-content | `daily-share-of-signed-max` | Без правки: цитата на прежнем механизме | `nullif(max(abs(net)), 0) as top` | та же | делить выручку дня на наибольшую выручку, а не на наибольшую по модулю | прежнее — своё: «доля дня — от наибольшей по модулю выручки дня, процентами со знаком» | цитата — делитель долей ряда. В проверках средней (задача 3) наибольшая выручка дня положительна и совпадает с наибольшей по модулю — они не краснеют |
| s11-content | `daily-raw-date-label` | Без правки: цитата на прежнем механизме | `       extract(day from d.day)::int \|\| ' ' \|\|\n` | та же | отдать подпись дня сырой датой, оставив форматирование разметке | прежнее — своё: «подпись дня приходит готовой строкой»; заодно: «отчёт несёт ряд из всех дней месяца, прочитанный тем же снимком» | цитата — подпись дня; средняя (задача 3) дописана ниже в том же запросе и подписи не читает |
| s11-content | `daily-not-in-report` | Без правки: цитата на прежнем механизме | `    const dailyResult = await client.query(MONTH_DAILY, [dayParam])\n` | та же | не прочитать ряд по дням в отчёте | прежнее — своё: «отчёт несёт ряд из всех дней месяца, прочитанный тем же снимком» | цитата — чтение ряда отчётом; рядом дописаны чтения окупаемости, полосы и выводов. Проверка команды метрик о средней по дням подставляет ряд сама и отчёта не читает |
| s11-content | `daily-bar-other-value` | Перевод: только имя проверки заодно | `'--day-size': день.sharePct` | та же | взять для высоты столбика дня не ту долю, что в отчёте | было своё: «столбик дня берёт ту же долю и тот же край, что в отчёте»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «столбик дня берёт ту же долю и тот же край, что в отчёте»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `daily-aria-dropped` | Перевод | `                  data-empty={день.net === null ? 'true' : undefined}\n                  aria-label={` | `                    data-empty={день.net === null ? 'true' : undefined}\n                    aria-label={` | снять доступную подпись у дней ряда | было своё: «у каждого дня доступная подпись с готовой строкой»; заодно: «день без заказов — подпись словами, столбика нет, линия отсчёта помечена»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «у каждого дня доступная подпись с готовой строкой»; заодно: «день без заказов — подпись словами, столбика нет, линия отсчёта помечена»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | доступная подпись дня жива. Задача 11 вложила список столбиков в область `daily-plot`, и отступ строк вырос на два знака — цитата переведена на нынешний отступ, замена та же |
| s11-content | `daily-empty-day-zero-label` | Перевод: только имя проверки заодно | `` `${день.label}: заказов не было` `` | та же | подписать день без заказов «0,00 €», а не словами | было своё: «день без заказов — подпись словами, столбика нет, линия отсчёта помечена»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «день без заказов — подпись словами, столбика нет, линия отсчёта помечена»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `daily-ticks-every-day` | Перевод: только имя проверки заодно | `<li key={день.day}>{день.tick}</li>` | та же | подписать под осью каждый день, а не те, что дал отчёт | было своё: «видимые подписи дней — ровно те, что дал отчёт»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «видимые подписи дней — ровно те, что дал отчёт»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `daily-empty-month-frame` | Без правки: цитата на прежнем механизме | `{report.daily !== undefined && report.daily.hasOrders && (` | та же | нарисовать месяцу без заказов пустой график вместо слов | прежнее — своё: «месяц без заказов — слова «нет данных за месяц», а не пустой график» | цитата — условие блока ряда; рядом задача 11 дописала пунктир средней. Проверки средней на экране берут раскладку с заказами и порчу условия не видят |
| s11-content | `daily-profit-word` | Без правки цитаты: объявлена проверка куска S13 заодно | `<h2>Чистая выручка по дням</h2>` | та же | назвать блок ряда прибылью по дням | было своё: «в блоке ряда слово «выручка» есть, а слова «прибыль» нет»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «ряд рисуется только тогда, когда в отчёте есть его поле»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «вкладка из адреса определяет, какие разделы на экране»; заодно: «адрес без вкладки — первая вкладка, и только она»; заодно: «каждый раздел — ровно на одной вкладке, порядок вкладок — порядок отчёта»; стало своё: «в блоке ряда слово «выручка» есть, а слова «прибыль» нет»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «ряд рисуется только тогда, когда в отчёте есть его поле»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «все блоки на одной странице, в порядке договора» | цитата — заголовок блока ряда. Заодно сняты три проверки вкладок (учёт задачи 8: файлы удалены целиком) и переименован генератор (учёт задачи 16); дописана проверка задачи 8 «все блоки на одной странице, в порядке договора» — она ищет блоки по буквальным заголовкам |
| s11-content | `payback-label-without-base` | Вывод из списка | `<dt>Порог от вклада — наш счёт</dt>` | — | убрать из подписи порога его базу | было своё: «подпись порога называет его базу — вклад, а не маржу экрана»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «вклад стоит числом рядом с порогом»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «пометка «наш счёт» стоит в подписях наших же величин, а не только в пояснении»; заодно: «подпись порога называет свою базу, а противопоставление ушло в пояснение» | блок окупаемости снят решением владельца Э2 (договор, «Что уходит с экрана»: «Окупаемость по прибыли и вклад отдельными строками»; задача 8): подписи «Порог от вклада — наш счёт» на экране нет, проверки удалены под учёт задачи 8. Пометку «наш счёт» у порога в выводах сторожит слом `findings-drop-our-mark` списка S13 |
| s11-content | `payback-contribution-hidden` | Вывод из списка | `            <dt>Вклад с евро оборота — наш счёт</dt>\n            <dd>{percent(report.payback.contributionPct)}</dd>\n` | — | убрать вклад с экрана, оставив порог | было своё: «вклад стоит числом рядом с порогом»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «ни одна подпись не длиннее предела»; заодно: «в ячейке значения лежит значение, а не предложение»; заодно: «пометка «наш счёт» стоит в подписях наших же величин, а не только в пояснении» | блок окупаемости снят решением владельца Э2 (договор, «Что уходит с экрана»: «Окупаемость по прибыли и вклад отдельными строками»; задача 8): строки вклада на экране нет, проверки удалены под учёт задачи 8. Вклад стоит в пояснении вывода о рекламе (договор, пункт 12) |
| s11-content | `payback-ours-unsaid` | Вывод из списка | `            Вклад и порог — наши определения. Вклад` | — | не сказать рядом с порогом, что определения наши | было своё: «рядом с порогом сказано, что определения наши»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «пометка «наш счёт» стоит в подписях наших же величин, а не только в пояснении» | блок окупаемости снят решением владельца Э2 (договор, «Что уходит с экрана»: «Окупаемость по прибыли и вклад отдельными строками»; задача 8): пояснения «Вклад и порог — наши определения» нет, проверка удалена под учёт задачи 8. «Наш счёт» у вклада, порога и маржинального дохода сторожит слом `findings-drop-our-mark` списка S13 |
| s11-content | `payback-no-threshold-as-no-data` | Вывод из списка | `              {report.payback.breakevenNote !== null\n` | — | печатать «нет данных» там, где порога нет по делу | было своё: «порога нет — сказано словами, а не «нет данных» и не отрицательным порогом» | блок окупаемости снят решением владельца Э2 (договор, «Что уходит с экрана»: «Окупаемость по прибыли и вклад отдельными строками»; задача 8): строки `breakevenNote` на экране нет, проверка удалена под учёт задачи 8. «Порога нет» словами — слово признака выводов (слом `ads-verdict-without-ads` и проверки выводов S13) |
| s11-content | `payback-profit-base-unsaid` | Вывод из списка | `            Окупаемость по прибыли считается после постоянных расходов — они от рекламы не зависят,\n` + andThen `            поэтому окупается ли реклама, говорит порог, а не это число.\n` | — | убрать у окупаемости по прибыли фразу о её базе — после постоянных расходов | было своё: «у окупаемости по прибыли названа база — после постоянных расходов, и сказано, чем она не порог»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц» | блок окупаемости снят решением владельца Э2 (договор, «Что уходит с экрана»: «Окупаемость по прибыли и вклад отдельными строками»; задача 8): окупаемость по прибыли с экрана ушла («по нашей же оговорке, не отвечает, окупается ли реклама»), фразы о её базе нет, проверка удалена под учёт задачи 8 |
| s11-content | `items-counter-own-flag` | Без правки: цитата на прежнем механизме | `(count(*) filter (where k.loss) over ())::int` | та же | завести счётчику «в минусе» своё условие, отдельное от признака строки | прежнее — своё: «в минусе — прибыль строго меньше нуля, и счётчик берёт тот же признак» | цитата — счётчик «в минусе»; рядом задача 12 дописала признак пятёрки, который счётчика не читает |
| s11-content | `items-share-header-no-base` | Перевод | `<th>Доля в прибыли товаров</th>` | `<th>Доля в валовой прибыли товаров</th>` | назвать колонку «Доля в прибыли», без слова «товаров» | было своё: «заголовок доли называет базу — прибыль товаров, а заголовок маржи — чистую выручку»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «новые колонки и строка над таблицей рисуются только при своих полях»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; стало своё: «заголовок доли называет базу — прибыль товаров, а заголовок маржи — чистую выручку»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «новые колонки и строка над таблицей рисуются только при своих полях»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц» | заголовок доли с базой жив; задача 12 назвала базу валовой прибылью — «Доля в валовой прибыли товаров» (учёт задачи 12). Порча та же: снять слово «товаров». Цитата стоит в общей для двух таблиц разметке заголовков. Имя генератора — по учёту задачи 16 |
| s11-content | `items-summary-no-base` | Перевод | ``  Прибыль товаров — выручка минус себестоимость, ${вместе(money(report.itemsSummary.productsProfit))}; это не прибыль месяца, ${вместе(money(report.bottom.profit))}.` `` | ``  Валовая прибыль товаров — выручка минус себестоимость, ${вместе(money(report.itemsSummary.productsProfit))}; это не прибыль месяца, ${вместе(money(report.bottom.profit))}.` `` | убрать из строки над таблицей базу долей и её сумму | было своё: «строка над таблицей называет базу долей и её сумму, и прибыль месяца рядом»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; стало своё: «строка над таблицей называет базу долей и её сумму, и прибыль месяца рядом»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц» | строка над таблицей с базой и суммами жива; задача 12 назвала второе упоминание «Валовая прибыль товаров» (учёт задачи 12, решение контролёра). Порча та же — срезать базу и суммы. Имя генератора — по учёту задачи 16 |
| s11-content | `items-loss-from-profit` | Без правки: цитата на прежнем механизме | `data-loss={item.loss === true ? 'true' : undefined}` | та же | сравнить прибыль строки с нулём в разметке вместо готового признака | прежнее — своё: «разметка печатает признак «в минусе», который ей дали, а не сравнивает прибыль с нулём» | цитата — признак строки в минусе; задача 12 вынесла строку в `СтрокаТовара`, тело то же |
| s11-content | `items-loss-margin-hidden` | Перевод | `{report.itemsSummary !== undefined && <td>{percent(item.marginPct ?? null)}</td>}` | `{итоги !== undefined && <td>{percent(item.marginPct ?? null)}</td>}` | подсветить строку в минусе, убрав рядом число маржи | прежнее — своё: «у строки в минусе рядом стоит само число маржи — цвет не единственный признак» | ячейка маржи у строки жива; задача 12 вынесла строку в `СтрокаТовара`, условие колонки читает довод `итоги`, а не `report.itemsSummary`. Порча та же — снять маржу у строки в минусе |
| s11-content | `items-words-when-nonpositive` | Без правки цитаты: объявлена проверка куска S13 заодно | `{report.itemsSummary.skusFor80 !== null\n` | та же | печатать «дают … артикулов» и тогда, когда считать 80 % не от чего | было своё: «сумма прибыли товаров не положительна — строка над таблицей говорит словами»; стало своё: «сумма прибыли товаров не положительна — строка над таблицей говорит словами»; заодно: «сумма валовой прибыли товаров не положительна — строка над таблицей называет её валовой» | цитата — условие ветки строки над таблицей. Задача 12 дописала «сумма валовой прибыли товаров не положительна — строка над таблицей называет её валовой»: она ждёт ветку «— не положительна». Объявлена заодно |
| s11-content | `loss-row-refusal-token` | Без правки: цитата на прежнем механизме | `  background: var(--colorPaletteRedBackground2);\n` | та же | взять для подсветки строки в минусе токен отказа | прежнее — своё: «подсветка строки в минусе — свой красный, а не токен отказа» | цитата — заливка строки в минусе; рядом задача 12 дописала правила раскрытия и полоски доли, а заливка стоит в своём правиле |
| s11-content | `loss-row-muted-text` | Без правки: цитата на прежнем механизме | `  background: var(--colorPaletteRedBackground2);\n  color: var(--colorNeutralForeground1);\n` | та же | оставить в подсвеченной строке приглушённый текст | прежнее — своё: «в подсвеченной строке нет приглушённого текста — все ячейки основным цветом» | то же правило строки в минусе |
| s11-content | `cost-share-other-step` | Вывод из списка | `<ДоляСтатьи report={report} ключ="cogs" />` | — | взять статье долю чужой ступени водопада | было своё: «доля статьи — та же, что у её ступени водопада»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «полоска статьи берёт ту же долю, что напечатана»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц» | доли статей затрат сняты (договор, «Что уходит с экрана»: «доля от оборота на экране не остаётся нигде»; задача 1): компонента `ДоляСтатьи` нет, файл проверок удалён целиком (учёт задачи 1) |
| s11-content | `cost-share-bar-other-value` | Вывод из списка | `style={{ '--cost-share': доля } as CSSProperties}` | — | взять для полоски статьи не ту долю, что напечатана | было своё: «полоска статьи берёт ту же долю, что напечатана»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц» | доли статей затрат сняты (договор, «Что уходит с экрана»: «доля от оборота на экране не остаётся нигде»; задача 1): полоски статьи нет, файл проверок удалён целиком (учёт задачи 1) |
| s11-content | `cost-share-no-base` | Вывод из списка | `` `${percent(доля)} оборота` `` | — | печатать долю статьи без базы — без слова «оборота» | было своё: «у каждой статьи затрат доля названа от оборота»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «доля статьи — та же, что у её ступени водопада»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц» | доли статей затрат сняты (договор, «Что уходит с экрана»: «доля от оборота на экране не остаётся нигде»; задача 1): строки «% оборота» у статьи нет, файл проверок удалён целиком (учёт задачи 1) |
| s11-content | `cost-share-zero-bar` | Вывод из списка | `      {доля !== null && (\n        <span className="cost-bar"` | — | рисовать полоску статьи и тогда, когда доли нет | было своё: «оборот ноль — у статей слова, полосок нет»; заодно: «месяц без выгрузки рекламы: сумма рекламы — слова, а не ноль» | доли статей затрат сняты (договор, «Что уходит с экрана»: «доля от оборота на экране не остаётся нигде»; задача 1): полоски статьи нет, файл проверок удалён целиком (учёт задачи 1) |
| s11-content | `daily-edge-first-day` | Без правки: цитата на прежнем механизме | `'--day-from': день.basePct` | та же | взять для каждого дня край первого дня ряда | прежнее — своё: «каждый день берёт свой край, а ряд — свои пределы шкалы» | цитата — край столбика дня; отступ вырос, но цитата его не включает |
| s11-content | `daily-scale-foreign` | Перевод | `'--scale-from': report.daily.scaleLowPct ?? undefined,` | `{\n                    '--scale-from': report.daily.scaleLowPct ?? undefined,` | взять для ряда по дням нижний предел шкалы водопада | было своё: «каждый день берёт свой край, а ряд — свои пределы шкалы»; стало своё: «каждый день берёт свой край, а ряд — свои пределы шкалы»; заодно: «пунктир средней берёт те же пределы шкалы, что столбики ряда» | пределы шкалы ряда живы. Задача 11 поставила те же пределы и на пунктир средней, и цитата стала встречаться дважды; новая цитата начинается с открывающей скобки стиля списка столбиков. Заодно дописана проверка задачи 11 «пунктир средней берёт те же пределы шкалы, что столбики ряда»: порча меняет только список |
| s11-content | `read-at-formatted-in-markup` | Перевод: только имя проверки заодно | `` `Источники прочитаны по состоянию на ${report.sourcesReadAt}` `` | та же | форматировать время в разметке вместо того, чтобы печатать готовую строку | было своё: «разметка получает время готовой строкой и печатает его как есть»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «генератор отказывает, когда разметка разошлась с эталоном»; стало своё: «разметка получает время готовой строкой и печатает его как есть»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «генератор отказывает, когда разметка разошлась с эталоном» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `read-at-empty-when-no-mark` | Без правки: цитата на прежнем механизме | `'Время чтения источников неизвестно: отметки о чтении нет.'` | та же | печатать пустую строку, когда отметки о чтении нет | прежнее — своё: «отметки нет — на экране слова, а не пустая строка и не эпоха» | цитата — текст строки времени в шапке; соседство сменилось вместе с шапкой без полосы вкладок |
| s11-content | `read-at-dropped` | Перевод: только имя проверки заодно | `        {report.sourcesReadAt !== undefined && (` | та же | снять строку времени чтения с экрана | было своё: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «строка времени рисуется только тогда, когда в отчёте есть её поле»; заодно: «разметка получает время готовой строкой и печатает его как есть»; заодно: «отметки нет — на экране слова, а не пустая строка и не эпоха»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «генератор отказывает, когда разметка разошлась с эталоном»; стало своё: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «строка времени рисуется только тогда, когда в отчёте есть её поле»; заодно: «разметка получает время готовой строкой и печатает его как есть»; заодно: «отметки нет — на экране слова, а не пустая строка и не эпоха»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «генератор отказывает, когда разметка разошлась с эталоном» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `deltas-zero-without-base` | Перевод | `case when k.has_orders then k.cur - k.prev end as d` | та же | отдать ноль вместо пустоты, когда у прошлого месяца нет заказов | было своё: «нет предыдущего месяца с заказами — дельты пустые, а не нулевые, у всех четырёх разом»; заодно: «база сравнения — предыдущий календарный месяц, даже когда он пуст»; заодно: «появился предыдущий месяц — дельты считаются; исчез — снова слова»; стало своё: «нет предыдущего месяца с заказами — дельты пустые, а не нулевые, у всех трёх разом»; заодно: «база сравнения — предыдущий календарный месяц, даже когда он пуст»; заодно: «появился предыдущий месяц — дельты считаются; исчез — снова слова» | цитата та же. Своя проверка переименована под учёт задачи 6 («…у всех четырёх разом» → «…у всех трёх разом») |
| s11-content | `deltas-prev-with-orders` | Без правки: цитата на прежнем механизме | `where m.month = to_char($1::date - interval '1 month', 'YYYY-MM')\n                   and m.has_orders)` | та же | сравнивать с любым прошлым месяцем с заказами, а не с предыдущим календарным | прежнее — своё: «база сравнения — предыдущий календарный месяц, даже когда он пуст» | цитата — отбор прошлого месяца с заказами; соседство сменилось, когда задача 6 убрала признак рекламы прошлого месяца |
| s11-content | `deltas-locked-no-base` | Перевод | `                   and m.has_orders)                                   as has_orders,` | `                   and m.has_orders)                                   as has_orders\n` | запереть полосу постоянным «нет базы» | прежнее — своё: «появился предыдущий месяц — дельты считаются; исчез — снова слова»; заодно: «прошлый месяц с заказами — дельты считаются от его итогов» | переход «нет базы → есть база» жив (решение контролёра: слом не про долю рекламы). Задача 6 убрала следующую колонку `has_ads`, и у строки не стало запятой — цитата кончается переводом строки |
| s11-content | `deltas-not-in-report` | Без правки цитаты: объявлена проверка куска S13 заодно | `    const deltasResult = await client.query(MONTH_DELTAS, [dayParam])\n` | та же | не прочитать полосу показателей в отчёте | было своё: «отчёт несёт полосу показателей: значения — те же, что в итогах и в водопаде»; заодно: «появился предыдущий месяц — дельты считаются; исчез — снова слова»; стало своё: «отчёт несёт полосу показателей: значения — те же, что в итогах и в водопаде»; заодно: «появился предыдущий месяц — дельты считаются; исчез — снова слова»; заодно: «показателей в полосе три: прибыль, маржа, чистая выручка» | цитата — чтение полосы отчётом. Задача 6 дописала «показателей в полосе три: прибыль, маржа, чистая выручка» — она идёт настоящим путём через отчёт, а полоса под сломом пуста. Объявлена заодно |
| s11-content | `deltas-margin-relative` | Перевод | `(2, 'margin',   'pp',  true,  c.margin_pct, c.margin_pct::numeric, p.margin_pct::numeric),` | та же | считать дельту маржи в процентах от прошлой, а не в процентных пунктах | было своё: «дельта маржи — в процентных пунктах»; стало своё: «счёт дельт дельта маржи — в процентных пунктах» | цитата та же. Имя «дельта маржи — в процентных пунктах» подходит к двум проверкам: к своей и к переименованной под учёт задачи 9 «подписи называют базу отношений, дельта маржи — в процентных пунктах». Своя названа вместе с именем набора — «счёт дельт дельта маржи — …»: в каталоге ровно одна |
| s11-content | `deltas-ad-share-good-flipped` | Вывод из списка | `(4, 'ad_share', 'pp',  false,` | — | перевернуть признак «рост — это хорошо» у доли рекламы | было своё: «у доли рекламы рост — это плохо, у прибыли — хорошо» | доля рекламы снята с полосы показателей (договор, пункт 8 и «Что уходит с экрана»; задача 6): строки `ad_share` в запросе нет, проверка удалена под учёт задачи 6 |
| s11-content | `deltas-ad-share-zero` | Вывод из списка | `         case when cs.has_ads then round(c.ads::numeric / nullif(c.gross::numeric, 0) * 100, 1) end::text,` | — | отдать ноль вместо пустоты у доли рекламы, когда оборот ноль | было своё: «оборот ноль — доля рекламы пустая»; заодно: «месяц без строк рекламы: пусто и в полосе, и у ступени водопада — обе стороны разом»; заодно: «отчёт несёт полосу показателей: значения — те же, что в итогах и в водопаде»; заодно: «у текущего месяца нет ни одной строки рекламы — доля рекламы пустая, а не ноль»; заодно: «в месяце нет строк рекламы — боевой запрос отдаёт долю рекламы пустой» | доля рекламы снята с полосы показателей (договор, пункт 8 и «Что уходит с экрана»; задача 6): выражения доли рекламы в запросе нет, проверка удалена под учёт задачи 6 |
| s11-content | `deltas-cur-no-ads-zero` | Вывод из списка | `         case when cs.has_ads then round(c.ads::numeric / nullif(c.gross::numeric, 0) * 100, 1) end::text,` | — | считать долю рекламы текущего месяца нулём, когда строк рекламы за него нет | было своё: «у текущего месяца нет ни одной строки рекламы — доля рекламы пустая, а не ноль»; заодно: «месяц без строк рекламы: пусто и в полосе, и у ступени водопада — обе стороны разом»; заодно: «отчёт несёт полосу показателей: значения — те же, что в итогах и в водопаде»; заодно: «в месяце нет строк рекламы — боевой запрос отдаёт долю рекламы пустой» | доля рекламы снята с полосы показателей (договор, пункт 8 и «Что уходит с экрана»; задача 6): выражения доли рекламы и `cur_state` в запросе полосы нет, проверки удалены под учёт задачи 6 |
| s11-content | `waterfall-ads-zero-without-upload` | Перевод | `     (6, 'ads',       'вычитание', case when cs.has_ads then t.ads::numeric end,` | `     (3, 'ads',           'вычитание', case when cs.has_ads then t.ads::numeric end,` | печатать сумму и долю ступени «Реклама» нулём, когда выгрузки за месяц нет | было своё: «месяц без строк рекламы: у ступени «Реклама» пусты сумма и доля»; заодно: «отчёт несёт полосу показателей: значения — те же, что в итогах и в водопаде»; заодно: «месяц без строк рекламы: пусто и в полосе, и у ступени водопада — обе стороны разом»; стало своё: «без строк рекламы ступень рекламы пуста, маржинальный доход считается с нулём» | пустая ступень рекламы без выгрузки жива (договор, «Отступления, названные заранее»). Ступень рекламы — третья, строка с новым отступом. Своя проверка переписана под учёт задачи 1 в «без строк рекламы ступень рекламы пуста, маржинальный доход считается с нулём». Заодно сняты две проверки доли рекламы полосы — удалены под учёт задачи 6 и водопада не читают |
| s11-content | `deltas-cur-no-ads-delta-kept` | Вывод из списка | `         case when cs.has_ads then round(c.ads::numeric / nullif(c.gross::numeric, 0) * 100, 1) end,` | — | оставить дельту доли рекламы считаемой, когда выгрузки за месяц нет | было своё: «у текущего месяца нет ни одной строки рекламы — доля рекламы пустая, а не ноль»; заодно: «в месяце нет строк рекламы — боевой запрос отдаёт долю рекламы пустой» | доля рекламы снята с полосы показателей (договор, пункт 8 и «Что уходит с экрана»; задача 6): дельты доли рекламы нет, проверки удалены под учёт задачи 6 |
| s11-content | `deltas-prev-no-ads-zero` | Вывод из списка | `case when s.has_ads then round(p.ads::numeric / nullif(p.gross::numeric, 0) * 100, 1) end)` | — | считать долю рекламы прошлого месяца нулём, когда строк рекламы у него нет | было своё: «в прошлом месяце нет рекламы — дельта доли рекламы пустая, остальные считаются» | доля рекламы снята с полосы показателей (договор, пункт 8 и «Что уходит с экрана»; задача 6): признака рекламы прошлого месяца в запросе нет, проверка удалена под учёт задачи 6 |
| s11-content | `kpi-value-hidden-without-base` | Перевод | `<span className="kpi-value">{значениеПоказателя(п)}</span>` | `<p className="result-profit">{money(report.bottom.profit)}</p>` | скрыть значение показателя, когда базы для сравнения нет | было своё: «нет базы для сравнения — значения четырёх показателей стоят числами, пуста только дельта»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; стало своё: «нет базы для сравнения — три числа результата стоят числами, строк дельты у чисел нет»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц» | значения при отсутствии базы стоят числами и сейчас — в блоке результата (договор, пункт 11). Своя проверка переписана под учёт задачи 9 в «нет базы для сравнения — три числа результата стоят числами…»; порча скрывает главное из трёх чисел — прибыль. Имя генератора — по учёту задачи 16 |
| s11-content | `kpi-no-base-zero` | Перевод | `` : `нет базы: в ${полоса.prevMonth} заказов нет` `` | `` : `Сравнить с ${полоса.prevMonth} нельзя: в ${полоса.prevMonth} заказов нет.` `` | нарисовать ноль вместо слов, когда базы для сравнения нет | было своё: «нет базы — слова, а не ноль»; заодно: «нет базы для сравнения — значения четырёх показателей стоят числами, пуста только дельта»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; стало своё: «нет базы — слова, а не ноль»; заодно: «нет базы — одна строка на блок, а не по строке на число»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц» | слова вместо нуля без базы живы: одна строка на блок (договор, пункт 11). Порча та же — «+0,00» вместо слов. Своя проверка прежняя (ждёт строку «Сравнить с 2026-02 нельзя…»). Заодно: «нет базы для сравнения — значения четырёх…» переписана под учёт задачи 9 и строки не читает — снята; дописана «нет базы — одна строка на блок…» задачи 9, которая ждёт ту же строку; имя генератора — по учёту задачи 16 |
| s11-content | `kpi-verdict-from-sign` | Перевод: только имя проверки заодно | `data-verdict={п.verdict ?? undefined}` | та же | красить дельту по знаку, а не по признаку из счёта | было своё: «разметка печатает дельту и её смысл, которые ей дали, а не выводит смысл из знака»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «разметка печатает дельту и её смысл, которые ей дали, а не выводит смысл из знака»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `kpi-meaning-only-colour` | Перевод: только имя проверки заодно | `` к ${полоса.prevMonth} · ${п.verdict}` `` | та же | оставить у дельты только цвет, убрав слово смысла | было своё: «смысл дельты назван не только цветом: у каждой строки знак и слово»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «разметка печатает дельту и её смысл, которые ей дали, а не выводит смысл из знака»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «генератор отказывает, когда разметка разошлась с эталоном»; стало своё: «смысл дельты назван не только цветом: у каждой строки знак и слово»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «разметка печатает дельту и её смысл, которые ей дали, а не выводит смысл из знака»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «генератор отказывает, когда разметка разошлась с эталоном» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `kpi-label-no-base` | Вывод из списка | `  ad_share: 'Доля рекламы от оборота',` | — | убрать из подписи доли рекламы её базу — оборот | было своё: «подписи называют базу отношений, дельты маржи и доли рекламы — в процентных пунктах»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «генератор отказывает, когда разметка разошлась с эталоном» | доля рекламы снята с полосы показателей (договор, пункт 8 и «Что уходит с экрана»; задача 6): подписи «Доля рекламы от оборота» нет, её проверка переписана под учёт задачи 6 без неё |
| s11-content | `kpi-points-as-percent` | Перевод | `вместе(money(п.delta)) : points(п.delta)` | та же | печатать дельту маржи и доли рекламы процентами, а не пунктами | было своё: «подписи называют базу отношений, дельты маржи и доли рекламы — в процентных пунктах»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «разметка печатает дельту и её смысл, которые ей дали, а не выводит смысл из знака»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «генератор отказывает, когда разметка разошлась с эталоном»; заодно: «денежное значение внутри предложения не бывает без защиты от переноса»; стало своё: «подписи называют базу отношений, дельта маржи — в процентных пунктах»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «разметка печатает дельту и её смысл, которые ей дали, а не выводит смысл из знака»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; заодно: «генератор отказывает, когда разметка разошлась с эталоном»; заодно: «денежное значение внутри предложения не бывает без защиты от переноса» | дельта маржи в пунктах жива; доли рекламы в полосе нет (задача 6), утверждение строки прежнее. Цитата та же. Своя проверка переименована под учёт задач 6 и 9 в «подписи называют базу отношений, дельта маржи — в процентных пунктах» (строк в пунктах — 1) |
| s11-content | `green-second-place` | Перевод | `.kpi-value {\n  font-size: var(--fontSizeHero700);` | `.result-profit {\n  font-size: var(--fontSizeHero700);` | поставить зелёный во второе место на экране | прежнее — своё: «зелёный — только в строке дельты «лучше» полосы показателей: ровно одно правило» | граница зелёного жива. Крупное число экрана — прибыль блока результата: класс `kpi-value` переведён в `result-profit` (решение контролёра 2 к задаче 9, учёт задачи 9). Порча та же — зелёный у крупного числа |
| s11-content | `green-under-another-name` | Перевод | `  --colorPaletteGreenForeground1: #0e700e;\n` + andThen `.kpi-value {\n  font-size: var(--fontSizeHero700);` | та же; andThen `.result-profit {\n  font-size: var(--fontSizeHero700);` | поставить зелёный во второе место под другим именем величины | прежнее — своё: «зелёный — только в строке дельты «лучше» полосы показателей: ровно одно правило» | то же, что `green-second-place`: вторая правка переведена на правило `.result-profit` |
| s11-content | `worse-refusal-token` | Без правки: цитата на прежнем механизме | `  color: var(--colorPaletteRedForeground3);\n}` | та же | взять для дельты «хуже» цвет отказа | прежнее — своё: «дельта «хуже» — свой токен красного, а не токен отказа» | цитата стоит в правиле `.kpi[data-verdict='хуже'] .kpi-delta` — единственное вхождение; у пометки «приблизительно» и черты вывода цвет не кончает правило |
| s11-content | `worse-as-plaque` | Без правки: цитата на прежнем механизме | `  color: var(--colorPaletteRedForeground3);\n}` | та же | оформить дельту «хуже» плашкой с фоном и рамкой, как отказ | прежнее — своё: «дельта — простой текст: без фона и рамки, в отличие от плашки отказа» | та же цитата, то же правило |
| s11-content | `better-not-green` | Без правки: цитата на прежнем механизме | `  color: var(--colorPaletteGreenForeground1);\n}` | та же | снять зелёный у дельты «лучше» | прежнее — своё: «дельта «лучше» — зелёный»; заодно: «зелёный — только в строке дельты «лучше» полосы показателей: ровно одно правило» | цитата стоит в правиле `.kpi[data-verdict='лучше'] .kpi-delta` |
| s11-content | `tab-not-read` | Вывод из списка | `  const tabParam = Array.isArray(params.tab) ? params.tab[0] : params.tab\n` | — | не читать вкладку из адреса — всегда первая вкладка | было своё: «вкладка из адреса определяет, какие разделы на экране»; заодно: «страница отчёта с годной cookie печатает числа»; заодно: «месяц из адреса определяет, что видно на экране»; заодно: «из двух значений вкладки берётся первое, как у месяца»; заодно: «неизвестная вкладка — отказ словами, а не первая вкладка»; заодно: «отказ на незнакомую вкладку называет разделы ссылками, и первая — «Главное»»; заодно: «утверждение над таблицей говорит, что счёт идёт по прибыли, когда порядок другой» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): вкладка из адреса не читается (пункт 18: `?tab=` открывает тот же экран), проверки вкладок удалены под учёт задачи 8. Старые адреса сторожит слом `tabs-come-back` списка S13 |
| s11-content | `tab-not-read-s6` | Вывод из списка | `  const tabParam = Array.isArray(params.tab) ? params.tab[0] : params.tab\n` | — | не читать вкладку из адреса — проверка S6 «страница отчёта с годной cookie печатает числа» | было своё: «страница отчёта с годной cookie печатает числа»; заодно: «вкладка из адреса определяет, какие разделы на экране»; заодно: «месяц из адреса определяет, что видно на экране»; заодно: «из двух значений вкладки берётся первое, как у месяца»; заодно: «неизвестная вкладка — отказ словами, а не первая вкладка»; заодно: «отказ на незнакомую вкладку называет разделы ссылками, и первая — «Главное»»; заодно: «утверждение над таблицей говорит, что счёт идёт по прибыли, когда порядок другой» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): вкладки из адреса нет, и слом не может быть переведён; проверка S6 «страница отчёта с годной cookie печатает числа» переписана под учёт задачи 8 (снят только `tab`) и своего слома в этом списке больше не имеет — названо в замечаниях отчёта |
| s11-content | `no-tab-other-default` | Вывод из списка | `  const вкладка = tabParam === undefined ? 'glavnoe' : ВКЛАДКИ.find((имя) => имя === tabParam)` | — | адресу без вкладки отдать не первую вкладку | было своё: «адрес без вкладки — первая вкладка, и только она» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): умолчания вкладки нет, проверка удалена под учёт задачи 8 |
| s11-content | `unknown-tab-silent-first` | Вывод из списка | `  const вкладка = tabParam === undefined ? 'glavnoe' : ВКЛАДКИ.find((имя) => имя === tabParam)` | — | незнакомую вкладку молча показать первой | было своё: «неизвестная вкладка — отказ словами, а не первая вкладка»; заодно: «отказ на незнакомую вкладку называет разделы ссылками, и первая — «Главное»» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): отказа на незнакомую вкладку нет (договор, «Отступления»: «Старые адреса без отказа»), проверки удалены под учёт задачи 8 |
| s11-content | `tab-last-value` | Вывод из списка | `  const tabParam = Array.isArray(params.tab) ? params.tab[0] : params.tab\n` | — | из двух значений вкладки взять последнее | было своё: «из двух значений вкладки берётся первое, как у месяца» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): вкладка не читается, проверка удалена под учёт задачи 8 |
| s11-content | `unknown-tab-no-links` | Вывод из списка | `      <ПолосаВкладок месяц={месяц} текущая={null} />\n` | — | отдать незнакомой вкладке отказ без ссылок | было своё: «отказ на незнакомую вкладку называет разделы ссылками, и первая — «Главное»»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): отказа и полосы вкладок нет, проверка удалена под учёт задачи 8 |
| s11-content | `roas-as-percent` | Перевод | `          <dd className="payback-main">{ratio(report.bottom.roasByGross)}</dd>` | `            {ratio(report.bottom.roasByGross)}\n` | печатать окупаемость по обороту форматом процента | было своё: «экран / окупаемость печатается отношением, а не процентами»; заодно: «экран / экран печатает поля отчёта, а не свои числа»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «окупаемость по обороту стоит в блоке всегда, а три числа шага 3 — только при своём поле»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; стало своё: «нет базы для сравнения — три числа результата стоят числами, строк дельты у чисел нет»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц» | окупаемость по обороту отношением жива — в блоке результата (договор, пункт 11). Прежняя своя проверка удалена под учёт задачи 8 вместе с блоком окупаемости, не переименована; знак «×» у числа сейчас утверждает «нет базы для сравнения — три числа результата стоят числами…» (ждёт `ratio('45.6')`). Заодно сняты удалённая «окупаемость по обороту стоит в блоке всегда…» и «экран печатает поля отчёта…» (после задачи 8 окупаемости не читает); перепись ждёт «45,6 ×» в блоке результата |
| s11-content | `margin-raw` | Перевод | `          <dd>{percent(report.bottom.marginPct)}</dd>` | `<dd>{percent(report.bottom.marginPct)}{строкаПоказателя(полоса, 'margin')}</dd>` | печатать маржу «Итога» сырым значением, мимо формата | было своё: «нет данных печатается словами, а не как 0 и не как NaN»; заодно: «экран / экран печатает поля отчёта, а не свои числа»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; стало своё: «нет базы для сравнения — три числа результата стоят числами, строк дельты у чисел нет»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц» | маржа итога через формат жива — в блоке результата (договор, пункт 11); утверждение строки прежнее, «Итог» в нём — имя поля `bottom`. Прежняя своя проверка «нет данных печатается словами…» удалена под учёт задачи 8, не переименована; формат маржи утверждает «нет базы для сравнения — три числа результата стоят числами…» (ждёт «12,3 %»). «Экран печатает поля отчёта…» маржи после задачи 8 не читает — снята. Пустую маржу словами сторожит слом `margin-prints-nan` списка S13 |
| s11-content | `no-price-line-dropped` | Перевод: только имя проверки заодно | `          <p>Без цены поставщика (запасные 40%): {report.honesty.skusWithoutPrice.join(', ')}</p>\n` | та же | снять строку «Без цены поставщика» | было своё: «экран называет товары без цены поставщика»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; стало своё: «экран называет товары без цены поставщика»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `section-two-tabs` | Вывод из списка | `  return вкладка === текущая ? (` | — | отдать один раздел двум вкладкам | было своё: «каждый раздел — ровно на одной вкладке, порядок вкладок — порядок отчёта»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «вкладка из адреса определяет, какие разделы на экране»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): компонента `Раздел` нет, проверки удалены под учёт задачи 8 |
| s11-content | `section-no-tab` | Вывод из списка | `      <Раздел вкладка="dengi" текущая={tab} className="block bottom-line">` | — | не отдать раздел ни одной вкладке | было своё: «каждый раздел — ровно на одной вкладке, порядок вкладок — порядок отчёта»; заодно: «экран / экран печатает поля отчёта, а не свои числа»; заодно: «экран / нет данных печатается словами, а не как 0 и не как NaN»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «вкладка из адреса определяет, какие разделы на экране»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): компонента `Раздел` и карточки «Итог» нет, проверки удалены под учёт задачи 8 |
| s11-content | `tab-link-drops-month` | Вывод из списка | ``               href={месяц === null ? `/?tab=${имя}` : `/?m=${месяц}&tab=${имя}`} `` | — | ссылке вкладки потерять месяц | было своё: «ссылки вкладок сохраняют месяц»; заодно: «адрес без вкладки — первая вкладка, и только она»; заодно: «отказ на незнакомую вкладку называет разделы ссылками, и первая — «Главное»»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): ссылок вкладок нет, проверки удалены под учёт задачи 8. Ссылки месяцев сторожат проверки S5 и слом `nav-links-drop-month` |
| s11-content | `month-link-drops-tab` | Вывод из списка | ``   const хвостВкладки = tab === 'glavnoe' ? '' : `&tab=${tab}` `` | — | ссылке месяца потерять вкладку | было своё: «ссылки месяцев сохраняют вкладку»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): хвоста вкладки у ссылок месяцев нет, проверка удалена под учёт задачи 8 |
| s11-content | `first-tab-root` | Вывод из списка | ``               href={месяц === null ? `/?tab=${имя}` : `/?m=${месяц}&tab=${имя}`} `` | — | ссылке первой вкладки дать адрес корня | было своё: «ни одна ссылка вкладок не ведёт на корень»; заодно: «ссылки переключателя месяца ведут на /?m=<месяц>, а не на /»; заодно: «адрес без вкладки — первая вкладка, и только она»; заодно: «отказ на незнакомую вкладку называет разделы ссылками, и первая — «Главное»»; заодно: «ссылки вкладок сохраняют месяц»; заодно: «у текущей вкладки признак текущей в разметке, у прочих его нет»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): ссылок вкладок нет, проверка удалена под учёт задачи 8 |
| s11-content | `current-mark-dropped` | Вывод из списка | `              aria-current={имя === текущая ? 'page' : undefined}` | — | снять признак текущей у текущей вкладки | было своё: «у текущей вкладки признак текущей в разметке, у прочих его нет»; заодно: «адрес без вкладки — первая вкладка, и только она»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): полосы вкладок нет, проверка удалена под учёт задачи 8 |
| s11-content | `current-mark-all` | Вывод из списка | `              aria-current={имя === текущая ? 'page' : undefined}` | — | поставить признак текущей всем вкладкам | было своё: «у текущей вкладки признак текущей в разметке, у прочих его нет»; заодно: «отказ на незнакомую вкладку называет разделы ссылками, и первая — «Главное»»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): полосы вкладок нет, проверка удалена под учёт задачи 8 |
| s11-content | `fifth-tab` | Вывод из списка | `export const ВКЛАДКИ = ['glavnoe', 'dengi', 'tovary', 'kachestvo'] as const` | — | дописать пятую вкладку | было своё: «в полосе вкладок ровно четыре ссылки, в названном порядке»; заодно: «отказ на незнакомую вкладку называет разделы ссылками, и первая — «Главное»»; заодно: «ссылки вкладок сохраняют месяц»; заодно: «ни одна ссылка вкладок не ведёт на корень»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): списка `ВКЛАДКИ` нет, проверка удалена под учёт задачи 8 |
| s11-content | `tab-removed` | Вывод из списка | `export const ВКЛАДКИ = ['glavnoe', 'dengi', 'tovary', 'kachestvo'] as const` | — | убрать вкладку из приложения | было своё: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «вкладка из адреса определяет, какие разделы на экране»; заодно: «отказ на незнакомую вкладку называет разделы ссылками, и первая — «Главное»»; заодно: «в полосе вкладок ровно четыре ссылки, в названном порядке»; заодно: «ссылки вкладок сохраняют месяц»; заодно: «ни одна ссылка вкладок не ведёт на корень»; заодно: «у текущей вкладки признак текущей в разметке, у прочих его нет»; заодно: «генератор отказывает, когда разметка разошлась с эталоном»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): списка `ВКЛАДКИ` нет |
| s11-content | `header-first-tab-only` | Вывод из списка | `      <header className="report-head">` + andThen `      </header>\n` | — | рисовать шапку только на первой вкладке | было своё: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «ссылки месяцев сохраняют вкладку»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «ряд стоит после шапки и полосы вкладок, а не вровень с плавающей полосой действия» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): шапка одна на единственном экране, условия по вкладке нет |
| s11-content | `tabs-client-code` | Вывод из списка | `import type { CSSProperties, ReactNode } from 'react'\n` | — | сделать полосу вкладок клиентским кодом | было своё: «полоса вкладок без клиентского кода» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): полосы вкладок нет, проверка удалена под учёт задачи 8 |
| s11-content | `tab-indicator-dropped` | Вывод из списка | `  height: var(--strokeWidthThicker);\n` | — | убрать у текущей вкладки полосу-указатель | было своё: «у текущей вкладки полоса-указатель толщиной strokeWidthThicker» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): правила `.tabs` в таблице стилей нет, проверка удалена под учёт задачи 8. Прежняя цитата сейчас попадала в высоту полоски доли товаров (`.items-bar`) — не в свой механизм |
| s11-content | `tab-weight-dropped` | Вывод из списка | `  color: var(--colorNeutralForeground1);\n  font-weight: var(--fontWeightSemibold);\n}\n\n.tabs a[aria-current='page']::after {` | — | убрать у текущей вкладки насыщенность | было своё: «у текущей вкладки насыщенность fontWeightSemibold» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): правила `.tabs` нет, проверка удалена под учёт задачи 8 |
| s11-content | `tab-indicator-colour-outside` | Вывод из списка | `  background: var(--colorCompoundBrandStroke);\n` | — | вписать указателю текущей вкладки цвет вне выписки | было своё: «в таблице стилей нет ни одной величины и ни одного цвета вне набора Fluent 2»; заодно: «у текущей вкладки полоса-указатель толщиной strokeWidthThicker» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): правила указателя вкладки нет — ломать нечего; своя проверка набора Fluent 2 жива и сторожится своими сломами |
| s11-content | `census-to-contain` | Перевод | `)))).toEqual([` | `<Dashboard report={ПЕРЕПИСЬ} />))).toEqual([` | заменить в переписи сравнение списков на поиск вхождения | прежнее — своё: «перепись экрана: весь видимый текст, по порядку и без единой потери» | перепись жива; задача 8 свела четыре списка вкладок в один список единственного экрана (учёт задачи 8), и хвост цитаты изменился. Порча та же — `toContain` вместо `toEqual` |
| s11-content | `roas-back-to-bottom` | Вывод из списка | `          <dt>Окупаемость рекламы (по обороту)</dt>\n          <dd className="payback-main">{ratio(report.bottom.roasByGross)}</dd>\n` + andThen `          <dd>{percent(report.bottom.marginPct)}</dd>\n` | — | вернуть окупаемость по обороту в «Итог» | было своё: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «окупаемость по обороту стоит в блоке всегда, а три числа шага 3 — только при своём поле»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «ни одна подпись не длиннее предела»; заодно: «в ячейке значения лежит значение, а не предложение» | карточки «Итог» и блока окупаемости нет (договор, «Что уходит с экрана»; задача 8): вернуть окупаемость «в Итог» некуда. Возврат карточек сторожит слом `money-cards-come-back` списка S13 |
| s11-content | `generator-drops-tab-state` | Вывод из списка | `  { имя: 'kachestvo', разметка: вкладка('kachestvo') },\n` | — | выронить состояние вкладки из описи генератора | было своё: «генератор собирает полный набор и сверяет все двадцать страниц» | вкладки сняты решением владельца Э2 (договор, «Два прежних решения, которые кусок отменяет»; задача 8): состояний вкладок в описи генератора нет (учёт задачи 16: 14 страниц). Состав описи сторожит проверка полного набора |
| s11-content | `unit-join-dropped-items-summary` | Перевод: только имя проверки заодно | `себестоимость, ${вместе(money(report.itemsSummary.productsProfit))}; это не прибыль месяца` | та же | снять защиту от переноса у суммы в строке над таблицей товаров | было своё: «денежное значение внутри предложения не бывает без защиты от переноса»; заодно: «строка над таблицей называет базу долей и её сумму, и прибыль месяца рядом»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; стало своё: «денежное значение внутри предложения не бывает без защиты от переноса»; заодно: «строка над таблицей называет базу долей и её сумму, и прибыль месяца рядом»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `unit-join-dropped-gap` | Перевод: только имя проверки заодно | `расходятся с прибылью на ${вместе(money(report.waterfall.profitGap))}.` | та же | снять защиту от переноса у суммы в строке о центах под водопадом | было своё: «денежное значение внутри предложения не бывает без защиты от переноса»; заодно: «строка о расхождении — только когда оно есть, и с тем числом, что дал отчёт»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «денежное значение внутри предложения не бывает без защиты от переноса»; заодно: «строка о расхождении — только когда оно есть, и с тем числом, что дал отчёт»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `unit-join-dropped-largest` | Вывод из списка | `` {`Съедает больше всего: ${подписьСтроки(съедает[0])} · ${вместе(percent(съедает[0].sharePct))} оборота`} `` | — | снять защиту от переноса у доли в строке «Съедает больше всего» | было своё: «денежное значение внутри предложения не бывает без защиты от переноса»; заодно: «строка «съедает больше всего» называет помеченную ступень и берёт её же долю»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор отказывает, когда разметка разошлась с эталоном»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | строка «Съедает больше всего» и признак `largest` сняты (договор, «Что уходит с экрана»: «эту работу берут выводы»; задача 1): строки нет — снимать защиту не с чего; сторож переноса обходит всю разметку и сторожится прочими сломами этого шага |
| s11-content | `unit-join-dropped-kpi-delta` | Перевод: только имя проверки заодно | `${п.unit === 'eur' ? вместе(money(п.delta)) : points(п.delta)}` | та же | снять защиту от переноса у денежной дельты полосы показателей | было своё: «денежное значение внутри предложения не бывает без защиты от переноса»; заодно: «разметка печатает дельту и её смысл, которые ей дали, а не выводит смысл из знака»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор отказывает, когда разметка разошлась с эталоном»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «денежное значение внутри предложения не бывает без защиты от переноса»; заодно: «разметка печатает дельту и её смысл, которые ей дали, а не выводит смысл из знака»; заодно: «перепись экрана: весь видимый текст, по порядку и без единой потери»; заодно: «генератор отказывает, когда разметка разошлась с эталоном»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `share-value-wraps` | Перевод: только имя проверки заодно | `.daily-axis span,\n.share-value {\n  white-space: nowrap;\n}` | та же | снять запрет переноса у доли честности | было своё: «элементам под защитой таблица стилей запрещает перенос»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все двадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали»; стало своё: «элементам под защитой таблица стилей запрещает перенос»; заодно: «генератор собирает снимки на настоящем пути»; заодно: «генератор собирает полный набор и сверяет все четырнадцать страниц»; заодно: «генератор отказывает, когда картинка не оказалась на диске»; заодно: «генератор отказывает, когда страница свёрстана не на запрошенной ширине»; заодно: «генератор отказывает, когда замер ширины не удался»; заодно: «генератор отказывает, когда светлая и тёмная картинки совпали» | проверка полного набора снимков переименована под учёт задачи 16 («…двадцать страниц» → «…четырнадцать страниц»); цитата стоит на прежнем механизме |
| s11-content | `ads-screen-zero-instead-of-no-data` | Вывод из списка | `            {moneyMaybe(report.costs.ads)}` | — | подставить на экране ноль вместо пустой суммы рекламы | было своё: «месяц без выгрузки рекламы: сумма рекламы — слова, а не ноль» | карточки «Затраты» нет (договор, «Что уходит с экрана»; задача 8), проверка «месяц без выгрузки рекламы: сумма рекламы — слова…» удалена под учёт задачи 1. Пустую сумму рекламы словами на экране сторожит слом `waterfall-ads-zero-on-screen` этого списка |
| s11-content | `waterfall-real-path-other-month` | Перевод | `    const waterfallResult = await client.query(MONTH_WATERFALL, [dayParam])` | та же | спросить водопад про другой месяц, чем итоги, — изъян, который видит только настоящий путь | было своё: «отчёт несёт водопад из девяти ступеней, прочитанный тем же снимком»; заодно: «отчёт несёт полосу показателей: значения — те же, что в итогах и в водопаде»; стало своё: «отчёт несёт водопад из семи ступеней, прочитанный тем же снимком» | цитата та же. Своя проверка переименована под учёт задачи 1 («…девяти…» → «…семи…»). Заодно снята «отчёт несёт полосу показателей…»: после задачи 6 она водопада не читает |
| s11-content | `kpis-real-path-other-month` | Без правки: цитата на прежнем механизме | `    const deltasResult = await client.query(MONTH_DELTAS, [dayParam])` | та же | спросить полосу показателей про другой месяц, чем итоги | прежнее — своё: «отчёт несёт полосу показателей: значения — те же, что в итогах и в водопаде»; заодно: «появился предыдущий месяц — дельты считаются; исчез — снова слова» | цитата — чтение полосы отчётом; своя проверка ждёт значения итогов марта и `prevMonth` «2026-02». Дописанная задачей 6 «показателей в полосе три…» ключей не теряет и зелена |
| s11-content | `frame-guard-off-for-stale-numbers` | Перевод | `  { файл: '04-chisla-otstali.png', состояние: 'chisla-otstali', тёмная: false, ширина: 1280, высота: 1300, кадр: true },` | `  { файл: '04-chisla-otstali.png', состояние: 'chisla-otstali', тёмная: false, ширина: 1280, высота: 2000, кадр: true },` | снять сторож кадра у снимка «числа отстали» — вернуть его к состоянию до круга проверки кода 3 | прежнее — своё: «опись картинок: сторож кадра стоит у всех снимков «Главного»» | сторож кадра у снимка «числа отстали» жив; задача 16 подняла высоту снимка с 1300 до 2000 — цитата переведена, замена прежняя |

Строк по графам (вывод сборки таблицы): «Перевод» — 29, «Перевод: только имя проверки заодно» — 15,
«Без правки цитаты: объявлена проверка куска S13 заодно» — 6, «Без правки: цитата на прежнем механизме» — 17,
«Вывод из списка» — 47; всего — 114. Сломов в списке до и после: `s11-content` 149 → 102.
