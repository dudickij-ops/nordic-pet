# S11 — содержание экрана: стало лучше или хуже

> Контракт. Кода по нему не написано ни строки: он существует, чтобы владелец увидел развилки
> до работы, а не после неё.
>
> Голова, от которой считается кусок: **`feat/s10-fluent`, `6f301cc`**, а не `main`. Почему —
> первый раздел.
>
> Снимок «до»: `npm test` — **61 файл, 778 проверок, код возврата 0** (снято на `6f301cc`).

## Открытый вопрос, который надо закрыть до первой строки кода: S10 не влит

Ты сказал, что прошлый кусок принят. Вид — да. **Ветка при этом не влита, и шесть её задач не
сделаны.** Это надо назвать до начала, а не обнаружить на приёмке.

| Что | Состояние |
|---|---|
| `main` | `d979e62` — оформления по Fluent 2 в ней нет ни строки |
| `feat/s10-fluent` | `6f301cc`, выложена на GitHub |
| Задача 12 — доказательства, что числа не поехали | не сделана |
| Задача 13 — семь списков сломов на голове слияния | не сделана |
| Задача 14 — отчёт и `README.md` корня по списку из шести мест | не сделана |
| Задача 15 — живой прогон | не сделана |
| Задача 16 — проход целиком в бою | не сделана |
| Задача 17 — pull request и проверка кода | не сделана |

**Рекомендую: доделать и влить S10, и только потом начинать код S11 — от `main`.** Довод не
организационный, а по существу: S11 правит перепись экрана и ту самую разметку, о которой S10
обязан доказать «ничего не поехало». Если S11 начнётся раньше, рецензент S10 получит дифф,
которого больше нет, а доказательства S10 смешаются с правками S11 и перестанут доказывать что бы
то ни было. Это ровно та ошибка, за которую отдельно ругались: проверка выглядит пройденной, хотя
ничего не проверила.

Пока это не решено, контракт считается от головы ветки S10 — иначе его числа не про тот код,
который будет править кусок.

## Цель

Экран сегодня отвечает на вопрос «сколько». Кусок добавляет ответ на вопрос **«стало лучше или
хуже»**. Меняется то, **что** показано; то, **как** показано, не переделывается вовсе — палитра,
шрифт, карточки, отступы и состояния остаются такими, как их принял владелец.

## Жёсткая граница

| Что не открывается | Как это будет доказано |
|---|---|
| Пять сверенных владельцем итогов: оборот 18 764,00 €, чистая выручка 17 277,04 €, прибыль 1 738,53 €, маржа 10,1 %, окупаемость по обороту 4,23 × | живой заход «после», побайтное сравнение с файлом «до» |
| Все принятые проверки, кроме переписи экрана | дифф по `__tests__/` состоит **только из добавленных строк**, кроме одного файла переписи; число выводится командой |
| Значение и место прежних колонок `MONTH_TOTALS` и `MONTH_ITEMS` | новые колонки добавляются поверх, прежние не трогаются; принятые проверки слоя метрик остаются зелёными без единой правки |
| Вид: величины Fluent 2, палитра, шрифт, скругления, тени | проверка чисел таблицы стилей остаётся зелёной; новые элементы берут те же токены |
| `supabase/migrations/` | дифф пуст — при выборе А в развилке 5 |

Отдельно: **индиго и Poppins из присланного образца не берутся.** Внешний шрифт запрещён
устройством проекта, а смена акцента отменяет задание заказчика «интерфейс, который принял бы
Microsoft».

## Цена перевода якорей — ноль, и это обязательство, а не надежда

Сломы прежних кусков цитируют разметку побайтно. Я выписал **все** такие цитаты: **28 якорей** в
`app/page.tsx` и `app/refresh-panel.tsx` (ещё шесть якорей в `app/` цитируют действия и страницу
здоровья — их кусок не открывает вовсе).

Кусок берёт обязательство: **новая разметка только дописывается — новыми соседними разделами и
новыми парами внутри существующих списков. Ни один существующий блок не оборачивается и не
переотступается.** Отсюда цена перевода — **0 якорей**.

| Группа якорей | Сколько | Почему переживает |
|---|---|---|
| Итоговый блок: `<dt>Прибыль</dt>`, `<dt>Окупаемость рекламы (по обороту)</dt>` | 2 | две новые окупаемости дописываются парами **после** существующих; ни одна процитированная строка не двигается |
| Блок выручки: пары «Скидки» и «Возвраты» | 3 | не трогаются вовсе |
| Таблица товаров: `{report.items.map((item) => (` | 1 | цитируется только строка перебора; новые колонки дописываются в `<tr>` внутри, отступ строки перебора прежний |
| Блок неполноты: `<ul>`, перебор, адреса | 3 | не трогаются вовсе |
| Полоса доли честности: три якоря | 3 | не трогаются вовсе; водопад берёт свои имена классов, а не эти |
| Отказы, сторож доступа, месяц из адреса, кнопка выхода, журнал причины | 12 | живут в `HomePage`, а `HomePage` не открывается: новые числа приходят полями того же `report` |
| `app/refresh-panel.tsx`: устаревание, «занято», атрибут и класс | 4 | не трогается вовсе |

**Если по ходу работы окажется, что без обёртки не выходит, это находка к владельцу**, а не
решение по дороге: перевод якоря делается вместе с кодом, старая и новая цитата идут отдельным
разделом в тело pull request, а утверждение и ожидание остаются прежними.

## Перепись экрана — правка разрешена, и вот её условия

Перепись (`__tests__/screen/census.test.tsx`) перечисляет весь видимый текст поимённо. Кусок
добавляет текст, значит список ожидаемого меняется. Условия, под которыми это делается:

- правится **только список ожидаемого**. Утверждение (`toEqual` по всему тексту, по порядку) и
  способ снятия (`текстПоПорядку`) не меняются ни на символ;
- в диффе файла не должно быть ни одной правки вне литерала списка и вне раскладки `fixture.ts`;
- **каждый прежний слом обязан по-прежнему красить именно перепись.** Их четыре, и они называются
  здесь заранее:

| Слом | Как относится к переписи |
|---|---|
| `census-blind-to-swap` | перепись — его собственная проверка |
| `labels-swapped` | перепись — его собственная проверка |
| `items-order-reversed` | перепись — его собственная проверка |
| `gaps-truncated` | перепись краснеет заодно, и причина записана в самом сломе |

Доказательство — прогон `npm run breaks -- s9-screen` после правки: у трёх первых «покраснела
своя», у четвёртого перепись по-прежнему в списке заодно. Вывод идёт в тело pull request.

## Главный риск куска и чем он закрывается

Риск один: **новые числа приезжают, а старые незаметно едут вместе с ними.** Пять итогов сверены
владельцем независимо, прямо из источников, и обязаны остаться теми же.

| Доказательство | Что именно показывает |
|---|---|
| Живой заход «после» и побайтное сравнение с `docs/сверка/метрики-2026-03-до.txt` | между заходами не сдвинулось ничего |
| Живая загрузка даёт прежние 4 файла, 279 строк сырья, 186 после свёртки копии | источник прочитан тот же и так же |
| Принятые проверки слоя метрик зелёные без единой правки | прежние колонки запросов не тронуты |
| Перепись экрана: прежние строки стоят на прежних местах, новые дописаны | показанное не переехало |

**Сверку нельзя снимать на посеве, и это сказано вслух.** Местная база держит посев без единого
заказа: вывод команды за март на нём — нули по всем полям, и сравнение нулей с нулями не может
покраснеть ни при каких обстоятельствах. Прецедент уже был на S10 и записан в
`docs/сверка/README.md`. Значит «до» снимается живым заходом, живой заход разрешается отдельным
словом владельца, и после него база возвращается посевом с наблюдением, а не по привычке.

## Что появляется на экране

### 1. Полоса ключевых показателей с дельтами к прошлому месяцу

Четыре показателя: прибыль, маржа, чистая выручка, доля рекламы в обороте. У каждого — значение
за месяц и дельта к предыдущему календарному месяцу. Маржа и доля рекламы — в **процентных
пунктах**; прибыль и выручка — в евро.

Доля рекламы в обороте — новое число: реклама ÷ оборот × 100. **Наш счёт по мартовским цифрам:**
4 431,37 ÷ 18 764,00 = 23,6 %.

### 2. Водопад «куда ушли деньги» — главный элемент куска

Восемь ступеней: оборот → скидки → возвраты → себестоимость → реклама → комиссии → постоянные →
прибыль. Первая и последняя — столбики от нуля, шесть средних — вычитания.

Сходимость — не украшение, а условие: оборот − скидки − возвраты = чистая выручка; чистая выручка
− себестоимость − реклама − комиссии − постоянные = прибыль. Это ровно те же выражения, что уже
стоят в `MONTH_TOTALS`, и второго определения прибыли кусок не заводит.

**Геометрию считает SQL и отдаёт процентами.** Разметка получает готовую долю и ставит её в
величину стиля — тем же приёмом, которым уже живёт полоса доли честности: одно значение читается
один раз и уходит и в текст, и в геометрию.

### 3. Три окупаемости рекламы вместо одной

| Что | Определение | Наш счёт по марту |
|---|---|---|
| По обороту (есть сегодня) | оборот ÷ реклама | 4,23 × |
| По прибыли | прибыль ÷ реклама | 0,39 × |
| Порог безубыточности | см. развилку 4 — от неё зависит число | 1,75 × / 2,80 × / 9,90 × |

Одна окупаемость по обороту — самообман: 4,23 × выглядит отлично при марже 10 %. Порог отвечает
на вопрос, ниже какого значения реклама себя не окупает.

### 4. Две новые колонки в таблице товаров и строка над ней

Колонки: маржа строки в процентах (прибыль строки ÷ чистая выручка строки × 100) и доля строки в
общей прибыли (прибыль строки ÷ прибыль месяца × 100).

Строка над таблицей: сколько артикулов дают 80 % прибыли и сколько работают в минус. Оба числа —
из SQL, готовыми.

### 5. Время последнего разбора источников — строкой под названием

См. развилку 5: того, что просит буква задания, в базе сегодня нет.

## Где считается каждое новое число

Железное правило: **экран не считает.** Ниже — где считается всё новое.

| Число | Где считается | Почему не в разметке |
|---|---|---|
| Дельты четырёх показателей | SQL | деньги в этом проекте живут строкой и ни разу не становятся числом; вычесть их в TypeScript нельзя, не нарушив это |
| Доля рекламы в обороте | SQL | деление |
| Доли восьми ступеней водопада | SQL, процентами | геометрия — это счёт |
| Три окупаемости | SQL | деление |
| Маржа строки и доля строки в прибыли | SQL, по строке товара | деление |
| Сколько артикулов дают 80 % прибыли, сколько в минусе | SQL | накопительная сумма и счёт строк |
| Время последнего разбора | SQL, готовой строкой | форматирование даты — тоже счёт |

Округление остаётся там, где оно и живёт: **один раз, в последнем `select`**. Второго округления
кусок не заводит.

## Правило «нет данных — словами, а не нулём»

Это то же правило, по которому уже живёт блок неполноты: пропущенное не равно нулю.

| Случай | Что на экране |
|---|---|
| Прошлого месяца нет или в нём нет заказов | «нет базы для сравнения» — у всей полосы дельт разом, а не по одной |
| Прошлый месяц есть, но у него нет рекламы | дельта доли рекламы — «нет данных»; остальные дельты считаются |
| Прибыль месяца ноль или отрицательна | доля строки в прибыли — «нет данных»; «80 % прибыли» — «нет данных» |
| Чистая выручка строки ноль | маржа строки — «нет данных» |
| Маржа `null` | порог безубыточности — «нет данных» |

Ноль в этих местах не рисуется никогда. В SQL это уже привычный приём: `nullif` на делителе,
дальше `null` доезжает до экрана и превращается в слова функциями формата.

## Цвет дельты определяется смыслом, а не знаком

У каждого показателя заводится признак «рост — это хорошо»: рост прибыли зелёный, рост доли
рекламы красный, снижение возвратов зелёное. Признак приходит из слоя счёта вместе с числом, а не
выводится разметкой из названия.

**Цветом смысл не кодируется в одиночку**: рядом стоит знак (+ или −) и подпись. Это не
вкусовщина — экран читают и те, кто цвета не различает, и красный на этом экране уже занят
отказами.

## Что обязано уцелеть

Все нынешние показатели до единого; блок честности данных с полосой; блок неполноты со всеми
одиннадцатью строками; пометки устаревания; кнопки обновления и выхода; переключатель периода;
все отказы человеческим текстом; видимый фокус с клавиатуры; чтение экрана при ширине 390
пикселей.

## Развилки

Каждая меняет результат, поэтому решает владелец. У каждой — моя рекомендация.

### Развилка 1. Где считается дельта — в SQL или в слое метрик на TypeScript

- **А (рекомендую). В SQL.** Один запрос берёт месяц и предыдущий, считает обе стороны и отдаёт
  готовые дельты строками. Довод решающий, а не вкусовой: деньги в этом проекте приходят из базы
  строкой и **ни разу не становятся числом** — так записано в слое метрик и так сторожится
  принятыми проверками. Вычитание в TypeScript потребовало бы превратить строку в число, то есть
  сломать то самое правило, ради которого точность и сохраняется.
- Б. В `report.ts`, из двух вызовов `MONTH_TOTALS`. Дешевле по SQL, но заводит второе место, где
  считаются деньги, и первое, где они становятся числом.

### Развилка 2. Округлять ли таблицу товаров до целых евро

Задание называет округление до целых евро в таблице. У него есть цена, и она измерима.

- **А (рекомендую). Не округлять — оставить копейки.** Таблица остаётся такой, какой её принял
  владелец, и ни одна принятая проверка не двигается.
- Б. Округлять, изменив существующие колонки `MONTH_ITEMS`. **Цена названа числом: краснеет
  принятая проверка прошлого куска** — `__tests__/metrics/report.test.ts`, строки 270–271, где
  ожидаются `net: '600.00'` и `net: '100.00'`. Править её ради нового куска запрещено правилом
  проекта; значит вариант Б требует твоего прямого слова.
- В. Округлять, добавив параллельные колонки. Тогда одно и то же число живёт в отчёте дважды в
  двух видах — ровно тот случай, который однажды разъезжается молча.

Если хочется тесноты в таблице, дешевле и честнее сузить её другим способом — но это уже вид, а
вид кусок не переделывает.

### Развилка 3. Печатает ли команда метрик новые числа

`npm run metrics` печатает тот же отчёт в терминал, и его вывод — эталон сверки «до / после».

- **А (рекомендую). В этом куске команду не трогаем.** Тогда побайтное сравнение «до» и «после»
  остаётся настоящим сторожем старых чисел: любое их движение видно сразу. Цена названа: команда
  временно показывает меньше, чем экран, и это отступление уходит строкой в тело pull request и в
  отчёт.
- Б. Дописать новые числа в команду. Тогда побайтное сравнение с файлом «до» невозможно, и его
  придётся заменить сравнением по части вывода — то есть ослабить главный сторож куска ровно там,
  где он нужен.

### Развилка 4. Какой маржой считать порог безубыточности — и это меняет ответ на противоположный

Числа ниже — **наш счёт по сверенным мартовским цифрам**, посчитанный вручную для этого контракта;
в коде его будет считать SQL.

| Вариант | Определение | Порог | Что говорит рядом с 4,23 × |
|---|---|---|---|
| А (рекомендую) | вклад на евро оборота без рекламы и без постоянных: (чистая выручка − себестоимость − комиссии) ÷ оборот = 57,1 %; порог = 1 ÷ 0,571 | **1,75 ×** | реклама окупается с запасом |
| Б | вклад после постоянных: (чистая выручка − себестоимость − комиссии − постоянные) ÷ чистая выручка = 35,7 %; порог = 1 ÷ 0,357 | **2,80 ×** | окупается |
| В | маржа как она есть на экране, 10,1 %; порог = 100 ÷ 10,1 | **9,90 ×** | не окупается |

Разница не косметическая: **А и Б говорят «реклама окупается», В говорит «нет».** Причина в том,
что маржа на экране считается уже **после** вычета рекламы, и порог, построенный на ней, вычитает
рекламу дважды.

Рекомендую А: это единственный вариант, сопоставимый с числом, которое стоит рядом (окупаемость по
обороту тоже считается от оборота), и единственный, который отвечает на вопрос «окупится ли
следующий евро рекламы» — постоянные расходы от рекламы не зависят и в предельный счёт не входят.
Определение и вывод помечаются нашими: в справочниках Fluent их нет, это наша арифметика.

### Развилка 5. Что показывать как «время последнего разбора источников»

Того, что просит буква задания, в базе сегодня нет. Есть `meta.fact_freshness.raw_seen_at` — **не
время разбора, а время самого свежего сырья, по которому собраны факты**. Разбор, прогнанный
дважды подряд без изменений в источнике, эту отметку не двигает.

- **А (рекомендую). Показывать то, что есть, и назвать это верно:** «источники прочитаны по
  состоянию на …». Ни миграции, ни новых полей; строка отвечает на более полезный вопрос — насколько
  свежи сами данные, — и не врёт. Отступление от буквы задания названо здесь и уйдёт в тело pull
  request.
- Б. Завести колонку `parsed_at` миграцией. Тогда подпись совпадает с буквой задания, но кусок
  тянет за собой миграцию, а с ней — **боевой накат, который делается только с твоего прямого
  слова** и ставится последней задачей куска, после доказательства на местной базе.

Мелочь при любом варианте: часового пояса в проекте нет нигде. Предлагаю печатать `ГГГГ-ММ-ДД
ЧЧ:ММ` по UTC и назвать пояс прямо в строке — иначе время будет читаться как местное и однажды
собьёт с толку.

### Развилка 6. Где на экране стоит водопад

- **А (рекомендую). Сразу после полосы показателей, до «Выручки».** Порядок чтения получается
  «что изменилось → куда ушли деньги → подробности», и главный элемент куска стоит там, где на него
  смотрят первым.
- Б. После «Итога». Тогда читатель сначала видит слагаемые, потом их сумму — но именно это
  складывание в уме кусок и должен отменить.

### Развилка 7. С каким месяцем сравнивать

- **А (рекомендую). С предыдущим календарным.** Нет заказов в предыдущем — «нет базы для
  сравнения», и никакого молчаливого перескока.
- Б. С предыдущим месяцем, в котором есть заказы. Тогда для февраля сравнение уедет на декабрь, а
  на экране это будет выглядеть как сравнение с январём. Молчаливый перескок — худший вид лжи на
  этом экране.

Оговорка, которая проверяется только на живых данных: у января заказов нет (`hasOrders` ложно), у
февраля есть. Значит для марта база для сравнения существует, а для февраля — нет, и путь «нет
базы» увидит владелец сразу, не выдумывая случай.

## Чего не делаем

Кольцевых диаграмм, спидометров, тепловых карт; иконок, лап и эмодзи; второго шрифта; анимации
появления; переименования метрик и текста кнопок. Дельт по товарам к прошлому месяцу тоже нет —
задание их не просит, а это ещё один запрос и ещё две колонки.

## Пути, ни разу не встреченные в настоящих данных

Список ведётся с прошлых кусков: владелец смотрит на приёмке именно сюда.

| Путь | Почему опасен |
|---|---|
| Прошлый месяц без рекламы | дельта доли рекламы уходит в «нет данных» — надо увидеть, что не в ноль |
| Прибыль месяца ноль или отрицательна | доля строки в прибыли и «80 % прибыли» уходят в слова; на мартовских данных этого не увидеть |
| Артикул с отрицательной прибылью | счётчик «работают в минус» на марте покажет ноль: все двенадцать в плюсе |
| Маржа `null` | порог безубыточности уходит в слова |
| Месяц без предыдущего вовсе (самый ранний в списке) | вся полоса дельт уходит в «нет базы для сравнения» |

Ни ключей, ни паролей, ни новых переменных среды кусок не заводит. Миграции — только по варианту Б
развилки 5, и тогда боевой накат идёт последней задачей и с твоего прямого слова.

---

## Дополнение от 10 сентября 2026 года — изменение принятого решения

> Записывается отдельным разделом, а не правкой текста выше: принятый контракт остаётся таким,
> каким его приняли. **Причина:** владелец посмотрел предпросмотр S10 и попросил три вещи.
> **Решения по семи развилкам выше в силе все до одной.**

### Что добавляется

| № | Что | Зачем, словами владельца по смыслу |
|---|---|---|
| 1 | Подсветка убыточных строк в таблице товаров | второй вопрос экрана после «сколько заработали» — «какие товары тянут вниз»; сегодня на него отвечает только чтение колонки глазами |
| 2 | Столбики прибыли по месяцам | видно движение по месяцам, не открывая каждый |
| 3 | Полоса быстрого перехода по разделам | экран после S11 — около трёх экранов прокрутки; на двух это было бы украшением, поэтому раньше и не делали |

Общее для всех трёх: вид — из тех же токенов Fluent 2; проверка чисел таблицы стилей остаётся
зелёной **без правки её исключений**; каждое новое число считает SQL; где данных нет — слова, а не
ноль; у каждого механизма свой слом.

### Цена перевода якорей — пересчитана: по-прежнему ноль, и вот чем это доказано

Пересчитано командой, а не по памяти: те же **28 якорей** в разметке отчёта, и для каждого
проверено, цитирует ли он хоть что-то из того, что трогают три добавления, — открывающие теги
разделов, заголовки, ссылки месяцев, строки таблицы товаров.

**Задевают трое. Переводится ноль.**

| Якорь | Что цитирует | Почему переживает |
|---|---|---|
| `nav-links-drop-month` (S5) | `href={`/?m=${m.month}`}` | столбик вкладывается внутрь ссылки месяца **после** текста месяца; строка с `href` не меняется ни на символ |
| `items-order-reversed` (S9) | `{report.items.map((item) => (` с отступом в двенадцать пробелов | признак убыточности дописывается атрибутом к `<tr>` внутри перебора; строка перебора и её отступ прежние |
| `gaps-only-when-nonzero` (S5) | `<ul>` блока неполноты и строку перебора под ним | якорная полоса заводит **свой** список; `id` получает раздел, а не этот `<ul>`. Цитата из двух строк остаётся единственной в файле |

`id` для якорной полосы ставится на открывающие теги `<section>`. **Ни один из 28 якорей не
цитирует ни одного открывающего тега раздела** — проверено тем же перебором.

**Условие, без которого ноль не держится: единственность цитаты.** Прибор сломов требует, чтобы
каждая цитата встречалась в файле ровно один раз. Новая разметка обязана не повторить ни одной
из 28 цитат — иначе слом становится двусмысленным, даже если исходная строка не тронута.
Доказательство — прогон всех прежних списков на голове слияния: неединственная цитата там
отказывает сама.

Если по ходу работы окажется, что без обёртки или переотступа не выходит, — **находка к
владельцу**, а не решение по дороге.

### Ограничение, найденное разведкой дополнения и касающееся всего куска

**Новые поля отчёта обязаны быть необязательными.** Проверка типов покрывает весь проект вместе
с `__tests__/` (`tsconfig.json`: `**/*.ts`, `**/*.tsx`), а принятые проверки прошлых кусков
собирают отчёт руками — без дельт, водопада, долей и столбиков. Обязательное новое поле уронило бы
`npm run build` на их раскладках, и починить это можно было бы только правкой принятых проверок,
что запрещено. Прецедент уже есть и работает: `устарели?: boolean` в `lib/metrics/report.ts`
заведено необязательным ровно по этой причине. Отсутствие поля экран читает как «сказать нечего» и
не рисует блок вовсе.

Это не отступление, а условие, которое план S11 до сих пор не называл, — хотя касается оно не
только дополнения, а всех новых полей куска.

### Развилки дополнения

#### Развилка Д1. Какую строку считать убыточной

- **А (рекомендую). Прибыль строки строго меньше нуля** — то же определение, что у счётчика
  «работают в минус» над таблицей. Признак приходит из SQL одной колонкой, и подсветка со счётчиком
  берут его оба: они не могут разойтись.
- Б. Маржа строки ниже маржи месяца. **Сравнивает несравнимое:** прибыль строки в таблице — это
  чистая выручка минус себестоимость, а маржа месяца считается уже после рекламы, комиссий и
  постоянных. Строки идут около 60 %, месяц — 10,1 %, и подсвечено не будет ничего по другой,
  ложной причине.
- В. Маржа строки ниже медианы по таблице. Подсвечивает половину строк всегда — это украшение, а не
  ответ на вопрос.

**Цена рекомендации названа заранее: на мартовских данных не подсвечено ничего.** Все двенадцать
артикулов в плюсе. Путь «строка в минусе» ни разу не встречен в настоящих данных, и проверяется он
выдуманной раскладкой — это сказано вслух, а не выяснится на приёмке.

Условия подсветки владельца переносятся в задачу дословно: цвет — не единственный признак, рядом
стоит само число маржи, по которому строка подсвечена; красный — смысловой и не спорит с красным
отказов; контраст текста на подсветке замеряется числами в обеих темах, крупный и мелкий.

#### Развилка Д2. Где живут столбики прибыли

- **А (рекомендую, и это совет владельца). Внутри кнопок переключателя месяцев.** Нового видимого
  текста нет, перепись экрана этой части не касается, отдельного места на экране элемент не
  занимает.
- Б. Отдельным блоком с подписями: месяц и прибыль числом. Перепись правится, экран растёт.

**Цена рекомендации — названа, а не спрятана:**

| Цена | Что это значит |
|---|---|
| У столбика нет числа | высота говорит «больше или меньше соседних», а не «сколько». Точное число — одним нажатием: та же кнопка открывает отчёт месяца |
| Столбик скрыт от читалок экрана | он декоративен (`aria-hidden`); имя ссылки остаётся только месяцем. Кто не видит экран, движения по месяцам не узнает |
| Кнопка месяца вырастает по высоте на постоянную величину | отрицательной прибыли нужно место **под** линией отсчёта, положительной — над ней |

Если нужно число рядом со столбиком — это вариант Б, и тогда перепись правится.

#### Развилка Д3. Месяц без заказов — рисовать ли ему столбик

Этот путь **встречен в настоящих данных**: у января заказов нет (`hasOrders` ложно), а в списке
месяцев он есть — значит у него есть расходы. Его прибыль по формуле — минус все его затраты.

- **А (рекомендую). Без столбика.** Переключатель уже помечает такой месяц пустым; столбик из одних
  затрат выглядел бы как катастрофический месяц, хотя вероятнее всего это месяц, продажи которого
  не загружены. Пустота здесь честнее числа.
- Б. Рисовать отрицательный столбик из затрат. Арифметически верно, по смыслу — вводит в
  заблуждение ровно на том месяце, который виден на экране сегодня.

Отрицательная прибыль месяца **с заказами** показывается как отрицательная — столбиком под линией
отсчёта, а не нулём и не пустотой. Это путь, ни разу не встреченный в настоящих данных.

### Как считается прибыль по месяцам — без второго определения прибыли

Отдельного выражения прибыли кусок не заводит — это уже обещано для водопада и держится здесь так
же. Прибыль всех месяцев и доли их столбиков считает **один** запрос, который берёт выражения
`MONTH_TOTALS` без переписывания. Долю каждого столбика — прибыль месяца, делённую на наибольшую по
модулю прибыль среди месяцев с заказами, — считает этот же запрос, в процентах со знаком.

**Якорь снаружи у этой сверки один и уже есть:** прибыль столбика марта обязана совпасть с прибылью,
которую отчёт печатает за март, а та сверена с независимым счётом владельца — 1 738,53 €. Второе
определение прибыли, если оно появится, разъедется с первым именно здесь.

### Якорная полоса — условия, которые сторожат принятые проверки

| Условие | Кто сторожит |
|---|---|
| На странице входа ссылок нет ни одной | принятая проверка S6, `__tests__/auth/login.test.tsx`: запрещён любой `<a` |
| На странице отчёта нет ссылки на корень | принятая проверка S5, `__tests__/metrics/screen-month.test.tsx`: запрещено `href="/"`. Якорные ссылки `#…` под этот запрет не подпадают и обязаны его не задеть |
| Полоса проходится клавиатурой, фокус на ней виден | существующее правило `:focus-visible` — **слома у него нет, и это названо ещё в S10** |
| Плавность подматывания отключается при `prefers-reduced-motion` | правило таблицы стилей; проверка текста, и названа слабой |

Полоса живёт только в `Dashboard`: страница входа и страницы отказов её не получают по устройству,
потому что `Dashboard` на них не рисуется.

---

## Поправка от 10 сентября 2026 года к дополнению — столбики по месяцам отменены

> Отдельным разделом. **Причина:** владелец принял развилку Д2 ошибочно — не передал договорённость.
> Столбики прибыли по месяцам **не делаются**; вместо них — динамика по дням за выбранный месяц.
> Довод владельца: на вопрос «лучше или хуже прошлого месяца» уже отвечает полоса дельт, а кривая
> внутри месяца отвечает на вопрос, которого не отвечает ничто, — как шёл месяц, был ли провал и
> когда. Разделы Д2 и Д3 выше остаются как были — как след решения; действует этот.

### Проверка довода владельца — сделана, а не принята на веру

**«Пользы больше» — верно, но по другой причине, чем сказано.** Столбики по месяцам не повторяли
бы полосу дельт буквально: дельта сравнивает с одним прошлым месяцем, столбики показали бы все.
Но месяцев с заказами в настоящих данных два — февраль и март, январь пуст. Два столбика почти
ничего не добавляют к одной дельте. Суточный ряд за март — около тридцати точек настоящих данных.

**«Примерно та же работа» — верно только для одного из трёх вариантов того, что рисовать.** См.
развилку Д2′-а: прибыль по дням сделать честно нельзя вовсе, а вклад по дням заметно дороже.

### Развилка Д2′-а. Что рисует суточный ряд

| Вариант | Что это | Цена и честность |
|---|---|---|
| **А (рекомендую). Чистая выручка по дням** | готовая цепочка `money` из `MONEY_CTES`, сгруппированная по дню заказа | один дешёвый запрос, второго определения денег нет. **Якорь снаружи:** сумма ряда обязана дать чистую выручку месяца — 17 277,04 €, сверенные с независимым счётом владельца |
| Б. Вклад по дням | выручка − себестоимость − реклама − комиссии | у всех слагаемых есть день, но правило комиссий живёт в `MONTH_TOTALS`, а не в общей цепочке: его пришлось бы повторить — второе определение. Дни без заказов несут рекламу и дают отрицательные столбики по неясной причине |
| В. Прибыль по дням | как в итоге | **невозможна без выдуманного правила.** Постоянные расходы лежат в базе только помесячно — у `fact.opex` есть `month` и нет дня. Разнести 4 552,90 € по дням — число без источника |

### Развилка Д2′-б. День без заказов внутри месяца: ноль или разрыв

При варианте А у дня без заказов нет ни одной строки выручки.

- **А (рекомендую). Разрыв.** Столбика нет, а линия отсчёта под этим днём идёт **пунктиром** —
  тем же знаком «данных нет», которым переключатель месяцев уже помечает январь и который владелец
  принял. Доступная подпись дня — «заказов не было», а не «0,00 €». Ноль и разрыв различаются
  формой, а не цветом.
- Б. Ноль. Кривая утверждала бы «в этот день продано на ноль», а мы не можем отличить день без
  продаж от дня, продажи которого не загружены. Молчаливый ноль у нас запрещён.

Нулевой столбик при этом возможен и честен: день, в который заказы были, но всё вернули, даёт
чистую выручку ноль. На мартовских данных такой день не встречен.

### Д3 — переформулировано владельцем

Месяц без заказов: **блока нет вовсе, на его месте слова «нет данных за месяц»**, а не пустая рамка.
Путь встречен в настоящих данных: январь.

### Цена — числом, и сравнение с прежним Д2

| | Прежний Д2: столбики в кнопках месяцев | Суточный ряд чистой выручки |
|---|---|---|
| Новых запросов | 1, дорогой: выражения `MONTH_TOTALS` подставляются по каждому месяцу | 1, дешёвый: готовая цепочка `money`, сгруппированная по дню |
| Новых блоков разметки | 0 | **1** |
| Строк переписи экрана | 0 | **5**: заголовок блока, наибольшее значение и ноль по вертикали, первый и последний день по горизонтали |
| Якорей переводится | 0 | **0** — пересчитано ниже |
| Доступных подписей | до 12 | до 31 |
| Развилок | 1 | 2 |
| Строк в таблицах сломов | 10 | 10 — посчитано по таблицам плана командой |

**Итог сравнения: не заметно дороже.** Запрос дешевле, разметка и перепись дороже — на один блок и
пять строк переписи. Это верно для варианта А; вариант Б дороже заметно.

### Цена якорей — пересчитана для нового блока: ноль

Новый блок встаёт соседним разделом между полосой показателей с водопадом и «Выручкой». Из 28
якорей границу раздела, шапки или отчёта цитирует **один** — `bad-month-crashes-page` (S5), и он
цитирует `<main>` ветки отказа в `HomePage`, а не `<main className="report">` отчёта. Новый блок в
ветку отказа не попадает. **Переводится ноль**, условие единственности цитат прежнее.

### Что остаётся от прежнего дополнения без изменений

Д1 — строго отрицательная прибыль строки. Полоса быстрого перехода. Принцип, который владелец
поставил к столбикам и который переходит на суточный ряд целиком: у каждого графического элемента,
несущего число, есть доступная подпись с той же готовой строкой из слоя счёта; отсутствие данных
в такой подписи — слова, а не ноль.

---

## Решения владельца по поправке — 10 сентября 2026 года

> Отдельным разделом. Одно из решений меняет принятое определение контракта — долю строки в
> прибыли, — и владелец назвал это ошибкой своего решения.

| Вопрос | Решение | Условие владельца |
|---|---|---|
| Д2′-а — что рисует суточный ряд | **Чистая выручка по дням** | Блок называется выручкой и только выручкой. Человек, увидевший кривую на экране про прибыль, прочтёт её как прибыль по дням, если подпись позволит, — подпись обязана не позволить |
| Д2′-б — день без заказов | **Разрыв, линия отсчёта пунктиром** | Доступная подпись — «заказов не было» |
| Делитель доли строки в прибыли | **Сумма прибыли строк** — меняет определение в разделе «Что появляется на экране», пункт 4 | База названа **в самой подписи**: доля считается от суммы прибыли строк, 11 248,93 €, а не от прибыли месяца, 1 738,53 €. Человек, поделивший одно на другое сам, обязан увидеть из подписи, почему у него не сходится. Строка над таблицей про 80 % считается от той же базы и говорит это же |
| Отметка строк, дающих 80 % прибыли | **Не делается** | На настоящих данных она отмечает восемь строк из двенадцати и говорит «важно почти всё» — второй способ подсветки в одной таблице при таком результате шум, а не внимание к нужному. Строка словами над таблицей остаётся |

**Прежнее определение, дословно, для следа:** «доля строки в общей прибыли (прибыль строки ÷
прибыль месяца × 100)». При таком делителе доли двенадцати мартовских строк давали бы в сумме
647 %. Действующее: прибыль строки ÷ сумма прибыли строк × 100 — ровно 100 % в сумме.

---

## Макет и вкладки — 10 сентября 2026 года

> Отдельным разделом: всё выше остаётся как было, как след решений. **Причина:** владелец прислал
> макет экрана и решил развилку «вкладки или полоса быстрого перехода» — **вкладки**. Полоса
> быстрого перехода из дополнения **отменена**. Решения по семи развилкам контракта, по дополнению
> и по поправке в силе все, кроме двух мест, названных ниже: цена якорей и место окупаемости.
>
> **Макет прислан картинкой в переписке, в репозитории его нет.** Рецензент без моего контекста его
> не увидит, пока файл не лежит в ветке. Всё, что ниже сказано о макете, — **моё чтение картинки**,
> а не свойство файла, который можно открыть.

### Что в макете обязательно — и где это у нас

Обязательны, словами владельца: раскладка, порядок блоков и расстановка смысловых ударений.

| Что обязательно | Где у нас |
|---|---|
| Полоса показателей с дельтами наверху | вкладка «Главное», первым блоком — задача 8 |
| Водопад «куда ушли деньги» | вкладка «Главное», вторым — задача 9 |
| Динамика по дням | вкладка «Главное», третьим — суточный ряд чистой выручки, задача Д-8 |
| «Выручка» и «Затраты» рядом | вкладка «Деньги»: две колонки на широком экране, одна под другой на узком |
| Окупаемость отдельным блоком | вкладка «Деньги», свой блок после «Итога» — развилка В7 |
| Строка времени чтения источников в шапке | шапка, общая для всех вкладок — задача 12 |
| Итоги водопада выделены, вычитания — нет | оборот, чистая выручка и прибыль — столбики от нуля, вычитания висят — развилка В6 |

### Что из макета не берётся и почему

| На макете | Решение | Почему |
|---|---|---|
| Числа | не берутся | выдуманы; на экране только то, что отдаёт слой счёта |
| Индиговый акцент; прибыль и чистая выручка покрашены им | **не берётся без слова владельца** — развилка В0 | смысловой цвет у нас — Fluent 2, `#0f6cbd`, с якорем на источник |
| Пустой график динамики | дефект, не повторяется | ряд строится из данных; месяц без заказов — слова «нет данных за месяц» (решение Д3) |
| Подпись «Себест.» наезжает на число столбика | дефект, не повторяется | подписи ступеней стоят своей строкой вне области столбиков — задача В-8 плана |
| Вычитания водопада нарисованы от нуля вниз | не берётся | **наше чтение картинки:** это столбики со знаком, а не водопад — не видно, от какого остатка вычитается ступень. Принятый контракт: вычитания висят от текущего остатка |
| Вторая линия «Прибыль» на графике динамики | не берётся | **прибыль по дням не рисуется: у постоянных расходов в базе есть только месяц, дня нет, и разнести 4 552,90 € по дням значило бы выдумать число** (развилка Д2′-а, вариант В). Отсюда же решение владельца по поправке: блок — выручка и только выручка, слова «прибыль» в нём нет. Взять вторую линию только потому, что она нарисована на макете, — ровно тот случай, от которого предостерегает подпись к макету |
| База долей: «Доля рекламы 16 %», полоски затрат 38 / 16 / 6 / 8 % | не берётся | **наш счёт по числам самого макета:** это доли от **чистой выручки** (43 869,71 ÷ 115 446,61 = 38,0 %), а плашка «35,2 % оборота» над водопадом — доля той же себестоимости от **оборота** (43 869,71 ÷ 124 805,40 = 35,2 %). Макет делит одну статью на две базы. У нас база одна — **оборот**: так сказал владелец («доля статьи в обороте») и так уже стоит в контракте (доля рекламы 23,6 %) |
| Дельты денег в процентах («↗ 14,8 %») | не берётся | принятое решение: прибыль и выручка — в евро, маржа и доля рекламы — в процентных пунктах |
| «было 32 240,16 €» под каждой дельтой | не берётся | четыре новых числа сверх плана. Цена, если нужно: 4 необязательных поля (запрос задачи 1 их уже считает), 4 строки переписи, 0 якорей |
| Плашка «Съедает больше всего: себестоимость · 35,2 % оборота», оранжевый столбик | не берётся | повторяет самую высокую ступень водопада; оранжевый — второй сигнальный цвет, которого в сдержанной палитре нет, а красный занят отказами. Цена, если нужно: 1 признак из SQL, 1 строка переписи, без цвета |
| «Итог месяца» и «+4 758,48 € к прошлому месяцу» | не берётся | повтор дельты прибыли из полосы показателей |
| Надстрочные подписи «Структура месяца», «Эффективность вложений», «Сентябрь 2026 · календарный месяц» | не берётся | украшение, и каждая — строка переписи. Диапазон дней над рядом у нас уже есть: первый и последний день оси |
| Значки ⓘ у блоков | не берётся | «Чего не делаем»: иконок нет; подсказка по наведению — клиентский код |
| Плитка «NP» и подзаголовок «Прибыль и экономика» | не берётся | знак и переименование; название экрана не меняется |
| Переключатель месяца стрелками «‹ Сентябрь 2026 ›» | не берётся | принятый список месяцев сторожат проверки S5, и он показывает пунктиром месяц без заказов — стрелки это спрятали бы |
| «сегодня, 10:14» | не берётся | решение по развилке 5: `ГГГГ-ММ-ДД ЧЧ:ММ` по UTC с названным поясом. «Сегодня» требует знать часы и пояс читателя — это счёт на экране |
| Скидки и возвраты со знаком минус | не берётся | в отчёте они положительны; минус поставила бы разметка, а принятые проверки ждут число как есть |
| Короткие подписи «Себестоимость», «Комиссии» | не берётся | метрики не переименовываются |
| Шесть вкладок, среди них «Динамика» и «Экономика» | не берётся | четыре — раздел «Сколько вкладок» |
| Весь экран нарисован под выбранной вкладкой «Деньги» | не берётся | **наше чтение:** макет не показывает, что на какой вкладке; разбиение ниже выведено из данных |

### Сверх принятого плана — берётся отдельной задачей: доли статей затрат

Просьба владельца: у каждой статьи затрат — её доля в обороте, числом и полоской. Считает SQL и
отдаёт готовыми процентами; разметка только рисует — приём полосы доли честности.

**Эти доли у нас уже есть — это доли ступеней водопада.** Задача 2 считает долю каждой ступени от
оборота; себестоимость, реклама, комиссии и постоянные — четыре из этих ступеней. Второго выражения
доли кусок не заводит: блок затрат берёт **те же готовые значения**, что и водопад, и каждое
уходит один раз и в текст, и в длину полоски.

**Наш счёт по сверенным мартовским цифрам**, от оборота 18 764,00 €: себестоимость 32,1 %, реклама
23,6 %, комиссии 2,8 %, постоянные 24,3 %.

| Цена | Сколько |
|---|---|
| Новых запросов | 0 — доли ступеней водопада |
| Новых полей отчёта | 0 сверх полей водопада |
| Строк переписи | 4 — на вкладке «Деньги» |
| Якорей переводится | 0 — ни один из 28 не цитирует блок затрат (проверено перебором цитат) |
| Пар контраста для замера | 2 в каждой теме: заполнение полоски к дорожке (не текст, 3 : 1) и число доли к фону (мелкий текст, 4,5 : 1) |
| Строк в таблице сломов | 4 |

**Якорь снаружи у этой доли уже есть:** доля рекламы в полосе показателей (задача 1) и доля ступени
рекламы (задача 2) — два запроса, считающих одно и то же. Проверка утверждает их равенство, а
мартовское значение обязано совпасть с нашим счётом по сверенным владельцем числам: 4 431,37 ÷
18 764,00 = 23,6 %.

### Вкладки: пять условий владельца и чем каждое исполняется

| Условие | Чем исполняется |
|---|---|
| 1. Вкладка — ссылка с признаком в адресе; сервер отдаёт запрошенную | адрес вида `/?m=2026-03&tab=dengi`. Страница читает `tab` так же, как `m`, и рисует разделы только этой вкладки. Полоса вкладок — обычные ссылки в `<nav>`: ни клиентского кода, ни зависимостей; доказательство — пустой дифф `package.json` и `package-lock.json` |
| 2. Первая вкладка отвечает за десять секунд | «Главное»: полоса показателей, водопад, суточный ряд. Адрес без `tab` — это она |
| 3. Шапка общая | название, месяц, строка времени, «Обновить данные», «Выйти» и пометка устаревания стоят вне переключаемых разделов и рисуются на каждой вкладке. По виду шапка общая уже с S10 — снимок `01` |
| 4. Перепись — свой полный список у каждой вкладки | раздел «Перепись экрана по вкладкам» |
| 5. Цена якорей — заново | **2 из 28**, раздел «Цена» |

**Разметка: ссылки с признаком текущей, а не роли вкладок.** Образец вкладок в руководстве WAI-ARIA
(APG, Tabs Pattern, раздел Keyboard Interaction) даёт вкладкам переход стрелками. Дословно:
«Left Arrow: moves focus to the previous tab.» Условие, под которым строка стоит там (пересказ, не
цитата): фокус находится на вкладке в горизонтальном списке вкладок.
Стрелки требуют клиентского кода, а условие 1 его запрещает. **Наш вывод:** роль `tab` обещала бы
поведение, которого нет, поэтому полоса — `<nav>` со ссылками, у текущей `aria-current="page"`
(MDN, `aria-current`, значение `page` — текущая страница в наборе страниц; пересказ). Цена названа:
каждая вкладка — своя остановка клавиши Tab, четыре вместо одной со стрелками.

**Вид текущей — рецепт вкладки Fluent 2, сверяемый целиком по свойствам.** Прочитано в
`microsoft/fluentui`, файл
`packages/react-components/react-tabs/library/src/components/Tab/useTabStyles.styles.ts` на ветке
`master` (последний коммит файла — `d2375b2`, 1 июня 2026). **В задаче перечитывается на коммите
выписки S10 `43665d5`**, иначе величины придут не из той версии. Что там стоит, выписками из кода:

| Свойство | В исходнике | Что даёт |
|---|---|---|
| Текст выбранной | `selected: { ...typographyStyles.body1Strong }`, у прочих `body1` | насыщенность — не цвет |
| Указатель выбранной | `'::after'`: `backgroundColor: tokens.colorCompoundBrandStroke`, `borderRadius: tokens.borderRadiusCircular` | полоса — форма |
| Геометрия указателя, средний размер, горизонтально | `bottom: '0'`, `height: tokens.strokeWidthThicker`, `left` и `right`: `tokens.spacingHorizontalM` | где и какой толщины полоса |
| Цвет текста | прочие — `colorNeutralForeground2`, выбранная — `colorNeutralForeground1` | |
| Режим принудительных цветов | указатель — `'ButtonText'` | указатель виден и там |

Итого у текущей вкладки **три признака помимо цвета**: `aria-current` в разметке, полоса под
текстом, полужирное начертание.

Двух величин рецепта в нашей таблице стилей нет: `colorCompoundBrandStroke` и `strokeWidthThicker`
(поиск по объявлениям — 0 вхождений у каждой). Просьба П3.

### Сколько вкладок — от данных, а не от макета

Что несёт каждый блок после куска, на мартовских данных:

| Блок | Что несёт |
|---|---|
| Полоса показателей | 4 значения и 4 дельты |
| Водопад | 8 ступеней; 9 при варианте А развилки В6 |
| Суточный ряд | 31 день марта, у каждого доступная подпись; 5 видимых строк осей |
| Выручка | 4 числа |
| Затраты | 4 числа и 4 доли с полосками |
| Итог | 2 числа — прибыль и маржа; окупаемость уходит в свой блок (В7) |
| Окупаемость | 4 числа: по обороту, по прибыли, вклад, порог |
| Товары | 12 строк по 7 колонок и строка над таблицей |
| Честность данных | доля с полосой и список артикулов без цены — на марте 2 |
| Неполнота данных | 11 строк с адресами |

**Рекомендую четыре вкладки:**

| Вкладка | Блоки | Чем держится как вкладка |
|---|---|---|
| **Главное** | полоса показателей, водопад, суточный ряд | условие 2 владельца |
| **Деньги** | «Выручка» и «Затраты» рядом, «Итог», «Окупаемость» | 18 чисел одного рассказа: откуда выручка, куда ушли затраты, что осталось, окупается ли реклама |
| **Товары** | таблица и строка над ней | 84 клетки — самый объёмный блок и одна тема |
| **Качество данных** | «Честность данных», «Неполнота данных» | обе о том, насколько верить числам остальных вкладок |

Порядок разделов в отчёте совпадает с порядком вкладок: переключение вырезает из отчёта кусок, а не
переставляет его.

- **Почему не шесть, как на макете.** «Динамика» — один блок, который по условию 2 уже стоит на
  первой вкладке: вкладка была бы повтором или пустой. «Экономика» — четыре числа окупаемости, и
  порог окупаемости читается только рядом с затратами, от которых он считается.
- **Почему не пять.** Та же «Экономика» отдельно: вкладка ради четырёх чисел.
- **Почему не три.** «Качество данных» внутри «Товаров» смешало бы две темы: неполнота — про все
  источники (заказы, реклама, комиссии, постоянные), а не про товары.

### Что вкладки прячут — названо, а не обнаружено

- **Честность данных уходит с первой вкладки.** На марте прибыль посчитана по настоящей цене
  поставщика для 80,3 % чистой выручки, и с первой вкладки этого не видно. Механизма не добавляю:
  довод владельца «вкладки прячут» снят им самим. Если нужно — одна строка на первой вкладке с той
  же готовой долей: 0 новых чисел, одна-две строки переписи.
- **Пометка устаревания не прячется:** она общая и стоит над вкладками.
- **Клавиатура:** четыре остановки Tab вместо одной со стрелками — см. выше.
- **Пары снимков «до / после» перестают сравнивать одно с одним:** «до» — весь экран S10, «после» —
  первая вкладка; остальное «до» разъезжается по трём другим вкладкам.

### Цена — пересчитана до первой строки кода

**Якоря прежних сломов: переводится 2 из 28.** Обещание «ноль из двадцати восьми» перестаёт быть
верным. Посчитано перебором всех 28 цитат по списку строк, которые вкладки меняют, сдвигают или
между которыми вставляют новое: теги разделов, шапка, ссылки месяцев, вызов `Dashboard`, строки
окупаемости и блока затрат, чтение адреса. Задеты две.

| Слом | Старая цитата | Новая цитата | Утверждение и ожидание |
|---|---|---|---|
| `nav-links-drop-month` (S5) | `` href={`/?m=${m.month}`} `` | `` href={`/?m=${m.month}${хвостВкладки}`} `` — при варианте А развилки В3; имя рабочее | прежние слово в слово |
| `button-not-on-screen` (S5) | в `find` и в `replace`: `<Dashboard report={report} />` | `<Dashboard report={report} tab={вкладка} />`; имя рабочее | прежние слово в слово |

Почему не ноль. Страница обязана передать вкладку разметке, а единственная строка, где это можно
сделать, целиком процитирована сломом кнопки. Ссылка месяца обязана нести вкладку — развилка В3; при
варианте Б перевод один.

Почему уцелели остальные 26:

- **Теги разделов не цитирует ни один якорь** — перебор дал 0. Раздел показывается только на своей
  вкладке тем, что его тег `<section>` заменяется тегом маленького компонента, который рисует
  `<section>` на своей вкладке и ничего на чужих. **Это отступление от буквы прежнего обязательства**
  «ни один существующий блок не оборачивается»: блок не оборачивается и не переотступается, но его
  открывающий и закрывающий теги меняются — по две строки у шести существующих разделов, двенадцать
  строк. Строки внутри разделов не сдвигаются ни на байт; доказательство — дифф этих разделов, где
  изменены только строки тегов.
- **Пара окупаемости по обороту переезжает из «Итога» в свой блок** (В7). Её якорь
  `roas-label-removed` цитирует строку `<dt>` без отступа — переживает переезд; цитата остаётся
  единственной в файле.

**Сверх якорей кусок трогает принятое прошлых кусков — всё числом:**

| Что трогается | Сколько | Почему не обойти | Статус |
|---|---|---|---|
| Перепись экрана S9 | 1 проверка: один список → четыре, и строка вызова | у каждой вкладки свой полный список — условие 4 | **разрешено владельцем**; в теле заявки отдельной строкой как разрешение шире прежнего |
| Обвязка двух принятых проверок | S5 «месяц из адреса определяет, что видно на экране…» — 2 строки вызова; S6 «страница отчёта с годной cookie печатает числа» — 1 строка | обе рисуют страницу без `tab` и ищут на ней артикул; артикулы на «Товарах», а адрес без `tab` — первая вкладка по условию 2 | просьба П1 |
| Проверка генератора снимков S10 «генератор собирает полный набор и сверяет все десять страниц» | имя и два числа: 10 → 18 страниц, 12 → 26 картинок | состав описи меняют сами вкладки | просьба П2 |
| Имя этой проверки в прежних списках сломов (поле «покраснеет заодно») | 9 строк: 8 в `breaks/s9-screen.ts`, 1 в `breaks/s10-fluent.ts` | прибор ищет проверку по имени; со старым именем эти сломы получат «такой проверки в наборе нет» | просьба П2 |
| Выписка величин S10 | 2 дописанные строки: `colorCompoundBrandStroke` (обе темы) и `strokeWidthThicker` | закрывающая проверка ищет у каждой величины имя в выписке | просьба П3; дифф — только добавленные строки |
| Генератор снимков S10 (`generate.tsx`, раскладка `fixture.ts`) | состояния вкладок, `tab` у прежних состояний, новые поля мартовской раскладки | генератор рисует `Dashboard` без адреса | код, не проверка; называется в заявке |

**Чего стоил бы вариант Б развилки В2, для сравнения:** 13 принятых проверок краснеют —
`__tests__/metrics/screen.test.tsx` все 9, `__tests__/screen/honesty-bar.test.tsx` все 3 и вторая
проверка файла переписи. **Счёт по чтению проверок, не прогоном:** кода нет.

**Сломы.** В таблицах плана по вкладкам, долям затрат, раскладке, снимкам и водопаду — **29 строк**
(посчитано по таблицам плана командой); четыре из них — проверки текста или устройства разметки,
названы слабыми в своих строках. Отменяются 4 строки полосы быстрого перехода; строка «ровно восемь
ступеней» заменяется строкой «ровно девять» при варианте А развилки В6.

### Перепись экрана по вкладкам — что меняется в файле

- **Меняется:** строка вызова. Было — одна отрисовка `<Dashboard report={ПЕРЕПИСЬ} />`; станет —
  четыре, по списку имён вкладок, **выписанному в самой проверке буквально**, а не взятому из
  приложения: иначе пропавшая вкладка пропала бы и из переписи. `toEqual` сверяет четыре списка.
- **Не меняется ни на символ:** имя проверки, тело `текстПоПорядку`, сравнение `toEqual`.
- **Не меняется вовсе:** вторая проверка файла — «все одиннадцать видов неполноты». Она рисует
  `Dashboard` без вкладки, то есть весь отчёт (В2-А), и слом S9 `gaps-truncated` красит её
  по-прежнему.
- **Каждый список** начинается шапкой — название, месяцы, строка времени, подписи четырёх вкладок —
  и продолжается разделами своей вкладки.
- **Четыре слома S9** — `census-blind-to-swap`, `labels-swapped`, `items-order-reversed`,
  `gaps-truncated` — прогоняются до правки и после; вывод обоих прогонов идёт в тело заявки.

### Снимки — 26 вместо 12

| Что | Картинок |
|---|---|
| 4 вкладки × 2 темы × широкий экран | 8 |
| 4 вкладки × 2 темы × узкий экран | 8 |
| Прежние состояния × 2 темы: числа отстали, отказ шага, отказ базы, вход | 8 |
| Новое состояние × 2 темы: неизвестная вкладка в адресе (В4) | 2 |
| **Итого** | **26** |

Страниц генератора: 9 состояний × 2 темы = **18** вместо 10. Первая вкладка занимает прежние
номера `01`–`03`: на них ссылаются сданные документы. При варианте Б развилки В5 — 20 картинок.

### Развилки

Каждая меняет результат. У каждой — рекомендация.

#### В0. Цвет акцента

- **А (рекомендую). Остаётся Fluent 2, `#0f6cbd`.** Ударение итогов с макета берётся ступенью шкалы и
  насыщенностью, а не цветом.
- Б. Индиго с макета. **Цена:** отменяется задание заказчика «интерфейс, который принял бы
  Microsoft»; 7 смысловых величин выписки S10 в двух темах — 14 значений — получают цвет, которого
  нет в палитре Fluent, то есть без источника; ещё один якорь прошлого куска — слом S10
  `brand-colour-invented` цитирует `--colorBrandBackground: #0f6cbd;`; таблица контраста S10
  перемеряется целиком.

#### В1. Сколько вкладок

- **А (рекомендую). Четыре:** «Главное», «Деньги», «Товары», «Качество данных» — довод выше.
- Б. Пять — «Экономика» отдельно: вкладка ради четырёх чисел.
- В. Шесть, как на макете: «Динамика» повторяет первую вкладку или пуста.

#### В2. Что рисует разметка отчёта, когда вкладка не передана

Страница передаёт вкладку всегда. Без вкладки разметку рисуют только проверки — принятые проверки
S5 и S9 рисуют её именно так.

- **А (рекомендую). Весь отчёт, как сегодня.** Принятые проверки остаются зелёными без единой правки
  и продолжают сторожить содержимое разделов. **Цена названа:** это вид, которого человек не видит.
  Разбиение по вкладкам сторожат новые проверки — перепись по вкладкам и «каждый раздел ровно на
  одной вкладке». Полосы вкладок в этом виде нет: текущей вкладки у него нет.
- Б. Первую вкладку. Тогда 13 принятых проверок краснеют, и у каждой правится обвязка с прогоном
  её слома.

#### В3. Сохраняет ли переключение месяца вкладку

- **А (рекомендую). Да.** Кто смотрит «Товары» за март и нажал февраль — видит «Товары» за февраль.
  Цена: +1 якорь (`nav-links-drop-month`).
- Б. Нет, месяц ведёт на «Главное». 0 якорей, но таблицу приходится искать заново каждый месяц.

#### В4. Неизвестная вкладка в адресе (`?tab=boom`)

- **А (рекомендую). Отказ словами.** Шапка и полоса вкладок на месте, текущей нет, вместо разделов —
  «Раздела «boom» нет. Разделы отчёта — ссылками выше.» Тот же приём, что у кривого месяца: отказ
  говорит, что сделать. Цена: одно новое состояние, 2 снимка.
- Б. Молча первая вкладка. Подстановка по умолчанию без называния — у нас запрещена.

#### В5. Узкий экран на снимках

- **А (рекомендую). У каждой вкладки, в обеих темах — 26 картинок.** Прежний узкий снимок видел все
  разделы: таблицу товаров и список неполноты на узком экране. Плюс новое: «Выручка» и «Затраты» на
  узком экране становятся одна под другой, и это надо увидеть.
- Б. Только у первой вкладки — 20 картинок. Узкий вид «Денег», «Товаров» и «Качества» не виден никому.

#### В6. Чистая выручка — промежуточным итогом водопада

- **А (рекомендую). Да: девять ступеней, три из них итоги от нуля** — оборот, чистая выручка,
  прибыль. Так расставлены ударения на макете, а они обязательны. Значение — та же чистая выручка,
  что в блоке «Выручка»: второго определения нет, а сходимость «оборот − скидки − возвраты = чистая
  выручка» уже стоит в контракте. Цена: +1 ступень, 2 строки переписи, строка слома «ровно восемь
  ступеней» становится «ровно девять». Меняет принятое определение — пишется новой задачей плана.
- Б. Восемь ступеней, как принято.

#### В7. Где окупаемость по обороту

- **А (рекомендую). В блоке «Окупаемость», крупным числом; три соседние — ступенью ниже.** Порог
  1,75 × читается только рядом с 4,23 ×, с которым его сравнивают. «Итог» сохраняет два крупных
  числа — прибыль и маржу, — и крупных на вкладке остаётся три, как принято в S10. 0 якорей.
- Б. Оставить в «Итоге». Тогда в блоке окупаемости нет того, с чем сравнивается порог.

### Просьбы к владельцу — без них вкладки не делаются

| № | Что | Почему нужна |
|---|---|---|
| П1 | Разрешить правку обвязки двух принятых проверок: S5 «месяц из адреса…» (2 строки вызова) и S6 «…печатает числа» (1 строка) — дописать `tab: 'tovary'` | без неё обе краснеют от условия 2. Утверждения не трогаются; живость каждой доказывается сломом: у S5 — прежним `month-param-ignored`, у S6 прежнего слома нет — доказывает новый слом S11 «страница не читает вкладку», объявляющий её «покраснеет заодно» |
| П2 | Разрешить правку проверки генератора S10: имя и два числа (18 страниц, 26 картинок) — и перевод её имени в 9 строках прежних списков сломов | опись меняется самими вкладками; со старым именем 9 прежних сломов перестают что-либо утверждать |
| П3 | Подтвердить, что две дописанные строки в выписке величин S10 — добавление, а не правка | условие механическое: в диффе файла только добавленные строки; каждая — с цепочкой звеньев до источника на `43665d5` |
| — | Положить макет файлом в ветку контракта | рецензент читает требования с ветки и должен видеть то же, что я |

### Что не меняется

Экран не считает. Где данных нет — слова, а не ноль. Смысл не кодируется одним цветом. Новые
величины — из токенов Fluent 2, проверка чисел таблицы стилей остаётся зелёной. Перепись правится
только списком ожидаемого и строкой вызова. Якоря переводятся вместе с кодом, старая и новая цитата
— отдельным разделом в заявке. Код S11 начинается после слияния S10.

---

## Решения владельца по макету и вкладкам — 10 сентября 2026 года

> Отдельным разделом. Одно решение — **В2** — принято **против моей рекомендации**. Одно — **В6** —
> меняет принятое определение контракта. По В2 владелец потребовал замер прогоном, а не чтением,
> с условием: если красных окажется заметно больше тринадцати — стоп и разговор. **Их 23. Замер
> и вопрос, который он открывает, — ниже.**

| Развилка | Решение | Условие владельца |
|---|---|---|
| В0 — цвет | **Fluent 2, `#0f6cbd`** | индиго — 14 значений без источника и отмена задания заказчика, за которое заплачен круг |
| В1 — вкладки | **четыре**, разбиение от данных | — |
| В2 — вид без вкладки | **«Главное». Вида «весь отчёт» нет** | против моей рекомендации; довод ниже |
| В3 — месяц помнит вкладку | **да**, +1 якорь | — |
| В4 — незнакомая вкладка | **отказ словами** | отказ называет существующие вкладки и даёт вернуться на «Главное». Тупик защитой не считается |
| В5 — узкий экран | **у каждой вкладки, 26 снимков** | цена честного охвата |
| В6 — чистая выручка в водопаде | **девять ступеней** | записать изменением принятого «восьми ступеней», с датой и причиной — ниже |
| В7 — окупаемость по обороту | **крупным числом рядом с порогом** | там же назвать базу счёта: порог — от вклада, а не от маржи экрана |
| П1 | **разрешено** | только строки вызова; утверждения не трогаются; доказательство — прежние сломы |
| П2 | **разрешено** | только имя и два числа; имя переводится в девяти строках прежних списков; после перевода **все семь списков гоняются целиком** |
| П3 | **подтверждено** | дифф только из добавленных строк; у каждой строки якорь «файл:строка» на том же коммите `43665d5` |
| Макет | **в ветку, с подписью** | это макет, а не спецификация; при расхождении верен контракт; несходимость макета с самим собой названа там же |
| После «Обновить данные» | **требование, а не допущение** | страница обязана остаться на той же вкладке; у поведения должен быть слом |

### В2 — довод владельца, против которого я рекомендовал

Словами владельца: вид, который видят только проверки, — путь, которого нет в бою; он не стареет
вместе с экраном, врёт молча, и зелень на нём доказывает вымысел. Правило об этом у нас записано,
и нарушать его ради того, чтобы не трогать обвязку, владелец не стал.

**Что из этого следует в устройстве.** Разметка без вкладки рисует «Главное». Проверка «каждый
раздел ровно на одной вкладке» больше не может сравнивать вкладки с видом «весь отчёт» — его нет;
она сравнивает заголовки разделов четырёх вкладок с **буквальным списком десяти заголовков**,
выписанным в самой проверке. Обвязка принятых проверок, которые рисуют разметку без вкладки,
дописывается так же, как в П1.

### Замер по В2 — прогоном, а не чтением

**Где.** Отдельная местная ветка от головы S10 `bbda713` — S10 не влит, поэтому не от `main`. Ветка
**не для слияния и не выложена**: это прибор замера. В ней: адрес без вкладки — «Главное»; разделы
получают вкладку заменой тега, строки внутри не сдвинуты; незнакомая вкладка — отказ словами со
ссылками на разделы; слом кнопки обновления (S5) переведён вместе с кодом. Проверка типов и линтер
на ней — код возврата 0.

**Исходное состояние.** Первый прогон на голове S10 дал 1 красную из 778: в местной базе лежали три
записи попыток входа с адреса `::1` — следы местных заходов на страницу, — а проверка обнуления счёта
ждёт в таблице только свои строки. Причина не в коде. После пересборки местной базы: **778 из 778,
код возврата 0**.

**После изменения: 755 зелёных, 23 красных, код возврата 1.**

| Группа | Красных | Было предсказано |
|---|---|---|
| Проверки экрана S5 и S9, рисующие разметку без вкладки | 13 | **13 — совпало по составу** |
| Перепись экрана S9, сам список | 1 | названа; правка разрешена |
| Страница без вкладки: S5 «месяц из адреса…», S6 «…печатает числа» | 2 | названы в П1 |
| Генератор снимков S10 | 7 | **не предсказаны** |
| **Итого** | **23** | 16 |

**Почему краснеют семь проверок генератора — одна причина на всех.** Сторож генератора S10 сверяет
разметку снимка с эталоном, а эталон — весь экран. Без вкладки теперь рисуется «Главное», в котором
до готовности его блоков нет ничего, кроме шапки. Сторож отказывает первым — «было «Выручка», стало
ничего» — и заслоняет собственный отказ каждой из проверок. Это не обвязка: у генератора нет строки
вызова с вкладкой, которую можно дописать, — у него есть эталон, который надо пересобрать. Текст
самого сторожа, дословно: «Если разметка изменена сознательно — это находка к владельцу, а не строка,
которую поправляют в снимке.» Условие: текст написан в S10, куске, который не имел права менять
показанное; S11 меняет показанное сознательно — и это та самая находка.

**По условию владельца — стоп.** Обвязка не тронута ни в одной проверке.

**Что замер добавил к цене П1:**

- обвязку правят **15 принятых проверок**: 13 экранных и 2 страничных; плюс перепись по разрешению;
- у одной из них — S5 «экран печатает поля отчёта, а не свои числа» — утверждения охватывают три
  вкладки сразу: «Деньги», «Товары», «Качество данных». Её строка вызова рисует три вкладки и
  складывает их, а не одну;
- **у 4 из 15 прежнего слома нет**, и доказать их «своим прежним сломом» нельзя: S6 «страница отчёта
  с годной cookie печатает числа», S5 «окупаемость печатается отношением, а не процентами», S5 «нет
  данных печатается словами, а не как 0 и не как NaN», S5 «экран называет товары без цены
  поставщика». Для них нужны новые сломы S11 — они вписаны в план;
- прибор сломов отказывает на красном исходном состоянии. Значит ни одно доказательство сломом не
  снимается, пока не зелены все 23 — в том числе семь проверок генератора;
- строк сломов по вкладкам: к прежним 29 решения добавили 6 — **всего 35** (посчитано по таблицам
  плана командой).

### Вопрос Г — семь проверок генератора S10. Решаешь ты

- **А (рекомендую). Переключение на вкладки — одним шагом с пересборкой эталонов снимков, и этот
  шаг — после блоков «Главного»** (полоса показателей, водопад, суточный ряд). Эталоны пересобираются
  один раз, на экран с содержимым. Утверждения семи проверок не трогаются; если прогон покажет, что
  хоть одно требует правки, — стоп и к тебе. **Цена:** переключение уезжает в конец разметки, а
  эталоны снимков S10 переписываются — это находка, о которой говорит сам сторож, и твоё слово здесь
  — решение по ней.
- Б. Переключать сразу, первой задачей разметки, и пересобирать эталоны дважды: сейчас — на почти
  пустое «Главное», потом — на готовое. **Эталон пустой страницы не сторожит ничего**, и между двумя
  пересборками генератор доказывал бы вымысел — ровно то, от чего ты отказался в В2.

Ослабить сторож генератора не предлагаю.

### В6 — изменение принятого определения: восемь ступеней → девять

**Дата:** 10 сентября 2026 года. **Причина, словами владельца:** чистая выручка — настоящий
промежуточный итог, и она уже стоит крупным числом в полосе показателей; в водопаде её отсутствие
читалось бы как пропуск.

**Прежнее, дословно, для следа:** «Восемь ступеней: оборот → скидки → возвраты → себестоимость →
реклама → комиссии → постоянные → прибыль. Первая и последняя — столбики от нуля, шесть средних —
вычитания.»

**Действующее:** девять ступеней — оборот → скидки → возвраты → **чистая выручка** → себестоимость →
реклама → комиссии → постоянные → прибыль. Три итога — оборот, чистая выручка, прибыль — столбики от
нуля; шесть вычитаний висят от текущего остатка. Значение промежуточного итога — та же чистая
выручка, что в блоке «Выручка»: второго определения нет.

### В7 — база порога на новой раскладке

Окупаемость по обороту стоит крупным числом **рядом с порогом**, и соблазн поделить сто на маржу
экрана здесь вырастает. Поэтому у порога прямо в подписи названа база: **порог считается от вклада
(57,1 % на мартовских данных, наш счёт), а не от маржи экрана (10,1 %)**. Вклад стоит рядом числом.
Условия владельца из принятия контракта — порог и вклад числом, подпись базы, пометка «наше
определение» — в силе все три.

### В4 — отказ на незнакомую вкладку

Шапка на месте; вместо разделов — «Раздела «…» в отчёте нет. Выберите один из разделов:» и четыре
ссылки на существующие вкладки, первой — «Главное». Месяц из адреса ссылки сохраняют, если он
правильной формы. Кнопка выхода на месте, как на всякой странице отказа. В приборе замера это уже
написано; проверки у него пока нет — она в плане.

### После «Обновить данные» страница остаётся на той же вкладке — требование

**Что говорит документация Next** (`node_modules/next/dist/docs`, Next 16.3.3, справка
`revalidatePath`), дословно: «**Server Functions**: Updates the UI immediately (if viewing the affected
path).» Условие стоит в той же строке: если человек смотрит на затронутый путь. Наше действие
перепроверяет путь `/`, и отчёт живёт на `/` — условие выполнено. **Наш вывод, а не слова
справочника:** перерисовка идёт без перехода, и адрес — вместе с вкладкой — остаётся прежним.

**Что сторожит наш код.** Уйти со вкладки можно, только если действие уведёт страницу само — переходом
после работы. Слом: дописать действию переход на `/` после работы — обязана покраснеть проверка
«после «Обновить данные» действие не уводит со страницы».

**Чего наши проверки не сторожат — названо.** Сохранение адреса при перерисовке — поведение Next, а не
нашего кода. Оно наблюдается в живом проходе (задача 18) на каждой из четырёх вкладок, и вывод идёт в
заявку; не подтвердится — находка к владельцу.

### Макет в ветке

`docs/maket/2026-09-10-maket.png`, рядом подпись `docs/maket/README.md`: это макет, а не
спецификация; обязательное из него названо словами в этом контракте; при расхождении верен контракт.
Там же названа несходимость макета с самим собой — полоски затрат от чистой выручки против плашки от
оборота — и то, что у нас база одна, оборот, и на экране она названа в подписи доли.

**Макет подтверждён владельцем** по виду: та же раскладка, те же числа, то же наползание «Себест.» и
тот же пустой график. Побайтно с вложением из переписки не сверить — исходных байтов вложения нет ни
у кого из нас; хеш файла в ветке записан в подписи.

**Строка таблицы отклонённого про вторую линию «Прибыль» дополнена причиной по данным** по просьбе
владельца: прибыль по дням не рисуется, потому что у постоянных расходов в базе есть только месяц.
Прежняя причина — решение о названии блока — оставлена в той же строке.

---

## Решение владельца по вопросу Г — 10 сентября 2026 года

**Вариант А.** Переключение на вкладки — одним шагом вместе с пересборкой эталонов снимков, и шаг
ставится после того, как готовы блоки «Главного». Довод владельца: двойная пересборка с пустым
«Главным» между ними — тот же вымысел, от которого он отказался в В2, только в эталонах снимков.

**Четыре условия к шагу вкладок:**

| № | Условие | Что делаю, если не выходит |
|---|---|---|
| 1 | Эталоны пересобираются **один раз**, на экран с содержимым | понадобился промежуточный пересбор — стоп и к владельцу |
| 2 | Утверждения семи проверок генератора не трогаются | потребовало правки хоть одно — стоп и к владельцу, как велит их собственный сторож |
| 3 | Четыре проверки из пятнадцати без прежнего слома доказываются **новым сломом S11 до того**, как их обвязка считается доказанной; слом красит **именно ту** проверку, а не соседнюю | слом красит соседнюю — слом сужается, обвязка не считается доказанной |
| 4 | Доказательства снимаются **только на полностью зелёном наборе** | набор красный — доказательств нет, шаг не сдан |

**Следующий шаг работы — не S11:** заявка №14, слияние S10 и пересчёт якорей от `main`.

---

## Задача 0 — живой заход, 10 сентября 2026 года: на настоящих данных один месяц

> Отдельным разделом. S10 влит (`8bf2e3a`), работа идёт на ветке `feat/s11-content` от `main`.
> Живой заход — по разрешению владельца, за файлом «до». Он подтвердил пять итогов — и опроверг
> утверждение этого контракта о настоящих данных, которое я сделал сам. Прежний текст оставлен как
> был; что в нём неверно — перечислено ниже поимённо.

### Что показал заход

| Что | Итог |
|---|---|
| Исходный прогон на `8bf2e3a` | 61 файл, 778 проверок, 0 красных, код возврата 0 |
| Якоря | 28 цитат в разметке отчёта, все единственные; задеты вкладками 2 — как ожидалось |
| Таблица, шесть листов | 519, 19, 11, 2, 5, 62 строк — **сошлось** с независимым счётом владельца по всем шести |
| Папка рекламы | 4 файла (1699, 2814, 2908, 986 байт), 279 строк сырья, 186 после свёртки копии — как в S10 |
| Пять итогов файла «до» | оборот, чистая выручка, прибыль, маржа, окупаемость по обороту — **сошлись все пять**. Сверка командой по кодам символов; на копии с прибылью, изменённой на цент, она краснеет ровно на прибыли |
| Файл «до» S11 против файла «до» S10 | совпадает во всех строках, кроме четырёх строк шапки, которые печатает `npm` без `--silent`: S10 снимал без него, S11 — с ним. «После» снимается той же командой, с `--silent` |
| **Месяцы, которые видит отчёт** | **один — 2026-03, с заказами** |
| Возврат базы посевом | пересборка — код 0; за март после неё — нули по всем деньгам, маржа и окупаемость «нет данных» |

**Диапазоны дат в фактах после живой загрузки:** заказы — с 1 по 31 марта; постоянные расходы — один
месяц, март; реклама — с 1 по 31 марта; возвраты — с 3 марта по 9 апреля (апрельские возвраты
относятся к мартовским заказам); курсы — с 1 марта по 1 мая.

### Путь, который числился редким, на настоящих данных — единственный

Владелец увидел на боевом экране одну кнопку месяца. Живой заход из тех же источников даёт то же
самое: один месяц. **Таблица загружена вся** — число строк сошлось с независимым счётом владельца по
всем шести листам, — значит один месяц лежит в самих источниках, а не теряется загрузкой. Находки к
владельцу про неполную загрузку нет.

| Что | На настоящих данных |
|---|---|
| Полоса дельт | **все четыре дельты — «нет базы для сравнения», разом.** Путь с числами дельт на настоящих данных не встречается ни разу |
| Вопрос куска «стало лучше или хуже» | **на настоящих данных остаётся без ответа**, пока в источниках один месяц. Экран честно говорит «нет базы для сравнения» — и это единственное, что он говорит на этот вопрос |
| Переключатель месяцев | одна кнопка |
| Месяц без заказов (решение Д3) | не встречается |
| Суточный ряд | встречается: март, 31 день. Есть ли в марте дни без заказов, этим заходом не проверено |

Путь «нет базы для сравнения» переезжает из списка редких в список **единственных**. Путь с числами
дельт, месяц без заказов и прошлый месяц без рекламы — в список **ни разу не встреченных**: они
доказываются только выдуманными раскладками, и это сказано вслух.

### Моя ошибка — утверждения контракта о настоящих данных взяты из выдуманной раскладки

Три месяца — январь без заказов, февраль и март с заказами — это раскладка снимков S10
(`docs/screens/fixture.ts`), а не настоящие данные. Я перенёс её в контракт как свойство данных и
ни разу не проверил заходом. Неверны, дословно:

| Где | Утверждение |
|---|---|
| развилка 7, оговорка | «у января заказов нет (`hasOrders` ложно), у февраля есть. Значит для марта база для сравнения существует, а для февраля — нет, и путь «нет базы» увидит владелец сразу, не выдумывая случай.» |
| развилка Д3 | «Этот путь **встречен в настоящих данных**: у января заказов нет (`hasOrders` ложно), а в списке месяцев он есть — значит у него есть расходы.» |
| поправка, проверка довода владельца | «Но месяцев с заказами в настоящих данных два — февраль и март, январь пуст.» |
| развилка Д2′-б | «тем же знаком «данных нет», которым переключатель месяцев уже помечает январь» — так он помечает его на снимках, а не на боевом экране |
| Д3 переформулировано | «Путь встречен в настоящих данных: январь.» |
| развилка В3, пример | «Кто смотрит «Товары» за март и нажал февраль» — пример верен как устройство, но февраля в данных нет |

И в плане, раздел «Что дополнение меняет в названных ценах и ловушках»: «**Путь, встреченный в
настоящих данных:** январь — месяц без заказов, но с расходами.»

**Что это меняло.** Решение Д3 владелец принимал с доводом, что путь встречен и будет виден ему
сразу. Решение от этого не становится неверным — месяц без заказов по-прежнему возможен, — но теперь
это путь, ни разу не встреченный в данных.

### Вопрос Е1 — какой путь полосы дельт показывают снимки «Главного»

Раскладка снимков держит три месяца, и на снимках «Главного» полоса дельт идёт путём с числами —
тем, которого на боевых данных нет.

- **А (рекомендую). Добавить состояние «Главное» на пути «нет базы для сравнения», в обеих темах:
  26 снимков → 28.** Владелец принимает вид того, что увидит на бою, а не только того, что на бою
  не встречается.
- Б. Оставить как есть: путь с настоящих данных не виден на снимках ни разу.

---

## Решения владельца после задачи 0 — 10 сентября 2026 года

> Отдельным разделом. Владелец спросил работодателя; ответ: **сейчас в источниках только март,
> дальше месяцы будут добавляться.** Решения ниже приняты с этим ответом.

| Вопрос | Решение | Довод или условие владельца |
|---|---|---|
| Е1 — снимки «Главного» | **А: «Главное» на пути «нет базы для сравнения», обе темы — 28 снимков** | принимать глазами то, что будет на бою, а не то, что рисует раскладка |
| Порядок задач | **дельты — в конец куска.** Вперёд — то, что на одном месяце работает полностью: водопад, суточный ряд, три окупаемости, колонки товаров, доли статей затрат, время чтения источников | на боевых данных полоса дельт сегодня не отвечает ни на что, а остальное отвечает; механизм дельт всё равно делается и сдаётся |
| Механизм дельт | **делается полностью** | месяцы будут добавляться; переделывать потом дороже |
| Вид полосы без прошлого месяца | **значение показывается всегда; пустой бывает только дельта** — одной короткой строкой на карточку | четыре карточки со словами «нет базы для сравнения» — первое, что владелец увидит на бою, и выглядит это как сломанный экран |
| Путь «нет базы для сравнения» | **боевой, а не редкий**: идёт на снимки, принимается глазами, получает свой слом наравне с путём с числами | — |
| Переход между путями | **сам, от данных**: без правки кода, без настройки, без флагов и переменных среды. Слом: появился предыдущий месяц — дельты считаются; исчез — снова слова | иначе добавление месяца станет работой |
| Дни без заказов в марте | проверяются в живом заходе задачи суточного ряда. **Разрешение на этот заход дано**, условия прежние: местная база, наружу ничего, после — возврат посевом с наблюдением | отдельного захода ради этого не делается |
| Ручная работа владельца | **когда в Таблице появится второй месяц — владелец заходит на боевой адрес и проверяет дельты глазами** | до того дня путь с числами живьём не видел никто, и первым его увидит владелец, а не проверка |

### Что меняется в прежнем тексте

**Правило «нет данных — словами»**, строка «Прошлого месяца нет или в нём нет заказов», прежде
дословно: «нет базы для сравнения» — у всей полосы дельт разом, а не по одной. **Действует:**
дельты пусты у всех четырёх показателей разом — это по-прежнему решает SQL одним признаком, — но на
экране **значение каждого показателя стоит числом**, а на месте дельты — одна короткая строка.
Пустой бывает дельта, а не показатель.

**Список путей.** Две строки поменялись местами:

| Путь | Было | Стало |
|---|---|---|
| Нет базы для сравнения | редкий: «месяц без предыдущего вовсе (самый ранний в списке)» | **боевой и единственный на настоящих данных** |
| Дельты с числами | обычный путь | **ни разу не встречен в настоящих данных**; доказывается только выдуманной раскладкой. Первым его увидит владелец, когда в Таблице появится второй месяц |

**Снимки: 28.** К 26 добавлены два — «Главное» на пути «нет базы для сравнения», светлая и тёмная
тема, широкий экран. Страниц генератора — 10 состояний × 2 темы = 20. **Числа просьбы П2 меняются:**
в проверке генератора станет 20 страниц и 28 картинок, а не 18 и 26, — правка по-прежнему только
имя и два числа.

### Наш вывод из двух решений: шаг вкладок — последним шагом содержания

Полоса показателей — блок «Главного», а дельты теперь делаются последними. Условие владельца —
эталоны снимков пересобираются один раз, на экран с готовым «Главным», — значит шаг вкладок
встаёт **после дельт**. Иначе дельты поменяли бы «Главное» после пересборки, и понадобилась бы
вторая.

**Почему до шага вкладок генератор снимков остаётся зелёным — проверено по устройству его
сторожа.** Сторож сравнивает разметку страницы с эталоном целиком. Новые поля отчёта
необязательны, и новый блок рисуется только тогда, когда у отчёта есть его поле; в раскладке
генератора новых полей нет до шага вкладок. Значит, пока **разметка существующих блоков не меняется
ни на байт**, страница генератора совпадает с эталоном. Отсюда два условия на работу до шага вкладок:

- новые блоки рисуются только при наличии своих полей — это принятое правило необязательных полей,
  а не новое;
- **переезд окупаемости по обороту из «Итога» в блок «Окупаемость» (В7) делается на шаге вкладок**,
  а не в задаче окупаемостей: он меняет разметку существующего блока. До шага вкладок блок
  «Окупаемость» рисуется после «Итога» с тремя новыми числами, а окупаемость по обороту стоит в
  «Итоге», как сегодня.

**Цена названа:** до шага вкладок принятые проверки прошлых кусков и генератор снимков видят экран
без новых блоков — их раскладки новых полей не несут. Новые блоки видят новые проверки S11 и
перепись, чья раскладка поля получает. Раскладка генератора получает все новые поля на шаге
вкладок, и эталоны пересобираются тогда один раз.

---

## Решение владельца по развилке Ж1 — 10 сентября 2026 года: горизонтальный водопад

**Вариант А.** Довод владельца — не цена, а подписи: вертикальный водопад не держит полных
названий; на макете их пришлось сократить до «Себест.» и «Комис.», и одна всё равно наехала на число.
Полные названия дороже сходства с картинкой.

| № | Условие владельца | Чем исполнено |
|---|---|---|
| 1 | Три итога сплошные от нуля; вычитания светлее и висят | итог — сплошной столбик смыслового цвета от нуля, подпись и сумма полужирные; вычитание — светлая заливка с обводкой от остатка. Различие не одним цветом: признак в разметке и насыщенность, у каждого — слом |
| 2 | Каждая ступень несёт сумму и долю; база доли названа | у каждой ступени сумма и доля «… % оборота» — слово «оборота» стоит у каждой; слом «печатать долю без базы» |
| 3 | Сумма ступеней сходится с прибылью; якорь снаружи — последняя ступень марта 1 738,53 €, то же число, что в «Итоге» | якорь — живая проверка: оборот 18 764,00, чистая выручка 17 277,04, прибыль 1 738,53 и прибыль ступени = прибыль итога. Запускается в живом заходе шага 2. **Сходимость до цента на настоящих данных не выполняется — развилка Ж2 ниже** |
| 4 | Нулевой оборот — пусто; отрицательная прибыль — в списке ни разу не встреченных путей | нулевой оборот — доли, края и шкала пусты, у ступеней слова «нет данных», столбиков нет; оба пути — в списке ниже |
| 5 | На снимках водопад виден целиком, не обрезан краем кадра | шаг вкладок: высота снимков «Главного» подбирается по замеру, и генератор отказывает, если нижний край блока ниже края кадра. Это новый сторож в генераторе S10 — со своей проверкой и сломом |

### Находка по условию 3: на мартовских данных ступени расходятся с прибылью на один цент

**Наш счёт по сверенным числам:** 18 764,00 − 427,50 − 1 059,46 = 17 277,04 — первая цепочка
сходится точно. Вторая — **нет**: 17 277,04 − 6 028,11 − 4 431,37 − 526,12 − 4 552,90 = **1 738,54**,
а прибыль — **1 738,53**. Разница — один цент.

**Причина — округление, а не счёт.** Каждая сумма округляется до цента отдельно, в последнем
`select`; прибыль считается из неокруглённых сумм и округляется один раз, и сумма округлённых
слагаемых отходит от округлённой суммы. **Наш вывод по устройству запроса, не замер:** доли цента
несут суммы, полученные делением и процентом, — реклама, переведённая из долларов по курсу дня, и
комиссии; какая из них дала этот цент на марте, не измерено.

**Это расхождение есть на экране уже сейчас** — в блоках «Затраты» и «Итог» стоят те же числа, и
человек, сложивший их сам, получит 1 738,54. До водопада его никто не назвал: цепочку в уме не
складывал никто, а водопад складывает её на глазах.

### Развилка Ж2 — как утверждать сходимость, когда её не даёт округление

- **А (рекомендую). Сходимость с точностью до округления — и расхождение называется на экране.**
  SQL отдаёт готовой строкой расхождение между прибылью и цепочкой показанных сумм. Если оно не
  ноль, под водопадом одна строка: «Суммы ступеней округлены до цента по отдельности; сложенные, они
  расходятся с прибылью на 0,01 €». Утверждение: расхождение не больше двух центов — у цепочки пять
  округлённых слагаемых, у каждого ошибка до половины цента. Якорь снаружи — счёт владельца: прибыль
  1 738,53 и расхождение на марте ровно 0,01 — живой проверкой.
- Б. Сходимость до цента по устройству: одно вычитание в водопаде подправляется на остаток
  округления. Тогда реклама в водопаде — 4 431,38, а в блоке «Затраты» — 4 431,37: одна статья, два
  числа на экране. Хуже, чем названный цент.
- В. Утверждать сходимость на неокруглённых суммах внутри SQL. Она верна по определению и ничего не
  доказывает: человек на экране по-прежнему сложит 1 738,54.

### Пути, ни разу не встреченные в настоящих данных — дописываются

| Путь | Почему опасен |
|---|---|
| Нулевой оборот — водопад без долей, слова вместо столбиков | на мартовских данных оборот есть |
| Отрицательная прибыль — последняя ступень влево от нуля, шкала шире ста | на мартовских данных прибыль положительна |
| Отрицательное вычитание — ступень идёт вправо от остатка | в грязных данных возможно; на марте не встречено |

---

## Решения владельца по Ж2 и по виду водопада — 10 сентября 2026 года

### Ж2 — вариант А, и утверждение поправлено владельцем

**Моя ошибка, названная:** в варианте А я написал «расхождение не больше двух центов» — число из
головы. Счёт владельца: в цепочке пять округлённых слагаемых, каждое отстоит от точного не больше чем
на полцента, и сама прибыль округлена один раз — худший случай три цента, а не два. Порог выводить
не нужно вовсе.

**Действующее утверждение, словами владельца:** напечатанное расхождение равно фактической разнице
между суммой показанных ступеней и показанной прибылью. Оно проверяется на любых данных, не требует
границы и краснеет, если цепочка разойдётся по настоящей причине, а не по округлению. Проверка
считает разницу сама, в целых центах, из сумм, которые отдал запрос.

**Сверх просьбы, названо:** расхождений два — до прибыли и до чистой выручки. Первая цепочка
(оборот − скидки − возвраты) округляется так же и может разойтись по той же причине; на марте она
сходится точно. Строка под водопадом — у каждого, только когда оно не ноль.

| Что | На марте |
|---|---|
| Расхождение с прибылью | 0,01 € — строка на экране |
| Расхождение с чистой выручкой | нет — строки нет |

### Вычитания — светлая заливка вместо обводки

Замечание владельца: тонкая обводка превращала малую ступень в точку, и экран не отвечал на «кто
съедает больше». Заливка — `colorBrandStroke2`, светлый оттенок бренда: brandWeb[140] в светлой теме,
brandWeb[50] в тёмной (`alias/lightColor.ts:167`, `alias/darkColor.ts:167` на `43665d5`).

| Токен | К карточке, светлая | К карточке, тёмная |
|---|---|---|
| `colorBrandStroke2` — вычитание | 1,51 : 1 | 1,51 : 1 |
| `colorBrandBackground2` — светлый фон бренда, уже в выписке | 1,12 : 1 | 1,10 : 1 |
| `colorCompoundBrandBackground` — итог | 5,38 : 1 | 5,18 : 1 |

Числа — **наш счёт по формуле контраста WCAG из значений токенов**, а не замер с отрисованной
страницы; замер — в задаче вида перед сдачей.

**Отступление от роли, названное:** у Fluent это токен обводки; заливочного токена такой светлоты
нет — светлый фон бренда на карточке не виден. Столбик числа не несёт в одиночку: сумма и доля
напечатаны рядом, поэтому порог 3 : 1 для графики к нему не применяется — **наш вывод**, тот же, что
в S10 про разделители.

**Это третья новая величина в выписке S10 сверх двух, подтверждённых в П3.** Дифф выписки — три
добавленные строки, ни одной удалённой. Просьба П4 — подтвердить.

### Цена строки «Съедает больше всего: … · … % оборота» — решает владелец

Число уже посчитано: это самая большая доля среди вычитаний. На марте, наш счёт по сверенным числам:
**себестоимость проданного, 32,1 % оборота**; следующая — постоянные расходы, 24,3 %.

| Что | Сколько |
|---|---|
| Новых запросов | 0 — признак «самое большое вычитание» дописывается колонкой в запрос водопада |
| Новых полей отчёта | 1, необязательное |
| Строк переписи | 1 |
| Строк сломов | 3: выбрать самое большое среди всех ступеней, а не среди вычитаний; взять подпись у одной ступени, а долю у другой; при нулевом обороте напечатать строку |
| Новых проверок | 3 — по одной на слом |
| Снимков | 0 новых — строка видна на уже посчитанных снимках «Главного» |
| Якорей | 0 |
| Пар контраста | 0 — строка обычным текстом, без сигнального цвета |

Мой вывод по цене: недорого. Строка повторяет словами самую длинную светлую ступень — теперь, когда
вычитания залиты, её видно и без строки. Решение — владельца.

### Отложенная находка

Проверка чисел таблицы стилей S10 не ловит ссылку на величину, которой в таблице нет: такую ссылку
она засчитывает как «ссылку на величину». Найдено на шаге 1, когда я сослался на несуществующий
отступ, — исправлено в таблице, вся таблица проверена командой, необъявленных ссылок 0. Сама
проверка не открывается — решение владельца; находка идёт в список отложенных.

---

## П4, строка «съедает больше всего» и живой заход шага 2 — 10 сентября 2026 года

**П4 подтверждён владельцем:** `colorBrandStroke2` в выписке S10 — три добавленные строки с якорем на
`43665d5`; отступление по роли названо у правила.

### Строка «Съедает больше всего» — делается

Довод владельца — наше же правило: смысл не кодируется одним признаком. Геометрия и слово — два
канала об одном, как у доли честности.

| Условие владельца | Чем исполнено |
|---|---|
| Признак из той же колонки запроса, что и ступени | запрос водопада ставит признак в строке ступени; разметка своего «самого большого» не ищет |
| Равенство двух вычитаний до цента — назвать, что делаем, и завести слом | помечены все равные, строка называет их: «Съедает больше всего поровну: … и … · по … % оборота». Выбрать одно наугад — молчаливое решение за читателя. Слом «пометить только первое» — «покраснела своя». **Путь ни разу не встречен в настоящих данных** |
| Без сигнального цвета — обычный текст | у правила строки нет ни цвета, ни фона; слом «покрасить сигнальным цветом» краснеет проверкой текста таблицы стилей — слабой и так названной |

На марте, наш счёт по сверенным числам: «Съедает больше всего: себестоимость проданного · 32,1 %
оборота». Все вычитания ноль — строки нет; нулевой оборот — строки нет.

### Живой заход шага 2 — итог

| Что | Итог |
|---|---|
| Итоги ступеней марта | 18 764,00 / 17 277,04 / 1 738,53 — числа счёта владельца; прибыль ступени = прибыль «Итога» |
| Расхождение цепочки | с прибылью — 0,01 €; с чистой выручкой — нет |
| Ряд по дням | 31 день; сумма ряда — 17 277,04 €, точно до цента |
| **Дни без заказов** | **один — 9 марта.** Путь «день без заказов: разрыв, линия отсчёта пунктиром» встречен в настоящих данных |

**Найдено и исправлено:** уход моей живой проверки возвращал посевом только сырьё, а факты живого
захода переживали замену — за март на «посеве» оставалось 179,00 € оборота. База возвращена
пересборкой и подтверждена нулями; уход переписан и проверен без похода наружу. Заход запущен
дважды: первый утвердил всё, но наблюдение о днях спрятала среда проверок; второй — тот же заход
с видимым выводом.

### Развилка Ж3 — высота области столбиков ряда по дням

Тридцать один столбик лёжа не уместить — ряд вертикальный, и области столбиков нужна высота. В шкалах
Fluent такой длины нет (наибольший отступ — 32 пикселя), это та же беда, что у вертикального
водопада.

- **А (рекомендую). Высота — названным исключением в принятой проверке чисел S10:** одна строка в
  списке длин с доводом, `8rem` — 128 пикселей, **наше число**: при нём процент доли — около 1,3 пикселя
  высоты. Поместится ли ряд в первый экран вместе с водопадом и полосой показателей — не замерено. Проверка продолжает
  сторожить длину: чужая длина на месте этой покраснит. Цена: правка принятой проверки S10 — одна
  добавленная строка в списке и одна правленая строка шапки («их четыре» → «их пять»). Просьба П5.
- Б. `aspect-ratio` вместо длины: высота следует ширине. Длины нет вовсе, но и сторожа нет: проверка
  чисел не читает чисел без единиц, и пропорцию не сторожит ничто. На узком экране ряд сжимается
  примерно до 57 пикселей — наш счёт при пропорции 6 : 1.
- Кратное токена через `calc` не предлагаю: это обход проверки, а не величина с именем.

---

## Правило владельца и две просьбы после шагов 2–4 — 10 сентября 2026 года

### Правило: любое отношение на экране называет свою базу рядом с собой

Словами владельца: не в отчёте и не в контракте, а на экране, рядом с числом. Порог — от вклада;
доля в прибыли — от суммы прибыли строк; доли ступеней и статей затрат — от оборота; окупаемость по
прибыли — после постоянных. Повод — окупаемость по прибыли 0,39 × рядом с порогом 1,75 × читалась как
противоречие; у числа теперь фраза: «После постоянных расходов — они от рекламы не зависят, поэтому
окупается ли реклама, говорит порог, а не это число». Проверка всего экрана этим правилом — в плане,
таблицей; нарушение нашлось одно, прежнее, — просьба П7.

### П6 — цвет подсветки строки товара в минусе

Условие владельца (Д-4): красный — смысловой и не спорит с красным отказов. Отказы окрашены **всеми
тремя** красными токенами нашей выписки — фоном `colorPaletteRedBackground1`, текстом
`colorPaletteRedForeground1`, рамкой `colorPaletteRedBorder2`. Значит подсветке нужна четвёртая
величина — это новая строка в выписке S10.

- **А (рекомендую). `colorPaletteRedBackground2` — заливка строки.** Светлая тема — red.tint40
  `#f1bbbc`, тёмная — red.shade30 `#751d1f` (`alias/lightColorPalette.ts:15`,
  `alias/darkColorPalette.ts:15`, `global/colors.ts` на `43665d5`). **Наш счёт по формуле контраста:**
  обычный текст на заливке — 9,29 и 10,76 : 1; заливка к карточке — 1,67 и 1,35 : 1; к фону отказа —
  1,57 и 1,51 : 1, то есть не сливается с ним. Приглушённого текста в строках таблицы нет; если бы был,
  в светлой теме он дал бы 3,71 : 1 — ниже нормы, и это названо. Цвет не единственный признак: рядом
  стоит число маржи со знаком минус. Цена: три добавленные строки в выписке S10 и слом «взять для
  подсветки токен отказа».
- Б. Без красного вовсе: у строки в минусе — только число маржи со знаком минус и признак формы.
  Держит правило «красный — только отказам», но подсветки как таковой нет.

**На мартовских данных подсветка не видна ни в одной строке** — все двенадцать артикулов в плюсе.

### П7 — база у прежней «Маржи» в «Итоге»

По правилу владельца у маржи в «Итоге» базы нет: подпись — «Маржа», а считается она от чистой
выручки. Предлагаю «Маржа (от чистой выручки)» — тем же приёмом, что у «Окупаемость рекламы (по
обороту)». Правка — разметка прежнего блока, поэтому она уходит на шаг вкладок, вместе с пересборкой
эталонов: сейчас она сломала бы сверку генератора снимков. За подпись держится только перепись —
принятых проверок, которые её читают, поиск не нашёл. Решение — владельца: метрики у нас не
переименовываются, а это добавка базы, не новое имя.
